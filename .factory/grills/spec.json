{
  "generated_by": "griller",
  "gate": "spec",
  "verdict": "pass",
  "questions_asked": 2,
  "gaps": [
    "Old-key handling corrected against code: the document key is already rejected by the strict key check; LEGACY-2 removes only the in-memory shadow field and dual-reads (no silent drop).",
    "providerConnectionId and the missing_provider_connection enum are out of scope (deferral)."
  ],
  "contradictions": [
    "Earlier ruling 'ignore silently' assumed the document key was dual-read; the code rejects it today \u2014 resolved by keeping the rejection (no new code)."
  ],
  "resolutions": [
    "Keep rejecting the old document key; delete the shadow field, the ~20 dual-read sites, rename-only locals and the 19 exceptions.",
    "Leave providerConnectionId and the OpenAPI enum untouched; record a deferral.",
    "Spec doc edited: locked decision 1 and AC2 now say the old document key stays rejected by the existing strict key check; the earlier 'ignore silently' wording is gone."
  ],
  "rounds": [
    {
      "question": "Correction from the code read: a settings.yaml / stored revision with the old `provider_connection` key is ALREADY rejected today as an unknown key (strict key check). The in-memory shadow field is the only dual-read. Given that, what should LEGACY-2 do with the old key in documents?",
      "options": [
        "Keep rejecting it \u2014 no new code (Recommended)",
        "Add a special-case silent drop"
      ],
      "chosen": "Keep rejecting it \u2014 no new code (Recommended)"
    },
    {
      "question": "Scope guard: `providerConnectionId` (route/event payloads, CLI setup return names) and the OpenAPI enum `missing_provider_connection` are NOT part of the expired gate and touching them is a wider/breaking change. Leave them out of LEGACY-2?",
      "options": [
        "Leave them out (Recommended)",
        "Rename providerConnectionId too"
      ],
      "chosen": "Leave them out (Recommended)",
      "frontier_empty": true
    }
  ],
  "citations": [
    {
      "finding": "strict key check rejects any unlisted conversation key today",
      "path": "apps/core/src/config/settings/runtime-settings-compact.ts",
      "line": 150
    },
    {
      "finding": "parser fills the shadow field from providerAccount",
      "path": "apps/core/src/config/settings/runtime-settings-parser.ts",
      "line": 241
    },
    {
      "finding": "no public providerConnection field in packages/contracts or the SDK; missing_provider_connection enum is external",
      "path": "packages/contracts/src",
      "line": 1
    }
  ],
  "input_sha256": "2dd4df72dbf05582f62de247879e421b5ef41653d830616d01a9ddf3ec75b5a1",
  "recorded_at": "2026-08-29T03:33:27+00:00",
  "commit": "16de17a064b25463c6cebaf7ea8f1266c8724271"
}
