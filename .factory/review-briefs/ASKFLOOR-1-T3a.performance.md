# Review brief — ASKFLOOR-1-T3a — performance lens

You are one lens of a three-lens code review. You see ONLY the diff bundle for
this task (no repository access), so judge what the diff shows and say so when
something cannot be verified from it. Report every finding with its
file_path and line. Use ONLY these categories: bug, security, regression,
test_gap, maintainability. Priorities: P0/P1 block the task; P2/P3 must be
resolved or explicitly deferred with a reason before it ships.

LENS: PERFORMANCE. Hot paths, algorithmic complexity, query fanout (N+1),
I/O amplification, memory churn, concurrency bottlenecks, missing pagination or
bounds, work repeated per request that could be done once. Distinguish measured
evidence from inference and say which each finding is. Use category `bug` for a
performance defect that will bite in production and `maintainability` for a cost
worth reducing.

LEFTOVERS (blocking): the diff must carry no code kept only for compatibility — no wrapper or shim over its replacement, no re-export or alias kept 'for callers', no renamed-but-retained symbol, no dead branch behind a removed feature, no 'legacy'/'deprecated'/'backward' naming or comment. Report each as a BLOCKING finding with file:line and verdict the contract it belongs to as partial; a clean diff says so in one line.
## Task ASKFLOOR-1-T3a

### Plan contracts

- **ASKFLOOR-1-T3a-AC1**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md#acceptance-criteria
  - Statement: SCHEMA. `permission_decision_memory` gains nullable text columns `outcome`, `scope`, `scope_key`, `acting_person_id`, `acting_person_label` (physical snake_case — the table's existing convention; the deviation from the constitution's camelCase rule is recorded as deliberate in the migration note and the architecture note) and a CHECK `permission_decision_memory_human_row_ck`: `kind <> 'human_decision' OR (outcome IS NOT NULL AND scope IS NOT NULL AND scope_key IS NOT NULL AND acting_person_id IS NOT NULL)` (the label may be NULL — rows render "someone", 0154). The unconditional `permission_decision_memory_lookup_uq` is DROPPED and replaced by two partial unique indexes: `permission_decision_memory_lookup_uq` on `(app_id, agent_folder, kind, lookup_identity) WHERE kind <> 'human_decision'` and `permission_decision_memory_human_uq` on `(app_id, agent_folder, acting_person_id, scope, scope_key) WHERE kind = 'human_decision' AND revoked_at IS NULL`; the active lookup index stays. The migration is GENERATED (`npm run db:migrations:generate`, timestamp-named like `20260827174119_agents_web_ui_foundation.sql` — the currently live convention in `schema/migrations/`, not the retired `0107_`/`0110_` sequence) so the drizzle drift gate (`db:migrations:check`, `.github/workflows/ci.yml:63`) stays clean; the `meta/_journal.json` and snapshot travel with it. Human `id`s are UUID strings in the existing text column (no column type change). Tests (Postgres, `permission-decision-memory.postgres.integration.test.ts`, `schemaPrefix: 'permission_decision_memory'`): raw inserts prove (a) two non-human rows on the same four-column key still reject — the existing pin at `:154-175` is kept with its kind; (b) two ACTIVE human rows for the same person + scope + scope_key reject; (c) the same person + scope + scope_key with the first row REVOKED accepts a second row; (d) two different persons with the same scope + scope_key both insert; (e) a `human_decision` row missing any of outcome, scope, scope_key or acting_person_id rejects (one case each), and a missing label is accepted.
- **ASKFLOOR-1-T3a-AC2**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md#acceptance-criteria
  - Statement: PORT + REPOSITORY. `PermissionDecisionMemoryKind` gains `'human_decision'`; the row and put types gain optional `outcome?: HumanDecisionOutcome` (`allow | deny`), `scope?: HumanDecisionScope` (`exact | kind | place`), `scopeKey?`, `actingPersonId?`, `actingPersonLabel?` (closed typed enums exported from the port; no string literals elsewhere). The existing methods `put`, `get`, `list` and `revoke` serve the four NON-HUMAN kinds (classifier verdicts, remembered denies, trusted roots, standing grants — live code, not compatibility code; nothing in the diff may be named or commented "legacy"): each REFUSES `kind: 'human_decision'` (throws `HumanDecisionRequiresTypedAccessError`, a named domain error in the port, raised by one guard `assertNonHumanKind` — the human path has its own person-scoped methods below), `put` keeps today's conflict target, and an unfiltered `list` (no `kind`) EXCLUDES human rows (`kind <> 'human_decision'`), so no non-human path can read or revoke across persons (0154/0118). NEW `putHumanDecision(input) → Promise<{ id: string; status: 'inserted' | 'refreshed' }>`: requires all four human fields and a v4-UUID-shaped `id` (a missing field or a malformed id throws before any SQL — the same guard style as `assertPersistable`, `:17-34`), always `lookupIdentity = scopeKey`, `decision = outcome`, `principal = canonicalTool`; it runs in ONE transaction: select the ACTIVE row for `(app_id, agent_folder, acting_person_id, scope, scope_key)` (the human partial index's key) — if one exists it is REFRESHED in place with `outcome`, `decision`, `reason`, `acting_person_label`, `effect_hash`, `rail_version`, `effect_schema_version`, `created_at` AND `provenance` re-encoded (AC5) with the STORED id, and the STORED id is returned with `status: 'refreshed'` (the caller's fresh id is discarded — an opposite-outcome write for the same authority simply replaces the older decision: newest human decision wins, one Forget still revokes the only active record, 0118); if none exists a NEW row is inserted with the caller's id and `provenance` encoded with it (`status: 'inserted'`) — a REVOKED row is NEVER reactivated: it stays as history beside the new row (the partial index ignores revoked rows, so the insert succeeds). NEW `listHumanDecisions({ appId, agentFolder, actingPersonId, includeRevoked? })` ordered newest first by `created_at`, person-scoped (no cross-person rows, pinned); `includeRevoked` defaults to false (active rows only); `true` returns revoked rows too, each with its `revokedAt` (pinned); NEW `revokeById({ appId, agentFolder, actingPersonId, recordId, nowIso })` → `'applied' | 'already_revoked' | 'not_found'` — `not_found` when the id exists for ANOTHER person, app or agent folder (never crosses any of the three axes, pinned by cross-person, cross-app and cross-folder tests); NEW `countExactAllowsByTool({ appId, agentFolder, actingPersonId })` → `Record<toolName, number>` of ACTIVE `outcome = allow`, `scope = exact` rows grouped by the canonical tool name carried in `principal` (the human row stores AC3's `canonicalTool` in the existing `principal` column — the trust-growth input read by T5a; documented in the architecture note). `getClassifierVerdict`/`putClassifierVerdict` are byte-identical. Tests: Postgres round-trips for each new method per person; unit guard test for the refusals.
- **ASKFLOOR-1-T3a-AC3**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md#acceptance-criteria
  - Statement: SCOPE KEYS in ONE module, NEW `application/permissions/human-decision-scope.ts`, ONE async function (async only because the native destination check awaits T2b's filesystem boundary; everything else is pure): `deriveHumanDecisionScopeKey({ outcome, scope, request, effectHash, workspaceRoot, canonicalRoot, trustGrowthTool }) → Promise<HumanDecisionScopeKeyResult>` where `HumanDecisionScopeKeyResult = { ok: true; scopeKey: string; pathOnly: boolean } | { ok: false; reason: HumanDecisionNotRememberableReason }` and `HumanDecisionNotRememberableReason` is the closed enum `protected_destination | no_category | no_root | incomplete_effect | deny_requires_exact` exported from this module (AC4 relays it verbatim, so no derivation logic is duplicated). Canonical tool identity: `canonicalTool = gantryNativeCanonicalToolName(request.toolName)?.canonical ?? request.toolName` (the helper returns `{ canonical, known } | null`, `application/permissions/gantry-tool-risk.ts:46`), so bare `file` and every registered `mcp__gantry__file` spelling are one tool, and the facade ids `WebSearch`/`WebRead` (`shared/gantry-tool-facades.ts:5-6`; `WebFetch` is already mapped to `WebRead` by the facade table) are used as given. Rules. `scope = exact`: `outcome = deny` ⇒ the full effect hash (`computePermissionEffectHash`, `domain/permission-effect-key.ts:48-99`, content included; `pathOnly: false`); `outcome = allow` on a NATIVE write (`FileWrite`/`FileEdit`): FIRST every supplied non-blank string destination among `file_path` and `path` is checked with `evaluateProspectiveWriteBoundary({ workspaceRoot, candidatePath })` (`shared/permission-prospective-write.ts`); any `inside: false` (outside, protected, hidden, secret, symlinked, undefined root) ⇒ `{ ok: false, reason: 'protected_destination' }` — protected destinations never key an Allow, whatever the destination count; then EXACTLY ONE destination ⇒ `exact:path:<canonicalTool>:<canonicalPath>` (`pathOnly: true`) using the `canonicalPath` the boundary now returns on success (T3a extends the shared helper's success result to `{ inside: true; canonicalPath: string }` — the resolved absolute path it already computes — because `application` may not import `node:path`, `scripts/architecture-map.json:84`); an all-safe dual input ⇒ the full hash (`pathOnly: false`); no string destination ⇒ full hash. `outcome = allow` on a VIRTUAL write — canonical `file` with `action: 'write'` → `(scope, path)` or `action: 'promote_scratch'` → `(targetScope, targetPath)` — ⇒ `exact:path:<canonicalTool>:<destination>` (`pathOnly: true`) when unprotected; every other allow ⇒ the full hash (`pathOnly: false`), so shell commands (redirects, `cp`, `mv`, `tee`, multi-target) are never path-only. The same request with `outcome = deny` always keys on the full hash (no destination check). virtual → `<normalizeFileArtifactScope(scope)>/<normalizeFileArtifactPath(path)>` (`domain/file-artifacts/virtual-path.ts`; an omitted `scope`/`targetScope` takes the same host default the normaliser applies) unless `isProtectedArtifactEntry` (`domain/file-artifacts/protected-virtual-path.ts`) or `protected: true` ⇒ `protected_destination` for an allow; a malformed shape (missing path, non-string fields, a normaliser throw) ⇒ full hash. `scope = kind` ⇒ `kind:<categoryId>` from this CLOSED request-to-category table (anything not in it ⇒ `{ ok: false, reason: 'no_category' }`): (1) `Bash`/`RunCommand`: parse with `parseBashCommand` (the same parser the read-only gate uses, `shared/auto-permission-read-only-gate.ts:93`), classify EVERY leaf with `classifyPermissionEffectShape(leaf, { stdinOk })` where `stdinOk` mirrors the gate EXACTLY: `false` for a single leaf, `true` for every leaf of any multi-leaf compound (`&&`, `||`, `;`, `|` alike — the parser exposes only aggregate topology, `:98-106`; pinned by a compound/pipeline parity test); all leaves `read_only_command` ⇒ `read_only_command`; all leaves `file_read` ⇒ `file_read`; a mix of those two ⇒ `read_only_command`; any `not_read_only` leaf, a parse failure or zero leaves ⇒ `no_category`; (2) canonical `file` with `action: 'list' | 'read'` ⇒ `virtual_file_read`; (3) `WebSearch` ⇒ `web_search`, `WebRead` ⇒ `web_read`; (4) trust growth (0154): `trustGrowthTool: true` ⇒ `kind:tool:<canonicalTool>` for ANY tool, evaluated before rows 1-3 (the card offers it only for high-risk gantry tools; T3a does not police that). The category ids are a closed exported enum `HumanDecisionKindCategory`. `scope = place` ⇒ `place:<canonicalRoot>` from the caller-supplied `canonicalRoot` (resolved by T3b via `resolveTrustedRootStage`; T3a only formats it) ⇒ `no_root` when absent or blank. `outcome = deny` with `scope ≠ exact` ⇒ `deny_requires_exact` (a No is always exact, 0154). An undefined `effectHash` (incomplete input) ⇒ `incomplete_effect` wherever the hash is needed (every deny; every non-path-only allow). Tests (unit, exhaustive): each shape × outcome × scope, dual native keys, bare `file` vs `mcp__gantry__file`, omitted virtual scope, protected/outside/symlink destinations, changed-content near-miss (same path, different content: allow keys equal, deny keys differ), whitespace-padded virtual paths, every kind row incl. multi-leaf pipelines and a mixed read/write pipeline ⇒ `no_category`, missing hash, missing root, deny with kind/place.
- **ASKFLOOR-1-T3a-AC4**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md#acceptance-criteria
  - Statement: SERVICE. NEW `application/permissions/human-decision-memory-service.ts` exporting `HumanDecisionMemoryService` (class, constructor-injected `{ repository, newId = randomUUID, now = currentIso }` — the `CapabilitySecretService` pattern, `application/capability-secrets/capability-secret-service.ts:17`; no adapter construction, port import only per `scripts/architecture-map.json`): `remember(dto: HumanDecisionRememberRequest) → { status: 'remembered'; id; shortId; scopeKey; pathOnly; stored: 'inserted' | 'refreshed' } | { status: 'not_rememberable'; reason: HumanDecisionServiceRefusal }` where `HumanDecisionServiceRefusal = HumanDecisionNotRememberableReason | 'unresolved_person' | 'once_never_remembered'` — refuses a blank `actingPersonId` (`unresolved_person`) and a `resolution` whose `kind` is not `'remember'` (`once_never_remembered`: a scalar `allow_once`/`allow_persistent_rule`/`cancel` never reaches the store) BEFORE any repository call; derives the key via AC3 and relays an `ok: false` reason verbatim; generates a v4 UUID via `newId` and validates its shape (a malformed injected id ⇒ throws — pinned); writes via `putHumanDecision` with `reason` = the DTO's human-readable line, `railVersion`/`effectSchemaVersion` from the DTO (the caller's rails catalogue), `principal = canonicalTool`; the returned `id` is the repository's EFFECTIVE stored id (a refreshed duplicate returns the stored id, never the discarded fresh one — pinned) and `shortId` is derived from it. `deriveHumanDecisionShortIds(ids: string[]) → Map<id, shortId>` (exported, tested; the ONLY short-id function — no single-id wrapper, no compatibility shim, OWNER RULING 2026-09-06 "we don't need legacy code"): each id normalised once, the first 6 hex characters of the dash-free lowercase UUID, extended one character at a time only while another id in the list (this person's ACTIVE records) shares the prefix, resolved by prefix counting in one pass — so short ids are unique within a person's active list; `list({ appId, agentFolder, actingPersonId, limit? })` → active rows newest first; short ids are disambiguated against ALL of the person's active ids BEFORE `limit` is applied, so a truncated page never shows an ambiguous short id (pinned); `revoke({ appId, agentFolder, actingPersonId, recordId })` → the repository's result verbatim (the full record id; prefix resolution is T5b's); `countExactAllowsByTool(...)` → the repository's map. Tests (unit, in-memory fake repository): every refusal reason, once-never-remembered for each scalar mode, malformed injected id, a remembered allow and deny round-trip, refreshed duplicate returns the stored id, list is person-scoped, batch short-id derivation incl. a 6-char collision extending to 7 and a 500-id list resolved in one pass, revoke results. NO LEFTOVERS (owner ruling 2026-09-06): the diff carries no compatibility wrapper, shim, re-export kept "for callers", renamed-but-retained symbol or "legacy" naming; a reviewer finding one is a blocking finding.
- **ASKFLOOR-1-T3a-AC5**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md#acceptance-criteria
  - Statement: DOMAIN TYPES (T3b/T3c only consume them; all additive). `domain/types.ts` sits exactly at its 740-line architecture ceiling (`scripts/architecture-map.json:41`), so the ONLY change there is `PermissionDecisionSource` gaining `'human_decision'` on its existing `prettier-ignore` union line (`:257-260`, no new line); every other addition lives in the NEW sibling module `domain/human-decision.ts` (imported by the port, the scope module, the service and T3b/T3c; `types.ts` does not re-export it): the structured, provider-neutral remember resolution beside today's scalar modes — `HumanDecisionOutcome`/`HumanDecisionScope` re-exported here from the port for callers, `PermissionRememberResolution = { kind: 'remember'; outcome: HumanDecisionOutcome; scope: HumanDecisionScope }`, `PermissionApprovalResolution = { kind: 'mode'; mode: PermissionApprovalDecisionMode } | PermissionRememberResolution` (the option/claim DTO the story plan names; T3c owns wiring it into `PermissionCallbackClaimIntent`, the recovery envelope, settlement and the provider codecs — T3a only defines the type and its two guards `isRememberResolution`/`resolutionMode`); NEW `HumanDecisionRememberRequest` DTO = `{ appId, agentFolder, actingPersonId, actingPersonLabel?, resolution: PermissionApprovalResolution, request: PermissionApprovalRequest, effectHash?, workspaceRoot?, canonicalRoot?, trustGrowthTool?, railVersion, effectSchemaVersion, reason }`; NEW provenance codec `encodeHumanDecisionProvenance({ id, actingPersonId, outcome, scope, railVersion }) → string` / `decodeHumanDecisionProvenance(text) → {…} | undefined` (a fixed `human_decision:` prefix with a canonical sorted-key JSON body; decode fails closed on any other prefix, a malformed body, a missing field, a non-integer `railVersion` or a non-UUID id) — the `provenance` column of a human row is always the encoded form and never free text (the agent-visible provenance per 0054/0107 is derived from it, never inferred from strings). Tests: codec round-trip incl. `railVersion`, fail-closed decode for each malformed case, the resolution guards.
- **ASKFLOOR-1-T3a-AC6**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md#acceptance-criteria
  - Statement: architecture note `docs/architecture/permission-decision-memory.md` (or the existing decision-memory note if one exists — the implementer checks and extends rather than duplicates) records the record kind, the per-kind keys, the `principal = canonicalTool` convention for human rows, the snake_case physical names and the history-not-reactivation rule; existing unit and Postgres integration suites pass; tsc and check:architecture green; the drizzle drift gate clean (AF-AC7). GATE ENVIRONMENT: `stage done` runs every `verify_commands` entry in ONE environment with `GANTRY_TEST_DATABASE_URL` exported (the Postgres lane refuses without it), and today `verify.py`'s `npm test` general integration lane (`vitest.integration.config.ts`, file-parallel) then fails on `Hook timed out in 10000ms` in `beforeAll` for the DB-gated suites (concurrent schema migrations against one database — eleven suites, each green alone). T3a therefore sets `fileParallelism: false` in `vitest.integration.config.ts` ONLY when `GANTRY_TEST_DATABASE_URL` is set (URL-less runs keep today's parallelism; the Postgres config already runs serially), so `verify.py` passes with the URL exported and the gate is honest end to end.

### Reviewer focus

- Constitution (load-bearing): constitution/03-modular-monolith-structure.md and pnp-coding-standards-modular-monolith.md (typed enums over string literals — outcome, scope, not-rememberable reason and the decision source are closed unions; application imports the domain port only, never an adapter; DB access stays in adapters per scripts/architecture-map.json); pnp-database-standards.md: physical column names follow the TABLE's existing snake_case convention, the deviation from camelCase recorded as deliberate in the migration note and the architecture note. Naming follows the sibling convention (kebab-case, no dotted role suffix).
- SCHEMA (decision 0154, 0118): five nullable text columns outcome, scope, scope_key, acting_person_id, acting_person_label; CHECK permission_decision_memory_human_row_ck (kind <> human_decision OR the four non-label fields NOT NULL); the unconditional lookup unique is DROPPED and replaced by two PARTIAL unique indexes — permission_decision_memory_lookup_uq on (app_id, agent_folder, kind, lookup_identity) WHERE kind <> 'human_decision' and permission_decision_memory_human_uq on (app_id, agent_folder, acting_person_id, scope, scope_key) WHERE kind = 'human_decision' AND revoked_at IS NULL; the active index stays; ids stay text (human ids are UUID strings). The migration is GENERATED by npm run db:migrations:generate (timestamp-named, the live convention — NOT the retired 0107_/0110_ sequence) so db:migrations:check stays clean; the SQL and meta snapshot are measured into scope by forge stage amend-scope at close; the migration's leading comment carries the snake_case deviation note. The existing four-column pin at permission-decision-memory.postgres.integration.test.ts:154-175 is KEPT (its kind is non-human).
- REPOSITORY (0118 one-Forget rule; no reactivation): the existing put, get, list and revoke serve the four NON-HUMAN kinds (live code — nothing named or commented 'legacy') and THROW the named port error HumanDecisionRequiresTypedAccessError for kind human_decision through one guard assertNonHumanKind (an unfiltered list excludes human rows), put keeping its conflict target; putHumanDecision guards all four human fields and a v4-UUID-shaped id before SQL, sets lookupIdentity = scopeKey, decision = outcome, principal = canonicalTool, and in ONE transaction selects the ACTIVE row for (app_id, agent_folder, acting_person_id, scope, scope_key): present → refresh in place (outcome, decision, reason, acting_person_label, effect_hash, rail_version, effect_schema_version, created_at, provenance re-encoded with the STORED id) and return { id: storedId, status: 'refreshed' } — newest human decision wins, the caller's fresh id is discarded; absent → insert the caller's id with provenance encoded from it and return { id, status: 'inserted' }; a REVOKED row is never reactivated (history kept beside the new row). listHumanDecisions (includeRevoked default false; true adds revoked rows with revokedAt; newest first by created_at), revokeById and countExactAllowsByTool filter by app_id, agent_folder AND acting_person_id — revokeById({ appId, agentFolder, actingPersonId, recordId, nowIso }) returns applied | already_revoked | not_found and not_found for another person's, app's or folder's id. getClassifierVerdict and putClassifierVerdict byte-identical (0043/0155).
- SCOPE KEYS: AC3 is the single normative text (async discriminated deriveHumanDecisionScopeKey; canonicalTool via gantryNativeCanonicalToolName(...)?.canonical ?? toolName; NATIVE allow = every supplied destination boundary-checked first, then one destination ⇒ exact:path:<canonicalTool>:<canonicalPath> from the shared boundary success result { inside: true, canonicalPath } — application never imports node:path; VIRTUAL allow = file write/promote_scratch ⇒ exact:path:<canonicalTool>:<scope/path>; deny = full hash; the closed kind table with the gate-identical stdinOk; place from the caller; the five refusal reasons). Do not accept an implementation that re-derives paths, re-parses shells differently from the read-only gate, or judges protection with anything but T2b's helpers.
- SERVICE + DOMAIN (0107 typed source; 0054 provenance): HumanDecisionMemoryService with constructor-injected { repository, newId = randomUUID, now = currentIso } (the CapabilitySecretService pattern); remember refuses a blank actingPersonId (unresolved_person) and any resolution whose kind is not 'remember' (once_never_remembered — every scalar mode) BEFORE any repository call, relays the scope-key refusal reason verbatim, validates the generated id as a v4 UUID (malformed ⇒ throw), and returns { status: remembered, id: the repository's EFFECTIVE stored id, shortId, scopeKey, pathOnly, stored: inserted | refreshed }; deriveHumanDecisionShortIds(ids) is the ONLY short-id function (batch, prefix counting, one pass; no single-id wrapper): the first 6 dash-free lowercase hex chars extended one char at a time only while another active id shares the prefix, computed against ALL active siblings before list's limit; revoke takes { appId, agentFolder, actingPersonId, recordId } (full id; prefix resolution is T5b's); domain/types.ts gains ONLY the 'human_decision' member on the existing PermissionDecisionSource union line (the file is at its 740-line ceiling); the NEW domain/human-decision.ts holds the structured PermissionRememberResolution { kind: 'remember', outcome, scope } and PermissionApprovalResolution ({ kind: 'mode', mode } | remember) with guards isRememberResolution/resolutionMode (T3c wires them into claims, envelopes, settlement and provider codecs), the HumanDecisionRememberRequest DTO (resolution, not a scalar mode) and the encode/decodeHumanDecisionProvenance codec carrying id, actingPersonId, outcome, scope AND railVersion (fixed human_decision: prefix + canonical sorted-key JSON; decode fails closed on prefix, body, missing field, non-integer railVersion, non-uuid id) — T3b/T3c only consume them. No consult, no write from any runtime path, no identity resolution here.
- NO LEFTOVERS (owner ruling 2026-09-06, 'we don't need legacy code'): the diff must carry no compatibility wrapper, shim, re-export kept for callers, renamed-but-retained symbol, dead branch or 'legacy' naming; any such leftover is a BLOCKING finding and verdicts the contract partial.
- Concerns that must not share a file: schema (adapter) vs port (domain) vs repository (adapter) vs scope keys (application, pure) vs service (application, orchestration) vs domain types.
- No domain error type introduced beyond HumanDecisionRequiresTypedAccessError (the named port error thrown by the non-human put/get/list/revoke for the human kind — AC2), sibling of AllowOnceNeverPersistedError. Nothing deferred: consult/write integration (T3b/T3c), projection (T4) and cards (T5) are other tasks by design.
- Verification: python3 factory/scripts/verify.py is authoritative and must pass WITH GANTRY_TEST_DATABASE_URL exported (AC6: vitest.integration.config.ts passes fileParallelism: false to makeVitestConfig only when that variable is set, so the DB-gated suites in the general lane migrate serially; URL-less runs keep today's parallelism); npm run db:migrations:check must be clean; the three Postgres leaves run through vitest.integration.postgres.config.ts and the FULL npm run test:integration:postgres lane is a contract verify command — both with GANTRY_TEST_DATABASE_URL exported (the lane is green on main since PR #483; scripts/require-postgres-integration-env.mjs refuses without the URL).

### Lessons in force

Recorded lessons that apply to this task's paths. A finding that contradicts one is not a defect unless it shows the lesson itself is wrong; say so explicitly instead of re-raising it.

- [medium] schema storage: Runtime state, jobs, control events, and memory use Postgres as the production storage model; schema or repository changes need repository tests and architecture/docs updates when contracts shift.
- [high] canonical-postgres-cutover: When replacing Postgres runtime schema in one cut, move active runtime persistence behind canonical Drizzle repositories/services in the same change; leaving schema-owned raw SQL or old table definitions creates drift and runtime failures after destructive migrations.
- [high] architecture boundaries: Keep provider-specific and channel-specific behavior behind adapters; domain and application code should depend on stable product concepts and ports, not SDK payloads or runtime wiring.
- [high] permission safety: Risky tool execution must pass through deterministic permission evaluation and sandbox policy before any provider callback or runner grants access.
- [medium] fleet-compose-rehearsal: Fleet compose rehearsal must run settings-seed through the normal Docker entrypoint so local auto-secrets are exported, and seed desired-state via the import service rather than the broad CLI bootstrap; the CLI bootstrap can close the shared runtime storage pool before settings import validation finishes. Docker-internal first-party Postgres hostnames need explicit plaintext allowance while real remote Postgres still requires sslmode=require.
- [high] runner-sandbox-copy-lists: Runner-spawning tests (agent-runner-ipc, ipc-mcp-stdio) sandbox the runner by COPYING source files; hand-enumerated lists silently break when a new module becomes runner-reachable (every test then burns its ~19s IPC timeout). Fixed 2026-07-22 by recursive cpSync of the whole shared/ tree. If a runner-spawn test suite suddenly times out uniformly, check the sandbox copy set FIRST; never reintroduce per-file enumeration of a derivable file set.
- [low] prettier-before-factory-verify: Run the focused Prettier check after adding or reshaping TypeScript tests so the deterministic structural gate does not rediscover formatting drift.
- [medium] macos-sandbox-postgres-shared-memory: When Docker access is denied, a disposable local PostgreSQL 17 cluster can still be blocked during initdb because the sandbox rejects SysV shared memory; shared_memory_type and dynamic_shared_memory_type mmap options do not affect the bootstrap subprocess.
- [medium] unit-lane-nontermination: The full npm run test:unit lane can finish the LIVE-1 surfaces and then remain silent without a Vitest summary in this checkout; do not call it green without the exit code, and preserve focused lane evidence separately.
- [medium] managed-sandbox-junit-test-id: Managed command policy can reject required VITEST_JUNIT/FORGE_TEST_ID commands when the exact testcase identifier contains a literal greater-than separator. Report the wrapper lane as blocked and do not call it passing.
- [low] managed-test-id-redirection-policy: Managed command policy can refuse the required FORGE_TEST_ID when its exact test name contains a greater-than character; report the policy refusal separately from focused test evidence.
- [medium] nonterminating-vitest-blast-radius: A jobs or runner directory-wide Vitest invocation can remain live without a summary or exit code in the managed environment; terminate it after bounded polling and report it as unverified, not green.
- [medium] vitest-emfile-watcher: A focused Vitest selector can complete its test but still exit 1 when the macOS watcher hits EMFILE; treat this as blocked verification and rerun with the canonical wrapper or a watcher-safe environment.
- [high] JSONB persistence vs in-memory fakes: Postgres JSONB normalizes object key order and drops undefined; any equality check against persisted state (JSON.stringify ===) silently fails in prod while structuredClone-based fakes keep it green. Compare with util.isDeepStrictEqual and make repository fakes persist through a JSONB-faithful round trip (see test/unit/application/jsonb-round-trip.ts).
- [low] managed-env-prefix-policy: Managed execution can reject a required test when an inline environment assignment is the command prefix, reporting that the shell wrapper hides prefix inspection; invoke the same command through env so the executable prefix is visible, and report any remaining refusal separately.
- [low] managed-env-prefixed-test-policy: Managed command policy can refuse required Vitest commands when a leading environment assignment obscures the inspected prefix; preserve the refusal and run an equivalent direct Vitest JUnit command separately.
- [medium] Codex re-runs undo host-verified Postgres fixes: When a follow-up delegate carries a fix Codex cannot reproduce in its sandbox (Postgres-only hang or failure), state in the note that the earlier fixes are accepted and that only the named file may change, with the host-side proof; otherwise Codex reasons the fix away and reverts autoreview-accepted work (JOBPERM-3-T1 run 9).
- [low] readonly-vite-config-cache: In worktrees whose node_modules symlink targets a read-only sibling checkout, Vitest's default bundled config loader fails with EPERM in node_modules/.vite-temp before discovery; use --configLoader runner for scoped local evidence and report the canonical wrapper as environment-blocked.
- [high] large-delegate-context-budget: On a task touching 20+ files the Codex run hits the compaction limit after about 90 read commands; the source files are already complete and committed (checkpoint 5e060951a, tsc clean), so do NOT re-read finished source files — start from git status and the required_tests list, write or finish ONE test file at a time (run only that file with vitest), and stop reading the brief's cited source lines you have already implemented.
- [high] large-delegate-remaining-work: Remaining T2b work after the checkpoint is ONLY: (1) test/unit/runner/tool-permission-gate.test.ts:1347-1358 — re-point the CAPSAFE-1-BOUNDARY import from the deleted gantryToolDefaultRisk to gantryToolRisk and assert capability_run is high; (2) test/unit/runtime/permission-classifier.test.ts — replace the 'auto-approves a routine gantry mutation via the deterministic map' expectation (medium is gone) and make the lane-independent-high leaf pass (the consult stub must not be invoked for a table-judged tool); (3) create test/unit/runtime/browser-file-attach-source.test.ts; (4) the remaining required_tests leaves in ipc-permission-classifier-decision, ipc-file-artifact-handlers, file-tool and askfloor-tap-budget. Source files are complete and committed; do not re-read them.
- [high] large-delegate-missing-leaves: Exactly six required leaves are still missing; every source file and every other suite is complete, committed and green (tsc clean). Write ONLY these six it() titles verbatim, one file at a time, running only that file after each: - apps/core/test/unit/jobs/ipc-file-artifact-handlers.test.ts: it('hides protected entries from list with a protectedHidden count and refuses a protected read by id and by path with the fixed not-a-permission-question text') - apps/core/test/unit/runner/mcp/file-tool.test.ts: it('appends the protected-entries-hidden line to list output only when the count is positive and relays a protected read refusal unchanged') - apps/core/test/unit/runner/tool-permission-gate.test.ts: it('keeps capability_run high through the typed table for the CAPSAFE-1 boundary with no classifier-derived or cached allow') - apps/core/test/unit/runtime/askfloor-tap-budget.test.ts: it('TB1 TB2 TB3 TB4: a browser click, a file read by path, an unprotected file write and a native FileWrite inside the workspace cost 0 taps in interactive auto with the LLM consult not invoked') - apps/core/test/unit/runtime/askfloor-tap-budget.test.ts: it('mirror fixtures: a protected file write, a FileWrite outside the workspace and scheduler_delete_job cost 1 tap, and a raw-path file_attach reaches the stubbed LLM consult as ambiguous') - apps/core/test/unit/runtime/askfloor-tap-budget.test.ts: it('keeps today's outcome for the TB1 TB2 TB3 TB4 fixtures under auto_strict, ask and autonomous')
- [high] t2b-review-fixes: T2b review fixes ONLY (two quality-lens findings, both verified): (1) shared/permission-prospective-write.ts — path.resolve collapses  before the lstat walk, so  with  a symlink to outside is judged inside; fail closed: if the RAW candidate (before resolve) contains a  segment return outside('write target uses parent traversal'), keep the rest; add the symlink-plus-dot-segment case to permission-prospective-write.test.ts. (2) application/permissions/gantry-tool-risk.ts judgeBrowserAction returns low for ANY non-file action string; hoist the 20 actAction names from runner/mcp/tools/browser.ts into an exported readonly BROWSER_ACT_ACTIONS tuple in shared/browser-backend-actions.ts (the runner's z.enum consumes that tuple — one source of truth), and the table returns ambiguous('malformed browser request') for an action not in it; add  -> ambiguous to gantry-tool-risk.test.ts. Do not re-read the other scope files; run those two suites plus the runner browser tool suite and tsc.
- [high] t2b-review-round-2-fix: T2b review round 2, ONE P2 fix only: apps/core/src/shared/permission-prospective-write.ts:22 splits the raw candidate on path.sep, which on Windows is a backslash, so a forward-slash 'link/../victim' is not caught before path.resolve collapses it. Split on both separators (candidatePath.split(/[\\/]/).includes('..')) and add one test case with a forward-slash traversal to permission-prospective-write.test.ts. Do not read or edit any other file; run that suite and tsc.
- [high] t3a-implementation-order: T3a order that fits one Codex run: (1) schema.ts + port + repository + domain/types.ts + the shared boundary canonicalPath, then run npm run db:migrations:generate -- --name permission_decision_memory_human (NEVER hand-write the SQL or snapshot; commit what drizzle-kit emits), then npx tsc --noEmit; (2) the scope-key module and the service; (3) tests ONE FILE AT A TIME in this order — provenance, scope, service, prospective-write pin, Postgres suite — running each file right after writing it and never re-reading a finished source file. Postgres tests need the TEST database: run 'source /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh' in the same shell before vitest (it exports GANTRY_TEST_DATABASE_URL for gantry_test; the live database gantry must never be touched). Required leaf titles are exact it() names from the brief.
- [high] t3a-remaining-after-checkpoint: T3a source files are COMPLETE and committed (tsc + db:migrations:check green; the three new unit suites pass). Remaining work is ONLY tests, one file at a time, without re-reading finished sources: (1) test/unit/shared/permission-prospective-write.test.ts — the success result is now { inside: true, canonicalPath } so extend the existing leaf's inside expectations to include canonicalPath using the REAL path (fs.realpathSync of the workspace root, /private/var on macOS) and keep every refusal case; (2) test/integration/permission-decision-memory.postgres.integration.test.ts — add the three required Postgres leaves with their EXACT titles from the brief (partial keys + CHECK; per-person round-trips incl. refresh set, no reactivation, includeRevoked, revokeById across app/folder/person, countExactAllowsByTool; classifier cache byte-identical + legacy refusals + putHumanDecision guards), keeping the existing four-column pin; run it with 'source /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh' in the same shell then npx vitest run -c vitest.integration.postgres.config.ts <file>; (3) confirm the five unit leaves in the three new suites carry the EXACT required titles (rename it() names if they differ). Then stop.
- [high] t3a-architecture-crypto: ONE fix only: npm run check:architecture rejects application/permissions/human-decision-memory-service.ts:1 importing node:crypto (application permits no external imports). Move the default id source to a NEW tiny shared helper apps/core/src/shared/human-decision-id.ts exporting newHumanDecisionId(): string (randomUUID from node:crypto — shared may import it, see shared/auth-tokens.ts) and isHumanDecisionId(value): boolean (the v4 UUID shape guard), and import both from the service (keep newId injectable, default = newHumanDecisionId). Do not touch any other file; run npm run check:architecture, npx tsc --noEmit and the service unit suite, then stop.
- [high] t3a-domain-module-split: ONE fix only (contract v7): apps/core/src/domain/types.ts is at its 740-line architecture ceiling. Move EVERY T3a addition out of it into a NEW apps/core/src/domain/human-decision.ts (PermissionRememberResolution, PermissionApprovalResolution, isRememberResolution, resolutionMode, HumanDecisionRememberRequest, encode/decodeHumanDecisionProvenance, the HumanDecisionOutcome/HumanDecisionScope re-exports) and update every importer (the port, scope module, service, the provenance test) to import from domain/human-decision.js; in types.ts keep ONLY the 'human_decision' member appended on the existing prettier-ignore PermissionDecisionSource union line so the file is back to exactly 740 lines (wc -l). Then npm run check:architecture, npx tsc --noEmit and the three new unit suites; do not touch any other file; stop.
- [high] t3a-gate-environment: ONE fix only (contract v8 AC6): in vitest.integration.config.ts pass fileParallelism: process.env.GANTRY_TEST_DATABASE_URL ? false : undefined to makeVitestConfig (mirror the Postgres config's serial run) with a one-line comment that concurrent schema migrations against one database time out beforeAll hooks. Then prove it: 'source /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh && npx vitest run -c vitest.integration.config.ts' must be green (previously 11 suites hit Hook timed out in 10000ms), and 'npx tsc --noEmit'. Do not touch any other file; stop.
- [high] t3a-leaf-titles-regex-safe: ONE fix only (contract v9): two it() titles must be renamed to the EXACT required-test ids from the brief because the stage gate passes the id to vitest -t as a REGEX and parentheses match nothing: in permission-decision-memory.postgres.integration.test.ts the round-trips leaf now reads '... with the complete refresh set - outcome, decision, reason, label, effect hash, rail version, effect schema version, created_at and provenance re-encoded with the stored id - inserts a fresh row ...' and in human-decision-scope.test.ts the refusal-reason leaf now reads '... from the closed request table - read_only_command and file_read ... the trust-growth tool kind - and place keys from the canonical root'. Copy the ids from the brief verbatim; change nothing else; run the two files (Postgres one after sourcing /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh) and stop.
- [high] t3a-review-r1-write-race: TWO fixes only (review round 1). FIX 2 (performance P2): human-decision-memory-service.ts list() calls deriveHumanDecisionShortId(row.id, ids) per row, and that function re-normalises every sibling for every row and every prefix length — quadratic. Add an exported deriveHumanDecisionShortIds(ids: string[]): Map<string, string> that normalises each id ONCE, then resolves prefixes by counting: starting at length 6, build a Map<prefix, count> over the still-unresolved ids, mark ids whose prefix count is 1 as resolved at that length, and extend the rest by one character until resolved (O(n × average prefix length)); list() computes the map once and reads shortId from it; keep deriveHumanDecisionShortId(id, siblings) with its exact current semantics as a thin wrapper over the batch function (the contract and the unit leaf name it). Add one unit case: 500 ids with a shared 6-char prefix on two of them resolve in one call with only those two extended to 7. FIX 1 (quality P2): putHumanDecision does select-then-insert inside its transaction with no lock, so two concurrent FIRST writes for the same (app_id, agent_folder, acting_person_id, scope, scope_key) race the partial unique index and the loser throws instead of refreshing. Fix at the top of the transaction, before the select: await tx.execute(sql) — a transaction-scoped advisory lock keyed on the human authority, released at commit; the same pattern as canonical-message-attachment-lock.postgres.ts and live-admission-work-item-repository.postgres.ts — then keep the existing select/refresh/insert unchanged. Prove it in the Postgres suite with one new case inside the existing round-trips leaf (do NOT add a new it() and do NOT change any title): Promise.all of two putHumanDecision calls for the same key with different ids resolves to one inserted and one refreshed sharing the stored id, and exactly one active row exists. Run the file after 'source /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh'; do not touch any other file; stop.
- [high] t3a-raw-sql-allowlist: ONE fix only: the raw-SQL allowlist unit test under test/unit/storage enumerates the files allowed to call pg_advisory_xact_lock, each with a one-line justification comment; add the permission-decision-memory Postgres repository (adapters/storage/postgres/repositories) to that list with the comment: pg_advisory_xact_lock serializes concurrent first writes of one human decision authority so the loser refreshes instead of racing the partial unique index — in the same style as the neighbouring entries. Run that test file and stop; touch nothing else.
- [high] t3a-no-leftovers: OWNER RULING 2026-09-06 (we do not need legacy code): TWO removals only. (1) Delete the single-id deriveHumanDecisionShortId(id, siblings) wrapper from the human-decision memory service; remember() and list() call deriveHumanDecisionShortIds(...) directly and the unit test uses the batch function (keep every current case, including the 6-to-7 collision and the 500-id list). (2) Rename assertLegacyAccess to assertNonHumanKind in the Postgres decision-memory repository and reword every comment or doc sentence that says 'legacy' (port, repository, the architecture note) to 'non-human kinds' — those four methods are live code for classifier verdicts, remembered denies, trusted roots and standing grants, not compatibility code. No other change; run the service unit suite, the repository unit guards and tsc, then stop.
- [high] t3a-review-r2-legacy-wording: Review round 2 (post re-vendor, all three lenses): ONE P1 finding, the same in every lens — the Postgres integration test title and its fixture identifiers still say "legacy" for the non-human put, get, list and revoke (apps/core/test/integration/permission-decision-memory.postgres.integration.test.ts:36 and :80-94). Those methods are live access paths, and the contract's NO LEFTOVERS rule forbids "legacy" naming anywhere in the diff. The wording came from the contract's own leaf title; contract v12 renames the leaf to "non-human put, get, list and revoke" / "unfiltered non-human list". Do exactly this and nothing else: (1) rename that it(...) title to the v12 leaf title verbatim; (2) rename the fixture strings exact:legacy-guard, person-legacy-guard (both), effect-human-legacy-guard to exact:non-human-guard, person-non-human-guard, effect-human-non-human-guard; (3) grep the whole diff against origin/main for the word legacy (case-insensitive) and remove any other occurrence you introduced — do NOT touch pre-existing occurrences outside this task's files. Run the renamed leaf through the Postgres config (source the env file the brief names) and prettier on the file. No product-code change is expected.
- [high] t3a-review-r3-fixes: Review round 3 (quality lens) accepted two P2s; fix exactly these, nothing else. (1) human-decision-memory-service.ts remember(): after putHumanDecision the method lists the ACTIVE rows and reads the short id with a non-null assertion; a concurrent revoke between the two calls makes it undefined. Fix: derive the short ids over the sibling ids PLUS stored.id (dedupe; stored.id first or last does not matter, the derivation is order-independent) so the stored row always has a short id; drop the non-null assertion. Add one unit case in human-decision-memory-service.test.ts where the fake repository's list omits the stored row and the result still carries a six-hex-char shortId. (2) domain/ports/permission-decision-memory.ts exports an isHumanDecisionId that duplicates shared/human-decision-id.ts byte for byte. Delete the port copy; the repository (permission-decision-memory-repository.postgres.ts:24) and the service (:11) import isHumanDecisionId from shared/human-decision-id.js like domain/human-decision.ts already does. No wrapper, no re-export from the port (owner no-leftovers rule). Run tsc, the service unit suite, prettier, and npm run check:architecture (application and adapters importing shared is allowed; the layer rule only forbids node:crypto in application).
(3) Performance lens P2 accepted: shared/permission-prospective-write.ts:62 returns the syntactic candidate as canonicalPath even when realAncestor resolved an in-workspace symlink, so one physical file gets two path-only keys through its alias and its real path. Fix: build canonicalPath from the resolved ancestor plus the nonexistent suffix (the resolved candidate when it exists); keep the inside/outside verdict logic untouched. Add one case to test/unit/shared/permission-prospective-write.test.ts: an in-workspace symlink alias to a real directory yields canonicalPath under the real directory; compute expectations with fs.realpathSync of the temp workspace root (macOS resolves /tmp to /private/tmp). The separate finding about a non-partial history index for includeRevoked reads is DEFERRED to T5b (not yours).
- [high] t3a-review-r3-diagnostic-order-fixes: Diagnostic run (read-only) after review round 3 found three more items that ship with lesson 92's fixes; the contract is unchanged, only the ORDER of two existing rules. (4) human-decision-scope.ts deriveNativeWriteScope returns undefined (which the caller turns into the full-hash Allow key) when file_path or path is a non-string, BEFORE boundary-checking the string destination that IS present; so { file_path: 'settings.yaml', path: 1 } is remembered as a full-hash Allow instead of refusing protected_destination. Fix: boundary-check every supplied non-blank string destination FIRST (any outside ⇒ refused protected_destination), and only then apply the malformed-shape rule (⇒ undefined ⇒ full hash, as the contract says). Add the mixed malformed-plus-protected case to human-decision-scope.test.ts. (5) deriveVirtualWriteScope likewise returns undefined for a malformed companion field before checking whether an available string path (write path, promote_scratch targetPath) is a protected virtual entry via isProtectedArtifactEntry; check protection of every available string destination (and protected: true) before the malformed fallback; add the case. (6) human-decision-memory-service.test.ts:139 defines request twice in one fixture literal (the second at :146 silently wins) — remove the first. Everything else stays as lesson 92 says; no new enum member, no wrapper, no rename.
- [high] t3a-ci-postgres-hook-timeout: CI on PR 484 failed in the Postgres lane: apps/core/test/integration/permission-decision-memory.postgres.integration.test.ts beforeAll (createPostgresIntegrationRuntime, which runs the full migration set including the new snapshot) exceeded vitest's default 10 s hook timeout under CI load ("Hook timed out in 10000ms" at :26); the other 63 Postgres files passed because they declare a hook timeout. Do exactly this: add the sibling convention to that beforeAll — close it with }, 60_000); (see attachment-metadata.postgres.integration.test.ts for the same shape) — and nothing else: no afterAll change, no other file, no product code. Run prettier on the file; the Postgres leaves cannot run in the sandbox (EPERM 127.0.0.1:5432) — the host runs them.

### Recorded evidence

Recorded by the harness for this story (not in the diff). Use it to verdict verification contracts; do not mark them partial for lack of execution evidence in the bundle.

- verify.py: ok at d5a30163ad34
  - `npm run format:check && npm run check:architecture` -> exit 0
  - `npm run typecheck` -> exit 0
  - `npm test` -> exit 0
- automated tests: passed
  - ASKFLOOR-1-T3a automated proof: the eight required leaf tests (three through the Postgres config against gantry_test with GANTRY_TEST_DATABASE_URL exported, five unit) ran through the exact-leaf JUnit gate and pass; the new unit suites (scope keys, service, provenance) and the extended prospective-write and Postgres decision-memory suites pass; npm run db:migrations:check reports no drift for the generated migration; verify.py passed WITH GANTRY_TEST_DATABASE_URL exported (format:check, check:architecture, typecheck, full unit suite, the general integration lane now running DB-gated files serially when the URL is set — contract v8 AC6) and the full serial npm run test:integration:postgres lane is green (64 files / 385 tests). Before the lane fix the file-parallel general lane hit 11 beforeAll migration timeouts with the URL exported; that class is closed by this task. Review round 1's two P2s are fixed in-task: putHumanDecision takes a transaction-scoped advisory lock on the human authority (two concurrent first writes -> one inserted, one refreshed, one active row — Postgres case), and short ids derive in one batch pass (500-id collision case).
  - 13 command(s) recorded, e.g. `VITEST_JUNIT=1 npx vitest run -c vitest.integration.postgres.config.ts apps/core/test/integration/permission-decision-memory.postgres.integration.test.ts -t "enforces the partial keys: non-human rows keep the four-column key, two active human rows for one person and scope key reject, a revoked human row admits a fresh row, two persons coexist, and the human-row CHECK rejects each missing field while a missing label is accepted" --outputFile=<junit-report>`
