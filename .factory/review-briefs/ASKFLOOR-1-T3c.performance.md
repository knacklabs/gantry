# Review brief — ASKFLOOR-1-T3c — performance lens

You are one lens of a three-lens code review. You see ONLY the diff bundle for
this task (no repository access), so judge what the diff shows and say so when
something cannot be verified from it. Report every finding with its
file_path and line. Use ONLY these categories: bug, security, regression,
test_gap, maintainability. Priorities: P0/P1 block the task; P2/P3 must be
resolved or explicitly deferred with a reason before it ships.

LENS: PERFORMANCE. Hot paths, algorithmic complexity, query fanout (N+1),
I/O amplification, memory churn, concurrency bottlenecks, missing pagination or
bounds, work repeated per request that could be done once. Distinguish measured
evidence from inference and say which each finding is. Use category `bug` for a
performance defect that will bite in production and `maintainability` for a cost
worth reducing.

LEFTOVERS (blocking): the diff must carry no code kept only for compatibility — no wrapper or shim over its replacement, no re-export or alias kept 'for callers', no renamed-but-retained symbol, no dead branch behind a removed feature, no 'legacy'/'deprecated'/'backward' naming or comment. Report each as a BLOCKING finding with file:line and verdict the contract it belongs to as partial; a clean diff says so in one line.
## Task ASKFLOOR-1-T3c

### Plan contracts

- **ASKFLOOR-1-T3c-AC1**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3c.md#acceptance-criteria
  - Statement: CODEC + CARRIERS. NEW `application/permissions/permission-remember-codec.ts` exports `PERMISSION_REMEMBER_CODES`, `decodePermissionDecisionCode(value: string) → { mode: PermissionApprovalDecisionMode; remember?: PermissionRememberResolution } | null` (scalars pass through; `remember_allow_*` → `allow_once` + `{ outcome: allow, scope }`; `remember_deny_exact` → `cancel` + `{ outcome: deny, scope: exact }`; anything else → `null`) and `encodePermissionRememberCode(resolution) → PermissionRememberCode | null` (FAIL-CLOSED: `null` for any resolution without a code — a deny with kind or place scope — never a throw); `PermissionRememberCode` and the two widened carrier fields live in `domain/types.ts` line-neutrally (ruling 1); `telegram/channel-shared.ts` and `slack/permission-action-id.ts` accept and emit the codes in the scalar-mode slot. Unit tests: codec round-trip for the four codes and three scalars, `null` for unknown; the provider-affordance parity suite round-trips all four codes and all three scalars through BOTH owned codecs, including Slack action-id registration and Telegram's 64-byte maximum with a 36-character id.
- **ASKFLOOR-1-T3c-AC2**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3c.md#acceptance-criteria
  - Statement: CONTEXT + DURABLE PATH. NEW `application/permissions/human-decision-learning.ts` declares `PermissionRememberContext` and exports async `derivePermissionRememberContext(input) → Promise<PermissionRememberContext>` (ruling 3) and `parsePermissionRememberContext(value: unknown) → PermissionRememberContext | null` (complete validation: `eligible` boolean; `lane` in the closed lane set; `laneInput.permissionMode` in the closed mode set and `hostJobId` string or absent; `appId`, `agentFolder`, `canonicalTool` non-empty strings; `personId`/`personLabel` strings or absent; `effectHash` non-empty string; `effectSchemaVersion`, `railVersion` integers; `workspaceRoot` string or absent; `kindVariant` in the closed pair; each candidate a well-formed `HumanDecisionScopeKeyResult`; anything else → `null`); the `requestPermissionApproval` closure in `ipc-interaction-processing.ts` derives the context and awaits `updatePendingInteractionPayload` BEFORE delegating to the durable flow (ruling 3; the helper's `IpcDeps.requestPermissionApproval` signature at `ipc-permission-classifier-decision.ts:68` gains the optional typed second argument, and its two prompt exits pass it); the inline path derives it in `inline-permission-memory.ts` and persists it with the member; `bindPendingPermissionInteractionMessage` finalizes `eligible` from `matchKind` (ruling 3) and its exposed options accept the widened carrier; `claimPermissionInteractionCallback` persists the tapped value verbatim in `claim_mode`; `rememberSettlementForClaim(claim)` is the single accessor every settlement path uses (context + decoded resolution from the same claimed row); the Postgres mapper validates `claimMode` and every rendered option through the codec on read; recovery decodes the persisted code into the identical `{ mode, remember }`; `already_decided` never learns; the control API stays scalar-only (ruling 9). Tests: the context round-trips through the member payload, a batch binding turns it ineligible, and `rememberSettlementForClaim` returns context and resolution by claim and by recovery (unit, `pending-interaction-durability.test.ts`); claim persists a code and the mapper refuses an unknown `claim_mode` or rendered option (Postgres, `worker-coordination.postgres.integration.test.ts`); exact, category-kind and tool-kind candidates in a persisted context equal a fresh derivation including each typed refusal, `kindVariant` selects between the two kind candidates, a place code resolves `no_root`, the encoder returns `null` for a deny with kind or place scope, and a malformed context (each field class) parses to `null` and learns nothing while the scalar current-request result still settles (`human-decision-learning.test.ts`); the control API renders a remember code as its scalar base and rejects a code on its POST (NEW `test/unit/control/session-interaction-approvals.test.ts`).
- **ASKFLOOR-1-T3c-AC3**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3c.md#acceptance-criteria
  - Statement: LEARNING HELPER + SERVICE ENTRY. `human-decision-learning.ts` also exports `learnRememberedDecision({ context, resolution, service, warn }) → Promise<LearnResult>` with `LearnResult = { status: 'remembered'; id } | { status: 'not_eligible' } | { status: 'not_rememberable'; reason: HumanDecisionServiceRefusal } | { status: 'unlearned' }`: `not_eligible` unless `context.eligible`; selects the candidate per ruling 5 (a refused candidate → `not_rememberable` with its persisted typed reason) and calls `service.rememberDerived` with the ruling-5 DTO (person, label and write identity from the context, reason `remembered from a card tap`); a throwing port → one `warn` and `unlearned`; it never throws and never touches the current decision. `HumanDecisionMemoryService.rememberDerived(dto)` shares `remember`'s write path with the key supplied. Unit tests with a fake service: every `eligible` input, each code's candidate selection including category-kind versus tool-kind by `kindVariant`, each refused candidate relays its persisted reason, each service refusal relayed, port failure → `unlearned`; the service suite pins `rememberDerived` (same row as `remember` for the same key; identical refusals).
- **ASKFLOOR-1-T3c-AC4**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3c.md#acceptance-criteria
  - Statement: WRITE-THEN-APPLY CALLERS. (a) IPC: NEW `runtime/permission-remember-settlement.ts` (the IPC module is at its 865-line ceiling) called from `ipc-interaction-processing.ts` just before `applyPermissionInteractionDecision` (`:269`): decodes the settled mode, reads the member's context, calls `learnRememberedDecision` with a `HumanDecisionMemoryService` over the coordinator's `decisionMemory` port; the application and event sequence (`:269-382`) are byte-identical for remembered and once-only taps. (b) Inline: `inline-permission-memory.ts` gains `inlineRememberSettlement(...)` called from the existing `afterDecision` hook (`inline-agent-loop-tools.ts:483-543`, ≤ 6 changed lines); it never throws into `runDurablePermissionInteraction`. (c) Recovery: `pending-interaction-permission-callback.ts` calls the optional `learn` dependency just before the recovered apply (`:491`); `pending-interaction-durability.ts` threads it; `runtime-store.ts` configures it (ruling 7). Tests: IPC suite (`ipc-interaction-handler.test.ts`) — the context is persisted before the durable flow is delegated to and its lane and effect hash equal the helper's; a crash after the provider claim but before the helper returns still recovers with a context; an eligible remembered Allow persists before application with unchanged events; a remembered No persists and the current call is denied; `ask`, `auto_strict`, group (blank person), batch and scheduled-job prompts persist nothing (one case each); a provider double-delivery is `already_decided` with zero writes; a persisted-eligible context whose `laneInput` re-derives a non-`interactive_auto` lane writes nothing. Inline suite — eligible Allow on a non-scheduled auto run persists once with `personId`/`personLabel` from the run's host identity (label undefined when the run has none); scheduled run persists nothing; port failure leaves the decision applied. Recovery suite — a recovered remember code learns once through the same helper; an unauthorized tapper is rejected before the claim with no write and no application; an authorized code on an ineligible card settles its scalar base once with no write; a crash before learning learns on recovery; a crash after learning ends with one active row through the refresh. Wiring — `test/unit/storage/runtime-store.test.ts` proves the production `learn` dependency reaches the callback path.
- **ASKFLOOR-1-T3c-AC5**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3c.md#acceptance-criteria
  - Statement: S2. `askfloor-tap-budget-harness.ts` gains a person, a fake decision-memory port and a provider-neutral durable fixture (in-memory durability backend, claim with a code, learn, apply) so the first run traverses claim → learning → application; `askfloor-tap-budget.test.ts` adds S2 — `cd ~/Workdir/<repo> && ls && git log` under `interactive_auto` with a person: first run at most one prompt answered with `remember_allow_exact` → one claimed code and one active row; second identical run through the real coordinator → zero prompts.
- **ASKFLOOR-1-T3c-AC6**
  - Source: .factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3c.md#acceptance-criteria
  - Statement: existing unit and Postgres suites pass; tsc and check:architecture green; `ipc-interaction-processing.ts` ≤ 865, `inline-agent-loop-tools.ts` ≤ 750, `domain/types.ts` ≤ 740; no leftovers (no wrapper, shim, alias, re-export, dead branch or "legacy" naming).

### Reviewer focus

- Constitution (load-bearing): constitution/03-modular-monolith-structure.md and pnp-coding-standards-modular-monolith.md — closed typed code set (no free strings past the codec), application owns the learning helper, runtime/adapters stay thin callers; line ceilings in scripts/architecture-map.json (ipc-interaction-processing.ts 865, inline-agent-loop-tools.ts 750, domain/types.ts 740 — untouched).
- ONCE-ONLY + FORGERY (AC2, AC4 normative; 0124, 0138): the write happens only after the existing claim boundary and BEFORE the current request is applied (story plan order: IPC just before applyPermissionInteractionDecision; inline inside the existing afterDecision hook; recovery just before the recovered apply) and NEVER throws (fail-open: warn + once-only result); idempotency is the T3a refresh (a duplicate write refreshes the one active row); eligibility and every candidate key come from the ONE host-derived rememberContext attached to the member payload (IPC: via updatePendingInteractionPayload after the decision helper; inline: at creation) and finalized by the prompt binding from the real matchKind, attached BEFORE the durable flow binds and claims, by the requestPermissionApproval closure ipc-interaction-processing.ts injects into the decision helper (that helper file belongs to other tasks and is untouched) — no KEY is derived at settlement, only the lane is re-derived from the context's persisted laneInput and must be interactive_auto; the context type lives in the application learning module, not in domain; the person label comes from the same host identity source as the person id, never from a callback (0154:14); domain/types.ts changes line-neutrally as a recorded T3c shared-file exception (the owner T3a is shipped); the typed hand-off in ipc-permission-classifier-decision.ts (the IpcDeps.requestPermissionApproval signature at :68 plus both prompt exits :561 and :599) is a sanctioned narrow edit; T3c's forgery guarantee is the shared claim/recovery seam only (per-provider is T5a's); T3c derives NO place candidate (remember_allow_place resolves no_root until T5a); every settlement path reads ONE accessor rememberSettlementForClaim; an unauthorized tapper is rejected before the claim exactly as today; a remember code on an ineligible card (ask, auto_strict, group, job, blank person) degrades to its scalar base and persists nothing; already_decided never learns; a port failure keeps the once-only result (0154 ruling 3).
- KEY MATCH (AC3, AC5 normative): the write uses deriveHumanDecisionScopeKey with the same inputs the T3b consult uses and trustGrowthTool false, railVersion RAIL_CATALOG_VERSION, effectSchemaVersion EFFECT_SCHEMA_VERSION, person = host-stamped request.personId; place writes need the canonical root the caller supplies (T5a) and otherwise refuse no_root. OWNERSHIP: no file under channels/ other than telegram/channel-shared.ts and slack/permission-action-id.ts (every provider handler and renderer is T5a's); domain/types.ts changes line-neutrally (the code union on one line, two carrier fields widened in place, no move, no re-export) — S2 proves the second run finds it.
- NO LEFTOVERS (owner ruling 2026-09-06): no wrapper, shim, alias, retained symbol, dead branch or 'legacy' naming anywhere in the diff — a blocking finding.
- Verification: python3 factory/scripts/verify.py with GANTRY_TEST_DATABASE_URL exported; npm run test:integration:postgres; the worker-coordination leaf runs through vitest.integration.config.ts with GANTRY_TEST_DATABASE_URL exported (the default Postgres lane deliberately excludes that suite); the twelve leaves by exact title (no regex metacharacters in ids).

### Lessons in force

Recorded lessons that apply to this task's paths. A finding that contradicts one is not a defect unless it shows the lesson itself is wrong; say so explicitly instead of re-raising it.

- [medium] schema storage: Runtime state, jobs, control events, and memory use Postgres as the production storage model; schema or repository changes need repository tests and architecture/docs updates when contracts shift.
- [high] canonical-postgres-cutover: When replacing Postgres runtime schema in one cut, move active runtime persistence behind canonical Drizzle repositories/services in the same change; leaving schema-owned raw SQL or old table definitions creates drift and runtime failures after destructive migrations.
- [high] architecture boundaries: Keep provider-specific and channel-specific behavior behind adapters; domain and application code should depend on stable product concepts and ports, not SDK payloads or runtime wiring.
- [high] permission safety: Risky tool execution must pass through deterministic permission evaluation and sandbox policy before any provider callback or runner grants access.
- [medium] fleet-compose-rehearsal: Fleet compose rehearsal must run settings-seed through the normal Docker entrypoint so local auto-secrets are exported, and seed desired-state via the import service rather than the broad CLI bootstrap; the CLI bootstrap can close the shared runtime storage pool before settings import validation finishes. Docker-internal first-party Postgres hostnames need explicit plaintext allowance while real remote Postgres still requires sslmode=require.
- [low] prettier-before-factory-verify: Run the focused Prettier check after adding or reshaping TypeScript tests so the deterministic structural gate does not rediscover formatting drift.
- [medium] macos-sandbox-postgres-shared-memory: When Docker access is denied, a disposable local PostgreSQL 17 cluster can still be blocked during initdb because the sandbox rejects SysV shared memory; shared_memory_type and dynamic_shared_memory_type mmap options do not affect the bootstrap subprocess.
- [medium] unit-lane-nontermination: The full npm run test:unit lane can finish the LIVE-1 surfaces and then remain silent without a Vitest summary in this checkout; do not call it green without the exit code, and preserve focused lane evidence separately.
- [medium] managed-sandbox-junit-test-id: Managed command policy can reject required VITEST_JUNIT/FORGE_TEST_ID commands when the exact testcase identifier contains a literal greater-than separator. Report the wrapper lane as blocked and do not call it passing.
- [low] managed-test-id-redirection-policy: Managed command policy can refuse the required FORGE_TEST_ID when its exact test name contains a greater-than character; report the policy refusal separately from focused test evidence.
- [medium] nonterminating-vitest-blast-radius: A jobs or runner directory-wide Vitest invocation can remain live without a summary or exit code in the managed environment; terminate it after bounded polling and report it as unverified, not green.
- [medium] vitest-emfile-watcher: A focused Vitest selector can complete its test but still exit 1 when the macOS watcher hits EMFILE; treat this as blocked verification and rerun with the canonical wrapper or a watcher-safe environment.
- [high] JSONB persistence vs in-memory fakes: Postgres JSONB normalizes object key order and drops undefined; any equality check against persisted state (JSON.stringify ===) silently fails in prod while structuredClone-based fakes keep it green. Compare with util.isDeepStrictEqual and make repository fakes persist through a JSONB-faithful round trip (see test/unit/application/jsonb-round-trip.ts).
- [low] managed-env-prefix-policy: Managed execution can reject a required test when an inline environment assignment is the command prefix, reporting that the shell wrapper hides prefix inspection; invoke the same command through env so the executable prefix is visible, and report any remaining refusal separately.
- [low] managed-env-prefixed-test-policy: Managed command policy can refuse required Vitest commands when a leading environment assignment obscures the inspected prefix; preserve the refusal and run an equivalent direct Vitest JUnit command separately.
- [medium] Codex re-runs undo host-verified Postgres fixes: When a follow-up delegate carries a fix Codex cannot reproduce in its sandbox (Postgres-only hang or failure), state in the note that the earlier fixes are accepted and that only the named file may change, with the host-side proof; otherwise Codex reasons the fix away and reverts autoreview-accepted work (JOBPERM-3-T1 run 9).
- [high] Job permission card retire: persist delete failure before the receipt: When a retire delete fails, the provider delivery must surface deleteFailedAt through the partial-delivery error BEFORE any receipt call (Telegram partialFallback pattern); the receipt edit runs on the next retry. A logging-only onDeleteFailure lets a crash between delete and edit re-issue the delete.
- [high] every non-relaxable rail ASK stays a classifier veto: In the IPC merge helper treat EVERY base rail ASK as requiring approval and exempt only the two relaxable conditions (out_of_trusted_root; unsupported_meta_executor AND readOnlyMetaExecutor) via relaxesRailVeto — autoreview r2 P1: railRequiresApproval was false for soft ASKs such as RailSignal.Destructive, so a classifier allow reached the auto-allow path without rail provenance. Add a negative leaf per non-relaxable signal.
- [high] soft rail ASKs keep today's classifier eligibility; only hard-floor asks veto: CORRECTS lesson 66: T1 must not turn every non-relaxable rail ASK into a classifier veto. Today a non-hard-floor ASK (e.g. RailSignal.Destructive for an ordinary single-file delete) is classifier-eligible and cacheable — pinned by the existing rails test 'keeps an ordinary single-file delete eligible for classifier allow and caching' — and the T1 contract keeps that byte-for-byte. Veto = hard-floor asks (missing/redacted/truncated input, protected/secret paths, capability gates) exactly as before; relaxation = the two signals; everything else = today's behaviour, no new veto.
- [high] cached classifier allows are consumed only in classifier-eligible lanes: Gate cached-verdict consumption and any auto-allow from it on the lane: a classifier allow cached while in auto must never be reused after switching to permissionMode ask (ask mode has no classifier authority), and auto_strict keeps its veto behaviour — autoreview r3 P1: with every analyzed rail ASK now reaching the cache stage, an ordinary single-file delete cached in auto was executed as auto_classifier in ask mode without a prompt. Add an ask-lane cached-allow negative leaf.
- [high] large-delegate-context-budget: On a task touching 20+ files the Codex run hits the compaction limit after about 90 read commands; the source files are already complete and committed (checkpoint 5e060951a, tsc clean), so do NOT re-read finished source files — start from git status and the required_tests list, write or finish ONE test file at a time (run only that file with vitest), and stop reading the brief's cited source lines you have already implemented.
- [high] large-delegate-remaining-work: Remaining T2b work after the checkpoint is ONLY: (1) test/unit/runner/tool-permission-gate.test.ts:1347-1358 — re-point the CAPSAFE-1-BOUNDARY import from the deleted gantryToolDefaultRisk to gantryToolRisk and assert capability_run is high; (2) test/unit/runtime/permission-classifier.test.ts — replace the 'auto-approves a routine gantry mutation via the deterministic map' expectation (medium is gone) and make the lane-independent-high leaf pass (the consult stub must not be invoked for a table-judged tool); (3) create test/unit/runtime/browser-file-attach-source.test.ts; (4) the remaining required_tests leaves in ipc-permission-classifier-decision, ipc-file-artifact-handlers, file-tool and askfloor-tap-budget. Source files are complete and committed; do not re-read them.
- [high] large-delegate-missing-leaves: Exactly six required leaves are still missing; every source file and every other suite is complete, committed and green (tsc clean). Write ONLY these six it() titles verbatim, one file at a time, running only that file after each: - apps/core/test/unit/jobs/ipc-file-artifact-handlers.test.ts: it('hides protected entries from list with a protectedHidden count and refuses a protected read by id and by path with the fixed not-a-permission-question text') - apps/core/test/unit/runner/mcp/file-tool.test.ts: it('appends the protected-entries-hidden line to list output only when the count is positive and relays a protected read refusal unchanged') - apps/core/test/unit/runner/tool-permission-gate.test.ts: it('keeps capability_run high through the typed table for the CAPSAFE-1 boundary with no classifier-derived or cached allow') - apps/core/test/unit/runtime/askfloor-tap-budget.test.ts: it('TB1 TB2 TB3 TB4: a browser click, a file read by path, an unprotected file write and a native FileWrite inside the workspace cost 0 taps in interactive auto with the LLM consult not invoked') - apps/core/test/unit/runtime/askfloor-tap-budget.test.ts: it('mirror fixtures: a protected file write, a FileWrite outside the workspace and scheduler_delete_job cost 1 tap, and a raw-path file_attach reaches the stubbed LLM consult as ambiguous') - apps/core/test/unit/runtime/askfloor-tap-budget.test.ts: it('keeps today's outcome for the TB1 TB2 TB3 TB4 fixtures under auto_strict, ask and autonomous')
- [high] t3a-implementation-order: T3a order that fits one Codex run: (1) schema.ts + port + repository + domain/types.ts + the shared boundary canonicalPath, then run npm run db:migrations:generate -- --name permission_decision_memory_human (NEVER hand-write the SQL or snapshot; commit what drizzle-kit emits), then npx tsc --noEmit; (2) the scope-key module and the service; (3) tests ONE FILE AT A TIME in this order — provenance, scope, service, prospective-write pin, Postgres suite — running each file right after writing it and never re-reading a finished source file. Postgres tests need the TEST database: run 'source /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh' in the same shell before vitest (it exports GANTRY_TEST_DATABASE_URL for gantry_test; the live database gantry must never be touched). Required leaf titles are exact it() names from the brief.
- [high] t3a-remaining-after-checkpoint: T3a source files are COMPLETE and committed (tsc + db:migrations:check green; the three new unit suites pass). Remaining work is ONLY tests, one file at a time, without re-reading finished sources: (1) test/unit/shared/permission-prospective-write.test.ts — the success result is now { inside: true, canonicalPath } so extend the existing leaf's inside expectations to include canonicalPath using the REAL path (fs.realpathSync of the workspace root, /private/var on macOS) and keep every refusal case; (2) test/integration/permission-decision-memory.postgres.integration.test.ts — add the three required Postgres leaves with their EXACT titles from the brief (partial keys + CHECK; per-person round-trips incl. refresh set, no reactivation, includeRevoked, revokeById across app/folder/person, countExactAllowsByTool; classifier cache byte-identical + legacy refusals + putHumanDecision guards), keeping the existing four-column pin; run it with 'source /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh' in the same shell then npx vitest run -c vitest.integration.postgres.config.ts <file>; (3) confirm the five unit leaves in the three new suites carry the EXACT required titles (rename it() names if they differ). Then stop.
- [high] t3a-architecture-crypto: ONE fix only: npm run check:architecture rejects application/permissions/human-decision-memory-service.ts:1 importing node:crypto (application permits no external imports). Move the default id source to a NEW tiny shared helper apps/core/src/shared/human-decision-id.ts exporting newHumanDecisionId(): string (randomUUID from node:crypto — shared may import it, see shared/auth-tokens.ts) and isHumanDecisionId(value): boolean (the v4 UUID shape guard), and import both from the service (keep newId injectable, default = newHumanDecisionId). Do not touch any other file; run npm run check:architecture, npx tsc --noEmit and the service unit suite, then stop.
- [high] t3a-domain-module-split: ONE fix only (contract v7): apps/core/src/domain/types.ts is at its 740-line architecture ceiling. Move EVERY T3a addition out of it into a NEW apps/core/src/domain/human-decision.ts (PermissionRememberResolution, PermissionApprovalResolution, isRememberResolution, resolutionMode, HumanDecisionRememberRequest, encode/decodeHumanDecisionProvenance, the HumanDecisionOutcome/HumanDecisionScope re-exports) and update every importer (the port, scope module, service, the provenance test) to import from domain/human-decision.js; in types.ts keep ONLY the 'human_decision' member appended on the existing prettier-ignore PermissionDecisionSource union line so the file is back to exactly 740 lines (wc -l). Then npm run check:architecture, npx tsc --noEmit and the three new unit suites; do not touch any other file; stop.
- [high] t3a-review-r1-write-race: TWO fixes only (review round 1). FIX 2 (performance P2): human-decision-memory-service.ts list() calls deriveHumanDecisionShortId(row.id, ids) per row, and that function re-normalises every sibling for every row and every prefix length — quadratic. Add an exported deriveHumanDecisionShortIds(ids: string[]): Map<string, string> that normalises each id ONCE, then resolves prefixes by counting: starting at length 6, build a Map<prefix, count> over the still-unresolved ids, mark ids whose prefix count is 1 as resolved at that length, and extend the rest by one character until resolved (O(n × average prefix length)); list() computes the map once and reads shortId from it; keep deriveHumanDecisionShortId(id, siblings) with its exact current semantics as a thin wrapper over the batch function (the contract and the unit leaf name it). Add one unit case: 500 ids with a shared 6-char prefix on two of them resolve in one call with only those two extended to 7. FIX 1 (quality P2): putHumanDecision does select-then-insert inside its transaction with no lock, so two concurrent FIRST writes for the same (app_id, agent_folder, acting_person_id, scope, scope_key) race the partial unique index and the loser throws instead of refreshing. Fix at the top of the transaction, before the select: await tx.execute(sql) — a transaction-scoped advisory lock keyed on the human authority, released at commit; the same pattern as canonical-message-attachment-lock.postgres.ts and live-admission-work-item-repository.postgres.ts — then keep the existing select/refresh/insert unchanged. Prove it in the Postgres suite with one new case inside the existing round-trips leaf (do NOT add a new it() and do NOT change any title): Promise.all of two putHumanDecision calls for the same key with different ids resolves to one inserted and one refreshed sharing the stored id, and exactly one active row exists. Run the file after 'source /private/tmp/claude-501/-Users-ravikiranvemula-Workdir-myclaw/b4051e43-dbea-4d62-ba73-ae6210455474/scratchpad/pgfix-env.sh'; do not touch any other file; stop.
- [high] t3a-no-leftovers: OWNER RULING 2026-09-06 (we do not need legacy code): TWO removals only. (1) Delete the single-id deriveHumanDecisionShortId(id, siblings) wrapper from the human-decision memory service; remember() and list() call deriveHumanDecisionShortIds(...) directly and the unit test uses the batch function (keep every current case, including the 6-to-7 collision and the 500-id list). (2) Rename assertLegacyAccess to assertNonHumanKind in the Postgres decision-memory repository and reword every comment or doc sentence that says 'legacy' (port, repository, the architecture note) to 'non-human kinds' — those four methods are live code for classifier verdicts, remembered denies, trusted roots and standing grants, not compatibility code. No other change; run the service unit suite, the repository unit guards and tsc, then stop.
- [high] t3a-review-r3-fixes: Review round 3 (quality lens) accepted two P2s; fix exactly these, nothing else. (1) human-decision-memory-service.ts remember(): after putHumanDecision the method lists the ACTIVE rows and reads the short id with a non-null assertion; a concurrent revoke between the two calls makes it undefined. Fix: derive the short ids over the sibling ids PLUS stored.id (dedupe; stored.id first or last does not matter, the derivation is order-independent) so the stored row always has a short id; drop the non-null assertion. Add one unit case in human-decision-memory-service.test.ts where the fake repository's list omits the stored row and the result still carries a six-hex-char shortId. (2) domain/ports/permission-decision-memory.ts exports an isHumanDecisionId that duplicates shared/human-decision-id.ts byte for byte. Delete the port copy; the repository (permission-decision-memory-repository.postgres.ts:24) and the service (:11) import isHumanDecisionId from shared/human-decision-id.js like domain/human-decision.ts already does. No wrapper, no re-export from the port (owner no-leftovers rule). Run tsc, the service unit suite, prettier, and npm run check:architecture (application and adapters importing shared is allowed; the layer rule only forbids node:crypto in application).
(3) Performance lens P2 accepted: shared/permission-prospective-write.ts:62 returns the syntactic candidate as canonicalPath even when realAncestor resolved an in-workspace symlink, so one physical file gets two path-only keys through its alias and its real path. Fix: build canonicalPath from the resolved ancestor plus the nonexistent suffix (the resolved candidate when it exists); keep the inside/outside verdict logic untouched. Add one case to test/unit/shared/permission-prospective-write.test.ts: an in-workspace symlink alias to a real directory yields canonicalPath under the real directory; compute expectations with fs.realpathSync of the temp workspace root (macOS resolves /tmp to /private/tmp). The separate finding about a non-partial history index for includeRevoked reads is DEFERRED to T5b (not yours).
- [high] t3a-review-r3-diagnostic-order-fixes: Diagnostic run (read-only) after review round 3 found three more items that ship with lesson 92's fixes; the contract is unchanged, only the ORDER of two existing rules. (4) human-decision-scope.ts deriveNativeWriteScope returns undefined (which the caller turns into the full-hash Allow key) when file_path or path is a non-string, BEFORE boundary-checking the string destination that IS present; so { file_path: 'settings.yaml', path: 1 } is remembered as a full-hash Allow instead of refusing protected_destination. Fix: boundary-check every supplied non-blank string destination FIRST (any outside ⇒ refused protected_destination), and only then apply the malformed-shape rule (⇒ undefined ⇒ full hash, as the contract says). Add the mixed malformed-plus-protected case to human-decision-scope.test.ts. (5) deriveVirtualWriteScope likewise returns undefined for a malformed companion field before checking whether an available string path (write path, promote_scratch targetPath) is a protected virtual entry via isProtectedArtifactEntry; check protection of every available string destination (and protected: true) before the malformed fallback; add the case. (6) human-decision-memory-service.test.ts:139 defines request twice in one fixture literal (the second at :146 silently wins) — remove the first. Everything else stays as lesson 92 says; no new enum member, no wrapper, no rename.
- [high] t3b-run1-continue: Run 1 of ASKFLOOR-1-T3b died mid-turn (companion failure) after writing most of the implementation; the working tree ALREADY holds it uncommitted: modified port, Postgres repository, provenance map, coordinator, inline tool types, inline tools file, scope helper, the Postgres/service/scope tests and the architecture doc, plus NEW runtime/permission-human-memory-stage.ts, app/bootstrap/inline-permission-memory.ts and test/unit/runtime/permission-human-memory-stage.test.ts. CONTINUE from that tree — never reset, never re-create files that exist; read them first. Run 1's last note: the core types and source wiring compile; the six contract-named leaves were being added (stage leaf first). Finish: the six leaves by their exact titles (ids are matched verbatim by the gate), the inline suite cases, the inline-permission-memory unit suite, tsc, check:architecture, prettier, and keep inline-agent-loop-tools.ts under 750 lines. Postgres leaves cannot run in the sandbox (EPERM 127.0.0.1:5432); the host runs them — do not raise a blocked signal for that.
- [high] t3c-run2-continue: Run 2 of ASKFLOOR-1-T3c died mid-turn on the Codex compaction bug after a small start; the working tree ALREADY holds it uncommitted: apps/core/src/application/permissions/human-decision-memory-service.ts apps/core/src/domain/types.ts apps/core/src/application/permissions/human-decision-learning.ts apps/core/src/application/permissions/permission-remember-codec.ts . CONTINUE from that tree — never reset, never re-create files that exist; read them first. Work in this order so each compaction-sized chunk lands whole: (1) codec module + the three line-neutral domain/types.ts edits + the two owned provider codecs; (2) the learning module (context type, derive/parse, learn, rememberDerived on the service); (3) the durable path (member payload, binding finalization, claim, accessor, mapper validation, recovery learn + runtime-store wiring); (4) the IPC hand-off (IpcDeps signature + both prompt exits) and the IPC settlement helper; (5) the inline path; (6) the twelve leaves by their exact titles; then tsc, check:architecture, prettier. Postgres leaves cannot run in the sandbox (EPERM 127.0.0.1:5432) — the host runs them; do not raise a blocked signal for that. The personLabel question is resolved (S-0082): undefined on both paths, no carrier change.
- [high] t3c-run3-continue: Run 3 of ASKFLOOR-1-T3c also died on the Codex compaction bug; the tree now holds 18 modified files plus NEW permission-remember-codec.ts, human-decision-learning.ts, permission-remember-settlement.ts and their three suites (codec, learning, control). Run 3's last note: the IPC and recovery chokepoints share the persisted claim accessor, the context is written before delegation, learning runs after the claim and immediately before application, and runtime-store supplies the decision-memory repository. CONTINUE from that tree — never reset. Remaining, in order: the inline path (inline-permission-memory.ts context derivation + afterDecision learning; inline file under 750 lines), the Postgres mapper validation + its integration case, the provider-affordance parity case, the S2 harness/fixture, then the twelve leaves by their exact titles, tsc, check:architecture, prettier. Commit nothing; the host commits. Keep each step small so a compaction cannot lose it.
- [high] t3c-run4-continue-ceilings: Run 4 of ASKFLOOR-1-T3c died on the Codex compaction bug with the bulk of the task in the tree: 26 files, about 1,700 lines, uncommitted. CONTINUE from that tree — never reset. Two ceilings are breached right now and check:architecture will fail until they are fixed: apps/core/src/domain/types.ts is 748 lines against 740 (the contract requires LINE-NEUTRAL edits there: declare PermissionRememberCode on one line and join existing multi-line declarations so the file returns to exactly 740 or fewer; nothing else may grow it) and apps/core/src/app/bootstrap/inline-agent-loop-tools.ts is 753 against 750 (move any added inline logic into inline-permission-memory.ts; the inline file may only gain the handful of call lines). ipc-interaction-processing.ts is 851/865 — fine. Then finish what remains (check each: the S2 harness/fixture, the Postgres mapper validation case, the parity case, any of the twelve leaves not yet green), run tsc, check:architecture, prettier. Commit nothing; the host commits.
- [high] t3c-verify-regression-binding: The host verify of ASKFLOOR-1-T3c FAILED: npm test has 47 regressions in 4 suites (channels/slack.test.ts, channels/teams/teams.test.ts, jobs/ipc-handler.test.ts, runtime/ipc-locked-permission-denial.test.ts) with one root cause — bindPendingPermissionInteractionMessage (pending-interaction-prompt-binding.ts:75) now calls active.repository.updatePendingInteractionPayload unconditionally to finalize the remember eligibility, so every existing prompt flow whose durability fake or backend has no such method (and whose members carry NO remember context) throws "Pending permission prompt binding could not be persisted" and 47 downstream expectations break. Fix the root cause, not the callers: finalize eligibility ONLY when the bound members actually carry a rememberContext in their payload; a prompt without a context must take exactly today's binding path (no update call, no new persistence, byte-identical behaviour) — that is the contract's own rule that context-less prompts are unchanged. Do NOT guard with a typeof-function check on the repository (that is a shim); do NOT touch the four failing suites. Then run the FULL unit suite (npm test) and confirm 0 failures before reporting — the focused suites were green while the full suite was red. Keep all twelve leaves green; tsc, check:architecture, prettier.
- [high] t3c-batch-finalize-always: Run 7's binding fix is too narrow: bindPendingPermissionInteractionMessage finalizes eligible=false for a batch ONLY when the rendered decisionOptions include a remember code. The contract (ruling 3, AC2) says EVERY coalesced batch ends ineligible: a batch member whose context is provisionally eligible must be set eligible=false whether or not a code is rendered (today no card renders codes, so the current condition never fires, and a forged remember code on a batch card would learn). Fix: drop the decisionOptions condition — for matchKind === 'batch', update every member that carries a parseable rememberContext to eligible=false; members without a context are untouched (no update call, today's path). Keep individual prompts exactly as they are (no update). Add the batch case to the durability suite's context round-trip leaf (a batch binding turns an eligible context ineligible; a batch without contexts makes no update call). Then re-run the 12 leaves, tsc, check:architecture, prettier. Do not run npm test in the sandbox (EMFILE); the host runs it.

### Recorded evidence

Recorded by the harness for this story (not in the diff). Use it to verdict verification contracts; do not mark them partial for lack of execution evidence in the bundle.

- verify.py: ok at d5a30163ad34
  - `npm run format:check && npm run check:architecture` -> exit 0
  - `npm run typecheck` -> exit 0
  - `npm test` -> exit 0
- automated tests: passed
  - ASKFLOOR-1-T3a automated proof: the eight required leaf tests (three through the Postgres config against gantry_test with GANTRY_TEST_DATABASE_URL exported, five unit) ran through the exact-leaf JUnit gate and pass; the new unit suites (scope keys, service, provenance) and the extended prospective-write and Postgres decision-memory suites pass; npm run db:migrations:check reports no drift for the generated migration; verify.py passed WITH GANTRY_TEST_DATABASE_URL exported (format:check, check:architecture, typecheck, full unit suite, the general integration lane now running DB-gated files serially when the URL is set — contract v8 AC6) and the full serial npm run test:integration:postgres lane is green (64 files / 385 tests). Before the lane fix the file-parallel general lane hit 11 beforeAll migration timeouts with the URL exported; that class is closed by this task. Review round 1's two P2s are fixed in-task: putHumanDecision takes a transaction-scoped advisory lock on the human authority (two concurrent first writes -> one inserted, one refreshed, one active row — Postgres case), and short ids derive in one batch pass (500-id collision case).
  - 13 command(s) recorded, e.g. `VITEST_JUNIT=1 npx vitest run -c vitest.integration.postgres.config.ts apps/core/test/integration/permission-decision-memory.postgres.integration.test.ts -t "enforces the partial keys: non-human rows keep the four-column key, two active human rows for one person and scope key reject, a revoked human row admits a fresh row, two persons coexist, and the human-row CHECK rejects each missing field while a missing label is accepted" --outputFile=<junit-report>`
