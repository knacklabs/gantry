{
  "generated_by": "docs-decomposer",
  "user_facing": true,
  "story": "ASKFLOOR-1",
  "epic": "permission-engine",
  "plan_file": "plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md",
  "epics": [
    {
      "id": "permission-engine",
      "title": "Permission engine",
      "objective": "Auto mode behaves like Claude Code auto mode: reads never tap, the judge decides unknowns, only genuine risk asks, every human decision is learned.",
      "source_refs": [
        "docs/specs/askfloor-1-judge-actually-judges.md",
        "plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md"
      ]
    }
  ],
  "tasks": [
    {
      "id": "ASKFLOOR-1-T1",
      "title": "Routing: the judge's verdict stands in interactive auto",
      "objective": "In interactive auto (permissionMode auto, no hostJobId) a rail ASK for out_of_trusted_root or a read-only find no longer vetoes a classifier allow; one typed AutoLaneAnalysis per request; the shared parser stays fail-closed (nine write-capable find actions refused); 2>/dev/null is never a path candidate; the IPC tail is pre-split into five named helpers; the AF-AC8 replay harness is scaffolded with S3.",
      "acceptance_criteria": [
        "a classifier low/medium allow is honoured with the rail reason as typed provenance only when `permissionMode` is `auto` and `hostJobId` is absent; `auto_strict`, ask and job lanes keep the veto (explicit tests).",
        "`find` with any of `-delete -exec -execdir -ok -okdir -fls -fprint -fprint0 -fprintf` stays refused by the shared parser; the rails' existing meta-executor refusal now carries the canonical typed signal `unsupported_meta_executor` (today it emits `privileged`, `domain/permission-deterministic-rails.ts:41-46,164-171`; verdict unchanged, A-0070) and a read-only `find` makes the analyzer's `readOnlyMetaExecutor` true; `pathCandidates` ignores stderr redirects to `/dev/null` with per-lane tests (assumption A-askfloor-1: an outcome may change only where a non-path stops being treated as a path).",
        "the ONE authoritative decision order \u2014 pre-coordination route analysis \u2192 exact remembered deny [T3b no-op slot] \u2192 hard restrictions \u2192 reviewed rules \u2192 deterministic rails (the single base evaluation) \u2192 conditional trusted-root handling (only on the rails' `out_of_trusted_root` ASK; its hypothetical rechecks unchanged) \u2192 remembered allows [T3b no-op slot] \u2192 classifier-cache read \u2192 tail \u2014 is the coordinator/tail contract (`permission-decision-coordinator.ts:97-200` enumerated as named stages, pinned by a stage-order test), with the base rail decision computed once and passed to the tail in an immutable typed context.",
        "the AF-AC8 replay harness exists as a reusable fixture module; S3 (`2>/dev/null` + read-only `find`) replays at 0 taps in interactive auto; later tasks add fixtures without editing T1's tests.",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green."
      ],
      "depends_on": [],
      "user_facing": false,
      "plan_contracts": [
        {
          "id": "ASKFLOOR-1-T1-AC1",
          "statement": "a classifier low/medium allow is honoured with the rail reason as typed provenance only when `permissionMode` is `auto` and `hostJobId` is absent; `auto_strict`, ask and job lanes keep the veto (explicit tests).",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T1.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T1-AC2",
          "statement": "`find` with any of `-delete -exec -execdir -ok -okdir -fls -fprint -fprint0 -fprintf` stays refused by the shared parser; the rails' existing meta-executor refusal now carries the canonical typed signal `unsupported_meta_executor` (today it emits `privileged`, `domain/permission-deterministic-rails.ts:41-46,164-171`; verdict unchanged, A-0070) and a read-only `find` makes the analyzer's `readOnlyMetaExecutor` true; `pathCandidates` ignores stderr redirects to `/dev/null` with per-lane tests (assumption A-askfloor-1: an outcome may change only where a non-path stops being treated as a path).",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T1.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T1-AC3",
          "statement": "the ONE authoritative decision order \u2014 pre-coordination route analysis \u2192 exact remembered deny [T3b no-op slot] \u2192 hard restrictions \u2192 reviewed rules \u2192 deterministic rails (the single base evaluation) \u2192 conditional trusted-root handling (only on the rails' `out_of_trusted_root` ASK; its hypothetical rechecks unchanged) \u2192 remembered allows [T3b no-op slot] \u2192 classifier-cache read \u2192 tail \u2014 is the coordinator/tail contract (`permission-decision-coordinator.ts:97-200` enumerated as named stages, pinned by a stage-order test), with the base rail decision computed once and passed to the tail in an immutable typed context.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T1.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T1-AC4",
          "statement": "the AF-AC8 replay harness exists as a reusable fixture module; S3 (`2>/dev/null` + read-only `find`) replays at 0 taps in interactive auto; later tasks add fixtures without editing T1's tests.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T1.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T1-AC5",
          "statement": "existing unit and Postgres integration suites pass; tsc and check:architecture green.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T1.md#acceptance-criteria"
        }
      ],
      "write_scope": [
        "apps/core/src/runtime/ipc-permission-classifier-decision.ts",
        "apps/core/src/application/permissions/auto-lane-analysis.ts",
        "apps/core/src/application/permissions/auto-lane-analysis-types.ts",
        "apps/core/src/shared/permission-trusted-paths.ts",
        "apps/core/src/runtime/permission-decision-coordinator.ts",
        "apps/core/src/domain/types.ts",
        "apps/core/src/domain/permission-lane.ts",
        "apps/core/src/domain/permission-deterministic-rails.ts",
        "apps/core/src/shared/ipc-signing.ts",
        "apps/core/src/runner/permission-ipc-client.ts",
        "apps/core/src/adapters/llm/anthropic-claude-agent/runner/permission-callback.ts",
        "apps/core/src/adapters/llm/anthropic-claude-agent/runner/types.ts",
        "apps/core/test/unit/runner/permission-ipc-client.test.ts",
        "apps/core/test/unit/runner/permission-callback.test.ts",
        "apps/core/test/unit/shared/ipc-signing.test.ts",
        "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
        "apps/core/test/unit/runtime/permission-decision-coordinator.test.ts",
        "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
        "apps/core/test/unit/runtime/askfloor-tap-budget-harness.ts",
        "apps/core/test/unit/application/auto-lane-analysis.test.ts",
        "apps/core/test/unit/shared/permission-deterministic-rails.test.ts",
        "apps/core/test/unit/shared/bash-command-parser.test.ts"
      ],
      "verify_commands": [
        "python3 factory/scripts/verify.py",
        "npx tsc --noEmit",
        "npm run check:architecture"
      ],
      "review_budget": {
        "max_changed_files": 26,
        "max_changed_lines": 1800,
        "reason": "S-0066: the grilled contract (9 rounds) spans 12 source files (IPC helper pre-split into five helpers, analyzer + types, domain lane enums, rails signal, coordinator seam, trusted-paths fix, railProvenance across domain types + IPC signing + two runner decoders) and 10 test files incl. the AF-AC8 harness module; 16 leaf tests are required proof, not creep. Raised from the 8-file default by the orchestrator per the owner's standing 'raise budget + re-grill' ruling."
      },
      "required_tests": [
        {
          "id": "honours a classifier allow over an out_of_trusted_root rail signal only in interactive auto: low",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "keeps the rail veto for out_of_trusted_root in auto_strict, ask and job lanes",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "honours a classifier allow for an unsupported_meta_executor refusal of a read-only find only in interactive auto and keeps the veto in auto_strict, ask and job lanes",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "emits the canonical unsupported_meta_executor rail signal for a meta-executor refusal with the ASK verdict unchanged",
          "path": "apps/core/test/unit/shared/permission-deterministic-rails.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "derives readOnlyMetaExecutor true for find without any of the nine write-capable actions only",
          "path": "apps/core/test/unit/application/auto-lane-analysis.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "refuses every one of the nine write-capable find actions as a meta-executor",
          "path": "apps/core/test/unit/shared/bash-command-parser.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "treats a stderr redirect to /dev/null as a non-path in every lane: auto",
          "path": "apps/core/test/unit/shared/permission-deterministic-rails.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "evaluates the rails once and threads the completed AutoLaneAnalysis unchanged into the tail",
          "path": "apps/core/test/unit/runtime/permission-decision-coordinator.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "keeps the veto in interactive auto for find with a sensitive shape in the first operand, a later operand or a predicate value, for .., hidden and secret names, for -H -L or -follow, for -files0-from or any indirect root option, any redirect, a compound or a pipeline, and allows a bare find dot and a find under src with a name glob",
          "path": "apps/core/test/unit/application/auto-lane-analysis.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "leaves ask, auto_strict and job-lane outcomes unchanged for 2>/dev/null except where the non-path stopped being a path",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "carries railProvenance through the signed IPC decision payload and the generic runner decoder",
          "path": "apps/core/test/unit/runner/permission-ipc-client.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "carries railProvenance through the Anthropic runner permission decoder",
          "path": "apps/core/test/unit/runner/permission-callback.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "derives the analysis exactly once before coordination and passes it unchanged into the tail context",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "runs the complete decision order as named stages with the T3b no-op slots in place",
          "path": "apps/core/test/unit/runtime/permission-decision-coordinator.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "keeps the veto for a safe-looking find when the base rail ASK is missing, redacted or truncated input",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "S3: 2>/dev/null and read-only find cost 0 taps in interactive auto",
          "path": "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        }
      ],
      "reviewer_focus": [
        "Constitution (load-bearing): constitution/README.md -> pnp-coding-standards-modular-monolith.md (types/enums/errors/data-access/thin coordinator in separate files; typed enums over string literals) and 03-modular-monolith-structure.md (application vs runtime vs shared placement). Conformance, not a new layout.",
        "PermissionLane, RailSignal and RailProvenance are DOMAIN-owned closed types (domain/permission-lane.ts) because domain/types.ts consumes them and domain never imports application (architecture-map.json:79-92); AutoLaneAnalysis is a typed value object whose input/output shapes live in application/permissions/auto-lane-analysis-types.ts; the analyzer is a GENUINELY PURE function (no filesystem, no I/O) whose readOnlyMetaExecutor is the HARD-BOUNDARY predicate: single simple find, no compound/pipeline/redirect/subshell, none of the nine write-capable actions, none of -H/-L/-follow, no indirect or unknown root-source option (-files0-from and the like fail closed), and NO operand \u2014 every positional path and every predicate value (-name, -iname, -path, -regex, -newer, -samefile, ...) \u2014 matching the conservative pure string predicate isSensitivePathShape (protected capability-path patterns; any segment starting with '.' except the bare '.' so `find .` passes and `..`/`.env` fail; well-known secret file names \u2014 a superset of the filesystem-backed checks; TODO(T2a) unify).",
        "Rails ownership (assumption A-0070): domain/permission-deterministic-rails.ts is a declared T1/T2a shared file; T1 owns ONLY the typed RailSignal value unsupported_meta_executor and its emission for the existing meta-executor refusal (today privileged, :41-46,164-171) with the ASK verdict unchanged \u2014 no widening; T2a owns the attachment_open birthright row.",
        "T1 is IPC-ONLY (assumption A-0069: the coordinator is a declared T1/T3b shared file, T1 limited to the optional analysis/context seam): the IPC helper computes the pre-analysis once in a NEW pre-coordination step before calling the coordinator (:95-170), distinct from the tail's route guard (:184-218) which only consumes the completed context (it alone knows permissionMode, hostJobId and the resolved route mode) and passes it to the coordinator as an OPTIONAL typed input analysis?: AutoLaneAnalysis; the coordinator derives no lane facts, and with an analysis runs ONE base rail evaluation and calls tail(context) with the immutable typed context { analysis, railDecision } replacing today's zero-argument tail; without one (inline-agent-loop-tools.ts:389-395, core-tool-permission-coordinator.ts:61) behaviour is byte-for-byte today's; trusted-root hypothetical rechecks (permission-decision-coordinator.ts:232-268) stay unchanged and are not the base evaluation.",
        "railProvenance is CANONICAL provenance: an optional typed field on PermissionApprovalDecision (domain/types.ts:298-311 \u2014 T1's only addition to that file; T3a's later additions are sequential), included in the signed IPC decision payload (shared/ipc-signing.ts:10-22) and decoded by ALL runner decoders \u2014 the generic IPC client (runner/permission-ipc-client.ts:59-75,371-390) and the Anthropic runner decoder (adapters/llm/anthropic-claude-agent/runner/permission-callback.ts:301-387, result type runner/types.ts:119-144); never a host-local side channel.",
        "The coordinator's authoritative decision order is enumerated as named stages \u2014 pre-coordination route analysis, [T3b] exact remembered deny, hard restrictions, reviewed rules, deterministic rails (the single base evaluation), conditional trusted-root handling (only on the rails' out_of_trusted_root ASK; hypothetical rechecks unchanged), [T3b] remembered allows, classifier-cache read, tail \u2014 and pinned by a stage-order test; one analysis per IPC permission request, other callers pass none; the harness return type uses PermissionApprovalDecision['decidedBy'], PermissionDecisionSource (domain/types.ts:255) and RailProvenance.",
        "The IPC tail becomes a thin sequence of five named helpers (route guard, classifier consult, rail-veto/risk merge, cache writeback, prompt/terminal) with explicit typed inputs/outputs; the relaxation lives ONLY in the merge helper, keyed on the base rail decision's typed signal \u2014 out_of_trusted_root, or unsupported_meta_executor AND analysis.readOnlyMetaExecutor \u2014 and guarded by lane === interactive_auto (never auto_strict, never a hostJobId); every other base ASK (incl. the hard-floor asks for missing/redacted/truncated input, permission-deterministic-rails.ts:127-171,259-276) keeps the veto; a relaxed allow keeps the canonical machine values decidedBy: auto_classifier and source: auto_classifier (domain/permission-decision.ts:69-115) and carries railProvenance: { signal: RailSignal; reason: string } as a separate typed field.",
        "Shared parser untouched (fail-closed; nine write-capable find actions pinned in the parser's own test file); pathCandidates ignores stderr-to-/dev/null with a per-lane pin; rails/sandbox policy still runs before any grant (0001).",
        "No domain error type introduced (no named failure contract in T1). DEFERRED with markers: TODO(T2a) typed classifier status; TODO(T3b) memory stages in the coordinator's decision order (documented no-op slots, not speculative abstractions).",
        "Concerns that must not share a file: the analyzer (application) and the IPC tail (runtime); the AF-AC8 harness is the reusable module apps/core/test/unit/runtime/askfloor-tap-budget-harness.ts exporting `replayPermissionRequest(fixture: TapBudgetFixture): Promise<{ taps: number; decidedBy: PermissionApprovalDecision['decidedBy']; source: PermissionDecisionSource; railProvenance: RailProvenance | null }>` (existing PermissionDecisionSource at domain/types.ts:255, the decided-by union from permission-decision.ts, RailProvenance from domain/permission-lane.ts \u2014 concrete types, never unknown) and the `TapBudgetFixture` type (lane inputs, command/tool input, trusted roots, stubbed classifier verdict) so later tasks add fixtures without editing T1's tests; no stringified JSONB equality; runner-spawn sandbox copy sets stay derivable.",
        "Verification: python3 factory/scripts/verify.py is authoritative; the Postgres lane (npm run test:integration:postgres, GANTRY_TEST_DATABASE_URL exported) is run by the orchestrator as stage evidence, not as a gate command: T1 touches no storage code, and two pre-existing suites (live-waiting-admission, inline-agent-runtime child runs) are red on base 00a6c01f9 / origin/main independent of this story and are tracked as a main quickfix before T3a, whose contract restores the lane as a gate."
      ]
    },
    {
      "id": "ASKFLOOR-1-T2a",
      "title": "Shape classifier extraction, attachment birthright, judge status",
      "objective": "Extract one typed effect-shape classifier and the containment helpers from the read gate into shared modules; add the attachment_open input-gated birthright to the deterministic rails for every mode (the only rails ROW added by the story; T1 separately owns the typed unsupported_meta_executor signal per A-0070); stamp a typed classifier status answered|unavailable|skipped at every branch of the classifier runtime, with the native-risk branch isolated into a named helper.",
      "acceptance_criteria": [
        "ONE RULE (owner ruling 2026-09-04, widen \u2014 every HOST-VALID id is zero-tap): `attachment_open` is an input-gated birthright in the deterministic rails keyed on a TYPED PRE-SANITIZATION FACT, `PermissionApprovalRequest.attachmentOpenIds?: { wellFormed: boolean; count: number }`, computed ONCE in the IPC parser from the raw `decisionToolInput` BEFORE either sanitizer runs (`runtime/ipc-parsing.ts:433-450`): `wellFormed` is true iff `attachment_ids` is an array of one to twelve strings, each non-blank after trim and at most 512 characters after trim \u2014 exactly the host's validity shape (`ipc-attachment-open-handler.ts:54-59`, `shared/object.ts:7` trims the same way). The rails allow the row when the fact is present and `wellFormed`, regardless of what the display or classifier sanitizers later shortened or redacted (the id is opaque, never executed, and validated by the host by exact lookup, `attachment-resolver.ts:167`); when the fact is absent (a request that did not come through the IPC parser) or `wellFormed` is false, the row ASKS with the existing input-gated outcome. So: a valid id of any content \u2014 including a token-like or 512-character one \u2014 never asks in ask, auto, auto_strict or autonomous mode (rails are mode-blind; the S1 fixture replays all four lanes at 0 taps); a missing, empty, more-than-twelve, blank, non-string or over-512 list asks (the spec's \"a malformed id asks\", AF-AC2, decision 0154). The sanitizers are NOT changed: the display copy stays masked and shortened exactly as today, and every other input-gated row keeps today's completeness, sanitization (`permission-deterministic-rails.ts:125,288`) and redaction (`:264`) vetoes byte-for-byte \u2014 only the `attachment_open` row consults the fact instead of those three checks, and only when the fact is present. Origin stays host-validated, unchanged: an id that fails conversation-origin validation (`ipc-attachment-open-handler.ts:43-84`, resolver `attachment-resolver.ts:167-186`) returns the existing not-found line \"I couldn't find that attachment in this conversation.\" \u2014 never a card; the handler and its tests are untouched.",
        "a pure effect-shape classifier (`shared/permission-effect-shape.ts`) classifies ONE PARSED LEAF at a time with a pure context `classifyPermissionEffectShape(leaf, { stdinOk })` \u2014 `stdinOk` is the compound/pipeline fact the gate already derives per leaf (standalone leaves get `false`, compound leaves `true`, `auto-permission-read-only-gate.ts:131,137`) and is what `requiresTarget` depends on, since `BashCommandLeaf` carries no compound context (`bash-command-parser.ts:14`) \u2014 covering the verb/argument shape logic INCLUDING `grepFileArgs` (`:429-468`, which moves into the shape module) and the native file-read shape, returning a DISCRIMINATED result `{ kind: read_only_command, executable, targets } | { kind: file_read, action, targets, requiresTarget } | { kind: not_read_only, reason }`, computed without any filesystem, capability or workspace input; the gate RETAINS, verbatim: compound orchestration over the ordered leaves (`:120-140`), the whole-command raw guards at `:115-122` and `:149-150` (protected-path and secret MENTIONS scanned over the command text before any target is discarded, e.g. `grep settings.yaml README.md` is refused on the mention), and the MCP read-binding branch (dispatch `:100-105`, evaluator `:311-345`); a hard-boundaries evaluator (`shared/permission-hard-boundaries.ts`) owns the TARGET-level checks from the file-read path (`:261-308`: capability boundary, realpath containment, protected-path, hidden-segment and secret on each target; the file-read SHAPE part `:248-260` and its pure path predicate `isProvablyWorkspacePath` `:361-366` belong to the shape module, so ownership is disjoint); `evaluateAutoPermissionReadOnlyGate` composes leaf shape \u2192 target boundaries inside its unchanged orchestration with EXACT result-and-reason parity, pinned by the existing suite plus explicit parity leaves in the composition test for (i) a standalone `cat` versus the same leaf inside `cat | wc -l`, (ii) a compound command, and (iii) a non-target protected mention (`grep settings.yaml README.md`) \u2014 the existing `:342-346` case is the workspace-root one and is not the mention proof.",
        "`PermissionClassifierResult` carries a REQUIRED closed `status` (`domain/permission-classifier-status.ts`: `answered | unavailable | skipped`): only a successful LLM verdict is `answered`; `llm_unconfigured timeout model_resolution_failure query_error parse_failure validation_failure` are `unavailable`; `aborted`, `input_truncated`, the native-risk, strict-deterministic and skipped-local branches are `skipped`; the native-risk branch lives in the named helper `runtime/permission-classifier-native-risk.ts` with an unchanged verdict; the cache-hit result constructed in `runtime/ipc-permission-classifier-decision.ts` (`:293`, the one constructor outside the classifier file) is stamped `skipped` (a replayed verdict is neither a fresh answer nor an outage).",
        "AF-AC8 S1 \u2014 opening an attachment already in the conversation \u2014 replays at 0 taps in every lane through T1's harness: `replayPermissionRequest` bypasses the IPC parser (`askfloor-tap-budget-harness.ts:11-17,55-64`), so `TapBudgetFixture` gains one optional field `attachmentOpenIds?: { wellFormed: boolean; count: number }` that the harness forwards onto the request unchanged; the S1 fixture pins `mcp__gantry__attachment_open` with `{ wellFormed: true, count: 1 }`; T1's leaves are not edited.",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T1"
      ],
      "user_facing": false,
      "plan_contracts": [
        {
          "id": "ASKFLOOR-1-T2a-AC1",
          "statement": "ONE RULE (owner ruling 2026-09-04, widen \u2014 every HOST-VALID id is zero-tap): `attachment_open` is an input-gated birthright in the deterministic rails keyed on a TYPED PRE-SANITIZATION FACT, `PermissionApprovalRequest.attachmentOpenIds?: { wellFormed: boolean; count: number }`, computed ONCE in the IPC parser from the raw `decisionToolInput` BEFORE either sanitizer runs (`runtime/ipc-parsing.ts:433-450`): `wellFormed` is true iff `attachment_ids` is an array of one to twelve strings, each non-blank after trim and at most 512 characters after trim \u2014 exactly the host's validity shape (`ipc-attachment-open-handler.ts:54-59`, `shared/object.ts:7` trims the same way). The rails allow the row when the fact is present and `wellFormed`, regardless of what the display or classifier sanitizers later shortened or redacted (the id is opaque, never executed, and validated by the host by exact lookup, `attachment-resolver.ts:167`); when the fact is absent (a request that did not come through the IPC parser) or `wellFormed` is false, the row ASKS with the existing input-gated outcome. So: a valid id of any content \u2014 including a token-like or 512-character one \u2014 never asks in ask, auto, auto_strict or autonomous mode (rails are mode-blind; the S1 fixture replays all four lanes at 0 taps); a missing, empty, more-than-twelve, blank, non-string or over-512 list asks (the spec's \"a malformed id asks\", AF-AC2, decision 0154). The sanitizers are NOT changed: the display copy stays masked and shortened exactly as today, and every other input-gated row keeps today's completeness, sanitization (`permission-deterministic-rails.ts:125,288`) and redaction (`:264`) vetoes byte-for-byte \u2014 only the `attachment_open` row consults the fact instead of those three checks, and only when the fact is present. Origin stays host-validated, unchanged: an id that fails conversation-origin validation (`ipc-attachment-open-handler.ts:43-84`, resolver `attachment-resolver.ts:167-186`) returns the existing not-found line \"I couldn't find that attachment in this conversation.\" \u2014 never a card; the handler and its tests are untouched.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2a-AC2",
          "statement": "a pure effect-shape classifier (`shared/permission-effect-shape.ts`) classifies ONE PARSED LEAF at a time with a pure context `classifyPermissionEffectShape(leaf, { stdinOk })` \u2014 `stdinOk` is the compound/pipeline fact the gate already derives per leaf (standalone leaves get `false`, compound leaves `true`, `auto-permission-read-only-gate.ts:131,137`) and is what `requiresTarget` depends on, since `BashCommandLeaf` carries no compound context (`bash-command-parser.ts:14`) \u2014 covering the verb/argument shape logic INCLUDING `grepFileArgs` (`:429-468`, which moves into the shape module) and the native file-read shape, returning a DISCRIMINATED result `{ kind: read_only_command, executable, targets } | { kind: file_read, action, targets, requiresTarget } | { kind: not_read_only, reason }`, computed without any filesystem, capability or workspace input; the gate RETAINS, verbatim: compound orchestration over the ordered leaves (`:120-140`), the whole-command raw guards at `:115-122` and `:149-150` (protected-path and secret MENTIONS scanned over the command text before any target is discarded, e.g. `grep settings.yaml README.md` is refused on the mention), and the MCP read-binding branch (dispatch `:100-105`, evaluator `:311-345`); a hard-boundaries evaluator (`shared/permission-hard-boundaries.ts`) owns the TARGET-level checks from the file-read path (`:261-308`: capability boundary, realpath containment, protected-path, hidden-segment and secret on each target; the file-read SHAPE part `:248-260` and its pure path predicate `isProvablyWorkspacePath` `:361-366` belong to the shape module, so ownership is disjoint); `evaluateAutoPermissionReadOnlyGate` composes leaf shape \u2192 target boundaries inside its unchanged orchestration with EXACT result-and-reason parity, pinned by the existing suite plus explicit parity leaves in the composition test for (i) a standalone `cat` versus the same leaf inside `cat | wc -l`, (ii) a compound command, and (iii) a non-target protected mention (`grep settings.yaml README.md`) \u2014 the existing `:342-346` case is the workspace-root one and is not the mention proof.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2a-AC3",
          "statement": "`PermissionClassifierResult` carries a REQUIRED closed `status` (`domain/permission-classifier-status.ts`: `answered | unavailable | skipped`): only a successful LLM verdict is `answered`; `llm_unconfigured timeout model_resolution_failure query_error parse_failure validation_failure` are `unavailable`; `aborted`, `input_truncated`, the native-risk, strict-deterministic and skipped-local branches are `skipped`; the native-risk branch lives in the named helper `runtime/permission-classifier-native-risk.ts` with an unchanged verdict; the cache-hit result constructed in `runtime/ipc-permission-classifier-decision.ts` (`:293`, the one constructor outside the classifier file) is stamped `skipped` (a replayed verdict is neither a fresh answer nor an outage).",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2a-AC4",
          "statement": "AF-AC8 S1 \u2014 opening an attachment already in the conversation \u2014 replays at 0 taps in every lane through T1's harness: `replayPermissionRequest` bypasses the IPC parser (`askfloor-tap-budget-harness.ts:11-17,55-64`), so `TapBudgetFixture` gains one optional field `attachmentOpenIds?: { wellFormed: boolean; count: number }` that the harness forwards onto the request unchanged; the S1 fixture pins `mcp__gantry__attachment_open` with `{ wellFormed: true, count: 1 }`; T1's leaves are not edited.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2a-AC5",
          "statement": "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7).",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md#acceptance-criteria"
        }
      ],
      "write_scope": [
        "apps/core/src/shared/auto-permission-read-only-gate.ts",
        "apps/core/src/shared/permission-effect-shape.ts",
        "apps/core/src/shared/permission-hard-boundaries.ts",
        "apps/core/src/domain/permission-deterministic-rails.ts",
        "apps/core/src/domain/permission-classifier-status.ts",
        "apps/core/src/runtime/permission-classifier.ts",
        "apps/core/src/runtime/permission-classifier-native-risk.ts",
        "apps/core/src/runtime/ipc-permission-classifier-decision.ts",
        "apps/core/src/runtime/ipc-parsing.ts",
        "apps/core/src/domain/types.ts",
        "apps/core/test/unit/runtime/ipc-parsing.test.ts",
        "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
        "apps/core/test/unit/shared/auto-permission-read-only-gate.test.ts",
        "apps/core/test/unit/shared/permission-effect-shape.test.ts",
        "apps/core/test/unit/shared/permission-hard-boundaries.test.ts",
        "apps/core/test/unit/shared/permission-deterministic-rails.test.ts",
        "apps/core/test/unit/runtime/permission-classifier.test.ts",
        "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
        "apps/core/test/unit/runtime/askfloor-tap-budget-harness.ts"
      ],
      "verify_commands": [
        "python3 factory/scripts/verify.py",
        "npx tsc --noEmit",
        "npm run check:architecture"
      ],
      "review_budget": {
        "max_changed_files": 20,
        "max_changed_lines": 1600,
        "reason": "The grilled contract (13 rounds; owner ruling 2026-09-04: widen so every host-valid attachment id is zero-tap) spans 10 source files (read gate split into two NEW shared modules; rails birthright row + typed-fact map; NEW domain status enum; classifier status stamping with the native-risk branch moved to a NEW helper file; the IPC cache-hit constructor; the parser's attachmentOpenIds fact and its one optional request field) and 9 test files (two NEW module suites, moved cases, the S1 fixture plus the one harness field that carries the fact, the parser and cache-hit leaves) = 19 files; one file of headroom under 20 for a barrel or map edit the implementer must raise as a signal. The split moves existing lines rather than adding logic."
      },
      "required_tests": [
        {
          "id": "allows attachment_open as an input-gated birthright when the typed pre-sanitization fact says attachment_ids is well-formed, even when the display copy was shortened or redacted",
          "path": "apps/core/test/unit/shared/permission-deterministic-rails.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "asks for attachment_open when the typed fact is absent or not well-formed, while another tool's redacted input still asks",
          "path": "apps/core/test/unit/shared/permission-deterministic-rails.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "keeps every existing birthright row and read-only allow unchanged after the shape and boundary split",
          "path": "apps/core/test/unit/shared/permission-deterministic-rails.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "classifies read-only command and file-read shapes with action, targets and requiresTarget without touching the filesystem",
          "path": "apps/core/test/unit/shared/permission-effect-shape.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "refuses out-of-workspace, protected, hidden-segment, secret and capability-boundary targets exactly as the read gate did",
          "path": "apps/core/test/unit/shared/permission-hard-boundaries.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "reproduces every prior allow and refuse verdict by composing shape and hard boundaries, including standalone versus piped cat, a compound command and a non-target protected mention",
          "path": "apps/core/test/unit/shared/auto-permission-read-only-gate.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "stamps status answered only for a successful LLM verdict",
          "path": "apps/core/test/unit/runtime/permission-classifier.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "stamps status unavailable for the six unusable-result failure codes",
          "path": "apps/core/test/unit/runtime/permission-classifier.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "stamps status skipped for aborted, input_truncated and every non-LLM branch",
          "path": "apps/core/test/unit/runtime/permission-classifier.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "routes the native-risk branch through the named helper with an unchanged verdict",
          "path": "apps/core/test/unit/runtime/permission-classifier.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "S1: opening an attachment already in the conversation costs 0 taps in every lane with the typed fact carried by the fixture",
          "path": "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "stamps a cached classifier verdict with status skipped",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "derives the attachment_open id fact from the raw input before sanitization: 512 after trim is well-formed, 513 is not, a token-like id is well-formed yet stays redacted in both sanitized copies, blank and thirteen entries are not, another tool has no fact",
          "path": "apps/core/test/unit/runtime/ipc-parsing.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        }
      ],
      "reviewer_focus": [
        "Constitution (load-bearing): constitution/03-modular-monolith-structure.md (modules with clear boundaries, strict separation of concerns, no circular dependencies, predictable ownership) and pnp-coding-standards-modular-monolith.md. DELIBERATE RECORDED DEVIATION: the new modules are named like their siblings in apps/core/src/shared (kebab-case.ts, no dotted role suffix, e.g. permission-trusted-paths.ts) instead of the constitution's suffix table (pnp-coding-standards-modular-monolith.md:22-54, which would want a lone .helper.ts) \u2014 the repo deviates from that table repo-wide and consistency with siblings wins; no other rule is claimed from those files.",
        "SPLIT IS A MOVE, NOT A REWRITE: shared/permission-effect-shape.ts is ONE small module holding the closed enum PermissionEffectShape and the GENUINELY PURE per-LEAF classifyPermissionEffectShape(leaf, { stdinOk }) (no fs, no capability ids, no workspace root; stdinOk is the compound/pipeline fact the orchestration already derives per leaf \u2014 standalone false, compound true, auto-permission-read-only-gate.ts:131,137 \u2014 and requiresTarget is derived from it INSIDE the classifier because BashCommandLeaf carries no compound context, bash-command-parser.ts:14) returning a DISCRIMINATED result \u2014 { kind: read_only_command, executable, targets } | { kind: file_read, action, targets, requiresTarget } | { kind: not_read_only, reason }; it takes over the per-leaf shape logic (:152-246,248-260,348-359,361-366,368-469 \u2014 includes grepFileArgs :429-468 and the pure isProvablyWorkspacePath :361-366; NOT :79-105, which is capability normalization and top-level dispatch and stays in the gate). NOT MOVED, verbatim in the gate: compound orchestration over ordered leaves (:120-140), the whole-command raw guards at :115-122 and :149-150 (protected-path/secret MENTIONS over the command text; grep settings.yaml README.md is refused on the mention), and the MCP read-binding branch (dispatch :100-105, evaluator :311-345). shared/permission-hard-boundaries.ts owns the per-TARGET checks from the file-read path VERBATIM (:261-308 only \u2014 capability boundary, realpath containment, protected-path, hidden-segment, secret; disjoint from the shape module's :248-260,361-366). The gate keeps its exported signature and result; inside its unchanged orchestration each leaf becomes shape -> boundaries with EXACT result-and-reason parity; three explicit parity leaves in the composition test: standalone cat vs the same leaf in cat | wc -l; a compound command; a non-target protected mention (the existing :342-346 case is the workspace-root one, not the mention proof). Both callers (rails :149-158, classifier :305-314,341-356) untouched. Any verdict or reason change in an existing test is a defect, not a fixture to update.",
        "Rails ownership (assumption A-0070): T2a owns ONLY the attachment_open input-gated birthright row and ONE typed fact map (ReadonlyMap<string, (request) => boolean>, single entry) consulted by the input-gated evaluation (:110-142). ONE RULE (owner ruling 2026-09-04, widen \u2014 every HOST-VALID id is zero-tap): the fact is PermissionApprovalRequest.attachmentOpenIds?: { wellFormed: boolean; count: number } (domain/types.ts beside classifierToolInput :220-226), computed ONCE in the IPC parser from the RAW decisionToolInput before either sanitizer (runtime/ipc-parsing.ts:433-450, returned at :487-491) by a pure attachmentOpenIdsFact(input): undefined unless the tool is attachment_open; wellFormed iff attachment_ids is an array of 1..12 strings, each non-blank and <=512 after trim \u2014 the host's own validity shape (jobs/ipc-attachment-open-handler.ts:54-59, shared/object.ts:7). For the attachment_open row the fact REPLACES the completeness, sanitization (:125,288) and redaction (:264) checks; rows without an entry keep those checks byte-for-byte; fact absent or false -> the EXISTING input-gated ASK outcome. The sanitizers are NOT changed (display stays masked/shortened as today; classifier copy untouched). Origin stays host-validated (:43-84; handler and tests untouched). Rejected: sanitizer special-casing (display length after redaction is not the host's trimmed length; rails receive only the union of altered paths) and any veto exemption keyed on sanitized marks. No other rails row, signal or verdict changes; the unsupported_meta_executor signal (T1) is untouched.",
        "Rails are mode-blind (no permission-mode input), so 'every mode' holds by construction; the S1 fixture replays the four PermissionLane values through T1's replayPermissionRequest and asserts 0 taps with the birthright decidedBy. The host handler and resolver are NOT touched: origin validation (ipc-attachment-open-handler.ts:43-84; attachment-resolver.ts:167-186) and the not-found copy (attachment-failure.ts:4-5) are already pinned by their own suites.",
        "Typed status: domain/permission-classifier-status.ts exports the closed enum PermissionClassifierStatus (answered | unavailable | skipped); PermissionClassifierResult.status is REQUIRED and stamped at EVERY branch: failedResult (:642-658) maps llm_unconfigured/timeout/model_resolution_failure/query_error/parse_failure/validation_failure -> unavailable and aborted/input_truncated -> skipped; strict-deterministic (:341-353), skipped-local (:339-340) and native-risk -> skipped; LLM success (:240-246) -> answered; the ONE structural constructor outside the classifier file \u2014 the cached-verdict result in runtime/ipc-permission-classifier-decision.ts:293 (T1 file, edited for that constructor only) \u2014 stamps skipped. No string literals for status. The status is INTERNAL in T2a: the runtime decision-event payload (:624) and its exact-payload test (permission-classifier.test.ts:1689) are unchanged; T6 publishes it. TODO(T6) marker: status is stamped, not yet consumed (latch + wiring-missing unavailable are T6).",
        "Native-risk branch (:327-330,354-355) moves to runtime/permission-classifier-native-risk.ts as evaluateNativeRiskBranch(input) -> PermissionClassifierResult | undefined with an UNCHANGED verdict and the same gantryRisk inputs; T2b later passes tool arguments into that helper. The classifier file must not grow a second native-risk path.",
        "Concerns that must not share a file: shape (pure, shared) vs boundaries (fs-backed, shared) vs the gate (composition); the status enum (domain) vs its stamping (runtime); the native-risk helper (runtime, T2b-owned next) vs the classifier orchestration.",
        "No domain error type introduced (no named failure contract). Nothing DEFERRED beyond the TODO(T6) consumption marker; T2b owns gantry-tool-risk.ts and the native Write/Edit path judgment \u2014 not touched here.",
        "Verification: python3 factory/scripts/verify.py is authoritative in the gate environment; the Postgres lane (npm run test:integration:postgres, GANTRY_TEST_DATABASE_URL exported) is orchestrator-run stage evidence as for T1 \u2014 T2a touches no storage code; two pre-existing suites are red on base/origin/main independent of this story (main quickfix before T3a)."
      ]
    },
    {
      "id": "ASKFLOOR-1-T2b",
      "title": "Default-allow risk table for gantry-native tools",
      "objective": "gantry-tool-risk.ts becomes ONE small typed table: every gantry-native tool is LOW by default in interactive auto (all browser actions, file list/read with host-side entry protection, scheduler/admin reads, attachment tools); only the closed high-risk rows are HIGH (destructive or protected writes incl. raw-path browser file actions, the registry scheduler-mutation and admin-mutation exports); unmapped or malformed shapes are ambiguous and go to the classifier.",
      "acceptance_criteria": [
        "`application/permissions/gantry-tool-risk.ts` is ONE typed table. The verdict type is the closed `GantryToolRiskVerdict = low | high | ambiguous` (the old `medium` and the identity HIGH-by-default are gone; `GantryToolRisk` keeps its name only if the enum is replaced in place \u2014 no parallel type). Input is `{ toolName: GantryMcpToolName-or-string, toolInput: unknown }`. Rules, in order: (1) a suffix not in `ALL_GANTRY_MCP_TOOL_NAMES` (`shared/admin-mcp-tools.ts:206-215`) \u2192 `ambiguous`; (2) the CLOSED HIGH-RISK rows \u2192 `high`: scheduler mutations = `SCHEDULER_MUTATION_MCP_TOOL_NAMES` (`:32-39`, incl. pause/resume/run_now); admin mutations = the registry's mutation exports `AUTHORITY_CHANGING_GANTRY_MCP_TOOL_NAMES`, `DECISION_ACTOR_GANTRY_MCP_TOOL_NAMES`, `DURABLE_GRANT_EXCLUDED_DISPATCHERS` and `DELEGATION_DISPATCHERS` (`:129-177`), IMPORTED, never restated; destructive or protected writes = `file` `write`/`promote_scratch` whose virtual target is protected or `protected: true`, and `browser_act` `file_attach`/`file_upload` whose payload source is a raw filesystem path (`paths`, or `source.type === \"path\"`) outside the workspace, protected or secret (AC3 helper); (3) OWNER RULING 2026-09-05: the generic executors `async_run_command`, `async_mcp_call`, `mcp_call_tool`, `capability_run` \u2192 `ambiguous` (the classifier judges them with their arguments; never forced HIGH, never LOW); (4) argument-shaped rows: `file` per-action discriminated predicate \u2014 `list` (`scope` + optional `path`, `limit`) \u2192 `low`; `read` (`artifactId` + optional `version`, or `scope` + `path`, + optional `offset`, `readLimit`) \u2192 `low`; `write` (`scope` + `path` + `content` + optional `encoding`, `contentType`, `protected`) \u2192 `low` to an unprotected virtual path without `protected: true`, else `high`; `promote_scratch` (`path` + `targetScope` + `targetPath`) \u2192 same rule on the target; any other shape \u2192 `ambiguous`; `browser_act` `file_attach`/`file_upload` with a raw-path source inside the workspace \u2192 `low`, artifact- or bytes-sourced `file_upload` \u2192 `low`, malformed \u2192 `ambiguous`; (5) every other registered tool \u2192 `low` (all remaining `browser_*` actions, `file` list/read, scheduler/admin READS, attachment tools, canvas, render, memory, brain, procedure, task reads and cancel). Exhaustive table test: every suffix in `ALL_GANTRY_MCP_TOOL_NAMES` is asserted with a valid shape and, for the argument-shaped rows, a malformed shape; the registry-import rows are asserted by iterating the imported sets, so a registry addition is covered without editing the test.",
        "the table is applied under lane `interactive_auto` ONLY. `evaluateNativeRiskBranch` (`runtime/permission-classifier-native-risk.ts`) gains `toolInput` (the classifier copy the caller already holds, `permission-classifier.ts:302`), `workspaceRoot` and `lane: PermissionLane`; under `interactive_auto` it returns `low` \u2192 the existing native `allow` result (`status: skipped`, provenance unchanged: `decidedBy`/`source` `auto_classifier`), `high` \u2192 the existing native ask result, `ambiguous` \u2192 `undefined` (LLM consult continues); under every other lane it returns exactly what it returns today. The lane is derived once in the classifier from `permissionMode` + `hostJobId` with T1's derivation (`application/permissions/auto-lane-analysis.ts:97-114` \u2014 reused, not re-implemented); the classifier input gains `hostJobId` if it does not carry it, and the inline live consult (`app/bootstrap/inline-agent-loop-tools.ts:389-445`) passes its job id so an inline SCHEDULED run in auto mode derives `autonomous` and never sees the table (the T2b lane-leak fix; the inline memory/latch work stays T3b/T6). `auto_strict` keeps the strict guard's precedence (`permission-classifier.ts:348-364`); `ask` and job lanes never reach the branch (IPC `:325-383`). Explicit tests: the same `browser_act` click fixture allows under `interactive_auto`, asks under `auto_strict`, and is never consulted under `ask`/`autonomous`; an inline run with a job id derives `autonomous`.",
        "NEW `application/permissions/native-file-write-risk.ts` exports `judgeNativeFileWrite({ toolName, toolInput, workspaceRoot })` \u2192 `low | high | ambiguous` for the native file-write facades (`FileWrite`/`FileEdit` \u2014 the ids the gate receives from both runners, `shared/gantry-tool-facades.ts:64-76`; argument key `file_path` from the Anthropic runner, `path` from the deepagents facade, `gantry-facade-tools.ts:110-132,160-189` \u2014 both accepted, both pinned) and the shared prospective-write containment predicate `isProspectiveWriteInsideWorkspace(workspaceRoot, candidatePath)`: lexical containment after normalisation, the nearest EXISTING ancestor realpath-contained, no symlink component or symlink target (`lstat`), a non-existent target allowed; protected/hidden/secret destinations refused via the existing capability-path predicates (`shared/tool-execution-protected-paths.ts:40-102`) \u2014 the same helper the AC1 browser raw-path row uses; `gantry-facade-file-safety.ts:39-67` stays the execution-time authority and is not imported (it is async, config-bound and creates directories). The eligibility gate (`application/permissions/permission-classifier.ts:11-28`) accepts exactly `FileWrite` and `FileEdit` in addition to today's predicates; the native-risk helper routes those two ids to `judgeNativeFileWrite` and every gantry name to the table. Tests: inside the workspace \u2192 allow with no LLM call; outside, protected (`settings.yaml`), hidden segment, secret name, symlinked parent, symlinked target \u2192 ask; missing target inside \u2192 allow; malformed input \u2192 ambiguous \u2192 LLM consult; a third facade id (`FileRead`) stays ineligible.",
        "host-side entry protection in `jobs/ipc-file-artifact-handlers.ts`: ONE `(scope, path)` predicate `isProtectedArtifactEntry` combining the generic virtual protection (`domain/file-artifacts/protected-virtual-path.ts:23-57`, which excludes `AGENTS.md` by design) with the prompt-profile scope's own protection (`isAgentProfileArtifactWrite`, so `prompt-profile/**/AGENTS.md` and `SOUL.md` stay hidden), applied during `list` (protected descriptors filtered out; the response gains `protectedHidden: number`) and after id resolution for `read` (a protected entry is rejected through the existing `reject(error, code)` convention with code `protected_entry` and message `Refused: <path> is protected. This is not a permission question \u2014 don't retry, tell the owner.`); the runner tool `runner/mcp/tools/file.ts` appends `(N protected entries hidden \u2014 not a permission question, don't retry.)` to the `list` text when `protectedHidden > 0` and relays the refusal text unchanged. Tests: ordinary-scope counter-tests (nothing hidden, `protectedHidden: 0`, no suffix line), a protected `settings.yaml` hidden from list and refused on read by id and by path, `prompt-profile/AGENTS.md` hidden and refused, `write`/`promote_scratch` protection unchanged.",
        "AF-AC8 fixtures through T1's `replayPermissionRequest`: S2 (browser action in interactive auto), S4 (`file` read by path), S5 (`file` write to an unprotected virtual path), S6 (native `FileWrite` inside the workspace) each cost 0 taps with `decidedBy: auto_classifier` and NO LLM call (the fixture's stubbed consult must not be invoked); the mirror fixtures \u2014 `file` write to `settings.yaml`, `FileWrite` outside the workspace, `scheduler_delete_job`, `browser_act file_attach` with a path outside the workspace \u2014 cost 1 tap; the same four S-fixtures under `auto_strict`, `ask` and `autonomous` produce today's outcome (pinned by value).",
        "existing unit suites pass with every verdict of `ask`, `auto_strict` and job lanes unchanged (a changed expectation in an existing test outside `gantry-tool-risk.test.ts` is a defect); tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T2a"
      ],
      "user_facing": false,
      "plan_contracts": [
        {
          "id": "ASKFLOOR-1-T2b-AC1",
          "statement": "`application/permissions/gantry-tool-risk.ts` is ONE typed table. The verdict type is the closed `GantryToolRiskVerdict = low | high | ambiguous` (the old `medium` and the identity HIGH-by-default are gone; `GantryToolRisk` keeps its name only if the enum is replaced in place \u2014 no parallel type). Input is `{ toolName: GantryMcpToolName-or-string, toolInput: unknown }`. Rules, in order: (1) a suffix not in `ALL_GANTRY_MCP_TOOL_NAMES` (`shared/admin-mcp-tools.ts:206-215`) \u2192 `ambiguous`; (2) the CLOSED HIGH-RISK rows \u2192 `high`: scheduler mutations = `SCHEDULER_MUTATION_MCP_TOOL_NAMES` (`:32-39`, incl. pause/resume/run_now); admin mutations = the registry's mutation exports `AUTHORITY_CHANGING_GANTRY_MCP_TOOL_NAMES`, `DECISION_ACTOR_GANTRY_MCP_TOOL_NAMES`, `DURABLE_GRANT_EXCLUDED_DISPATCHERS` and `DELEGATION_DISPATCHERS` (`:129-177`), IMPORTED, never restated; destructive or protected writes = `file` `write`/`promote_scratch` whose virtual target is protected or `protected: true`, and `browser_act` `file_attach`/`file_upload` whose payload source is a raw filesystem path (`paths`, or `source.type === \"path\"`) outside the workspace, protected or secret (AC3 helper); (3) OWNER RULING 2026-09-05: the generic executors `async_run_command`, `async_mcp_call`, `mcp_call_tool`, `capability_run` \u2192 `ambiguous` (the classifier judges them with their arguments; never forced HIGH, never LOW); (4) argument-shaped rows: `file` per-action discriminated predicate \u2014 `list` (`scope` + optional `path`, `limit`) \u2192 `low`; `read` (`artifactId` + optional `version`, or `scope` + `path`, + optional `offset`, `readLimit`) \u2192 `low`; `write` (`scope` + `path` + `content` + optional `encoding`, `contentType`, `protected`) \u2192 `low` to an unprotected virtual path without `protected: true`, else `high`; `promote_scratch` (`path` + `targetScope` + `targetPath`) \u2192 same rule on the target; any other shape \u2192 `ambiguous`; `browser_act` `file_attach`/`file_upload` with a raw-path source inside the workspace \u2192 `low`, artifact- or bytes-sourced `file_upload` \u2192 `low`, malformed \u2192 `ambiguous`; (5) every other registered tool \u2192 `low` (all remaining `browser_*` actions, `file` list/read, scheduler/admin READS, attachment tools, canvas, render, memory, brain, procedure, task reads and cancel). Exhaustive table test: every suffix in `ALL_GANTRY_MCP_TOOL_NAMES` is asserted with a valid shape and, for the argument-shaped rows, a malformed shape; the registry-import rows are asserted by iterating the imported sets, so a registry addition is covered without editing the test.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2b-AC2",
          "statement": "the table is applied under lane `interactive_auto` ONLY. `evaluateNativeRiskBranch` (`runtime/permission-classifier-native-risk.ts`) gains `toolInput` (the classifier copy the caller already holds, `permission-classifier.ts:302`), `workspaceRoot` and `lane: PermissionLane`; under `interactive_auto` it returns `low` \u2192 the existing native `allow` result (`status: skipped`, provenance unchanged: `decidedBy`/`source` `auto_classifier`), `high` \u2192 the existing native ask result, `ambiguous` \u2192 `undefined` (LLM consult continues); under every other lane it returns exactly what it returns today. The lane is derived once in the classifier from `permissionMode` + `hostJobId` with T1's derivation (`application/permissions/auto-lane-analysis.ts:97-114` \u2014 reused, not re-implemented); the classifier input gains `hostJobId` if it does not carry it, and the inline live consult (`app/bootstrap/inline-agent-loop-tools.ts:389-445`) passes its job id so an inline SCHEDULED run in auto mode derives `autonomous` and never sees the table (the T2b lane-leak fix; the inline memory/latch work stays T3b/T6). `auto_strict` keeps the strict guard's precedence (`permission-classifier.ts:348-364`); `ask` and job lanes never reach the branch (IPC `:325-383`). Explicit tests: the same `browser_act` click fixture allows under `interactive_auto`, asks under `auto_strict`, and is never consulted under `ask`/`autonomous`; an inline run with a job id derives `autonomous`.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2b-AC3",
          "statement": "NEW `application/permissions/native-file-write-risk.ts` exports `judgeNativeFileWrite({ toolName, toolInput, workspaceRoot })` \u2192 `low | high | ambiguous` for the native file-write facades (`FileWrite`/`FileEdit` \u2014 the ids the gate receives from both runners, `shared/gantry-tool-facades.ts:64-76`; argument key `file_path` from the Anthropic runner, `path` from the deepagents facade, `gantry-facade-tools.ts:110-132,160-189` \u2014 both accepted, both pinned) and the shared prospective-write containment predicate `isProspectiveWriteInsideWorkspace(workspaceRoot, candidatePath)`: lexical containment after normalisation, the nearest EXISTING ancestor realpath-contained, no symlink component or symlink target (`lstat`), a non-existent target allowed; protected/hidden/secret destinations refused via the existing capability-path predicates (`shared/tool-execution-protected-paths.ts:40-102`) \u2014 the same helper the AC1 browser raw-path row uses; `gantry-facade-file-safety.ts:39-67` stays the execution-time authority and is not imported (it is async, config-bound and creates directories). The eligibility gate (`application/permissions/permission-classifier.ts:11-28`) accepts exactly `FileWrite` and `FileEdit` in addition to today's predicates; the native-risk helper routes those two ids to `judgeNativeFileWrite` and every gantry name to the table. Tests: inside the workspace \u2192 allow with no LLM call; outside, protected (`settings.yaml`), hidden segment, secret name, symlinked parent, symlinked target \u2192 ask; missing target inside \u2192 allow; malformed input \u2192 ambiguous \u2192 LLM consult; a third facade id (`FileRead`) stays ineligible.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2b-AC4",
          "statement": "host-side entry protection in `jobs/ipc-file-artifact-handlers.ts`: ONE `(scope, path)` predicate `isProtectedArtifactEntry` combining the generic virtual protection (`domain/file-artifacts/protected-virtual-path.ts:23-57`, which excludes `AGENTS.md` by design) with the prompt-profile scope's own protection (`isAgentProfileArtifactWrite`, so `prompt-profile/**/AGENTS.md` and `SOUL.md` stay hidden), applied during `list` (protected descriptors filtered out; the response gains `protectedHidden: number`) and after id resolution for `read` (a protected entry is rejected through the existing `reject(error, code)` convention with code `protected_entry` and message `Refused: <path> is protected. This is not a permission question \u2014 don't retry, tell the owner.`); the runner tool `runner/mcp/tools/file.ts` appends `(N protected entries hidden \u2014 not a permission question, don't retry.)` to the `list` text when `protectedHidden > 0` and relays the refusal text unchanged. Tests: ordinary-scope counter-tests (nothing hidden, `protectedHidden: 0`, no suffix line), a protected `settings.yaml` hidden from list and refused on read by id and by path, `prompt-profile/AGENTS.md` hidden and refused, `write`/`promote_scratch` protection unchanged.",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2b-AC5",
          "statement": "AF-AC8 fixtures through T1's `replayPermissionRequest`: S2 (browser action in interactive auto), S4 (`file` read by path), S5 (`file` write to an unprotected virtual path), S6 (native `FileWrite` inside the workspace) each cost 0 taps with `decidedBy: auto_classifier` and NO LLM call (the fixture's stubbed consult must not be invoked); the mirror fixtures \u2014 `file` write to `settings.yaml`, `FileWrite` outside the workspace, `scheduler_delete_job`, `browser_act file_attach` with a path outside the workspace \u2014 cost 1 tap; the same four S-fixtures under `auto_strict`, `ask` and `autonomous` produce today's outcome (pinned by value).",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md#acceptance-criteria"
        },
        {
          "id": "ASKFLOOR-1-T2b-AC6",
          "statement": "existing unit suites pass with every verdict of `ask`, `auto_strict` and job lanes unchanged (a changed expectation in an existing test outside `gantry-tool-risk.test.ts` is a defect); tsc and check:architecture green (AF-AC7).",
          "source": ".factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md#acceptance-criteria"
        }
      ],
      "write_scope": [
        "apps/core/src/application/permissions/gantry-tool-risk.ts",
        "apps/core/src/application/permissions/native-file-write-risk.ts",
        "apps/core/src/application/permissions/permission-classifier.ts",
        "apps/core/src/runtime/permission-classifier-native-risk.ts",
        "apps/core/src/runtime/permission-classifier.ts",
        "apps/core/src/runtime/ipc-permission-classifier-decision.ts",
        "apps/core/src/app/bootstrap/inline-agent-loop-tools.ts",
        "apps/core/src/jobs/ipc-file-artifact-handlers.ts",
        "apps/core/src/domain/file-artifacts/protected-virtual-path.ts",
        "apps/core/src/runner/mcp/tools/file.ts",
        "apps/core/test/unit/application/gantry-tool-risk.test.ts",
        "apps/core/test/unit/application/native-file-write-risk.test.ts",
        "apps/core/test/unit/application/permission-classifier.test.ts",
        "apps/core/test/unit/runtime/permission-classifier.test.ts",
        "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
        "apps/core/test/unit/jobs/ipc-file-artifact-handlers.test.ts",
        "apps/core/test/unit/runner/mcp/file-tool.test.ts",
        "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
        "apps/core/test/unit/runtime/askfloor-tap-budget-harness.ts"
      ],
      "verify_commands": [
        "python3 factory/scripts/verify.py",
        "npx tsc --noEmit",
        "npm run check:architecture"
      ],
      "review_budget": {
        "max_changed_files": 20,
        "max_changed_lines": 1700,
        "reason": "The task plan spans 10 source files (the risk table rewrite; NEW native-file-write-risk helper; the eligibility gate; the native-risk helper gaining arguments and lane; one call site each in the classifier, the IPC helper and the inline consult for hostJobId/workspaceRoot; the file handler entry protection with its pure predicate; the runner file tool's count line) and 9 test files (the exhaustive table suite, NEW helper suite, gate/classifier/IPC leaves, handler + runner-tool leaves, the S2/S4/S5/S6 fixtures and one harness field) = 19 files; one file of headroom under 20 for a barrel or types edit the implementer must raise as a signal."
      },
      "required_tests": [
        {
          "id": "judges every registered gantry tool: low by default, the imported scheduler and admin mutation sets high, executors and unknown suffixes ambiguous",
          "path": "apps/core/test/unit/application/gantry-tool-risk.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "judges file by action: list and read low, write and promote_scratch low to an unprotected virtual path and high to a protected one or with protected true, malformed shapes ambiguous",
          "path": "apps/core/test/unit/application/gantry-tool-risk.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "judges browser file actions by payload source: raw path inside the workspace low, outside or protected or secret high, artifact and bytes sources low, malformed ambiguous",
          "path": "apps/core/test/unit/application/gantry-tool-risk.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "contains a prospective write: inside allows including a missing target, outside, protected, hidden, secret, symlinked parent and symlinked target refuse",
          "path": "apps/core/test/unit/application/native-file-write-risk.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "accepts FileWrite and FileEdit for classification and keeps FileRead ineligible",
          "path": "apps/core/test/unit/application/permission-classifier.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "applies the risk table under interactive_auto only: a browser click allows, the same fixture asks under auto_strict, and memory_save keeps its prior verdict outside interactive_auto",
          "path": "apps/core/test/unit/runtime/permission-classifier.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "judges a native FileWrite inside the workspace low without an LLM call and outside the workspace high, for both the file_path and path argument keys",
          "path": "apps/core/test/unit/runtime/permission-classifier.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "threads hostJobId and workspaceRoot into the classifier consult so a job-lane request never reaches the native-risk table",
          "path": "apps/core/test/unit/runtime/ipc-permission-classifier-decision.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "hides protected entries from list with a protectedHidden count and refuses a protected read by id and by path with the fixed not-a-permission-question text",
          "path": "apps/core/test/unit/jobs/ipc-file-artifact-handlers.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "appends the protected-entries-hidden line to list output only when the count is positive and relays a protected read refusal unchanged",
          "path": "apps/core/test/unit/runner/mcp/file-tool.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "S2 S4 S5 S6: a browser action, a file read by path, an unprotected file write and a native FileWrite inside the workspace cost 0 taps in interactive auto with no LLM call",
          "path": "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "mirror fixtures cost 1 tap: a protected file write, a FileWrite outside the workspace, scheduler_delete_job and a raw-path file_attach outside the workspace",
          "path": "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        },
        {
          "id": "keeps today's outcome for the S2 S4 S5 S6 fixtures under auto_strict, ask and autonomous",
          "path": "apps/core/test/unit/runtime/askfloor-tap-budget.test.ts",
          "command": "VITEST_JUNIT=1 npx vitest run -c vitest.unit.config.ts {path} -t {id} --outputFile={report}"
        }
      ],
      "reviewer_focus": [
        "Constitution (load-bearing): constitution/03-modular-monolith-structure.md and pnp-coding-standards-modular-monolith.md (typed enums over string literals; application vs runtime vs domain placement; no circular imports \u2014 application/permissions imports shared/admin-mcp-tools and shared/tool-execution-protected-paths, never runtime). Naming follows the sibling convention (kebab-case, no dotted role suffix) \u2014 the recorded repo-wide deviation from the constitution suffix table.",
        "ONE TYPED TABLE, DEFAULT LOW (spec AF-AC1 owner ruling 2026-09-03): gantry-tool-risk.ts judges { toolName, toolInput, workspaceRoot } to the closed verdict low | high | ambiguous (medium removed; no parallel type). Rule order: unknown suffix (not in ALL_GANTRY_MCP_TOOL_NAMES, shared/admin-mcp-tools.ts:206-215) -> ambiguous; closed HIGH rows -> high: SCHEDULER_MUTATION_MCP_TOOL_NAMES (:32-39), AUTHORITY_CHANGING_GANTRY_MCP_TOOL_NAMES, DECISION_ACTOR_GANTRY_MCP_TOOL_NAMES, DURABLE_GRANT_EXCLUDED_DISPATCHERS, DELEGATION_DISPATCHERS (:129-177) IMPORTED never restated; protected/destructive writes by argument (file write/promote_scratch to a protected virtual target or protected:true; browser_act file_attach/file_upload with a raw-path source \u2014 paths or source.type path \u2014 outside the workspace, protected or secret); OWNER RULING 2026-09-05: async_run_command, async_mcp_call, mcp_call_tool, capability_run -> ambiguous (judged with arguments; never forced HIGH, never LOW); argument-shaped rows (file per-action discriminated predicate over the complete contract, runner/mcp/tools/file.ts:12-62; browser raw-path predicate mirroring browser-artifact-policy.ts:47-153 by shape, that file untouched); everything else registered -> low. A shape failing the tool's own schema is ambiguous, never high. The exhaustive test iterates ALL_GANTRY_MCP_TOOL_NAMES and the imported sets so registry additions are covered without editing the test.",
        "LANE GATE (0121, spec 'ask mode, auto_strict and jobs unchanged'): evaluateNativeRiskBranch gains toolInput (the classifier copy already built at permission-classifier.ts:302), workspaceRoot and lane: PermissionLane; the table applies under interactive_auto ONLY; under every other lane the branch returns exactly today's result. Lane derived once in the classifier via T1's derivation (application/permissions/auto-lane-analysis.ts:97-114 \u2014 reused, not re-implemented) from permissionMode + hostJobId; hostJobId added to the classifier input and threaded from the IPC helper (one call site, :367-375) and from the inline live consult (app/bootstrap/inline-agent-loop-tools.ts:389-445, one call site) \u2014 closes the inline scheduled-run leak (an inline job in auto mode derives autonomous). auto_strict keeps the strict guard's precedence (permission-classifier.ts:348-364). The ONE legitimately changed existing expectation is the T2a helper pin for memory_save under interactive_auto (now low); any other changed expectation outside gantry-tool-risk.test.ts is a defect.",
        "NATIVE FILE WRITES (spec AF-AC2/AF-AC4): the eligibility gate (application/permissions/permission-classifier.ts:11-28) accepts exactly FileWrite and FileEdit in addition to today's predicates (these facade ids are what BOTH runners send: Anthropic maps native Write/Edit -> FileWrite/FileEdit, shared/gantry-tool-facades.ts:64-76; deepagents sends FileWrite/FileEdit with { path, ... }, gantry-facade-tools.ts:110-132,160-189; Anthropic input keeps { file_path, ... } \u2014 both keys accepted and pinned). NEW application/permissions/native-file-write-risk.ts: judgeNativeFileWrite({ toolName, toolInput, workspaceRoot }) -> low | high | ambiguous plus the shared PROSPECTIVE-write containment predicate (lexical containment after normalisation; nearest EXISTING ancestor realpath-contained; no symlink component or symlink target via lstat; missing target allowed) and the existing protected/hidden/secret capability-path predicates (shared/tool-execution-protected-paths.ts:40-102). gantry-facade-file-safety.ts stays the execution authority (async, config-bound, mkdir) and is NOT imported; T2a's hard-boundaries evaluator is NOT reused (it checks EXISTING read targets). The browser raw-path row uses the same helper.",
        "HOST-SIDE ENTRY PROTECTION (spec AF-AC2 owner ruling 2026-09-02): ONE pure (scope, path) predicate isProtectedArtifactEntry combining domain/file-artifacts/protected-virtual-path.ts:23-57 (excludes AGENTS.md by design) with the prompt-profile scope's own protection (isAgentProfileArtifactWrite \u2014 prompt-profile/**/AGENTS.md and SOUL.md stay hidden); list filters protected descriptors and adds protectedHidden: number to its { ok: true, artifacts } response; read refuses after id resolution through the existing reject(error, code) convention with code protected_entry and the exact text 'Refused: <path> is protected. This is not a permission question \u2014 don't retry, tell the owner.'; runner/mcp/tools/file.ts appends '(N protected entries hidden \u2014 not a permission question, don't retry.)' only when protectedHidden > 0 and relays the refusal unchanged. write/promote_scratch protection (authorizeProtectedPromptMutation) unchanged. Ordinary-scope counter-tests required.",
        "PROVENANCE UNCHANGED: a native low verdict is the existing native allow result (status skipped; decidedBy/source auto_classifier); no new provenance field; the harness asserts decidedBy and that the stubbed LLM consult was NOT invoked for S2/S4/S5/S6.",
        "Concerns that must not share a file: the table (application, pure over arguments + one fs-backed containment call) vs the native-risk helper (runtime, lane + result construction) vs the classifier orchestration; the entry predicate (domain or handler module, pure) vs the handler I/O vs the runner tool rendering.",
        "No domain error type introduced. Nothing DEFERRED: the rails are untouched (attachment_open birthright is T2a's); decision memory (T3), the outage latch (T6) and the inline memory/latch wiring stay out; browser-artifact-policy.ts runtime behaviour untouched.",
        "Verification: python3 factory/scripts/verify.py is authoritative in the gate environment; the Postgres lane (npm run test:integration:postgres, GANTRY_TEST_DATABASE_URL exported) is orchestrator-run stage evidence as for T1/T2a \u2014 T2b touches no storage code; four pre-existing suites are red on origin/main independent of this story (main quickfix before T3a)."
      ]
    },
    {
      "id": "ASKFLOOR-1-T3a",
      "title": "Human-decision memory persistence",
      "objective": "permission_decision_memory gains the human_decision kind with outcome, scope, scope_key, acting person id + label snapshot, uuid ids, a human-row CHECK and two partial unique indexes replacing the unconditional key; scope keys derived in one module (outcome-aware: path-only Allow for single-destination file writes, full hash for No); a host-owned service remember|list|revoke with person-scoped revokeById; typed remember DTO and human_decision provenance codec in domain types.",
      "acceptance_criteria": [
        "repository round-trips human rows per person and rejects rows missing outcome/scope/scope_key/person; non-human rows keep today's key (Postgres tests)",
        "revokeById returns applied|already_revoked|not_found and never crosses persons; short-id derivation tested; the service exposes a per-person count of remembered exact Allows by tool id (trust-growth input)",
        "scope keys: exact Allow on Write/Edit/file write/promote_scratch keys on tool + canonical destination; No keys on the full effect hash; shell writes never path-only; protected destinations never key an Allow",
        "architecture note for the record kind; migration follows the table's existing naming convention",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T2b"
      ],
      "user_facing": false
    },
    {
      "id": "ASKFLOOR-1-T3b",
      "title": "Memory consult in the coordinator and the inline runtime",
      "objective": "The coordinator consults human_decision records for interactive_auto only: exact remembered No first (before hard restrictions), remembered Allows after the non-overridable rails in place of the out-of-root tail exit (exact, then kind, then place via the trusted-root stage); the inline live runtime gets the decision-memory port and the same consult; ask, auto_strict and job lanes never consult.",
      "acceptance_criteria": [
        "seeded records resolve with decidedBy human_decision (allow) or deny without classifier consultation; kind matches read-only shapes anywhere; place matches under the root",
        "a rails-version bump or revoked record does not match and the re-ask carries the plan's reason line",
        "ask, auto_strict and job lanes never consult the memory (explicit tests); inline live runtime consults identically to IPC",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T3a"
      ],
      "user_facing": false
    },
    {
      "id": "ASKFLOOR-1-T3c",
      "title": "Remember settlement and learning writes",
      "objective": "The structured remember resolution flows through the closed codec set (normalization, settlement, prompt binding, callback reconstruction, recovery orchestrator, Postgres claim mapping, provider scalar encodings); both human-resolution chokepoints write the record only when the host-stamped rememberEligible lane re-derives true; forged/replayed/ineligible callbacks settle once-only; the end-to-end S2 fixture proves tap, persist, reuse.",
      "acceptance_criteria": [
        "a remembered Allow/No on an eligible DM card persists once with the default or chosen scope and applies the current request; Just-this-once never persists",
        "remember callbacks for ask, auto_strict, group or job prompts never persist (tests per lane); recovery/replay after restart preserves the structured intent",
        "unresolved person: nothing persisted, once semantics, the plan's post-tap line",
        "AF-AC8 S2 = 0 taps after at most one remembered Allow",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T3b"
      ],
      "user_facing": false
    },
    {
      "id": "ASKFLOOR-1-T4",
      "title": "Live job projection of learned decisions (0153)",
      "objective": "Scheduled and inline job permission requests consult the owner's active allow-outcome human_decision records as one deterministic declared-grant input, live and revocable, person-scoped via execution_context.personId in both job paths (unresolved identity projects nothing); the matched record id is stamped into the permission audit context in both lanes; classifier still never runs for jobs; inline-scheduled tests rewritten to the projection.",
      "acceptance_criteria": [
        "exact/kind/place match allows a job request with human_decision provenance; near-miss, rails-bump, revoked, deny-outcome or other-person records still card (projection matrix)",
        "both job lanes record the matched record id beside jobId; jobs never write the memory",
        "inline-scheduled expectations assert the projection, not classifier consultation",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T3c"
      ],
      "user_facing": false
    },
    {
      "id": "ASKFLOOR-1-T5a",
      "title": "Card UX and provider surfaces",
      "objective": "Eligible DM cards render Allow (remembered, default) / one alternative remembering button / Just this once / No with the plan's pre- and post-tap lines; destructive and protected cards offer no remembering Allow; batch cards carry the once-only line in both renderers; ineligible-lane cards are byte-for-byte today's; every provider file for the remember resolution and the memory_forget affordance, incl. the message-actions union entry and Teams per-row ActionSets.",
      "acceptance_criteria": [
        "labels, order and copy match the plan on all four providers for eligible, destructive, protected, batch and ineligible-lane cards (snapshot/parity tests)",
        "memory_forget renders and settles on all four providers; Teams renders ten Forget buttons via per-row ActionSets",
        "AF-AC8 S4: rm -rf build asks once; Allow remembers that exact command and target; the identical command never asks again; rm -rf dist asks; No is remembered exactly too",
        "trust growth: after three remembered exact Allows for the same high-risk gantry tool the card offers one broader option (kind scope keyed by tool id), never the default; destructive shapes never get it",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T3c"
      ],
      "user_facing": true
    },
    {
      "id": "ASKFLOOR-1-T5b",
      "title": "/permissions: remembered list, all, forget",
      "objective": "The existing /permissions command keeps its mode display and appends the ten newest remembered decisions with Forget buttons, used-by-job suffixes from actual audit consumption, /permissions all as a plain-text full list with short ids, and /permissions forget <id> with person-scoped prefix resolution; the caller binds app, agent and resolved person; every group variant is non-mutating DM guidance; one guidance line in the prompt profile.",
      "acceptance_criteria": [
        "bare /permissions, all and forget <id> behave per the plan with the exact replies for applied, already forgotten, not found and ambiguous; ambiguity never mutates",
        "rows show outcome, scope, date and the snapshotted approver label; used-by-job comes only from records a job actually consumed",
        "group variants never mutate and reply with the DM-feature line; the plain-chat pointer line exists in the prompt profile",
        "AF-AC8 S5 (remembered shape runs in a job with 0 cards, near-miss cards, after Forget it cards again) and the listing fixture",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T5a",
        "ASKFLOOR-1-T4"
      ],
      "user_facing": true
    },
    {
      "id": "ASKFLOOR-1-T6",
      "title": "Judge outage, invariance suite, tap budget",
      "objective": "A process-local outage latch keyed by app, provider account and conversation sends one notice per episode before the first offline card on both the IPC and inline consult paths (driven by the typed status incl. wiring_missing), the card reason line, and Allow still remembering offline; the AF-AC6 invariance suite across every lane; the S6 fixture and the AF-AC8 aggregation run.",
      "acceptance_criteria": [
        "judge unavailable: exactly one notice per (app, provider account, conversation) per episode on both paths; deterministic reads and learned scopes unaffected; an uncovered read asks at most once; latch resets on the next answered verdict",
        "AF-AC6: ask, auto_strict, autonomous incl. the 0153 matrix, YOLO backstop, unmapped-forced-ask, scheduler/admin/destructive asks, the CARDSIMPLE-1 family rail-hit and the inline-scheduled path behave as today except where AC2/AC3/AC4 deliberately change them",
        "AF-AC8 S6 fixture and the aggregation over S1-S6 pass",
        "existing unit and Postgres integration suites pass; tsc and check:architecture green (AF-AC7)."
      ],
      "depends_on": [
        "ASKFLOOR-1-T4",
        "ASKFLOOR-1-T5b"
      ],
      "user_facing": true
    }
  ],
  "project": "",
  "plan_sha256": "d9df9a3091b0c2be66a51bbeaf8d30f6ed1b13525b20f9267896c17648288ace",
  "commit": "06b7c093ae10959052d7945e03572e07d23a56e9"
}
