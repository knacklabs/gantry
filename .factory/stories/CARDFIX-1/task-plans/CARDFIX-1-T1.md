# CARDFIX-1-T1 — Pause stories carry real actions: retry-and-ask + real Pause job, neutral layer

Contract (CARDFIX-1-AC1..AC4 as recorded): every `formatSchedulerSetupStory` delivery carries at least one working action affordance on all four providers via the neutral path; the compound-command denial card offers exactly **Allow once for this run** (retry-and-ask) and **Pause job**, never a durable-grant button (0134); each action acts through the neutral router (retry-and-ask starts exactly one fresh run in ask mode, idempotent per pause story; Pause pauses the job for a same-channel approver); existing suites pass, tsc + architecture green. No provider-specific handlers of any kind (owner directive).

Steps:
1. Neutral types (`apps/core/src/domain/message-actions.ts`): add affordance kind `scheduler_retry_ask` (jobId + one-shot key) to the union at :3, and add `scheduler_pause_job` + `scheduler_retry_ask` variants to `MessageActionCallbackInput` at :62.
2. Build affordances where pause stories are built: `jobs/execution-notifications.ts:269` (`notifyJobSetupRequired` fallback prose) attaches the action set to the `sendJobNotification` options — the route already forwards `actionAffordances` (`jobs/delivery.ts:152`). Compound denial (`permission_denied` with the piped-RunCommand blocker) ⇒ [retry-and-ask, pause]; other blockers ⇒ existing grantable setup action + pause; assert ≥1 action. Sources wired at `execution-phases-setup.ts:276`, `execution-phases-run.ts:220`, `execution-finalization.ts:192`, `request-access-job-recovery.ts:68`. `transient_permission` delivers nowhere today — leave it.
3. Router (`app/bootstrap/channel-message-action-router.ts:27`): validate and forward the two new callback kinds to the registered handler, mirroring `scheduler_run_now`.
4. Handler (`app/bootstrap/runtime-live-stop-message-action.ts:199` neighbourhood): same-channel approver authorization exactly like run-now. `scheduler_pause_job` → scheduler pause via the existing IPC/management path. `scheduler_retry_ask` → exactly one fresh run of the job with a per-run interactive-ask override (the compound then asks via the normal ask-and-wait card; the human's Allow lands as the existing JOBPERM-2 once-grant; nothing persisted; the override applies to that run only). One-shot idempotency per pause story via the setup-fingerprint hash pattern (`setup-pause-permission-wiring.ts:230`); a second tap acks "already used".
5. Providers consume the neutral callback only: replace the guidance-only stubs — Telegram `channels/telegram/callback-handlers.ts:725`, Slack `channels/slack/channel-message-action-handler.ts:305`, Discord `channels/discord/interactions.ts:337`, Teams `channels/teams/message-actions.ts:407` — with dispatch through the shared callback (the renderers already emit the buttons; Slack keeps index-suffixed unique action_ids via `slackMessageActionBlocks`, never hand-built blocks).
6. Tests (implementer writes): `apps/core/test/unit/jobs/execution-notifications.test.ts` — `it('delivers every pause story with at least one action affordance')` (required leaf; drive all delivered sources); `apps/core/test/unit/application/scheduler-message-actions.test.ts` — `it('retry-and-ask starts exactly one fresh run in ask mode and is idempotent')` and `it('pause job action pauses the job for a same-channel approver')` (required leaves) + non-approver rejection; extend the provider render tests for the new kind (telegram.test.ts:3413, slack.test.ts:4819, discord.test.ts:470, teams/teams.test.ts:1744, provider-affordance-parity.test.ts:31); compound card asserts no durable-grant button.
7. Verify: `npx tsc --noEmit`; `npm run check:architecture`; `npx vitest run -c vitest.unit.config.ts apps/core/test/unit/channels/ apps/core/test/unit/application/ apps/core/test/unit/jobs/ apps/core/test/unit/domain/`; the orchestrator runs the Postgres lane host-side. Scoped `npx prettier --write` on touched files before finishing.

Out of scope: the durable setup approval card's own decision options (`setup-pause-permission-prompt.ts` unchanged), any 0134/0144 policy change, a pre-authorized once mechanism, the six GRACE-1 findings. If the per-run ask override appears to need a durable contract or a decision record, raise a signal — do not invent one.
