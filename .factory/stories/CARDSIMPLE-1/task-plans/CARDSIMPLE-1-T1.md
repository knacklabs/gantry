# CARDSIMPLE-1-T1 — Family-wide grants: shared synthesizer, family shape, rails-before-family, one narrow decision

Contract (CARDSIMPLE-1-AC2/AC4 as recorded): Allow on a simple command records the canonical family rule via ONE shared synthesizer so a later run invoking the same argv0 with different args proceeds without asking; a destructive/privileged/egress rail hit inside an allowed family still asks and permits Allow-once only, via the single coordinator seam; piped commands present no Allow anywhere; safe non-piped compounds resolve per-leaf; exact reviewed rules and capability grants behave byte-for-byte as today; pinned local_cli readiness matching unchanged; suites, tsc and check:architecture green; the narrow decision record exists. Minimum version per the simplicity loop (owner-approved 2026-09-01): no three-value classification, no prompt framework, no lane-parity cross-product, one decision amendment.

Steps:
1. Shared synthesizer helper (new, in `apps/core/src/shared/` next to `bash-command-parser.ts`): given a parsed eligible simple leaf, return the family rule content `<literal argv0> *`; pipes and every existing durable exclusion (meta-executor, stateful, interpreter, destructive redirect, remote-content — `nonDurableBashLeafReason`/parser guards) return no suggestion. Safe non-piped compounds synthesize per leaf.
2. Call it from the three rule-minting lanes, replacing their exact-argv synthesis: host `application/permissions/permission-suggestion-synthesis.ts:62` (`commandRules`); SDK runner `adapters/llm/anthropic-claude-agent/runner/permission-suggestions.ts` — grill F2: SDK-SUPPLIED RunCommand suggestions are canonicalized through the helper BEFORE the raw-SDK early return at `:145` (a supplied pipe suggestion must die there), and the inference path at `:262` uses the helper too; autonomous recovery `shared/autonomous-bash-recovery-rule.ts` — grill F2: the compound loop's direct rule minting at `:72` is replaced by per-leaf helper calls, not just the named leaf function.
3. Durable validator: amend the leading-wildcard-breadth rejection in `shared/durable-access-policy.ts:219/:330` to admit the family shape under a PRECISE predicate (grill F3): argv is exactly `[<literal argv0>, '*']`, argv0 is a bare command word or absolute path with NO wildcard/script-leaf content, and reviewed-script rule forms (`normalizeScriptLeafRuleContent` output, e.g. `skills/.../post.py *` shapes — `bash-command-parser.ts:629`) remain REJECTED as families. The previously rejected broad-prefix examples (`gh *`, `aws *`, `/usr/local/bin/acme *` — durable-access-policy.test.ts:406) become deliberately ADMITTED — that is this feature; revise those test expectations explicitly and add regressions asserting script-shape rejection. The matcher grammar already supports the shape (`tool-rule-matcher.ts:550/:622`) — no parser changes.
4. `isFamilyRule?: true` as an ADDITIVE optional marker on the successful match result (`shared/tool-execution-policy-service.ts:90-108`; grill F4 — exact/capability consumers see no new field, no contract change). Compound semantics: if ANY leaf of a compound matched via a family rule, the decision is family-provisional (rails run on the exact full command); add one mixed exact-plus-family compound regression.
5. Coordinator (`runtime/permission-decision-coordinator.ts:84`): when a reviewed-rule match has `isFamilyRule`, run the existing deterministic rails on the exact command first; honor the match only on rail silence. On a rail hit: clear/omit durable suggestions and select `allow_once | cancel`, computed HERE. Grill F1: the coordinator is only the sole seam once the INLINE lane stops overwriting it — `app/bootstrap/inline-agent-loop-tools.ts` captures suggestions before coordination and overwrites `request.suggestions`/`decisionOptions` in its tail (`:360`, `:465`); amend that tail to honor coordinator-mutated values, never the captured locals (the IPC tail already preserves pre-set options). ipc-permission-classifier-decision.ts:121 and core-tool-permission-coordinator.ts:61 remain unchanged; exact/capability matches keep the current early return byte-for-byte.
6. ONE narrow decision record (`./forge decision new family-match-provisional-to-rails` or similar slug): family matches are provisional to deterministic rails; expressly amends 0040 (rails precede only literal-argv0 family matches; exact reviewed rules keep 0040's order); links 0121, 0144 and JOBPERM-2 as compatible and unchanged (a family rail hit attaches no durable suggestion, so the existing `once` path applies without a grant-selection amendment).
7. Tests (implementer writes): required leaves — `apps/core/test/unit/shared/family-rule-synthesis.test.ts` `it('family rule allows the same command with different args without asking')` and `it('pipes and durable exclusions synthesize no family rule in any lane')` (parameterized over the three lane entry points as smoke calls); `apps/core/test/unit/runtime/permission-decision-coordinator.test.ts` `it('a rail hit inside an allowed family asks and permits allow-once only')`. Regressions: exact reviewed rule behavior unchanged, capability rule behavior unchanged, rails on non-family paths unchanged, pinned local_cli readiness matching untouched (`application/jobs/job-readiness-service.ts:393`).
8. Verify: `npx tsc --noEmit`; `npm run check:architecture`; `npx vitest run -c vitest.unit.config.ts apps/core/test/unit/shared/ apps/core/test/unit/runtime/ apps/core/test/unit/application/`; orchestrator runs the Postgres decision-chain/memory/durable-authority suites host-side. Scoped `npx prettier --write` on touched files.

Out of scope: T2's card merge and T3's late-tap work; any change to exact-rule or capability matching, the classifier, YOLO policy, autonomous-lane behavior (0121), ask/auto_strict modes; the lane-parity cross-product (deferred: when a lane acquires distinct synthesis policy). If the boolean cannot be threaded without changing exact-rule behavior, or the coordinator seam turns out not to be the single suggestion point, raise a signal — do not invent a framework.

Successor coupling (owner 2026-09-01): ASKFLOOR-1 (docs/specs/askfloor-1-judge-actually-judges.md, roadmap-dependent on CARDSIMPLE-1) lands NEXT on this same decision pipeline — it calibrates auto-mode FIRST-asks (browser action matrix, enumerated read bindings) as auto-only native-risk inputs while this task fixes REPEAT-asks. Keep the seams separable for it: the family-rail path in the coordinator must stay a small, clearly bounded branch (ASKFLOOR's invariance suite will assert around it), do not touch `gantryToolDefaultRisk` or the classifier entry conditions here, and name the family-rail-hit reason string stably so ASKFLOOR's provenance tests can reference it.
