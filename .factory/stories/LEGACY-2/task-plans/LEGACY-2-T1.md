# LEGACY-2-T1 — Remove the providerConnection shadow field, dual-reads and the 19 expired exceptions

Contract (LEGACY-2-AC1..AC4 as recorded): the in-memory `providerConnection` shadow field is deleted (types + parser fill); every `providerAccount ?? providerConnection` read collapses to `providerAccount`; writers stop creating the shadow property; rename-only locals lose the token; the 19 `symbol: providerConnection` entries leave `scripts/architecture-exceptions.json` and `npm run check:architecture` passes with none left; a settings document that still carries `provider_connection` keeps being rejected by the EXISTING strict key check — no new code for it; existing suites pass with only shadow-field assertions changed; tsc green. No behaviour change.

Steps (do them in this order — tsc after step 1 lists every dependent site):
1. Types + parser: delete `providerConnection?: string` from `apps/core/src/config/settings/runtime-settings-types.ts:40`; delete the fill `providerConnection: providerAccount` in `runtime-settings-parser.ts:241`; delete the two secondary optional shadows `application/control-plane/control-plane-storage-model.ts:141` and `cli/provider-utils.ts:114`.
2. Dual-reads → `providerAccount` only: `channels/slack/permission-approval-delivery.ts:122` (keep `conversation.providerAccount === providerAccountId`), `control-plane-storage-model.ts:191`, `cli/provider-utils.ts:140`, `config/settings/runtime-settings-binding-derivation.ts:29,44`, `conversation-install-settings.ts:123`, `settings-revision-document.ts:51`, `runtime-settings-renderer.ts:436`, `runtime-settings-validation.ts:190,194,219`, `desired-state-export-helpers.ts:182`, `desired-state-provider-conversations.ts:103`, `desired-state-service-helpers.ts:169,187,227,365`, `desired-state-conversation-reconcile.ts:103`, `observer-activation.ts:86`.
3. Writers: stop creating the shadow property in `conversation-install-settings.ts:67`, `runtime-settings.ts:406`, `desired-state-current-export.ts:248,515`, `desired-state-conversation-reconcile.ts:204,283`; remove the strip in `config/index.ts:163`.
4. Rename-only: `control/server/routes/agents.ts:638-659` local → `providerAccount`; helper/local names in `desired-state-export-helpers.ts:126,135,175`, `desired-state-service-helpers.ts:486`, `runtime-settings.ts:344,361,484` → `providerAccount…` (names only).
5. Exceptions: delete exactly the 19 entries with `"symbol": "providerConnection"` from `scripts/architecture-exceptions.json` (introduced 2026-08-07, remove_by 2026-08-28). Keep every other entry byte-identical.
6. Tests (implementer writes/updates): in `apps/core/test/unit/config/runtime-settings.test.ts` add `it('rejects provider_connection as an unsupported conversation key')` (required leaf; proves AC2 through the existing strict check) and `it('parses conversations without a providerConnection shadow field')` (required leaf; parsed conversation has no `providerConnection` property). Update only the test/fixture lines that asserted or constructed the shadow field (control-plane-read-model.test.ts, guided-action-model.test.ts, channels/cli tests that spread `providerConnection`); assertions on `provider_account` stay.
7. Verify: `npx tsc --noEmit`; `npm run check:architecture` (must pass — zero providerConnection exceptions); `npx vitest run -c vitest.unit.config.ts apps/core/test/unit/config/ apps/core/test/unit/channels/ apps/core/test/unit/cli/ apps/core/test/unit/application/ apps/core/test/unit/control/`; the orchestrator runs the Postgres lane host-side. Scoped `npx prettier --write` on touched files before finishing.

Out of scope (deferral D-0070): `providerConnectionId` anywhere (ConversationRoute, identity-event payloads, CLI setup return names in cli/slack.ts, discord.ts, teams.ts, telegram-connect.ts, group-helpers.ts) and the OpenAPI enum `missing_provider_connection`; the `channel-providerConnection:` id prefix accepted by the exporter filter (`desired-state-current-export.ts:665`) is an account-id prefix, not the field — leave it. No migration, no silent-drop code, no compat shim.
