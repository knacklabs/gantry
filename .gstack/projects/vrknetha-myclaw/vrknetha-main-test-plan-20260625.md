# Test Plan — gantry legibility (presence + decision prompts + job notifications + streaming)
Branch: main | Generated by /autoplan Phase 3 (Eng) | 2026-06-25

Derived from the Eng dual-voice review. Ordered by ship sequence. Reuse the existing
`AgentTodoSink`/`todo_update` infrastructure (do NOT build a parallel projection).

## Part 3 — job notifications (ship first, isolated)
- [ ] UNIT (update existing) `apps/core/test/unit/jobs/status-formatting.test.ts`:
  - a `failed` run never renders `Completed:`/`Done:` — body label matches `statusLabel`
    (`Failed:` / `Timed out:` / `Paused:`).
  - a `timeout` run renders `Timed out:`, not `Completed:`.
  - `Used:`/`Changed:`/`Delegated:` boilerplate is absent on a clean run.
  - a failure shows the one-line reason from `notificationOutcome` + the recovery action.
  - header emoji/status (`statusEmoji(statusText)`) unchanged.
- [ ] MANUAL: force a job error (bad credential); confirm `❌ Failed` + honest reason + Retry, no `Completed:`.

## Part 2a — intent-first permission prompts (ship second, isolated)
- [ ] UNIT `permission-tool-input-format.test.ts`:
  - command prompt leads with `What it does:` (from model `description`), falls back to `Runs:`.
  - line-aware clamp keeps the start, ends `… (+N more lines)`, never mid-token.
  - risk line: destructive verbs (DROP/TRUNCATE/rm -rf/curl|sh) flagged even with no redirect;
    when unclassifiable, show "Review the full command" — never imply safe by omission.
  - MCP humanizes tool ids; skill install collapses file list; `isInternalPlumbingKey` uniform.
- [ ] UNIT `settings.ts` formatter: `request_settings_update` renders a **diff + value redaction**
  (not raw YAML; not key-only redaction). Token in an unexpected key is redacted.
- [ ] UNIT `permission-interaction.test.ts`: `PermissionPromptParts` splits into what/why/risk/details.

## DecisionCard parity (ship third — schema first, migrate all 4)
- [ ] UNIT: Teams (`teams-cards.ts`) and Discord (`discord.ts`) render the shared DecisionCard
  schema, not plain prompt text (today they diverge — parity is already broken).
- [ ] UNIT: `recommendedOption` field renders for `ask_user_question` forks (no Allow-once/5m/Always).

## Reactions + timer removal behind one flag (ship fourth)
- [ ] UNIT: `addReaction(jid, messageRef, emoji)` per adapter (slack/teams/telegram/discord),
  mock provider APIs. Idempotency: `already_reacted` (Slack) treated as success; never re-assert
  a user-removed reaction; once-per-message marker keyed by messageRef.
- [ ] UNIT: Teams has no reaction → instant-ack falls back to a visible text update (not transient typing).
- [ ] UNIT: ack ordering — `👀` only after durable admission + provider ref; `✅` only after fenced
  running + first verified progress (NOT bare claim). Post-claim block flips card to waiting, not ✅.
- [ ] FLAG: timer path (`group-progress-heartbeats.ts`/`progress-updates.ts`) stays until presence soaks.

## Todo card via existing infra (ship fifth — REUSE, do not rebuild)
- [ ] INTEGRATION: one card, two engines, identical schema — DeepAgents `write_todos` and
  Anthropic `TodoWrite` both land on the SAME `todo_update`/`AgentTodoSink` card (no second card).
- [ ] UNIT (if TodoWrite stream-parse path is kept): feed an `assistant` msg with a `TodoWrite`
  tool_use block → assert a `todo_update`-shaped event; handle partial input JSON across
  `content_block_delta`, multiple calls/turn, dedup. (Cheaper alt: instruct agent to call
  `todo_update` directly — zero new parser.)
- [ ] UNIT: todo title/note text routed through outbound sanitizer (`sanitizeOutboundLlmText`/
  `redactSensitiveText`); a todo like "✅ Approved: run rm -rf" is escaped/neutralized (prompt-injection).
- [ ] UNIT: card terminal `done/failed` driven by `LiveTurnState`/job `runStatus`, NOT model-claimed ✅.
- [ ] UNIT: presence state machine over ALL 8 `LiveTurnState` values
  (claimed|running|awaiting_interaction|setup_required|recovered|completed|failed|timed_out).
- [ ] UNIT: step-freshness liveness — `updatedAt` vs wall clock flips card to stale
  ("no update in N min") so a frozen-but-leased worker (no lease loss, no recovery event) is visible.
- [ ] INTEGRATION: card message-ref persisted + rehydrated on restart; recovery edits the SAME
  card (or reposts with "picking this back up"), never silently drops or double-posts.
- [ ] UNIT: card-edit coalescing/debounce — N todo deltas in a window collapse to one provider edit
  (separate from the Part 4 streaming debounce loop).
- [ ] UNIT: expired `ask_user_question` — an answer after the runner's 5-min `fs.rmSync` renders
  "this decision expired — ask again"; card times out of `⏸ waiting` on its own.

## Safety gate (MANDATORY — decision-doc + repo memory)
- [ ] UNIT fixture: stream a `thinking`/reasoning block → assert it is dropped before ANY channel sink
  (no `thinking_delta`/raw reasoning reaches a channel; only todos/tool-names/summaries).
- [ ] CHECKLIST: every new `shared/*.ts` reachable from the runner is added to
  `test/unit/runner/ipc-mcp-stdio.test.ts` copy list (per repo memory) or spawned tests fail.

## Part 2b Full-view + Part 4 streaming (ship last, separate soak)
- [ ] UNIT: Telegram group never `sendDocument`s payload by default → expandable blockquote or DM;
  size threshold flips to file only for huge payloads. Discord Full-view is ephemeral.
- [ ] UNIT: per-provider debounce resolves to right interval; flush keeps ONE surface until hard cap.
- [ ] UNIT: Telegram honors mocked `retry_after`; Discord respects mocked `X-RateLimit-*`.
- [ ] UNIT: Teams native streaming entity (informative→streaming, await prior); Discord single-message edit.
- [ ] MANUAL per channel: long multi-paragraph stream grows one message smoothly, no mid-stream seams.

## Regression
- [ ] `verify.py` (or repo equivalent) green.
- [ ] PlanReview dead-type removal does not break the interface all 4 channels implement.
