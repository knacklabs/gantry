// Provider artwork belongs here when a licensed source asset is available.
// The UI deliberately falls back to a text monogram rather than embedding
// provider SVG markup or data URIs in components.
export const providerAssetById: Record<string, string> = {};
