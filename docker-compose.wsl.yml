services:
  postgres:
    # WSL/Docker Desktop amd64 hosts can force the amd64 platform while keeping
    # the same pinned multi-platform image digest as the default compose file.
    image: pgvector/pgvector:0.8.2-pg16-trixie@sha256:fce8fb583b92ef8af5150b373a96415d46a8cd38ba09b38efeea17c8b4c7d782
    platform: linux/amd64
