# Assumptions Ledger: Permission Auto-Mode UX + Skill Installs

Companion to `docs/architecture/runtime-permission-ux-goal-prompt.md`. Every
implementation stage records assumptions it made because information was
missing — one row per assumption, under its stage section. The orchestrator
fills `Validated` (`ok` / `fixed` / `escalate`) before the stage commit. A
stage with no assumptions writes "None."

Format:

| #   | Assumption | Missing info that forced it | Choice taken | Impact if wrong | Validated |
| --- | ---------- | --------------------------- | ------------ | --------------- | --------- |

## Stage 0 — Plan decisions (user-approved)

| #   | Assumption                                                              | Missing info that forced it                                                   | Choice taken                                                                                                                                                          | Impact if wrong                                                         | Validated                      |
| --- | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------ |
| 0.1 | Relaxing auto mode reverses part of the recorded "trust pause" decision | —                                                                             | User explicitly approved: `auto` consults the classifier for unproven commands; `auto_strict` preserves the strict behavior; denylist/sanitized-input rails unchanged | —                                                                       | ok — user direction 2026-07-15 |
| 0.3 | `auto` = allow-leaning classifier                                       | User challenge 2026-07-15: "how is it a classifier if we still have to allow" | Classifier allows unless it identifies concrete risk; ask is the exception; `yolo` stays the pure denylist-reverse mode; `auto_strict` stays paranoid                 | Riskier gray-zone commands auto-run — bounded by denylist + risk rubric | ok — user direction            |
| 0.2 | These fixes ride the `feature/otel-llm-observability` branch            | User said "fix in the same branch"                                            | One branch, one PR carrying both observability and UX work                                                                                                            | Bigger PR to review                                                     | ok — user direction            |

## Stage A — Skill install reliability

| #   | Assumption                                                                     | Missing info that forced it                                                                                                   | Choice taken                                                                                                                                                                                 | Impact if wrong                                                                    | Validated                      |
| --- | ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------ |
| A.1 | Immediate same-session skill context remains singular for multi-skill installs | `installed_skill_context` supports one skill, and expanding its consumer is outside Stage A scope                             | Receipt wording contract: `Activation: The first installed skill, <name>, is active in this conversation immediately; the remaining installed skills are available from the next message.`   | A later contract may need plural immediate context                                 | ok — user direction 2026-07-15 |
| A.2 | Multi-skill installs use partial-success semantics                             | Atomicity was unresolved and cross-skill rollback would make unrelated valid skills unavailable                               | Each discovered skill uses the existing single-skill install, bind, settings-sync, and per-skill rollback path independently; one failure does not block or roll back other successes        | Users may need to retry only the named failures                                    | ok — user direction 2026-07-15 |
| A.3 | Install-command discovery is bounded                                           | An installer can produce an arbitrarily large skill tree                                                                      | Discover at most 25 nested skill roots in deterministic alphabetical path order, skip the remainder with a receipt note, and load assets only for each selected skill as it is processed     | Skills beyond the cap require another install command                              | ok — user direction 2026-07-15 |
| A.4 | Failure diagnostics can contain credentials in URIs or labelled values         | Existing failure sanitizers did not cover hierarchical URI credentials or complete multi-word labelled values                 | Mask URI credentials and suppress credential-bearing lines before caller redaction; preserve only the five exact tested safe status diagnostics through a positive allowlist                 | A missed credential could leak into a chat receipt                                 |                                |
| A.5 | Verbose command failures usually put the useful reason at the end              | Truncated output has no structured field identifying the actual failure reason                                                | Retain a bounded raw tail, discard its first retained line or fragment when truncated, sanitize the complete remaining lines, and prefix the truncation marker                               | Single-line overflow yields only the marker; earlier useful output may be omitted  |                                |
| A.6 | Complete PEM blocks must be redacted before labelled-value masking             | Review found that line-wise labelled-value masking can remove the PEM `BEGIN` line before shared PEM redaction sees the block | Redact complete PEM blocks across the whole retained text first, then apply line-wise labelled-value masking                                                                                 | Private-key body lines could leak into a failure receipt                           |                                |
| A.7 | Rollback must not overwrite concurrent newer skill state                       | Review found that unconditional snapshot restoration can race with newer catalog entries, bindings, or artifacts              | Compare current state with the catalog entry, binding, or artifact written by this install; restore only matching state, otherwise skip restoration and note the skip in the failure receipt | A failed install could overwrite newer state, or leave its own stale state behind  |                                |
| A.8 | A skill with more than 50 assets is a per-skill failure                        | Review found that asset collection stopped at 50 files without reporting that more files existed                              | Detect overflow, fail that skill with an explicit over-cap receipt reason, and preserve partial success for other skills                                                                     | Oversized skills could install incompletely, or require reducing their asset count |                                |

Autoreview decision note: the legacy single-skill receipt assertion was updated
to the user-approved A.1 `Installed: <names>` contract rather than reverting the
approved wording.

Autoreview safety note: ordinary package failures remain independent. If the
durable settings authority cannot be reconciled after a failed package, the
receipt preserves earlier successes and processing stops before another package
can widen the inconsistent state.

### Stage A orchestrator notes

- Two Codex sessions were lost to tooling issues during this stage (a wedged in-session autoreview, then a read-only sandbox); the orchestrator applied the final orphan-PEM tail hardening directly (A.9) after Codex's adjudication confirmed all findings real.

| #   | Assumption                                       | Missing info that forced it                                                   | Choice taken                                                                                                              | Impact if wrong                            | Validated                               |
| --- | ------------------------------------------------ | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | --------------------------------------- |
| A.9 | Tail truncation can orphan a PEM block's markers | Review r2: the retained window can slice off the BEGIN line (or both markers) | Orphaned END/BEGIN fragments and runs of 3+ long base64-only lines redact in the failure sanitizer; regression test added | Marker-less key bodies could relay to chat | ok — orchestrator-applied, test-covered |

### Stage A review round 3 (orchestrator-applied)

| #    | Assumption                                                                                  | Missing info that forced it                                                                                                                                         | Choice taken                                                                                                                                                                                 | Impact if wrong                                             | Validated |
| ---- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | --------- |
| A.10 | Bundle fingerprints must ignore store-inferred fields                                       | Review r3 (P1): stores infer `contentType` on read; fresh assets leave it undefined — every real-store compare differed, so rollback always skipped as "concurrent" | Fingerprint hashes path+content only                                                                                                                                                         | Failed replacements stay active with a failure receipt      | fixed     |
| A.11 | Compare-and-restore must be atomic vs concurrent installs                                   | Review r3 (P2): check-then-write window                                                                                                                             | In-process keyed mutex (`withSkillMaterializationLock`) serializes install writes and rollback compare+restore per materialization key — sufficient for the single-host-process install path | Stale snapshot could clobber a concurrent same-name install | fixed     |
| A.12 | The fresh-install cleanup block moved to `rollbackFreshInstallBinding` in the assets helper | Handlers file crossed its 700-line budget                                                                                                                           | Pure extraction, behavior preserved (existing tests cover both cleanup paths)                                                                                                                | —                                                           | ok        |

| A.13 | Rollback baselines must live in the store's own representation | Review r4 (P1): Postgres timestamptz formatting differs from `nowIso`, so in-memory rows never deep-equal reread rows — rollback always skipped on production PG | After bind, the persisted skill+binding are reread and used as the expected state for compare-before-restore | Failed replacements stay active on real Postgres | fixed |
| A.14 | The critical section spans snapshot → install → bind → persisted reread → settings sync | Review r4 (P1): snapshot-before-lock let two same-key installs snapshot the same old version and cross-clobber | Entire sequence runs inside `withSkillMaterializationLock`; rollback acquires the same key lock | Stale receipts / stale-snapshot restores under same-name concurrency | fixed |

| A.15 | `installSkill` can fail after overwriting the artifact but before the catalog save | Review r5 (P1) | No-installedSkill rollback path heals partial writes: if the catalog row still matches the snapshot but the stored artifact diverged, the snapshot bundle is restored (artifact only) | Old catalog hash pointing at a new bundle breaks the previously working skill | fixed |
| A.16 | The materialization lock covers only command-based installs in this process | Review r5 (P1) wants durable CAS shared by ALL writers (Control API, package installs, other processes) | Held: Stage A's surface is the chat-driven install path on the single-host runtime; durable versioned artifact CAS is the named follow-up | Cross-path same-name concurrent installs within the compare-restore window could still clobber | ok — documented residual, revisit: repository-level versioned CAS |
| A.17 | Stopping early must name every unattempted root | Review r5 (P2): `break` silently abandoned the remainder | `reportUnattemptedSkillRoots` adds each to the receipt's Failed list | Users unaware which skills still need installing | fixed |

| A.18 | YAML block-scalar credentials hide on continuation lines | Review r6 (P1): `password: \|` left the secret on more-indented lines | `redactLabelledContinuationBlocks` drops a credential label's block continuation lines entirely | Multiline secrets relayed to chat | fixed |
| A.19 | Rollback's artifact-then-catalog write order can desync on `saveSkill` failure | Review r6 (P2) | Compensating rewrite: on `saveSkill` failure the replacement bundle is re-put so catalog hash and stored bytes stay consistent, then the restore failure reports | Integrity-check failures on active bindings | fixed |
| A.20 | Recovered installs must count for duplicate detection | Review r6 (P2): keep-as-installed path skipped `installedMaterializationKeys` | Key recorded before continuing | Same-name later root overwrites the recovered skill with stale receipt context | fixed |

| A.21 | Every in-process skill writer shares the materialization lock | Review r7 (P1, sharpening A.16): permission-review installs and the Control API upload also write skills | Lock moved to `apps/core/src/shared/skill-install-lock.ts` (layer-legal for all writers); acquired at all three write seams and in rollback. Cross-PROCESS coordination remains the A.16 named follow-up | In-process same-name races closed; fleet multi-process window documented | fixed (in-process) |
| A.22 | Credential labels can introduce bracketed structures, not just YAML scalars | Review r7 (P1): `passwords: [\n "hunter2"\n]` leaked the continuation line | Block redaction also triggers on `[`, `{`, `(` openers | Structured multiline secrets relayed to chat | fixed |

| A.23 | Indentationless YAML sequences continue a credential block | Review r8 (P1): `passwords:\n- hunter2` — sequence items at the label's own indent escaped the continuation check | Continuation also covers same-indent lines starting `- ` | Listed secrets relayed to chat | fixed |
| A.24 | Fresh-install cleanup must re-verify state under the lock | Review r8 (P1): between a failed install and its cleanup, a queued same-name writer may have replaced the binding | Cleanup runs under the keyed lock with a `confirmState` deep-equal check against this attempt's binding; mismatch → skip with an explicit receipt line | Cleanup would unbind a newer writer's healthy skill | fixed |
| A.25 | Permission-review installs hold the lock across the full sequence | Review r8 (P1): lock covered only `installSkill`; bind, sync, and catch-side rollback ran outside it | Whole install→bind→sync→compensation block is one critical section (ordering proven by test) | Concurrent writer interleaves with a partial review install | fixed |

| A.26 | Truncation can slice off a credential label, stranding its continuation lines | Review r9 (P1): a 4 KB tail starting inside a `passwords:` block leaves label-less `- secret` lines no redactor recognizes | After dropping the partial first line, leading continuation-shaped lines (blank, indented, `- ` items) are dropped until the first fresh statement | Listed secrets relayed to chat from truncated diagnostics | fixed |
| A.27 | Command-install compensation must run inside the original lock, not a re-acquired one | Review r9 (P1): releasing the lock on failure lets a queued same-name install snapshot the failed intermediate state as its "previous" state | The whole snapshot→install→bind→sync→compensation sequence is one locked callback returning an outcome object; the loop acts on it outside the lock | Second writer restores a broken state instead of the original | fixed |
| A.28 | Partial-write heal must restore even when the broken artifact is unreadable | Review r9 (P1): stores replace artifacts in place; a failed write can leave a bundle `getSkillArtifact` cannot decode, and the heal bailed without rewriting the snapshot | Unreadable current artifact is treated as diverged (fingerprint `undefined`) so the snapshot rewrite still runs when the catalog row matches | Catalog left pointing at a corrupt bundle | fixed |
| A.29 | The materialization-lock registry must not grow unboundedly | Review r9 (P2): keys are request-derived; settled entries lingered for the process lifetime | Entry deleted after its sentinel settles, guarded by an identity check so a queued writer's newer sentinel is never removed | Slow memory growth in long-running runtimes | fixed |

| A.30 | In-lock compensation must restore, never skip on divergence (supersedes A.24's skip semantics) | Review r10 (P1×3, one root cause): with compensation inside the attempt's lock, deep-equal baselines are mutated by the attempt's OWN writes — installSkill's durable row before a later throw, sync reconciliation touching bindings, driver read-normalization — so "state changed → concurrent → skip" false-triggers and leaves broken state active | `rollbackInstalledSkillReplacement` is now compare-then-RESTORE: any divergence from the snapshot (row, binding, or bundle fingerprint, unreadable counts as diverged) rewrites the snapshot; `rollbackFreshInstallBinding` unbinds unconditionally (confirmState removed). In-process a held lock proves divergence is self-inflicted; cross-process stays the A.16 follow-up | Failed installs reported as failed while replacement state silently stayed active | fixed |
| A.31 | Compensation baselines must come from persisted reads, not in-memory returns | Review r10 (P1): bind-failure rollback compared `installSkill()`'s return against a storage re-read; drivers can normalize fields on read | The persisted row is re-read immediately after install, before binding | Restore skipped on representation-only differences | fixed |
| A.32 | The saveSkill-failure compensating rewrite survives the restore redesign (A.19 carried forward) | r10 redesign made the attempted bundle an explicit `attemptedAssets` input | On saveSkill failure with a diverged row, the attempted bundle is re-put so catalog hash and stored bytes stay consistent | Integrity mismatch on active bindings | fixed |
| A.33 | Compound credential labels contain spaces | Review r10 (P1): `database password: \|` missed the block redactor; only the label tail was redacted, continuation secret relayed | Label prefix/suffix accept spaces and tabs in `CREDENTIAL_LABEL_LINE_PATTERN` | Multiline secrets relayed from spaced labels | fixed |
| A.34 | An exact newline-boundary truncation keeps its first complete line | Review r10 (P2): the first tail line was dropped unconditionally; when the retained extra byte is `\n` the line is already complete and may be the only diagnostic | First-line drop now conditional on the boundary byte; label-less continuation dropping still runs after | Sole failure reason reduced to `[REDACTED_TRUNCATED_OUTPUT]` | fixed |
| A.35 | Headless installers must be told they run under an agent | Live smoke (2026-07-15): `npx skills add …` exited 0 in ~2s writing nothing — the CLI fell back to interactive prompts because `safeInstallerEnv` strips the agent markers its detect-agent check needs; `CI=1` is ignored by that flow | `safeInstallerEnv` sets the vendor-neutral `AI_AGENT=1`; verified end-to-end through the real runner (root discovered at `.agents/skills/…`) | Every `skills`-CLI install no-ops with "Installer command did not produce a SKILL.md file." | fixed |
| A.36 | Desired-state export must tolerate bindings whose conversation lost its provider connection | Live smoke (2026-07-15): installs that got past discovery crashed in `syncApprovedCapabilitySettings` → `exportCurrentDesiredState` with `Cannot read properties of undefined (reading 'installedAgents')` — the bindings loop can resolve a conversation id through the settings fallback that the first loop never exported (connection gone); the rejection surfaces only to the agent, nothing is logged | The binding is skipped together with its conversation (matching the first loop's drop-connectionless intent); regression test with an absent provider account | Every capability sync crashes, so no skill/tool install can complete | fixed |
| A.37 | Desired-state export must not strip settings-owned config the DB never projected | Live smoke (2026-07-15): past the A.36 crash, capability syncs failed full-file validation — the export rebuilt provider accounts with `runtime_secret_refs: {}` and conversations with `control_approvers: []` although the live settings.yaml carries both (env-ref token setups and hand-configured approvers have no DB rows) | Empty DB projections fall back to the existing settings entry (stored rows stay authoritative when present, per the desired-state-service precedence test); regression test with empty DB refs/approver rows | Every capability sync fails validation, so no skill/tool install can persist | fixed |
| A.38 | Oversized permission full views belong in the group, next to the prompt | User decision (2026-07-15): the `permission-payload.txt` DM confused the approver — the prompt is already visible in the group, so a private copy adds no privacy and strands context | Telegram full-view documents now go to the originating chat + thread with a "Full details for: …" caption; the approver-DM routing helper is deleted, prompt line reads "sent above for review." | Approvers hunt for an uncontextualized DM attachment | fixed |
| A.39 | Canonical fallback `channel-…` account rows are storage plumbing, never config | Live smoke (2026-07-15): `ensureConversation` mints `channel-providerAccount:default:<provider>` rows (with phantom agents) when ingestion lacks an attributed account; exporting them produced enabled telegram/slack accounts with no secret refs → validation failure. Rows must stay in the DB (messages reference them) | `exportCurrentDesiredState` filters them like internal app/control accounts; regression test | Every capability sync fails validation on phantom accounts | fixed |
| A.40 | Only operating surfaces belong in the settings export | Live dry-run (2026-07-15): observed-but-unconfigured chats (a group with no installs, a channel with only disabled installs) and composite live-turn queue jids (`<chat>::agent:…::provider_account:…`) leaked into the export as conversations with empty `control_approvers` → validation failure | Stored conversations export only with a settings entry, approver rows, or an active install; group routes with composite jids are skipped (the base chat exports through the other paths). Verified read-only against the live DB: validation ok | Unconfigured observed chats block every capability write | fixed |
| A.41 | Capability syncs must survive a stale settings base | Agent analysis, validated (2026-07-15): `restart-sync.ts` loaded the file base once while echo revisions (`settings.yaml:auto-import`) and `permission:persistent-tool-rule` writes keep advancing the DB head, so `SettingsStaleMutationError` hard-failed every install/approval sync, and the rollback's own sync failed the same way ("Failed …/Restore failed …" pairs). Live-verified: installs reached catalog+artifact+binding, then were unbound by the failing sync | Bounded reload+re-export+retry (3 attempts, stale/conflict errors only) in `syncRuntimeSettingsFromProjection` and `addAgentToolRulesToSyncedRuntimeSettings`, ported from the onboarding pattern (Codex-implemented); repro test proves retry-then-succeed and no-retry-on-real-errors. Stale-check semantics untouched | Any concurrent revision writer permanently breaks capability writes | fixed |
| A.42 | Revision documents must be canonical regardless of origin | The identity guard test (r-codex) failed: parse-time defaulting (`kind: group→channel`, `requires_trigger`, `persona`, `agent.default_model`) makes documents built from in-memory settings never equal documents built from parsed files — the source feeding the echo revisions | `settingsToRevisionDocument` canonicalizes through a render→parse round-trip at the choke point, so all four asymmetries (and future ones) collapse; the guard test now asserts identity holds | Every settings write mints an echo revision, keeping the base perpetually stale | fixed |

| A.43 | Async/delegated task tools follow the permission stack, not the sandbox | User decision (2026-07-15, grill-me): worker mode must work with or without an OS sandbox; four `enforcing === true` gates (spawn env flag, IPC `taskService`, `runSandboxedAsyncCommand` throw, recovery stub + queued-recovery skip) made `delegate_task`/async tools unmountable in direct mode despite an AgentDelegation grant | All four gates removed (Codex-implemented): tools mount on async-repo availability (+ AgentDelegation for delegation); direct mode executes background commands like foreground ones via the direct provider's `start()` (egress proxy env injected, `maxProcesses` cap dropped as user-wide RLIMIT_NPROC can't scope one tree); enforcing path unchanged; recovery builds the real service in both modes. No strict-mode knob by decision | Delegation and async tasks permanently unavailable on unsandboxed hosts | fixed |

| A.44 | Canonical revision docs must keep app-workspace account/conversation pairing | Live repro (2026-07-15): the A.42 render→parse canonicalization desynced internal `app`-provider accounts from their conversations; legacy-bindings reconstruction minted `app_default:agent:agent:…` refs and every sync failed validation ("references unknown provider account") — invisible in logs until the FIX-3 logging landed | Codex fixed the round-trip seam so app-provider pairs survive canonicalization; unit test pins it; acceptance = the real `syncRuntimeSettingsFromProjection` against the live DB prints SYNC SUCCEEDED (verified post-restart; the OLD running binary's echo revisions made retries legitimately non-convergent until restart) | Every capability sync fails; multi-skill installs roll back with "prior failure stopped this install" | fixed |
| A.45 | Explicit API clears must not be resurrected by export fallbacks (r12 P1×2) | `PUT approvers []` / provider-account PATCH `runtimeSecretRefs: {}` deleted rows, then the A.37 fallbacks re-copied old settings.yaml values and reconciliation re-inserted them — revoked authority revived | Explicit-clear markers (e.g. `__gantryRuntimeRefsAuthoritative`) make DB writes authoritative; file-only legacy setups still export (A.37 preserved); tests cover clear-then-export and file-only halves | Revoked approver/credential silently reauthorized | fixed |
| A.46 | Round-12 adjudication of remaining findings | P1 "keep async commands sandboxed" REJECTED — contradicts the explicit A.43 user decision; foreground direct-mode commands run equally unconfined, so no asymmetry. P2 cross-process restore clobber HELD — single-process hosts safe under the in-process lock; fleet-grade durable CAS remains the named A.16 follow-up | Documented here; no code change | — | held/rejected by decision |

## Stage B — Auto mode that decides

| #    | Assumption                                                                                             | Missing info that forced it                                                                                                                                                                                                  | Choice taken                                                                                                                                                                                 | Impact if wrong                                                                                        | Validated |
| ---- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | --------- |
| B.1  | “Workspace + granted folders” uses the existing workspace root and approved capability boundary        | The deterministic gate receives no separate granted-folder root list, and adding new authority plumbing is outside Stage B                                                                                                   | Preserve realpath confinement to `workspaceRoot` and require the existing matching approved capability; do not invent folder authority                                                       | A future explicit granted-root projection must extend the gate input and confinement check             |           |
| B.2  | “Recently approved” mirrors the existing seven-day recent-denial window                                | The contract sets the N≥2 threshold but does not define recency duration or approval/denial conflict precedence                                                                                                              | Set the boolean when `allowCount >= 2` and `updatedAt` is within seven days, unless a recent denial exists                                                                                   | A different duration or precedence changes classifier context, not durable authority                   |           |
| B.3  | The first `decisionOptions` entry is the current prompt’s primary/default action                       | The interaction contract has ordered options but no separate primary/default field                                                                                                                                           | At N≥2, reorder the existing persistent option ahead of allow-once in both IPC and inline callback seams                                                                                     | An adapter that ignores option order would need an explicit primary-action field                       |           |
| B.4  | The bounded “permission callback” scope follows the repository’s actual callback seams                 | The goal names `apps/core/src/runtime/permissions/`, but the mode and promotion callbacks live in runtime IPC, inline bootstrap, runner, and application permission modules                                                  | Touch only the existing seams needed to carry `auto_strict` and reorder the promotion action; add no new callback abstraction                                                                | A literal directory-only interpretation would leave one runtime lane or runner mode inconsistent       |           |
| B.5  | Existing public settings schemas must accept the same additive mode vocabulary as the settings parsers | Expanding the shared `PermissionMode` exposed compile mismatches in the existing settings response schemas                                                                                                                   | Add `auto_strict` to the existing permission-mode enums and schema tests without changing endpoint shapes                                                                                    | If public vocabulary were intentionally frozen, `auto_strict` would need a separate private projection |           |
| B.6  | Stage B2’s missing bounded scope is the sandbox adapter seam plus focused tests                        | Stage B2 gives behavior and acceptance criteria but no file list; the Gantry egress gateway on this branch already default-allows public hosts, while sandbox-runtime still gates undeclared domains before the parent proxy | Use the sandbox-runtime library callback in a tiny same-adapter runner so unmatched public destinations reach the Gantry parent proxy; keep the parent proxy as denylist and audit authority | A future sandbox-runtime default-allow configuration option could replace the wrapper                  |           |
| B.7  | Sandbox Runtime unmatched-destination approval must reject direct local-network targets                | Review P1: Sandbox Runtime dials loopback and private destinations directly instead of through the Gantry parent proxy                                                                                                       | Keep unmatched public destinations allowed, but reject localhost plus literal loopback, link-local, and private addresses in the callback                                                    | Local targets bypass Gantry egress private-address checks, denylist enforcement, and CONNECT audit     | fixed     |
| B.8  | Git commands are not deterministic silent reads                                                        | Review P1: workspace-owned Git config can execute fsmonitor, diff, and textconv helpers, while revision selectors can address hidden files                                                                                   | Remove every Git command from the deterministic silent-allow set and leave it to the Stage B classifier                                                                                      | Silent Git execution can run arbitrary helpers or expose hidden workspace content                      | fixed     |
| B.9  | Conversation permission commands must expose the strict auto mode                                      | Review follow-up: the shared mode vocabulary includes `auto_strict`, but `/permissions` parsing and help omitted it                                                                                                          | Accept, document, and test `/permissions auto_strict` alongside the existing conversation modes                                                                                              | Users cannot switch an individual conversation back to strict classifier behavior                      | fixed     |
| B.10 | Recursive grep is not a deterministic silent read                                                      | Review P1: validating only the explicit root still lets recursive grep traverse hidden descendants                                                                                                                           | Reject every recursive grep form from the deterministic proof set and leave it to the allow-leaning classifier                                                                               | Hidden credential files under an approved root could be read silently                                  | fixed     |
| B.11 | Unmodelled leading find options invalidate deterministic proof                                         | Review P1: `find -O1 /etc` and `find -f /etc` defaulted proof to workspace `.` while the command traversed `/etc`                                                                                                            | Reject any unmodelled option or expression before the first plain find root                                                                                                                  | Find could traverse an unvalidated root silently                                                       | fixed     |
| B.12 | Find `-follow` is a symlink-following traversal form                                                   | Review P2: the traversal denylist covered `-H`/`-L` but omitted the equivalent `-follow` expression                                                                                                                          | Reject `-follow` with the existing symlink-following find forms                                                                                                                              | Traversal could escape through a symlink after root validation                                         | fixed     |
| B.13 | Promotion ordering must synthesize default options                                                     | Review P2: interactive and DeepAgents IPC requests omit `decisionOptions`, so the N≥2 reorder never ran                                                                                                                      | Derive the default options from valid persistent suggestions when options are absent, then promote `Allow for future`                                                                        | Repeated approvals keep `Allow once` primary in production                                             | fixed     |
| B.14 | Active permission docs must describe both auto postures                                                | Review P2: capability guidance still promised that unprovable calls always ask and the classifier never widens the deterministic floor                                                                                       | Document Stage B's user-approved allow-leaning `auto` behavior and deterministic-proof-only `auto_strict`                                                                                    | Operators choose a mode using a false safety contract                                                  | fixed     |
| B.15 | Grep's `-d ACTION` consumes a value and can select recursive traversal                                 | Review P1: `grep -d recurse . .` consumed `recurse` as the pattern while the gate validated only the following roots                                                                                                         | Remove `d` from the no-value whitelist and reject `-d` forms from deterministic proof                                                                                                        | Grep could silently read hidden descendant contents under a validated root                             | fixed     |
| B.16 | Operand-less `df` has no workspace-local default                                                       | Review P2: the gate validated synthetic `.` while `df` actually reported every mounted filesystem                                                                                                                            | Require at least one validated `df` operand; only `du` retains the workspace default                                                                                                         | Host filesystem metadata could be exposed outside the validated boundary                               | fixed     |
| B.17 | Deterministic-proof docs must match the reviewed command set and traversal limits                      | Review P3: active guidance still described an `ls`/`cat`-only gate and incorrectly excluded all hidden-entry behavior                                                                                                        | Enumerate every reviewed shell command and document operand, recursive, hidden-descendant, symlink, and confinement restrictions                                                             | Operators cannot accurately evaluate the `auto_strict` safety boundary                                 | fixed     |
| B.18 | Recursive-by-default searchers cannot prove non-secret reads                                           | Review P1: `rg <pattern> <dir>` validates only the explicit directory while ripgrep opens every descendant, including secret files                                                                                           | Remove ripgrep and other recursive-by-default searchers such as `find` from deterministic proof; leave them to the allow-leaning classifier                                                  | Hidden descendant content could be read silently                                                       | fixed     |
| B.19 | Only real human approvals are operator-approval evidence                                               | Review P2: classifier auto-allows incremented the same counter used to claim that the operator recently approved the exact shape                                                                                             | Stop classifier auto-allows from incrementing the counter; only real approver decisions can produce `recentlyApprovedExactToolShape`                                                         | Automatic decisions could recursively justify later automatic decisions                                | fixed     |
| B.20 | Unmatched sandbox destinations require a Gantry parent proxy                                           | Review P2: the permissive callback was installed for no-network profiles, and hostname-based private destinations bypassed its literal-IP check                                                                              | Install the callback only when a Gantry parent proxy is configured; otherwise omit it so Sandbox Runtime denies unmatched destinations                                                       | No-proxy profiles could connect directly to public or hostname-hidden private destinations             | fixed     |
| B.21 | `file -f NAMEFILE` cannot prove that every opened target is safe                                       | Review P2: the gate validated only `NAMEFILE`, while `file` opened every target path listed inside it                                                                                                                        | Remove `f` from `file`'s deterministic option allowlist so every `-f` form remains unproven                                                                                                  | Hidden, protected, or out-of-workspace targets could be read silently                                  | fixed     |
| B.22 | Human approval and denial signals require explicit human attribution                                   | Review P2: Discord timeout and disconnect cancellations omitted `decidedBy` but were recorded as human denials                                                                                                               | Do not record approval or denial shape signals when `decidedBy` is absent or system-generated                                                                                                | System cancellations could poison `recentlyDeniedExactToolShape`                                       | fixed     |
| B.23 | Active deterministic-proof docs must exclude `rg` and `find`                                           | Review P3: capability guidance still listed both commands after B.18 and while implementation tests asserted they were unproven                                                                                              | Remove both commands and their deterministic traversal claims from the active command set                                                                                                    | Operators could rely on a safety contract the implementation does not provide                          | fixed     |
| B.24 | Active promotion docs must match the human-attributed inline prompt flow                               | Review P2: capability guidance still described classifier auto-allow counting, a threshold of three, a separate promotion offer, and omitted `auto_strict` from `/permissions`                                               | Document human-attributed approvals only, threshold two, `Allow for future` on the normal prompt, no separate offer flow, and the complete `/permissions` mode vocabulary                    | Operators could misunderstand which decisions count and expect a prompt flow that no longer exists     | fixed     |
| B.25 | The callerless classifier-auto-allow promotion scheduler is obsolete                                   | Review P3 and a repository-wide symbol search found `schedulePermissionPromotion` and `processPermissionPromotion` only in their definitions and old-flow unit tests                                                         | Delete the dead scheduler/process path and its obsolete tests; retain the counter threshold and prompt-facing types used by live prompting                                                   | Dead code and tests could preserve the false separate-offer model and invite its accidental reuse      | fixed     |

## Stage C — Prompt UX

| #    | Assumption                                                                                                                | Missing info that forced it                                                                                                                                                                                                     | Choice taken                                                                                                                                                                                                                                                                                                                                                                            | Impact if wrong                                                                    | Validated |
| ---- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | --------- |
| C.1  | Only requests from the same run are eligible for live batching                                                            | The goal specifies near-simultaneous requests but not whether unrelated runs may share one decision                                                                                                                             | Delay and batch only requests carrying the same `runId`; requests without a run id keep the existing immediate single-prompt path                                                                                                                                                                                                                                                       | A run-less producer cannot benefit from coalescing                                 |           |
| C.2  | A synthetic batch prompt is live-process UX, while each original request remains the durable authority                    | The durable interaction schema has no batch prompt record or bounded-scope API for binding one callback to several pending rows                                                                                                 | Render one synthetic prompt and resolve every original request individually; after a host restart the synthetic batch callback cannot be recovered and the requests must be retried/re-rendered                                                                                                                                                                                         | A restart while a batch prompt is open leaves its buttons stale                    |           |
| C.3  | `Review each` processes the batched requests sequentially                                                                 | The goal names the action but does not define concurrent or sequential follow-up prompt behavior                                                                                                                                | Delete the batch prompt, then use the existing individual prompt flow in request order so each authorization and durable resolution remains independent                                                                                                                                                                                                                                 | Reviewing a large batch takes multiple approval steps serially                     |           |
| C.4  | Telegram question callback ids only need live host-side recovery                                                          | The bounded channel scope exposes the in-memory pending-question map but no durable callback-id payload update for questions                                                                                                    | Map short opaque `q…` ids to request/question state in the Telegram adapter; legacy long callback parsing remains for already-rendered prompts                                                                                                                                                                                                                                          | A restart makes newly rendered short-id question buttons stale                     |           |
| C.5  | Slack permission prompts are normal group/thread messages                                                                 | `chat.delete` cannot remove ephemeral prompts, and Stage C requires deleting approved Slack prompts outright                                                                                                                    | Post the compact prompt in the originating channel/thread; authorization still gates every action callback                                                                                                                                                                                                                                                                              | Channel members can see pending approval context that was previously ephemeral     |           |
| C.6  | The optional auto-allow digest is skipped                                                                                 | No cheap turn-finalization seam is inside the Stage C bounded scope                                                                                                                                                             | Keep classifier and deterministic auto-allows completely silent in chat; audit remains the only record                                                                                                                                                                                                                                                                                  | Users do not get an aggregate auto-allow count in the final answer                 |           |
| C.7  | Provider account is part of permission batch identity                                                                     | A conversation id can be owned by more than one configured provider account                                                                                                                                                     | Coalesce only requests routed through the same provider account, in addition to the existing agent, conversation, run, and policy identity                                                                                                                                                                                                                                              | Equivalent requests routed through separate accounts appear in separate batches    |           |
| C.8  | A zero-width Teams card body is the minimal approved-state edit                                                           | Teams has card update support but no delete operation in the bounded adapter contract                                                                                                                                           | Replace an approved prompt card with a zero-width body; if the update fails, send the existing one-line receipt fallback                                                                                                                                                                                                                                                                | Teams retains a visually empty message shell after approval                        |           |
| C.9  | Batch actions need an explicit decision discriminator                                                                     | Review P1: Telegram replaces the human-readable decision reason before the batch requester sees it                                                                                                                              | Carry `batchDecision: review_each` on the decision and use only that typed field to enter sequential review                                                                                                                                                                                                                                                                             | A provider receipt reason can turn Review each into Allow all                      | fixed     |
| C.10 | Batch render identity must survive provider request cloning                                                               | Review P1: Teams spreads requests before rendering, so WeakMap object identity is not stable across the adapter seam                                                                                                            | Store batch request ids and compact rows on the typed permission request itself                                                                                                                                                                                                                                                                                                         | Cloned requests render ordinary buttons and can fan one decision across the batch  | fixed     |
| C.11 | Every original durable row owns the synthetic batch callback (supersedes C.2; claim recovery superseded by C.50 and C.52) | Review P1: synthetic `batch:` ids have no pending-interaction row after a process restart                                                                                                                                       | Bind the batch marker and provider alias to every original row; after a claim, recover only from its persisted scoped intent, with Review each consuming only synthetic batch authority and leaving every original row pending for individual retry                                                                                                                                     | Batch requests can hang forever or a restarted Review each can widen authority     | fixed     |
| C.12 | Telegram opaque question callback ids are durable interaction metadata (supersedes C.4)                                   | Review P2: an in-memory-only `q…` map disappears on restart and the legacy parser cannot recover the request and question index                                                                                                 | Persist each opaque callback id and question index on the original pending-question row before rendering, then recover it from that row when adapter memory is empty                                                                                                                                                                                                                    | Live question buttons become inert after restart                                   | fixed     |
| C.13 | Truncated permission batches cannot offer `Allow all`                                                                     | Review P1: the 2,800-character fallback can omit rows while one approval still fans out to every request id                                                                                                                     | Remove `Allow all` whenever the canonical rendered batch exceeds the message budget; keep `Review each` and `Deny all`                                                                                                                                                                                                                                                                  | An approver could authorize permission rows they never saw                         | fixed     |
| C.14 | Telegram question callback ids must be stable across process restarts                                                     | Review P1: the process-local counter restarts at `q1`, allowing an old button to alias a new pending question                                                                                                                   | Hash request id plus question index into a truncated base64url id that stays within Telegram's 64-byte callback limit                                                                                                                                                                                                                                                                   | A persisted old button could answer a different new question                       | fixed     |
| C.15 | Slack permission prompts are approver-visible ephemerals (supersedes C.5)                                                 | Review P1: normal channel messages exposed commands, paths, and URLs to every channel member                                                                                                                                    | Deliver the prompt with `chat.postEphemeral` to each configured approver in the originating channel/thread; never fall back to a public permission prompt                                                                                                                                                                                                                               | Sensitive permission context leaks to non-approver channel members                 | fixed     |
| C.16 | Durable opaque callback binding is a prompt-delivery precondition                                                         | Review P2: false binding results were ignored, leaving buttons that could not be decoded after restart                                                                                                                          | Bind Telegram question ids and every provider's synthetic batch callback before sending; fail closed without delivering when the binding is unavailable                                                                                                                                                                                                                                 | Users receive live-looking permission or question buttons that can never resolve   | fixed     |
| C.17 | Teams batch callback binding precedes card delivery, while message-id attachment follows it                               | Review P1: the pre-send binding required an external message id that cannot exist until `sendAdaptiveCard` returns                                                                                                              | Persist every original batch callback without a message id before sending, then update the same rows with the delivered card id                                                                                                                                                                                                                                                         | Every Teams batch is denied before a card can be sent                              | fixed     |
| C.18 | Slack ephemeral outcomes use only the interaction `response_url`                                                          | Review P1: Slack ephemerals cannot be changed through `chat.delete` or `chat.update`, and the public receipt fallback leaks data                                                                                                | Remove or replace the original ephemeral through `response_url`; when it is absent, leave the prompt stale and post no public receipt                                                                                                                                                                                                                                                   | Approved commands, paths, or URLs leak into the channel                            | fixed     |
| C.19 | Bulk approval requires complete prompt-grade scope for every row                                                          | Review P1: receipt summaries can redact distinct destructive MCP targets into identical generic labels such as `Command` or `File action`                                                                                       | Remove `Allow all` whenever any generated row lacks a complete, non-redacted parenthesized scope; keep `Review each` and `Deny all`                                                                                                                                                                                                                                                     | An approver could bulk-authorize destructive targets they cannot distinguish       | fixed     |
| C.20 | Bulk-scope completeness is formatter-owned structured metadata (supersedes C.19)                                          | Review P1: an untrusted display name or title can mimic the parenthesized receipt shape and pass a string-shape completeness check                                                                                              | Return the rendered receipt text with a typed completeness flag that is true only for a complete recognized command, file, URL, pattern, or interaction scope; gate `Allow all` only on that flag                                                                                                                                                                                       | An untrusted generic label can enable bulk authorization without concrete scope    | fixed     |
| C.21 | Discord consumes a visible batch prompt before releasing its decision                                                     | Review P2: the live pending record discarded the prompt message id, leaving old batch buttons active while `Review each` opened rows                                                                                            | Keep the Discord prompt message id, atomically mark the decision as settling, and clear the message components before resolving the batch decision                                                                                                                                                                                                                                      | A stale `Allow all` button can authorize rows still pending in sequential review   | fixed     |
| C.22 | Bulk eligibility requires the full unsanitized input of a whitelisted tool shape                                          | Review P1: finding one known field still ignored additional decision-bearing keys, sanitized input, and arbitrary third-party MCP tools                                                                                         | Return typed `bulkEligible` metadata only for exact-key Bash/RunCommand commands, known file paths, plain WebFetch URLs, or known patterns whose rendered scope is byte-for-byte unchanged                                                                                                                                                                                              | `Allow all` can authorize hidden, altered, or third-party tool input               | fixed     |
| C.23 | Discord batch settlement targets the component-bearing final chunk                                                        | Review P1: chunked prompts put components on the last message but retained the first message id for settlement                                                                                                                  | Store `externalMessageIds.at(-1)` with fallback to `externalMessageId` in the live pending settlement record                                                                                                                                                                                                                                                                            | Live buttons survive because consume patches a buttonless earlier chunk            | fixed     |
| C.24 | `Review each` consumes durable synthetic batch callback authority before individual review                                | Review P1: every pending row retained the batch callback id, so a stale second approver callback or replay could still resolve the full set                                                                                     | Remove the callback id from every matching pending row in one persisted update before opening individual prompts; fail closed if invalidation does not succeed                                                                                                                                                                                                                          | A stale batch button can grant all requests after individual review has started    | fixed     |
| C.25 | Telegram batch acknowledgement reflects the typed batch decision                                                          | Review P2: `allow_persistent_rule` always acknowledged `Allowed for future` even when its batch meaning was `Review each`                                                                                                       | Branch acknowledgement text on `batchDecision: review_each` and state that individual review is starting                                                                                                                                                                                                                                                                                | Telegram claims durable approval while presenting follow-up individual prompts     | fixed     |
| C.26 | Durable batch authority uses one canonical batch id                                                                       | Review P1: Telegram persisted its opaque `p…` callback id while requester invalidation used the synthetic `batch:` id                                                                                                           | Persist the synthetic request id as `permissionBatchCallbackId` for every provider and keep opaque provider ids only as aliases that resolve to the canonical id before pending lookup                                                                                                                                                                                                  | Review each can miss every durable row and cancel the live batch                   | fixed     |
| C.27 | Every batch decision claims the canonical callback before settlement (supersedes C.24)                                    | Review P1: Review each claimed after provider handlers removed live pending state, while Allow all never claimed at all                                                                                                         | Route live and recovered callbacks through one CAS on the full app, source-agent, and canonical batch identity; only the winner may remove prompts, resolve promises, or apply durable decisions, and losers report already decided                                                                                                                                                     | Concurrent callbacks can both settle the same batch with different authority       | fixed     |
| C.28 | A stale Slack native stream start is stopped after reset                                                                  | Review P2: targeted reset can run while `chat.startStream` is pending, before its stream id is assigned                                                                                                                         | Re-check the reset epoch immediately after start resolves and call `chat.stopStream` on the stale stream before returning                                                                                                                                                                                                                                                               | Reset leaves an orphaned native stream open                                        | fixed     |
| C.29 | Discord permission settlement reflects the terminal outcome                                                               | Review P2: settlement always patched `components: []`, leaving approved prompts visible and denied or timed-out prompts without a receipt                                                                                       | Delete approved prompt messages; replace denied and timed-out prompts with a minimal terminal receipt and no components                                                                                                                                                                                                                                                                 | Stale or ambiguous permission UI remains actionable-looking                        | fixed     |
| C.30 | Discord durable thread recovery authorizes in the parent conversation                                                     | Review P2: a recovered thread batch compared and authorized against the Discord thread id as if it were the owning conversation                                                                                                 | Resolve the callback channel to its parent conversation and pass that parent JID plus the durable thread id to approval authorization                                                                                                                                                                                                                                                   | Valid thread callbacks fail recovery or use the wrong conversation policy          | fixed     |
| C.31 | Post-delivery binding preserves a concurrent durable batch claim                                                          | Review P1: read-then-replace binding can race the batch claim and restore consumed callback authority                                                                                                                           | Merge prompt-delivery metadata atomically while retaining the structured scoped claim and never restoring active callback fields after that claim                                                                                                                                                                                                                                       | Two approvers can win the same batch decision                                      | fixed     |
| C.32 | Recovered individual callbacks do not inherit consumed batch identity                                                     | Review P2: rows retain the settled batch evidence after Review each, and lookup previously preferred that batch identity for every matching callback                                                                            | Match active callbacks only through their full scoped interaction identity; structured aliases stored inside a claim are non-matchable, while a fresh post-settlement individual alias receives its own individual scope                                                                                                                                                                | Individual prompts become inert after restart                                      | fixed     |
| C.33 | Discord approved-delete failure clears live components before resolution                                                  | Review P2: a failed approved-message delete was logged while the decision was released and actionable buttons remained                                                                                                          | Before releasing the decision, fall back to replacing the prompt with the one-line approved receipt and no components                                                                                                                                                                                                                                                                   | A stale Discord button can submit another decision                                 | fixed     |
| C.34 | Prompt stream-reset wiring stays within the existing bootstrap budget                                                     | Review P2: interaction reset wiring raised the `channel-wiring.ts` ratchet instead of preserving the 759-line limit                                                                                                             | Extract the owned interaction/stream-reset composition into a dependency-injected sibling module and restore the existing file budget                                                                                                                                                                                                                                                   | Bootstrap wiring can grow without the architecture ratchet catching it             |           |
| C.35 | Every permission callback claims its canonical request before settlement                                                  | Review P1: individual callbacks bypassed the batch-only claim, so a second approver could apply a conflicting durable decision                                                                                                  | Route individual and batch callbacks across Slack, Discord, Teams, and Telegram through one full-scope canonical interaction CAS; only winners terminalize and resolve, while losers report already decided                                                                                                                                                                             | Multiple approvers can release conflicting authority for one request               | fixed     |
| C.36 | Discord terminalization failure keeps the permission decision pending                                                     | Review P1: failed DELETE and fallback PATCH attempts still cleared pending state and released the decision                                                                                                                      | Retain the pending request and do not resolve its decision unless Discord deletes the prompt or replaces it with a component-free terminal receipt                                                                                                                                                                                                                                      | Live buttons remain actionable after authority has already been released           | fixed     |
| C.37 | Provider permission prompts terminalize before decision release                                                           | Review P1: Teams, Slack, Telegram, and Discord resolved agent-facing promises before provider update or deletion completed                                                                                                      | Complete the provider update/delete and existing fallback attempts before clearing pending state and resolving the agent-facing promise                                                                                                                                                                                                                                                 | The tool resumes while stale actionable buttons are still visible                  | fixed     |
| C.38 | Restart recovery uses the persisted target JID                                                                            | Review P1: recovery read provider conversation metadata instead of the durable `payload.targetJid` or request snapshot                                                                                                          | Recover routing from `payload.targetJid`, falling back to `request.targetJid`; never use the external prompt conversation id                                                                                                                                                                                                                                                            | Providers reject valid callbacks after restart                                     | fixed     |
| C.39 | Failed settlement releases the durable callback claim                                                                     | Review P1: the claim became permanently decided before decision application and terminal persistence completed                                                                                                                  | Release the exact scoped claim and restore its canonical callback marker plus provider alias when apply or terminalization fails so the pending interaction is retryable                                                                                                                                                                                                                | A transient failure strands the permission request                                 | fixed     |
| C.40 | Bulk-eligible scope values are single-line and control-free                                                               | Review P1: model-controlled multiline or Unicode/control-character values could inject fake batch rows or instructions                                                                                                          | Mark summaries ineligible for bulk when a scope contains CR, LF, control characters, or Unicode line separators                                                                                                                                                                                                                                                                         | `Allow all` can authorize a prompt whose visible rows were injected                | fixed     |
| C.41 | Discord live settlement failure rolls back the durable callback claim                                                     | Review P1: a failed delete and fallback update left the CAS claim consumed while the live request remained pending                                                                                                              | Release the claimed callback when provider settlement fails so a later click can claim and retry the same pending request                                                                                                                                                                                                                                                               | A transient Discord failure strands the request as already decided                 | fixed     |
| C.42 | Bulk eligibility excludes provider markup and Unicode format controls                                                     | Review P1: Slack mrkdwn tokens can display a benign label while preserving a different authorization value                                                                                                                      | Mark summaries ineligible for bulk when their scope contains angle brackets, pipes, backticks, or Unicode format-control characters                                                                                                                                                                                                                                                     | `Allow all` can authorize a value hidden behind provider formatting                | fixed     |
| C.43 | Discord acknowledges valid callbacks before recovery or authorization work                                                | Review P2: recovered callbacks performed durable lookups and provider requests before the first interaction response                                                                                                            | Send the generic ephemeral processing acknowledgement immediately after parsing a valid callback, then authorize, claim, and settle                                                                                                                                                                                                                                                     | Discord can reject a valid decision after its three-second acknowledgement window  | fixed     |
| C.44 | Batch claims consume provider callback aliases atomically                                                                 | Review P1: claiming the canonical batch id left Telegram's opaque alias active, so replay recovered as an individual callback                                                                                                   | Remove active batch and provider callback fields in the claim CAS, preserving aliases only inside structured non-matchable claim metadata; later individual prompt binding may add a fresh active alias                                                                                                                                                                                 | A stale `Review each` click can grant one durable rule after batch settlement      | fixed     |
| C.45 | Live batch pending state precedes post-send durable binding                                                               | Review P1: Telegram, Slack, and Discord awaited binding before registering live state, allowing recovery to settle first and a late promise to hang                                                                             | Register live state immediately after send, then bind; if binding finds the row resolved, unwind and reject that live entry                                                                                                                                                                                                                                                             | An already-resolved batch leaves an agent promise pending until timeout            | fixed     |
| C.46 | Released batch claims restore their opaque provider callback alias                                                        | Review P1: the batch claim removes Telegram's opaque alias from active callback fields, so a failed settlement release must restore both identities                                                                             | Preserve aliases in structured non-matchable `permissionCallbackClaim.match.providerAliases` metadata and restore the provider alias atomically with the canonical batch marker during scoped release                                                                                                                                                                                   | A transient settlement failure leaves pending Telegram buttons inactive            | fixed     |
| C.47 | Bulk eligibility rejects markup recognized by any target provider (supersedes C.42)                                       | Review P1: Markdown links and emphasis tokens could mask decision-bearing values in Discord or Teams even after Slack-specific markup was rejected                                                                              | Use one provider-neutral denylist for link, code, emphasis, strikethrough, angle-bracket, and existing Slack delimiter tokens; affected rows require individual review                                                                                                                                                                                                                  | `Allow all` can authorize a value hidden by provider-specific rendering            | fixed     |
| C.48 | Live callback claims remain owned through downstream durable settlement                                                   | Review P1: live providers claimed the canonical callback before resolving in-memory approval, but downstream apply or terminal-write failure left the row claimed                                                               | Carry the complete opaque scoped claim reference through live completion and release that exact claim when grant application or terminal resolution fails for every provider                                                                                                                                                                                                            | A transient downstream failure strands the request as already decided              | fixed     |
| C.49 | Teams post-send bind races settle the local waiter immediately                                                            | Review P2: another worker can consume the callback between card delivery and post-send binding, while the ignored false result leaves the local promise pending                                                                 | When post-send binding returns false, clear the timer and pending entry and settle the local promise as already decided                                                                                                                                                                                                                                                                 | A consumed callback waits for and renders a bogus timeout                          | fixed     |
| C.50 | Permission callbacks follow one durable scoped claim state machine                                                        | Repeated review rounds patched individual providers without stating the authority invariant                                                                                                                                     | Bind every claim to app id, source agent folder, and canonical interaction id; persist decision mode, approver, timestamp, and non-matchable provider aliases; claim before side effects; propagate the opaque claim; then settle or release                                                                                                                                            | A callback can win outside its authorized interaction or become stranded           | fixed     |
| C.51 | Request-id collisions never cross app or agent scope                                                                      | Provider request ids are not globally unique, while lookup, CAS, resolver, and fan-out seams previously treated a bare request id as authority                                                                                  | Normalize missing app ids to `default`, key batching and pending resolvers by app plus source agent plus canonical request, and constrain durable lookup, claim, release, and fan-out to the same full scope                                                                                                                                                                            | One agent's decision can resolve or grant another agent's colliding request        | fixed     |
| C.52 | Every post-claim failure releases, while crash recovery resumes persisted intent                                          | IPC, provider terminalization, late callbacks, and partial batch fan-out could fail after CAS; a process crash could otherwise leave claimed rows permanently inert                                                             | Carry the opaque scoped claim through all derived work; release it on every failure before durable settlement, restore aliases and retryability, never release after settlement, and let recovery complete the stored decision intent                                                                                                                                                   | Transient failures or restarts strand claims, or a settled interaction is reopened | fixed     |
| C.53 | Slack system settlement does not mutate ephemeral prompts                                                                 | Slack forbids `chat.update` and `chat.delete` for `chat.postEphemeral` messages without an interaction `response_url`                                                                                                           | On timeout or disconnect, settle the local promise and durable state without mutating the ephemeral; leave it stale to auto-expire, and mutate only user-clicked prompts through their `response_url`                                                                                                                                                                                   | Timeout or disconnect cleanup leaves the agent run hanging forever                 | fixed     |
| C.54 | Discord recovery binds the component-bearing final chunk                                                                  | Chunked prompts render components on the last message, while restart recovery persisted the first message id                                                                                                                    | Persist `externalMessageIds.at(-1)` with fallback to `externalMessageId` for restart-recovery binding                                                                                                                                                                                                                                                                                   | Recovered callbacks cannot find the message containing the controls                | fixed     |
| C.55 | Recovered Review each callbacks retain batch semantics                                                                    | After restart, the provider alias mode was validated against an original request whose options do not include the synthetic batch action                                                                                        | Derive recovered mode validation from the persisted claim match kind; treat batch aliases as batch actions and reconstruct `batchDecision: review_each`                                                                                                                                                                                                                                 | Non-bulk-eligible batches become impossible to review after restart                | fixed     |
| C.56 | Repository failures during callback settlement remain retryable                                                           | Repository exceptions were collapsed into `already_decided` even though the durable row remained pending                                                                                                                        | Return a distinct retryable result for repository errors; reserve `already_decided` for a successful compare-and-swap that returns no rows and keep the prompt waiter active on retryable failures                                                                                                                                                                                      | A transient database failure permanently kills an actionable prompt                | fixed     |
| C.57 | Disconnect cleanup settles every remaining local waiter                                                                   | Best-effort durable cancellation could fail while provider cleanup deleted local entries without resolving their promises                                                                                                       | Inspect settlement results and resolve with cancellation or hand off every remaining Slack, Teams, Discord, and Telegram waiter before removing local state                                                                                                                                                                                                                             | Permission approval requests can hang forever after channel disconnect             | fixed     |
| C.58 | Telegram question recovery preserves app and source-agent scope                                                           | Recovered opaque callbacks carried only request id and question index into later durable lookup and resolution                                                                                                                  | Carry the persisted app id and source agent folder through callback recovery, authorization lookup, and durable resolution                                                                                                                                                                                                                                                              | A colliding request can resolve another app's or agent's pending question          | fixed     |
| C.59 | Discord approvals use the shared interactive permission timeout                                                           | Discord approval prompts used a local ten-minute timeout instead of `GANTRY_INTERACTIVE_PERMISSION_TIMEOUT_MS`                                                                                                                  | Use the shared permission timeout for approval rendering and settlement; retain the Discord ten-minute constant only for unrelated interaction types                                                                                                                                                                                                                                    | Discord approvals ignore the operator-configured timeout                           | fixed     |
| C.60 | Canonical batch callbacks bind one exact member request set                                                               | Confirmed broken as reported: `batch:<first>:<count>` collided after re-coalescing, and an old provider alias could locate stale rows while the batch CAS updated newly bound rows that were never displayed                    | Add the SHA-256 of sorted member request ids to the canonical batch id; require a complete, distinct, set-equal durable member binding before updating only rows whose request id belongs to that binding; proved by `includes the exact member request set in the canonical batch id` and `rejects an old batch callback after the same first request is rebound to different members` | A recovered stale callback can authorize requests from a different rendered batch  | fixed     |
| C.61 | A row with a persisted callback claim cannot report successful prompt rebinding                                           | Confirmed broken as reported: payload merge preserved the existing claim and stripped incoming callback ids but still returned success, allowing adapters to render controls whose next claim always returned `already_decided` | Make claimed-row merge return false before provider rendering while retaining the persisted claim for restart recovery; proved by `rejects restart rebinding after claim and resumes the persisted intent` and `rejects prompt rebinding after a callback claim and does not revive settled callbacks`                                                                                  | Restart recovery can render an inert prompt and strand the approval waiter         | fixed     |
| C.62 | Permission timeout settlement has a bounded retry window and always resolves the local waiter                             | Confirmed broken as reported in Slack, Discord, Teams, and Telegram: every one-shot timeout discarded `retryable`, while existing retryable tests covered disconnect only                                                       | Retry transient timeout claims with bounded backoff through at most twice the original prompt lifetime, then remove and explicitly resolve any remaining local waiter with a fail-closed timeout denial; proved by each channel's `resolves the ... waiter after retryable timeout claims exhaust bounded retries` test                                                                 | A transient Postgres outage leaves an approval promise pending forever             | fixed     |
| C.63 | Permission batches share one approval context                                                                             | Confirmed broken as reported: `permissionBatchKey` omitted `approvalContextJid`, allowing requests governed by different approval contexts to share one synthetic decision                                                      | Include `approvalContextJid` in the batch identity so only requests with the same approval context coalesce; proved by `does not coalesce requests with different approvalContextJid`                                                                                                                                                                                                   | An approver authorized for one context can authorize another context's request     | fixed     |
| C.64 | A timeout loser preserves an in-flight callback winner                                                                    | Confirmed broken as reported in Slack, Discord, Teams, and Telegram: `already_decided` can mean another callback claimed the interaction but has not completed terminal settlement, while timeout handling emitted cancellation | Return from timeout settlement on `already_decided`, leaving the pending waiter for the callback winner and reserving synthetic cancellation for exhausted `retryable` claims; proved by `preserves the winner when timeout fires after another callback claimed`                                                                                                                       | A valid user decision is discarded while its callback is still terminalizing       | fixed     |
| C.65 | Permission callback claims use stable provider user identity                                                              | Confirmed broken as reported: channel handlers passed display names as `approverRef`, so a user displayed as `System` or `Runtime` was rejected by the reserved-decider gate                                                    | Pass each verified provider user id as the callback claim and decision identity across live and recovered Slack, Discord, Teams, and Telegram handlers; proved by `allows an authorized approver whose display name is System`                                                                                                                                                          | Authorized users with reserved-looking display names retry until timeout           | fixed     |
| C.66 | Durable recovery replays one complete delivered-interaction envelope                                                      | Three review rounds found the same class: restart paths re-derived rendering and authorization constraints from a member request or prompt target                                                                               | Persist the actual rendered decision options, target and approval context, thread/policy, complete request, scoped callbacks, batch identity/phase/member set, question progress, and delivered prompt identities; every recovered provider handler reads that envelope and fails closed when it is malformed                                                                           | Restart can expose a forbidden action or authorize against the wrong conversation  | fixed     |
| C.67 | Recovered `Review each` resumes the member-prompt phase                                                                   | A persisted batch claim was settled without dispatching individual prompts, leaving the original waiters pending                                                                                                                | Persist the `review_each` phase, settle the batch claim, dispatch every still-pending member through the direct provider surface, require its delivery callback, and reuse any already-persisted individual member claim on retry                                                                                                                                                       | Recovery reports success while batch members remain hung                           | fixed     |
| C.68 | A persisted callback claim is replayed before any prompt is rebound                                                       | Restart between claim persistence and live-waiter resolution reopened a fresh prompt whose controls contradicted the already-decided intent                                                                                     | Recover the claim or settlement by app, source agent, request, and member index; replay its exact decision into the waiter and reject prompt rebinding while claim state exists                                                                                                                                                                                                         | One interaction can present two conflicting decisions after restart                | fixed     |
| C.69 | Question recovery persists completion and delivery separately                                                             | Callback binding occurs before provider send, and live sequential answers previously existed only in memory                                                                                                                     | Persist answers/completed indexes after each live answer, persist delivered indexes only after provider send, resume at the first unanswered index, treat delivered timeouts as terminal attempts, and leave incomplete routing/send attempts retryable                                                                                                                                 | Restart repeats answered questions, skips undelivered questions, or loops forever  | fixed     |
| C.70 | Question callback and free-text identities are fully scoped and durable                                                   | Discord used reconstructible request ids, Slack Other depended on a live map, and Telegram ForceReply identity disappeared on a second restart                                                                                  | Persist opaque callback aliases and Other prompt ids with app id, source agent folder, request id, and question index; validate every recovered identity against the envelope before authorization or resolution                                                                                                                                                                        | Colliding or stale callbacks resolve another interaction, or free text is lost     | fixed     |

## Stage D — Stream boundary after prompts

| #    | Assumption                                                                                  | Missing info that forced it                                                                                                                                                                 | Choice taken                                                                                                                                                                                                                                                                               | Impact if wrong                                                                                            | Validated |
| ---- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- | --------- |
| D.1  | The existing channel `resetStreaming(jid)` granularity is the approved thread boundary seam | The exposed reset API accepts a conversation JID and provider account but no thread id, while adapter stream state can contain thread-specific keys                                         | Invoke that existing reset after successful permission/question delivery with provider-account routing; adapters clear the conversation's active stream state using their existing implementation                                                                                          | Concurrent streams in another thread of the same conversation may also start a new message                 |           |
| D.2  | Failed or unsupported prompt delivery must not split the stream                             | The goal asks for reset on delivery, not on an attempted prompt                                                                                                                             | Providers invoke the callback only after receiving a prompt message/card id; missing surfaces and send failures never invoke reset                                                                                                                                                         | A provider that accepts a prompt without returning its normal message identifier will not split the stream |           |
| D.3  | Each delivered question in a multi-question request is its own stream boundary              | The request contract can render several question messages sequentially, while the goal defines the boundary per delivered prompt                                                            | Invoke the delivery callback after every question message/card is registered, so any resumed streamed output starts separately from the most recent prompt                                                                                                                                 | A multi-question request may reset an otherwise idle stream more than once                                 |           |
| D.4  | Prompt stream resets are exact conversation-thread key operations (supersedes D.1)          | Review P2: the lifecycle callback dropped `threadId`, and every adapter cleared all streams for the conversation                                                                            | Carry `threadId` through permission and question lifecycle callbacks and delete only that adapter stream key; keep existing whole-conversation reset behavior for callers that omit prompt scope                                                                                           | A prompt in one thread tears down unrelated concurrent thread streams                                      | fixed     |
| D.5  | The shared interaction surface owns the prompt-delivery hook                                | Review P2: the shared two-argument contract made Discord silently ignore the bootstrap hook                                                                                                 | Add the third callback argument to the shared contract and Discord forwarding methods; invoke it only after Discord returns the prompt message id                                                                                                                                          | Discord resumes by editing the pre-prompt stream message                                                   | fixed     |
| D.6  | Prompt delivery resets the exact route-resolved channel instance                            | Review P2: resolving the reset channel only by `providerAccountId` misses host/admin prompts routed by request metadata on multi-account conversation ids                                   | Capture the channel selected by `findBoundChannelForRequest` and reset that same instance after delivery                                                                                                                                                                                   | A correctly delivered prompt can leave resumed output editing the pre-prompt stream                        | fixed     |
| D.7  | Targeted resets invalidate in-flight stream-state writers                                   | Review P2: Slack and Discord chunk sends can await provider I/O, then reinsert the captured pre-reset state after the targeted key was deleted                                              | Increment a per-key epoch on reset and let chunk handlers mutate or reinsert state only while their captured epoch remains current                                                                                                                                                         | Resumed output can edit the stream message that existed before the prompt                                  | fixed     |
| D.8  | Teams in-flight completions must remain in the captured reset generation                    | Review P2: a chunk send can finish after reset and delete or seal newly created stream state                                                                                                | Require both the per-key reset epoch and captured state identity to remain current before post-await cleanup or generation sealing                                                                                                                                                         | A stale completion rejects legitimate post-prompt chunks                                                   | fixed     |
| D.9  | Telegram group completions use reset epoch and state identity guards                        | Review P2: group and private draft completions can mutate replacement state after reset, but private draft call sites are in an explicitly excluded delivery module                         | Guard group post-await cleanup and sealing in the channel-state module; escalate private draft completion guards because their required call sites are explicitly excluded                                                                                                                 | Private draft completions can still delete or seal a new generation                                        | escalate  |
| D.10 | Slack partial-delivery fallback remains reset-generation scoped                             | Review P2: the final fallback cleaned up and sealed without the normal finalization path's epoch check                                                                                      | Check the captured reset epoch before fallback cleanup or generation sealing                                                                                                                                                                                                               | A stale fallback clobbers the post-prompt stream generation                                                | fixed     |
| D.11 | Telegram private draft completions use reset epoch and state identity guards                | Review P2: a pre-reset private draft completion could finish after a replacement stream started and seal its shared generation                                                              | Require the captured reset epoch and draft-state identity before fallback, cleanup, or generation sealing                                                                                                                                                                                  | A stale private completion rejects valid chunks from the replacement stream                                | fixed     |
| D.12 | Discord overflow delivery remains reset-generation scoped                                   | Review P2: the reset epoch was checked only once before the overflow loop, so reset during an awaited send did not stop later parts                                                         | Re-check the epoch after head delivery and immediately before every overflow send; abort when the captured generation is stale                                                                                                                                                             | Discord can send stale overflow parts after a prompt resets the stream                                     | fixed     |
| D.13 | Slack fallback delivery checks the captured epoch before every provider send                | Review P2: multipart fallback checked the reset epoch only after every part had already been posted                                                                                         | Pass the captured epoch guard into fallback delivery and abort before each update or post when that guard is stale                                                                                                                                                                         | Slack can post stale fallback parts after a prompt resets the stream                                       | fixed     |
| D.14 | Teams and Telegram overflow delivery checks stream state before every split send            | Review P2: both adapters checked stream state around finalization but not between awaited overflow sends                                                                                    | Pass the Teams epoch/state guard through its flush and split helpers, and re-check Telegram's epoch/state guard before every overflow or fallback chunk send                                                                                                                               | Teams or Telegram can send stale overflow parts after a prompt resets the stream                           | fixed     |
| D.15 | Stream reset epochs are removed after their stream lifecycle becomes idle                   | Review P3: per-thread epoch entries remained forever after stream state deletion                                                                                                            | Prune an epoch when its associated stream state is gone and no in-flight send still holds that epoch                                                                                                                                                                                       | Long-lived runtimes accumulate reset metadata for inactive threads                                         | fixed     |
| D.16 | Slack snippet fallback checks the captured reset epoch before and after delivery            | Confirmed broken as reported: both the normal oversized snippet path and the partial-native-error catch path awaited snippet delivery without the multipart fallback's targeted epoch guard | Check the captured epoch immediately before and after both snippet delivery calls; proved by `skips Slack snippet fallback when a targeted reset lands during native failure`, its catch-path variant, and `does not accept a Slack snippet fallback that finishes after a targeted reset` | A stale stream can deliver or accept oversized fallback output after a prompt reset                        | fixed     |
