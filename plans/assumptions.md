# Implementation Assumptions Ledger

One row per assumption made during implementation (`forge plan assume`).
The orchestrator reviews open rows and guides:
`./forge assumptions resolve <id> --status confirmed|fix-needed|promoted --notes "..."`.
`pr_ready.py` refuses while the task has rows at `open` or `fix-needed`.

| id | date | issue | assumption | status | guidance |
|----|------|-------|------------|--------|----------|
| A-0001 | 2026-07-22 | PAY-1 | Baseline File Size Budget inventory was undercounted (5 of 19 rows; truncated read of checker output). Stage E added for the remaining rows; mcp-tool-proxy ratcheted until CAP-1. | confirmed | Confirmed: the checker's File Size Budget had 19 rows, not 5 (truncated baseline read). Stage E split the remaining 13; mcp-tool-proxy ratcheted at 800 (D-0003). check:architecture exits 0 at HEAD. |
| A-0002 | 2026-07-24 | SEC-1 | The shared writer accepts a trusted workspace root plus a workspace-relative attachment path so it can prove directory containment instead of trusting a destination path alone. | confirmed | Correct: containment must be proven against the workspace root, not a caller path. |
| A-0003 | 2026-07-24 | SEC-1 | Atomic no-replace publication uses a same-directory hard link from the validated random temp file to the final name, followed by removal of the temp name. | confirmed | Same-directory hard-link publish + temp unlink is the standard no-replace atomic publish; matches decision 0041. |
| A-0004 | 2026-07-24 | SEC-1 | The writer follows the existing descriptor-containment platform contract: Darwin uses O_NOFOLLOW_ANY, Linux validates through /proc/self/fd, and other platforms fail closed. | confirmed | Matches the platform contract already used by workspace-message-attachment.ts; fail-closed elsewhere is right. |
| A-0005 | 2026-07-24 | SEC-1 | Within the SEC-1-T1 write scope, the repository guard lives at .github/workflows/check_ci_runner_isolation.py and is invoked by factory-scaffold.yml. | confirmed | Moved to scripts/check_ci_runner_isolation.py; all references and the workflow-dir resolution updated; guard passes. |
| A-0006 | 2026-07-24 | SEC-1 | The real-model step skips an absent E2E_MODEL_API_KEY with an early successful shell exit so the secret remains scoped to that step instead of the whole job. | confirmed | Early successful skip keeps the secret step-scoped and fork PRs green — exactly decision 0040. |
| A-0007 | 2026-07-24 | SEC-1 | Each workflow defaults contents to read, while jobs that publish branches, packages, issues, or labels retain only the additional write permissions their existing behavior requires. | confirmed | Top-level contents:read with narrowly scoped job-level writes is the intended minimization. |
| A-0008 | 2026-07-24 | SEC-1 | The process-local LLM concurrency admission defaults are 32 global in-flight requests and 8 in-flight requests per app/key. | confirmed | Conservative defaults (global 32, per-key 8) are sensible for a single process; SPS-4 revisits cluster authority. |
| A-0009 | 2026-07-24 | SEC-1 | LLM admission overrides use settings.yaml runtime.llm_admission.global_max_in_flight and runtime.llm_admission.per_app_key_max_in_flight because the ceilings are process-runtime controls, not provider rate limits. | confirmed | runtime.llm_admission.global_max_in_flight / per_app_key_max_in_flight are clear setting names consistent with the runtime.\* namespace. |
| A-0010 | 2026-07-24 | PERF-1 | An empty latest-run batch returns an empty map without querying Postgres, and requested job IDs with no eligible non-session run are absent from that map. | confirmed | Correct contract: empty batch short-circuits without a query; absent map entries mean no eligible non-session run — matches the old per-job semantics. |
| A-0011 | 2026-07-24 | PERF-1 | runtime.queue.max_live_admission_backlog is a strictly positive integer; zero is rejected because the approved finite-cap contract does not define a disabled-admission mode. | confirmed | Strictly-positive cap is right: 0 would mean 'reject everything' silently — an explicit validation error is the safer contract. Unlimited remains achievable only by a very large value, which is intentional post-audit. |
| A-0012 | 2026-07-26 | RACE-1 | Update the existing ipc-skill-permission-review lock-order test to use the new shared app-scoped key because the changed call site otherwise invalidates its synchronization assertion. | confirmed | Correct: the lock call site moved to the shared app-scoped key, so the lock-order test must assert against the new key. |
| A-0013 | 2026-07-26 | RACE-1 | Refresh existing Claude and DeepAgents selected-skill test fixtures to store real bundle hashes, and preserve their pre-existing malformed-bundle validation errors before the new final integrity comparison. | confirmed | Correct: Claude/DeepAgents fixtures need real bundle hashes for the new integrity check; preserving the pre-existing malformed-bundle errors before the final hash comparison keeps those cases failing for their own reason. |
| A-0014 | 2026-07-26 | RACE-1 | Encode app and catalog path segments injectively instead of using the existing lossy name sanitizer so distinct identifiers cannot collapse onto the same artifact directory. | confirmed | Correct and important: injective encoding of app/catalog segments (encodeStorageSegment) is what guarantees distinct identifiers cannot collapse onto one artifact dir — the lossy name sanitizer would have reopened the collision this task closes. |
| A-0015 | 2026-07-26 | RACE-1 | Move hashSkillBundle and bundle normalization into the existing shared skill-artifact helper so application and adapters reuse one backend-neutral implementation without violating domain external-import rules. | confirmed | Correct: hashSkillBundle + normalization belong in the shared skill-artifact helper so application and adapters share one backend-neutral implementation without a domain external-import violation. |
| A-0016 | 2026-07-27 | RACE-2 | The settings-projector lease retries every 50 ms for up to 30 seconds before failing, so a held lease delays projection without waiting forever. | confirmed | Bounded wait selected for low-frequency settings changes; the helper directly retries the existing advisory lease and the focused tests cover coordinator entry and serialized apply behavior. |
| A-0017 | 2026-07-27 | RACE-2 | Updated decision 0068 section 4 supersedes the approved head-fenced rollback criterion: capture the on-disk pre-apply state under the projector lease and restore it unconditionally after a failed apply unless no prior file exists. | promoted | Already established by accepted decision 0068 section 4: rollback uses the captured pre-apply disk state and is not fenced by the desired-revision head. -> 0068-race-2-cluster-fenced-settings-projection.md |
| A-0018 | 2026-07-27 | RACE-2 | The user's explicit current branch feat/RACE-2-cluster-fenced-settings-projection governs this correction even though .factory/run.json retains the older generated branch label. | confirmed | The user explicitly named the current worktree and branch for this correction. |
| A-0019 | 2026-07-27 | RACE-2 | Updated decision 0068 sections 4 and 4b supersede the approved rollback criteria: failed projection must forward-correct via retry without snapshot restore, and an unreadable superseding head must produce a non-success outcome. | confirmed | Accepted decision 0068 sections 4 and 4b are the authoritative updated contract. |
| A-0020 | 2026-07-27 | RACE-2 | Superseding unreadable heads throw a typed SettingsIncompatibleReaderError, preserving the existing successful import outcome union while forcing all required-projection callers down their failure paths. | confirmed | The user allowed a distinct outcome or a clear typed error and requested the smallest change. |
| A-0021 | 2026-07-27 | RACE-2 | Each listener wake gets one immediate coalesced retry after projection failure, bounding persistent-failure churn while the existing poll remains the longer-term recovery path. | confirmed | The user allowed a bounded retry when no existing backoff mechanism was present. |
| A-0022 | 2026-07-27 | RACE-4 | Use the user-specified feat/RACE-4-browser-profile-lock-aba branch and ./forge next implementing state as authoritative despite stale intake branch/phase fields in .factory/run.json; do not rewrite factory state for this bounded fix. | confirmed | Correct: the branch/phase in .factory/run.json was stale from intake; ./forge next reflected the real implementing state. Rewriting factory state was rightly avoided. |
| A-0023 | 2026-07-28 | RACE-4 | Implement the rewritten accepted Decision 0071 by replacing lockfile ownership with injected RuntimeLeasePort and extending the RACE-4 wiring scope beyond the superseded stale-reclaim plan. | confirmed | Correct and directed: decision 0071 was rewritten mid-task (user-approved) to replace lockfile ownership with the injected RuntimeLeasePort after three lockfile variants each failed review; the wider wiring scope follows from that decision, and stayed within the 10-file guard (4 production wiring files). |
| A-0024 | 2026-07-28 | RACE-4 | Treat advisory-lease ownership loss as part of RACE-4 by synchronously observing RuntimeLease.onLost and failing closed for the affected live browser session without changing the lease port or provider inbound paths. | confirmed | Correct: a connection-scoped advisory lease can drop while Chrome lives, so loss observation belongs in RACE-4. Scoped exactly right — uses the port's existing onLost, no port change, provider inbound leases untouched (that is RACE-5). |
| A-0025 | 2026-07-28 | RACE-4 | Extract the existing live-browser SIGTERM-to-SIGKILL shutdown sequence into browser-process.ts so normal close and lease-loss teardown share one path while browser-capability.ts stays within its 760-line budget. | confirmed | Correct: sharing one TERM->KILL path between normal close and loss teardown removed duplication and kept browser-capability.ts inside its budget without raising it — extraction over budget-bump was the right call. |
| A-0026 | 2026-07-28 | RACE-4 | Apply the user-specified fail-closed ownership-loss rule across adopted-session recovery, loss-mode shutdown, and snapshot publication/marker writes, extending RACE-4 into browser-profile-sync and its tests. | confirmed | Confirmed by the user's explicit five-fix consolidated-pass request. |
| A-0027 | 2026-07-28 | RACE-8 | Treat the checked-out feat/RACE-8-mandatory-atomic-async-admission branch and active RACE-8-a stage as authoritative despite stale branch and phase fields in .factory/run.json. | confirmed | Confirmed during RACE-8 review: port tightening is compiler-enforced, the contract test contends for the final cap slot (so it would fail with non-atomic admission), the async-mcp-tool-task reconciliation follows from the now-mandatory claim, and full paths in decision 0074 satisfy the architecture doc-reference gate. |
| A-0028 | 2026-07-28 | RACE-8 | Seed the concurrency contract one slot below the per-agent cap so simultaneous admissions contend for the final slot and can actually expose count-then-create races. | confirmed | Confirmed during RACE-8 review: port tightening is compiler-enforced, the contract test contends for the final cap slot (so it would fail with non-atomic admission), the async-mcp-tool-task reconciliation follows from the now-mandatory claim, and full paths in decision 0074 satisfy the architecture doc-reference gate. |
| A-0029 | 2026-07-28 | RACE-8 | Apply the minimal mandatory-claim reconciliation in async-mcp-tool-task.ts because typecheck reports its optional-method fallback as impossible and the task explicitly requires fixing compiler-reported ripples. | confirmed | Confirmed during RACE-8 review: port tightening is compiler-enforced, the contract test contends for the final cap slot (so it would fail with non-atomic admission), the async-mcp-tool-task reconciliation follows from the now-mandatory claim, and full paths in decision 0074 satisfy the architecture doc-reference gate. |
| A-0030 | 2026-07-28 | RACE-8 | Expand Decision 0074 shortened source references to repository-relative paths because the required architecture gate rejects the current unresolved references. | confirmed | Confirmed during RACE-8 review: port tightening is compiler-enforced, the contract test contends for the final cap slot (so it would fail with non-atomic admission), the async-mcp-tool-task reconciliation follows from the now-mandatory claim, and full paths in decision 0074 satisfy the architecture doc-reference gate. |
| A-0031 | 2026-07-28 | RACE-9 | The model-defaults unit test may be updated outside the recorded two-file write scope because the requested focused verification exposed its reliance on the deleted no-provider fallback. | confirmed | Confirmed during RACE-9 review: the write fallback is deleted in favour of a shared SETTINGS_MUTATION_REQUIRES_RUNTIME_STORAGE throw, the provider-configured path is untouched, and the read path deliberately stays lenient because a write without a provider now throws regardless. |
| A-0032 | 2026-07-28 | RACE-9 | Accepted rewritten Decision 0075 and the user correction supersede the stale decomposition text that says to delete the no-provider fallback. | confirmed | Confirmed during RACE-9 review: the no-provider path is a supported file-is-the-store mode (cli/group-helpers.ts:264-270), so it is serialized and delta-rebased rather than deleted; restartRequired keeps using the caller's previousSettings base while the SAVE rebases onto the freshly-read file. |
| A-0033 | 2026-07-28 | RACE-9 | For no-provider concurrency, rebase each caller's leaf changes from previousSettings onto the fresh serialized file state, preserve restart classification against previousSettings, and let the later queued writer win same-leaf conflicts. | confirmed | Confirmed during RACE-9 review: the no-provider path is a supported file-is-the-store mode (cli/group-helpers.ts:264-270), so it is serialized and delta-rebased rather than deleted; restartRequired keeps using the caller's previousSettings base while the SAVE rebases onto the freshly-read file. |
| A-0034 | 2026-07-28 | RACE-9 | Require every file-backed desired-settings write to carry the caller-captured previousSettings snapshot instead of inferring a baseline from the current file. | confirmed | Confirmed by the requested RACE-9 correction: writeDesiredRuntimeSettings and its file-backed helper require the caller-captured previousSettings snapshot, no baseline inference remains, and typecheck found no production caller gaps. |
| A-0035 | 2026-07-28 | RACE-9 | Capture structuredClone snapshots of both settings and previousSettings when a file-backed write is enqueued so later caller mutation cannot change the queued write. | confirmed | Confirmed by the requested RACE-9 correction: both input objects are structured-cloned before the queued callback, the regression test fails without cloning and passes after restoration. |
| A-0036 | 2026-07-28 | MOD-1 | Opus 5 support in MOD-1-T3 may extend to the existing Anthropic provider cache-threshold metadata and central model-control validation only where official provider behavior would otherwise be advertised or accepted incorrectly. | confirmed | Official Opus 5 behavior required the existing provider cache-threshold metadata and central model-control admission checks; the implementation stayed within those existing boundaries and passed focused review. |
| A-0037 | 2026-07-28 | RACE-5 | Decision 0077's abbreviated inline source paths will be normalized to repo-root paths so the required architecture reference gate can resolve them. | confirmed | Confirmed during RACE-5 review: isValid is required on the port so a fake that cannot report validity is a compile error; the extra channel-wiring lease fake followed from that; both new behaviours were negative-checked (each test fails without its fix). |
| A-0038 | 2026-07-28 | LAT-3A | AC3's promoted-vs-non-promoted memoryContextBlock equivalence cannot be proven in a unit test with a mocked getAgentTurnContext, because the mock supplies the block itself and any such assertion is tautological; the real proof moves from stage LAT-3A-1 to stage LAT-3A-3 against real Postgres, where promotion actually runs against real rows. | confirmed | Confirmed and discharged in stage LAT-3A-3. The real AC3 proof landed in session-continuity.postgres.integration.test.ts as 'hydrates identical non-empty memory before and during provider-session promotion': against real Postgres it writes a real memory_items row via AppMemoryService.save, promotes a genuinely ready provider session, then reads the same unchanged agent session twice (promoteReadyProviderSession false then true) and asserts the memoryContextBlock is byte-identical, NON-EMPTY, and contains the written marker. The non-empty and contains assertions are what stop it degrading into an empty-vs-empty tautology. 23/23 in that file. The unit-level version was correctly abandoned: a mocked getAgentTurnContext supplies the block itself, so no unit assertion could falsify the claim. |
| A-0039 | 2026-08-01 | LAT-5B | Model active inbound ownership as process-local provider-account lifecycle state maintained by channel connect/disconnect wiring; do not add durable state or block delivery. | confirmed | Inbound ownership stays process-local lifecycle state in channel connect/disconnect wiring — matches the adjudicated no-blocking design; durable/cluster-visible ownership is exactly the D-0036 deferral, not this stage. |
| A-0040 | 2026-08-01 | FILE-1B | A deletion event received on a shared provider credential applies to the same inbound provider-account set as message ingress, carried as one deduplicated repository call rather than one call per account. | confirmed | Matches ingress semantics: one deduplicated repository call carrying the full provider-account set for shared credentials. |
| A-0041 | 2026-08-01 | FILE-1B | Represent the atomic deletion result as tombstonedAttachments entries containing attachmentId and the persisted ISO deletedAt, including idempotent repeats, because the timestamp-shape acceptance criterion requires the operation to surface each row's actual tombstone timestamp. | confirmed | Result shape confirmed; the read mapping must normalize identically. |
| A-0042 | 2026-08-01 | FILE-1B | Raw Discord deletion route admission uses cached thread context when present; without cache it admits only an exact top-level channel route or an explicitly configured thread route, and never performs REST lookup before marker persistence. | confirmed | Superseded shape implemented: stored-message existence is the durable admission fallback (channel-scope admission, pair-keyed mapping); marked confirmed to close the loop on the implemented contract. |
| A-0043 | 2026-08-01 | FILE-1B | Live Discord attachment capture uses the existing 110-second attachment-open deadline; the internal capture helper accepts a test-only override so stalled-stream behavior can be falsified quickly. | confirmed | Correct reuse of the existing 110s attachment-open deadline for live capture; test-only override is the right falsification lever. |
| A-0044 | 2026-08-01 | ID-1 | Phone alias normalisation accepts international + or 00 input only, strips presentation punctuation, and rejects values that cannot be represented as E.164 without guessing a region. | confirmed | Implemented exactly as assumed: person-identity-service accepts + or 00 international input only, strips presentation punctuation, rejects anything not expressible as E.164 without region guessing; the backfill migration applies the same rule and aborts on cross-person collisions. |
| A-0045 | 2026-08-03 | LIVE-1 | Post-retention Discord create tombstones are lightweight and capped at 5,000 routes with oldest-first eviction so eventual settlement remains reconcilable without unbounded route-cache growth. | confirmed | Confirmed by implementation and review round 36: tombstones are lightweight per-key records, capped at 5,000 with oldest-first eviction that skips in-deadline-window attempts and neutralizes invalidated late creates; post-retention settlements reconcile via tombstone. Verified by falsifier tests in discord-progress.test.ts. |
| A-0046 | 2026-08-04 | LIVE-2 | LIVE-2-1 uses a 2-second default deadline for each liveness delivery attempt, matching the existing first-visible-output liveness bound because the approved plan does not specify a duration. | confirmed | Confirmed: 2s per-attempt deadline survived the 17-round review saga; matches the first-visible-output bound; configurable via attemptDeadlineMs for tests. |
| A-0047 | 2026-08-05 | LIVE-2 | LIVE-2-1 uses a 250ms post-abort settlement grace for live-UX target lanes; tests may override it, and expiry releases the lane while treating the abandoned attempt as ambiguous. | confirmed | Confirmed: 250ms post-abort grace with lane release + ambiguous marking (race+grace pattern, review rounds 7/13); configurable in tests. |
| A-0048 | 2026-08-05 | LIVE-2 | LIVE-2-1 retains deadline-ambiguous live-UX attempts for 30 seconds with a dispatcher-wide oldest-first cap of 256; focused tests override both bounds. | confirmed | Confirmed: 30s retention, 256-entry oldest-first cap, warn-visible eviction with documented residual (round-13 criterion). |
| A-0049 | 2026-08-05 | LIVE-2 | LIVE-2-1 uses the existing per-attempt deadline as the dispatcher's maximum rate-limit cooldown wait; longer provider retry delays are warned and declined without a premature retry. | confirmed | Confirmed: cooldown waits capped at the attempt deadline; longer provider retry_after values are warned and declined, never retried early (round-14 criterion). |
| A-0050 | 2026-08-06 | LIVE-2 | LIVE-2-2 uses the existing two-second live-UX operation deadline as the bound for waiting on a previous turn's terminal UI cleanup. | confirmed | Confirmed: reuse of the 2s live-UX operation deadline as the cleanup wait bound keeps one tunable for all liveness timing; expiry logs and proceeds so a stuck cleanup cannot deadlock the queue (round-7 criterion). |
| A-0051 | 2026-08-06 | LIVE-2 | LIVE-2-FUNCTIONAL treats App typing state as stale after 15 seconds, allowing three missed 4-second runtime heartbeats before isTyping reports false. | confirmed | Confirmed: 15s staleness vs the 4s refresh gives three missed heartbeats of tolerance (>3x, per the pass-8 criterion) so a healthy turn never expires while a crashed producer clears within a few seconds. |
| A-0052 | 2026-08-06 | FILE-3 | For an already-joined attachment open, a shared-fetch result caused by another waiter aborting is ignored until the joined waiter reaches its own deadline; the shared flight emits at most one timeout warning. | promoted | Promoted to decision 0111: timeout dedup is per shared logical open (input-keyed ref-counted registry) guaranteeing exactly one warning under every stale-retry/join topology. -> 0111-attachment-timeout-log-per-shared-open.md |
| A-0053 | 2026-08-05 | SCHED-4A | The repeat-escalation day span is the inclusive elapsed span: at least 1 day, otherwise ceil((now - firstAskedAt) / 24 hours). | confirmed | Confirmed: inclusive elapsed span with a 1-day floor (ceil of elapsed/24h) is the intended reading — 'in 1 day' for same-day repeats, matching the escalation copy and its tests. |
| A-0054 | 2026-08-06 | SCHED-4B | When run-scoped permission requests with the same full interaction scope coalesce, only the first caller receives the provider prompt delivery callback; all callers share the settlement decision. | confirmed | Owner-confirmed callback policy in the SCHED-4B-1 final amendment. |
| A-0055 | 2026-08-06 | SCHED-4B | A process crash between shared durable permission-row creation and provider delivery can leave the setup fingerprint with an instruction card but no approval button until its blocker set changes; the owner retains the manual request_access or manage-access path. | confirmed | Owner-confirmed containment: a canonical durable ownership or lease reissue design affects the shared permission path, including request_access, and is deferred. |
| A-0056 | 2026-08-07 | SCHED-4B | No durable scheduler run-to-progress-card identity exists; stale-lease cleanup after process death sends the terminal timeout receipt but cannot safely replace the old running card in SCHED-4B-2. | confirmed | Binding S2-7 explicitly selects bounded outcome B when no durable run-to-card identity exists; current evidence shows only process-local run ownership plus route-keyed channel state. |
| A-0057 | 2026-08-07 | LEGACY-1 | The legacy-bindings remediation error will instruct operators to reset the stored settings revision and re-import canonical settings without agents.*.bindings. | confirmed | Confirmed as implemented: settings-revision-document.ts rejects agents.*.bindings with a specific remediation error that tells the operator to reset the stored settings revision and re-import canonical settings without agents.*.bindings — no runtime migrateLegacyAgentBindings on the read path. DB inventory found 0 stored revisions carrying legacy bindings, so no live reset was needed. Enforces 0112. |
| A-0058 | 2026-08-07 | LEGACY-1 | The decision-0003 architecture rule matches an explicit reviewable list of exact runtime compat identifiers across production source files; adding a new compat seam requires adding its symbol to that sentinel list, while exception records key the exact symbol. | promoted | Implemented and promoted to decision 0113: check:architecture matches an explicit reviewable list of exact runtime compat identifiers (providerConnection, channel-providerConnection:, migrateLegacy*, and the other known symbols) across production source, after masking comments and string literals to avoid false positives. Exception records key the exact symbol and are file-scoped + count-capped {file, rule, reason, owner, removeBy, maxViolations}. Adding a new compat seam requires adding its symbol to the sentinel list — the accepted exact-list backstop tradeoff documented in 0113. -> 0113-enforce-no-backcompat-architecture-check.md |
| A-0059 | 2026-08-11 | CLIRUN-1 | CLIRUN-1-1 caps capability_run argv at 64 arguments, 4096 UTF-8 bytes per argument, and 16384 UTF-8 bytes total; each reviewed wildcard matches exactly one non-flag argument. | confirmed | Confirmed as implemented: capability_run caps argv at 64 entries / 4096 bytes per arg / 16384 bytes total, and structured authorization is arity-exact with each wildcard matching exactly one non-flag argument (tightened in the whole-branch review). |
| A-0060 | 2026-08-13 | JOBFLOW-1 | S2a provenance_lane uses the existing AgentEngine vocabulary plus host (anthropic_sdk / deepagents / host, imported from shared/agent-engine.ts constants) instead of the plan's literal anthropic - reusing the single sanctioned SDK-engine literal keeps the provider-boundary architecture gate count-exact with no exception record | open |  |
| A-0061 | 2026-08-13 | JOBFLOW-1 | S2b narrows persisted approve_grant blockers at the storage boundary to addRules-without-destination (the only variant the setup approval path executes); decision 0127's additions-or-replacements wording covers the domain union, not persisted setup state | open |  |
| A-0062 | 2026-08-13 | JOBFLOW-1 | Prepared permission-card send ports perform local validation before returning a one-shot provider-call closure, so the recovery flow can persist beginSend immediately before invoking the provider API. | open |  |
| A-0063 | 2026-08-14 | JOBFLOW-1 | Job deletion records the same structured cancellation reason { code: 'job_deleted', job_id: <job id> } on the pending interaction, delivery item, and delivery audit rows. | open |  |
| A-0064 | 2026-08-14 | JOBFLOW-1 | S4-COMPILER keeps host capability-template proposals dormant with a code-owned false constant in the capability_run host handler; S4-INTENT will activate that same gate atomically without adding a settings or API surface. | open |  |
