READ-ONLY investigation of this repo (do NOT modify files; no DB writes; no service restarts). Deliverable: SHORT report — findings with file:line evidence first, then the minimal fix plan (files + one line each), simplest option first; delete-over-add.

SYMPTOM: scheduled job job-knacklabs-lead-maintenance-43527c192a6e (cron "5 8,10,12,14,16,18,20,22 * * *" in Asia/Kolkata, timeout_ms 2400000, max_retries 3, retry_backoff_ms 5000, status active, lease_run_id NULL) failed at 2026-08-26T14:57:04Z (run abe4dbc4, reason scheduled_job_idle_stall; run_leases row status failed). The failure payload and the Telegram notice said nextRun 2026-08-26T14:57:08Z ("Next run: 8:27 PM"). gantry.jobs.next_run_at is still 14:57:08Z and NO run started afterwards (checked at 15:45Z; scheduler process restarted at 15:29Z and registered a worker; no scheduler log line mentions the job since 14:57). Earlier today the same pattern: run failed 05:43:26Z (timeout) and the next start was the 06:35Z cron slot, not a 5-second retry. Also system:brain-dreaming (next_run_at 2026-08-24 21:45Z) and system:dreaming:main_agent (2026-08-25 21:45Z) are overdue and not running — may be a separate gate; note it.

QUESTIONS: (1) Where does the scheduler pick due jobs (the loop that ends in canonical-job-ops-service.ts claimDueRunStart -> canonical-job-claim.postgres.ts claimDueCanonicalJobRunStart) and what predicate makes this job non-claimable right now? Candidates: existingRun status check at canonical-job-claim.postgres.ts:56, worker-instance health (worker-MacBookPro-72124 marked unhealthy at 15:31Z), consecutive_failures, setup_state, a per-job timer that was lost on SIGKILL/restart, or a policy that skips overdue next_run_at and waits for the next cron slot. (2) Is the post-failure retry (next_run_at = failure + retry_backoff_ms) ever honoured, or does the notice promise a time the scheduler never uses? Use ~/gantry/logs/gantry.log (grep with -m and cut -c1-300; never print more than 40 lines at once) and read-only psql via docker exec gantry-postgres psql -U postgres -d gantry (runtime_events.payload_json is TEXT, cast ::jsonb; job_runs table is empty by design). (3) Smallest fix so that a failed run's retry actually starts (or, if retries are intentionally not dispatched, the notice must not promise them).
