# Deferral Ledger

Deliberately-removed scope with explicit revisit triggers (`forge defer add`).
When a trigger fires, the item goes back on the roadmap and its row is
resolved: `./forge defer resolve <id> --notes "<what happened>"`.

| id | added | item | why deferred | trigger to revisit | status |
|----|-------|------|--------------|--------------------|--------|
| D-0001 | 2026-07-22 | Data retention for jobs/interactions/runtime events (split out of arch-quick-wins as cycle-sized) | Entangled with scheduler/lease/agent machinery; the promised ledger note never landed anywhere trackable | durable-work-primitive lane starts (it refactors the same jobs/interactions state) | open |
| D-0002 | 2026-07-22 | E2E persona/topology harness goal-prompt (re-draft) | goals-index referenced a scratchpad draft that did not survive; scope needs re-drafting from scratch | agent-e2e test-matrix reconciliation pass | open |
| D-0003 | 2026-07-22 | Split apps/core/src/application/mcp/mcp-tool-proxy.ts (ratcheted at 800 in architecture-map.json lineBudgets) | File is the capability-authoring lane's conflict window; splitting on main would collide with branch feature/capability-authoring @13ae2e698 | CAP-1 capability-authoring closeout merges | open |
| D-0004 | 2026-07-22 | Retire the provider_specific_path exception on apps/core/src/shared/sdk-native-skill-names.ts (4 anthropic matches, sentinel contract) | anthropic_sdk is a deliberate sentinel token per decision 0028; relocation breaks layer rules, so an exact-count exception is the sanctioned cap | any change to the agent-harness selection vocabulary superseding decision 0028 | open |
| D-0005 | 2026-07-23 | L2ii escape hatch (allowUnsandboxedCommands) for the video-render Mach-registration unblock | allowUnsandboxedCommands runs commands OUTSIDE the SDK sandbox, so the retained credential/protected denylist cannot guard escaped commands; the deterministic rails lack authoritative GANTRY*PROTECTED_FILESYSTEM*\* + local_cli paths and can be preempted by reviewed-rule allow. Enabling the escape hatch now would open a credential-read bypass (S-0005-5cf6). | rails gain authoritative protected-path coverage (thread GANTRY*PROTECTED_FILESYSTEM*\* + local_cli credentialDirs into the secrets/protected rail) AND rails run before any reviewed-rule allow for escape-eligible commands | open |
| D-0006 | 2026-07-23 | Pre-existing bug: inline delegation completes tasks without persisting child agent runs (inline-agent-runtime.integration 'persists child runs' fails on PAY-1 base, postgres-gated so hidden by the credential-free npm test gate) | Real candidate bug surfaced by PERM-1's postgres-lane verify but present before PERM; child-run lifecycle contract not persisting for inline+worker delegation targets | next permission/jobs cycle OR when the verify contract adds the postgres lane | open |
| D-0007 | 2026-07-23 | Pre-existing: live-waiting-admission 'reports oldest inbound message'/'reports again after turn' fail on PAY-1 base (postgres-gated, hidden by credential-free npm test) | Live-admission message-reporting broken independent of PERM-1; surfaced by postgres-lane runs | next live/admission cycle OR verify contract adds postgres lane | open |
| D-0008 | 2026-07-23 | PERM-2 job pause: resume-the-SAME-run via a non-terminal lease + actively route the pause-ask to controlApprovers | Both are cross-subsystem and can't be proven by unit tests: resume-same-run needs a non-terminal (paused/suspended) run-lease lifecycle in the postgres lease repo+schema+a resume orchestrator (the lease enum is terminal-only today and resume mints a new run); approver-routing is a scheduler notification-path change resolving per-conversation controlApprovers into scheduler deps. Task E shipped the paused end-state (fail->paused + migration 0108 + durable status) but released the lease in the interim. | next permission/jobs cycle OR when a paused-run resume is needed live; land with postgres/integration lanes | open |
| D-0009 | 2026-07-24 | Native-binding descriptor-relative publish for Darwin inbound attachments (renameat/unlinkat via a native fs \*at addon) | Node exposes no \*at primitives; Darwin path-based rename/unlink is bounded only by unpredictable random names. Production is Linux (fully dirfd-pinned), so this is dev-only today | Darwin becomes a production/fleet runtime, OR a vetted native fs \*at binding is adopted in the repo | open |
| D-0010 | 2026-07-24 | Live-admission terminal retention policy (ended_at index, purge/archival job, identity-metadata retention ownership) | Audit marks retention ownership as an architecture decision; PERF-1 ships only the atomic active cap + overloaded outcome | before enabling a production retention/purge job or when terminal-row growth exceeds the approved operational threshold (SPS-4) | open |
| D-0011 | 2026-07-26 | Harden hashSkillBundle framing (length-prefix assets) so contentHash cannot collide across different bundles; requires hash versioning + re-hash of existing catalog rows | RACE-1 isolation is provided by the appId+catalogId path segments, not by hash collision-resistance; a framing collision only risks a same-(app,skill) stale self-read, and fixing the hash changes every stored content_hash which decision 0066 §4 forbids (no migration/compat). Pre-existing hashSkillBundle weakness, not introduced by RACE-1. | content-hash becomes a cross-tenant trust boundary, OR a decision authorizes hash versioning + a re-hash migration -> Fixed in RACE-1 (decision 0066 §2b): hashSkillBundle now uses length-prefixed framing + normalizeSkillBundle rejects duplicate normalized paths. No longer deferred. | done |
| D-0012 | 2026-07-27 | Extract projectRequiredSettingsRevision from settings-import-service.ts into its own config/settings module and restore the file to the 700-line default budget | RACE-2 added a cohesive lease-projection function that pushed the file to 705; raised its lineBudget to 720 rather than risk a same-branch extraction refactor. The function belongs in a sibling config/settings module (same layer, no boundary issue). | next change to settings-import-service.ts, OR a settings-transport refactor touching this file | open |
| D-0013 | 2026-07-27 | RACE-2b: durable last-fully-applied-revision marker for reliable forward-correction (guarantee re-projection when a synchronous required projection fails after the listener advanced its applied-revision) | Deeper applied-projection contract, distinct from RACE-2's serialization core; 6 autoreview rounds showed it needs its own decision. Core (serialize + stale-skip + reader-fence + readiness-red-on-failure) is strictly better than main and lands now. | RACE-2 core merged, OR a settings projection strand is observed in a real fleet, OR RACE-2b is scheduled | open |
| D-0014 | 2026-07-27 | RACE-2b readiness completeness: mark /readyz red on failure across all synchronous mutation projection paths (control/CLI/watcher), not just the listener | Threading the readiness signal through the ~15-file composition to cover synchronous mutation paths broke a layer budget; it is part of the same reliable-failure contract as the last-fully-applied marker (D-0013), not the serialization core. | RACE-2b scheduled (bundle with D-0013), OR a partial-projection-with-green-readiness is observed | open |
| D-0015 | 2026-07-28 | Trim apps/core/src/runtime/browser-capability.ts back toward its 737-line budget (its lineBudget was raised to 760 for RACE-4's lease threading) | RACE-4's advisory-lease migration is a net -103 lines overall (browser-profiles.ts dropped to 252) but browser-capability.ts grew ~15 lines from lease-port threading and awaiting the async release; extracting from it mid-migration risked the delicate browser module. | next substantive change to browser-capability.ts, OR RACE-3 browser per-turn ownership work touches this file | open |
| D-0016 | 2026-07-28 | RACE-4b: lease-generation fencing for browser profile handoff and snapshot publication (new owner must quarantine/confirm-stop the previous generation; snapshot upsert needs a generation CAS or must share the advisory-lock session) | Autoreview rounds 5-6 showed loss DETECTION is not enough: a successor can acquire the freed lease while the old Chrome is still shutting down, and a stale owner's async snapshot upsert can commit after handoff. Both need a fencing/generation contract in the lease + repository, which is a deeper design than RACE-4's ownership migration. The migration itself removes the lockfile ABA class and is strictly better than main. | RACE-4 core merged, OR a concurrent browser-profile corruption is observed, OR RACE-5 lease fencing work lands (shares the fencing-token design) | open |
| D-0017 | 2026-07-28 | RACE-4b: scope snapshot loss-suppression to a session/lease generation instead of a process-global profile-name marker | The one-shot per-profile marker can suppress a later VALID owner's snapshot or let a second stale attempt through; correct scoping needs the same generation identity as the fencing work above. | bundle with the RACE-4b fencing task | open |
| D-0018 | 2026-07-28 | Add tier-aware long-context model cost estimation | MOD-1 is bounded model onboarding; shared tier pricing and billing semantics require a separate reviewed cost-accounting contract. | Before Gantry cost estimates are used for billing or when production prompts regularly exceed OpenAI 272K or xAI 200K input tokens. | open |
| D-0019 | 2026-07-28 | Expose provider-compatible GPT-5.6 max and xAI reasoning effort controls | MOD-1 adds catalog entries using existing execution; the current DeepAgents runner rejects max and does not configure xAI effort. | When an operator requests these effort controls or representative evals justify exposing them through the shared agent-control contract. | open |
| D-0020 | 2026-07-28 | Make an in-flight channel.connect abortable on inbound lease loss, or keep inbound admission disabled until ownership is revalidated post-connect | Providers start their inbound transport partway through a multi-step connect (Slack app.start before auth.test, Discord gateway.connect), so losing the lease mid-connect can leave a transport live with no owner. Closing it needs a cancellable/raced connect or deferred inbound enablement — an architectural change to the channel connect contract, not a check. RACE-5 closes loss DETECTION and the pre/post-connect gates; this is the in-flight window. | the durable lease-generation slice lands (a generation makes deferred inbound enablement checkable), OR an unowned inbound transport is observed in a real fleet -> Superseded by FENCE-2: FENCE-1 landed the durable generation, so the trigger fired, but this task deliberately scoped to the BROWSER consumers only (decision 0082). Re-raised as the inbound half — see D-0026. | done |
| D-0021 | 2026-07-28 | Preserve and act on a failed lease-loss teardown on the inbound path instead of only logging it | Same class as the browser teardown-confirmation work: an unconfirmed disconnect after lease loss is currently logged and dropped, so a successor can start while the previous transport may still be draining. Belongs with the lease-generation/fencing contract rather than RACE-5's detection slice. | bundle with the durable lease-generation slice / RACE-4b fencing work -> Superseded by FENCE-2, same reason as D-0020 — see D-0026. | done |
| D-0022 | 2026-07-28 | Scheduled-job agent-turn context hydration at apps/core/src/jobs/execution.ts:324-335 still hydrates memory by default, the same waste LAT-3A removes from the inbound turn. | A scheduled job is not an inbound interactive turn, so it is outside the response-latency program's primary metric (inbound message to first content-bearing delivery). Folding it into LAT-3A would widen the write scope past the hot path with no latency evidence. | Job start-to-first-output latency is measured and shows memory hydration is material, OR a later phase already needs to touch jobs/execution.ts context loading. | open |
| D-0023 | 2026-07-28 | Whether the 250ms first_visible lexical-only memory recall timeout behaves differently when memory hydrates once instead of twice per turn. | Recall scoring and its timeout are inside HydrateAgentContextService, which LAT-3A does not touch; a single hydration issues the same query with the same budget. Treating this as a LAT-3A question would mean changing recall behavior under a latency task. | Post-LAT-3A measurement shows recall hit-rate or first-content latency regressing on the first_visible path. | open |
| D-0024 | 2026-07-29 | Close the residual TOCTOU window between the last agent-turn-context read and the model call, so a /new reset landing in that window cannot put the previous session's memoryContextBlock in front of the model. Requires a fence or serialization protocol that stays valid through model-call commitment, not another pre-call read. | SPLIT OUT per WORKFLOW.md Recurring Findings: this is the second review finding of the same class on LAT-3A, and the remedy the reviewer names is a serialization protocol spanning the model call. That is cycle-sized and entangled with the runner, live-turn lease, and model-invocation boundary - it is not a review fix on a latency phase. The window is PRE-EXISTING on every path on main and is not introduced or widened by LAT-3A; LAT-3A materially narrows it on the pending-delta path (previously spanning getDeltaMessages plus the degrade writes, now spanning only the function return). Holding a lock across a model call would also serialize resets against long-running generations, which needs its own design. | A reset-during-turn memory-leak is observed in practice, OR the live-turn lease work reopens the runner/model-call boundary, OR any phase proposes holding session state across the model call. | open |
| D-0025 | 2026-07-29 | Split apps/core/src/runtime/browser-capability.ts (ratcheted 760 -> 780 in scripts/architecture-map.json lineBudgets); the lease-loss onLost handler in registerLiveBrowserSession is the natural first extraction into runtime/browser-session-shutdown.ts, which RACE-4 created for exactly this | FENCE-1-d pushed the file to 768 lines. Extracting the handler needs its deps (sessions map, shutdownLiveBrowserSession, assertProfileLockValid) injected, which is a real refactor of the live browser teardown path - not something to attempt at closeout of a stage whose whole subject is a concurrency fence. Ratcheting matches the repo's own precedent for mcp-tool-proxy.ts (D-0003, ratcheted at 800 with a split deferred). | the next change that grows browser-capability.ts, OR FENCE-2 (which touches the same inbound/teardown seam) | open |
| D-0026 | 2026-07-29 | FENCE-2: apply the lease generation to the provider INBOUND path — make an in-flight channel.connect abortable on lease loss (or defer inbound admission until ownership is revalidated post-connect), and preserve/act on a failed lease-loss teardown instead of only logging it | FENCE-1 deliberately scoped to the browser consumers (decision 0082). D-0020 changes the channel connect contract, which is where the review-round count explodes; RACE-2, RACE-4 and RACE-5 each proved that mixing exclusion with a handoff contract in one task is what costs 6-9 review rounds. | next concurrency slice after FENCE-1 merges, OR an unowned inbound transport is observed in a real fleet | open |
| D-0027 | 2026-07-30 | Convert the Telegram MEDIA ingress (channels/telegram/media-ingestion.ts) to the fused inbound envelope, after explaining why its fused run persisted no conversation row. | Converted during LAT-4A and REVERTED before merge. With the fusion applied, the media path ran ensureConversation exactly once and issued 13 statements, yet left NO conversation row in the schema — while the same fixture shape works for Telegram text, Slack and Teams. The most likely explanation is that the fused transaction rolled back and took the conversation row with it, where previously the metadata write committed independently and survived. That is a real behavioural consequence of fusing and it is not understood well enough to ship. The media ingress also needed more restructuring than the other three (route matching had to move above the metadata call), so it carries more risk for less certainty. | Someone reproduces and explains the missing conversation row on the fused media path, OR the atomicity tradeoff in decision 0082 is revisited because a rolled-back envelope losing its conversation row is judged acceptable or is fixed generally. | open |
| D-0028 | 2026-07-30 | Measure the Discord inbound envelope statement count before/after the LAT-4A fusion, once Discord has a callable ingress seam that an integration test can drive. | Discord IS converted in channels/discord.ts (it uses resolveInboundConversationIdentity and gates the metadata write) and has adapter-level unit coverage, but its inbound path is bound to the discord.js gateway event handler with no production seam an integration test can call without faking the transport. Telegram text, Slack and Teams were each measured at exactly -9 statements, so the structural change is the same; what is missing for Discord is the observed number, not the fix. Fabricating a count or copying another provider's would be worse than recording it as unmeasured. | Discord grows a callable inbound seam (as Telegram text, Slack and Teams have), OR a Discord latency regression makes the number worth extracting some other way. | open |
| D-0029 | 2026-07-30 | Persist ALL eligible admissions for a multi-route inbound message in one serialized transaction, notifying every wake only after that shared commit. | The roadmap's Phase 4A scope says 'metadata, one message, and all eligible admissions in one serialized transaction'. LAT-4A delivers that for the SINGLE-route path and removes the duplicate ensureConversation everywhere, but multi-route messages still call storeMessageWithLiveAdmission once per route (channel-persistence-handlers.ts:182). Each call commits and notifies independently, so the first admission can become visible before the second commits, and a failure on the second leaves a partially persisted envelope. Batching them needs a new repository operation over the durable admission path — that carries its own correctness and measurement burden and is a phase, not a tail on this one. Shipping it untested inside LAT-4A would risk the durable admission queue for a shape that is not the measured win. | A multi-route conversation is observed with a partially persisted envelope or an out-of-order wake, OR Phase 4B reopens the canonical graph write path and can carry the batched admission operation with it. | open |
| D-0030 | 2026-07-30 | Decide whether a sender-allowlist drop should suppress AGENT PROCESSING but still persist the message, so channel history the agent can see matches what humans see. | Found while investigating a live report: an agent had no record of a colleague's message in a shared channel and could only act once the text was pasted in. Root cause is at write time — when the allowlist is in drop mode and the sender is not on it, every route is filtered out and channel-persistence-handlers.ts:148-170 returns before persisting, so the message is never stored. The context query filters only on conversation, thread and direction with no sender filter, so the hole is not a context-window limit; it is permanent and no history hydration or watermark repairs it. Whether that is correct is a privacy-versus-context judgement (a drop-mode allowlist may be deliberately keeping those messages away from the agent), so it needs a decision rather than a patch. Provider-neutral: affects Telegram as much as Slack. | A user reports an agent missing channel context that humans can see, OR the allowlist semantics are revisited for any other reason. | open |
| D-0031 | 2026-07-30 | Paginate the Slack thread tail window with cursor accumulation so capped installations (15-object conversations.replies responses) can still fill the latest-39 selection. | Pre-existing ceiling of the original single-tail-response design that GH-352 restores, not a regression: under a capped installation the aggregate coverage protocol fails safe (unseen rows keep exhausted false, hydration retries) and the window degrades gracefully. Full tail pagination needs rate-limit handling and is its own bounded change beyond restoration scope (autoreview GH-352-1 round 8, adjudicated deferred). | A deployment on a capped Slack installation type observes persistently underfilled thread hydration, or thread-context quality reports implicate missing latest replies. | open |
| D-0032 | 2026-07-30 | record_signoff.py attests the run's client sign-off with candidates[-1] — the highest-numbered NNNN-client-signoff.md in the repo, whatever task it belongs to — and silently discards --notes; make it verify the record matches the active issue (or store the notes on run.json) | Found while running RACE-3 intake: the gate passed cleanly using CONV-001's record, confirmed_by Yash, for a completely unrelated feature. It is a HUMAN gate producing a false attestation with no warning. Out of scope for RACE-3, which is a browser-profile isolation fix, and it touches the harness scripts rather than product code. | next harness/forge maintenance pass, OR the next time a task's sign-off cannot be traced to its own decision record -> Fixed upstream in symphony-forge #26 and vendored here: sign-off is a one-time project gate pinned in harness.yaml and derived, never a per-worktree run.json flag. Decision 0093. | done |
| D-0033 | 2026-07-30 | Split apps/core/src/runtime/agent-spawn.ts (ratcheted 810 -> 812) and apps/core/src/runtime/ipc-parsing.ts (764 -> 766); both are orchestrators sitting at their line ceiling, so every feature that touches them pays a budget fight | RACE-3 adds one field to each: the issued browser profile at spawn, and the echoed profile on the browser IPC request. Both are irreducible one-line additions. I trimmed my own comments twice and still landed 1 line over on each, which is a signal the budgets are at their limit rather than that the change is bloated. Splitting either file is a real refactor of the spawn / IPC parsing paths and does not belong in a browser-profile scoping task. | the next change that grows either file, OR the FENCE-2 work which touches the same IPC boundary | open |
| D-0034 | 2026-07-30 | Teams file capture (live download + backfill refetch): widen the Teams adapter port with a Graph download operation and wire the attachment writer. | Teams' adapter port exposes no download method and the default TeamsSdkClient is null (teams-sdk-client.ts:3) — a download port over a null client cannot be end-to-end verified. Client chose to defer (2026-07-30) rather than build unverifiable scaffolding. | A real Teams SDK client lands, or a Teams deployment needs file reading. | open |
| D-0035 | 2026-07-31 | Reclaim provider-attachment bytes stranded by a crash between a REPLACED attachment row's commit and its post-commit unlink (row already deleted; ref existed only in memory). | Bounded disk-space leak of UNREACHABLE bytes: with no referencing row the resolver cannot serve them to anyone, so no retention or scoping promise is violated; the deleted-file (tombstone) direction is durable via retry-from-row. A durable work-item table or GC sweep is its own bounded change (branch autoreview FILE-1A, adjudicated deferred). | provider-attachments disk usage grows measurably beyond referenced bytes, OR a GC/sweep lane is built for any other artifact store. | open |
| D-0036 | 2026-08-01 | Cross-process coverage-invalidation closure: a turn in ANOTHER process during the <=2s window after DB recovery (reconnect bump not yet durable) can trust stale completeness; full closure needs an inbound-ownership contract (non-owner guards fail closed until the owner's transition is durable). | Adjudicated twice (rounds 5 and 7): the shipped deployment is single-process (launchd local runtime) where the in-memory epoch closes the window entirely; the multi-process residual requires DB-outage + reconnect + an immediate turn in another worker within the bounded retry cap, and its worst case is one skipped hydration of previously-attested-complete history. Documented in code; full ownership contract is its own bounded change. | A multi-process runtime deployment becomes real, OR a missed-message report implicates the post-recovery window. | open |
| D-0037 | 2026-08-01 | Discord deletion events received during a full DB outage and lost to a crash before the first successful pair-row insert are not durably retained | The raw-first insert plus coalesced in-process retry covers transient failures; surviving a crash DURING a complete Postgres outage needs an out-of-DB journal, which nothing else in the runtime has | A missed-deletion report correlated with a runtime crash during a DB outage, or the introduction of any out-of-DB durable event journal | open |
| D-0038 | 2026-08-01 | Cold-cache thread deletions that race the thread's first-ever message insert when only the parent-channel route is configured are dropped (no stored message to admit, no cached parent mapping) | Closing it needs a durable thread-parent mapping or event quarantine; the window requires a process restart, an inherited (non-explicit) thread route, AND a deletion racing the thread's very first message | A missed thread-deletion report after a restart, or the introduction of a durable thread-parent mapping for any other feature | open |
| D-0039 | 2026-08-01 | Attachment removals delivered via Discord MESSAGE_UPDATE do not tombstone already-captured live bytes (lazy re-fetch covers metadata-only rows; captured storageRefs stay openable) | Needs a selective attachment-level tombstone operation and MESSAGE_UPDATE reconciliation - a new event flow beyond the whole-message deletion this phase shipped | A report of an edit-removed attachment still being served, or the next provider gaining attachment-level deletion semantics | open |
| D-0040 | 2026-08-01 | Unmatched deletion-marker pair rows (e.g. for never-ingested bot messages) are retained indefinitely with no expiry | They are the ingest-race guard and are excluded from retry scans; growth is per-event bounded and only malicious bulk traffic amplifies it | Marker-table growth observed in metrics, or a review of storage hygiene sweeps | open |
| D-0041 | 2026-08-02 | Collapsing the CONDITIONAL provider/agent/account upserts on the message hot path (the remaining ~7 identity statements) requires a commit-backed graph-ready receipt | Deleting them without a receipt breaks first-contact creation for fallback accounts; the fallback account insert also has a latent identity-conflict landmine (uniq_provider_accounts_active_identity silently no-ops a second same-provider account) that receipt plumbing would subsume | Post-4B measurement showing identity upserts still dominating the envelope, receipt plumbing appearing for another feature, or the account-identity landmine biting outside tests | open |
| D-0042 | 2026-08-02 | Teams typing and reactions | The production Teams SDK client is still a null stub, so typing and reaction behavior cannot be verified against a real provider client. | A real Teams SDK client lands, or a Teams deployment needs typing or reaction liveness; shared with D-0034. | open |
