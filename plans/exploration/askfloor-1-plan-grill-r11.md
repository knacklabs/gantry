# ASKFLOOR-1 PLAN grill — round 11 (read-only, adversarial, emit under ~300 words; factory/prompts/griller.md --gate plan)

You did NOT author this plan. Round 10 returned 2 blockers + 3 gaps + 1 owner question. The plan at `plans/exploration/askfloor-1-plan-source.md` (skip the frontmatter decision list) and decision 0154 were revised:
1. 0154 explicitly AMENDS 0118 §45-47 for `human_decision` records only (unresolved person → persist nothing, act once).
2. T3a generates and validates a v4 uuid for every human row; short-id derivation test.
3. Decomposition split to 10 bounded tasks: T1, T2a (shape classifier + containment extraction, rails birthright, judge status), T2b (native tool risk + handler protection), T3a (persistence + all `domain/types.ts`), T3b (memory consult, coordinator + inline), T3c (remember settlement codec set + chokepoints), T4, T5a (card UX + EVERY provider file incl. `memory_forget` rendering/settlement), T5b (`/permissions`, no provider files), T6. Shared-file helper ownership named.
4. Approver display name = `acting_person_label` snapshot at decision time (fallback "someone") — the recommended answer taken.
5. `forget <prefix>` searches active rows only → stale prefix is `not_found`; "Already forgotten." reachable only from a full-id button.

Verify each is closed; hunt anything NEW (over-build, rails widening, invariance holes, AF-AC8 ownership S1 T2a / S2 T3b / S3 T1 / S4 T5a / S5 T4 / S6 T6 / listing T5b, boundedness of the 10 tasks, decision hygiene 0153/0154 vs 0052/0118/0121).

OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "PLAN SOUND" or the blocker count. No edits.
