# ASKFLOOR-1 PLAN grill — round 15 (read-only, adversarial, emit under ~200 words; factory/prompts/griller.md --gate plan)

You did NOT author this plan. Round 14 returned 1 blocker, no owner questions. The plan at `plans/exploration/askfloor-1-plan-source.md` (skip the frontmatter decision list) and decision 0154 were revised: remembering controls and "will remember" copy render ONLY on cards the host stamps `rememberEligible` (DM + `interactive_auto` + resolved person); ask, auto_strict, job and group cards keep today's once-only shape; four-provider tests per ineligible lane (T5a); 0154 §Decision says so.

Verify it is closed at §3/§5/T5a and 0154; hunt anything NEW (over-build, rails widening, invariance holes, AF-AC8 ownership, boundedness, decision hygiene).

OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "PLAN SOUND" or the blocker count. No edits.
