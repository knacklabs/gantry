# ASKFLOOR-1 PLAN grill — round 18 (read-only, adversarial, emit under ~150 words; factory/prompts/griller.md --gate plan)

You did NOT author this plan. Round 17 returned 2 blockers + 1 gap. The plan at `plans/exploration/askfloor-1-plan-source.md` (skip the frontmatter decision list) and decision 0154 were revised:
1. Ineligible-lane cards (ask, auto_strict, job, group) are byte-for-byte today's cards — labels incl. `Cancel`, copy, durable-rule controls; no human-decision-memory control or copy; four-provider snapshot tests per lane. 0154's "only once-only buttons" clause replaced with "no human-decision-memory controls or copy".
2. Every group `/permissions` variant (bare, `all`, `forget <id>`) is non-mutating DM guidance, one test each.
3. Short id FIRST in `/permissions all` rows.

Verify all three are closed; hunt anything NEW. OUTPUT: numbered findings — claim, file:line, class, severity, smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "PLAN SOUND" or the blocker count. No edits.
