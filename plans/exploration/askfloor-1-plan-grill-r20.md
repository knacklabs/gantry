# ASKFLOOR-1 PLAN grill — round 20, AMENDMENT (read-only, adversarial, emit under ~300 words; factory/prompts/griller.md --gate plan)

You did NOT author this plan. Rounds 18/19 found the plan sound. While authoring the T1 execution contract, the task grill (round 6) found that the T1 contract's refined decision order, analysis shape and ownership diverged from the approved story plan; the story plan `plans/exploration/askfloor-1-plan-source.md` (skip the frontmatter decision list) is now AMENDED so the story plan is the single authority:
1. §1: `AutoLaneAnalysis { lane, readOnlyMetaExecutor }` with domain-owned `PermissionLane`/`RailSignal`/`RailProvenance` (`domain/permission-lane.ts`); computed once per IPC permission request in a pre-coordination step of the IPC helper; passed to the coordinator as an OPTIONAL input (assumption A-0069 in `plans/assumptions.md`: coordinator = T1/T3b shared file, T1 limited to the seam); the coordinator runs one base rail evaluation and calls `tail(context)`; the tail's route guard only consumes it.
2. §1: THE ONE AUTHORITATIVE DECISION ORDER: pre-coordination route analysis → [T3b] exact remembered deny → hard restrictions → reviewed rules → deterministic rails (single base evaluation) → conditional trusted-root handling (only on out_of_trusted_root ASK; hypothetical rechecks unchanged) → [T3b] remembered allows → classifier-cache read → tail.
3. §1: hard-boundary `find` predicate incl. `-H/-L/-follow`, indirect root options (`-files0-from`) failing closed, operand + predicate-value scanning via pure `isSensitivePathShape`.
4. §1: canonical provenance `decidedBy/source: auto_classifier` + typed `railProvenance` on `PermissionApprovalDecision`, in the signed IPC payload and every runner decoder.
5. Task Decomposition: T1 line and the shared-file list updated (coordinator seam per A-0069; `domain/types.ts` sequential T1 → T3a).

Verify the amendment is internally consistent with §3 (memory stages reference the §1 order), the T3a/T3b/T4/T6 lines, AF-AC3/AC4/AC6, decisions 0153/0154, and introduces no rails widening or invariance hole. OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "PLAN SOUND" or the blocker count. No edits.
