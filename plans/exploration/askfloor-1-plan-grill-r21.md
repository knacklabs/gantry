# ASKFLOOR-1 PLAN grill — round 21, AMENDMENT (read-only, adversarial, emit under ~300 words; factory/prompts/griller.md --gate plan)

You did NOT author this plan. Since round 20 (two consistency blockers, both folded: §3 now consumes T1's optional `AutoLaneAnalysis` + the coordinator's own `railDecision`; T3b consumes T1's seam and owns only the memory stages) the OWNER RULED (2026-09-03): gantry-native tools auto-allow by default in interactive auto, browser included; only a closed high-risk list asks (non-own-destination `send_message`, destructive or protected writes, scheduler mutations, admin mutations); ambiguous shapes go to the classifier; simplify. The plan `plans/exploration/askfloor-1-plan-source.md` (skip the frontmatter decision list) is amended: AF-AC1/AC2/AC4, Scope item (3), §2 (T2b = ONE typed default-allow risk table replacing the closed browser matrix and per-argument `file` risk), the T2b task line, Decisions (NEW 0155 `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md`), Risks. The confirmed spec was amended the same way (re-grilled at spec round 5).

Verify: (1) the amended plan is internally consistent (§2 vs T2b line vs AF-AC1/AC2/AC4/AC6 vs Surface Impact); (2) the high-risk rows are closed, deterministic and testable, and the table cannot leak into rails/ask/auto_strict/jobs; (3) decision hygiene — 0155 vs 0043/0052/0121/0154 (own-channel predicate reuse; attachment birthright stays in 0154); (4) the round-20 folds hold; (5) anything NEW. OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "PLAN SOUND" or the blocker count. No edits.
