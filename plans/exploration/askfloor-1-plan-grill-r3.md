# ASKFLOOR-1 PLAN grill — round 3 (read-only, adversarial, emit under ~700 words; factory/prompts/griller.md --gate plan)

You did NOT author this plan. Round 2 returned 7 blockers + 1 gap. The plan at `plans/exploration/askfloor-1-plan-source.md` (skip the frontmatter decision list) was revised with seam facts from a read-only exploration (`plans/exploration/askfloor-1-seam-map-3.md` lists the questions asked). Owner rulings this round: `/permissions` "covered job" shows only ACTUAL consumption from the job permission audit trail; one remembering button per card (family button takes the alternative slot). Decisions: `docs/decisions/0153-*` now names the superseded 0121 clauses and fails closed on unresolved job identity; `0154-*` adds the acting person, deny-is-always-exact, and destructive-remembers-only-No.

Verify each round-2 fix is closed at the cited seams (read only what you need):
1. AF-AC2 file-read predicate — `file` has no capability id (`runner/mcp/tools/file.ts:65-74`); the plan's predicate is action ∈ {list, read} AND (artifactId OR scope+path inside the workspace) AND not protected. Is the "approved-capability requirement" honestly satisfied, or does the spec need a literal capability?
2. 0118 acting-person scope on rows/port/lookups/indexes/live writes/projection; unresolved job identity = `personId: undefined` + retained jobId → no projection. (`ipc-interaction-processing.ts:132-157`, `permission-decision-memory.ts:16-35`, `schema.ts:98-106`.)
3. `remember { outcome, scope }` as a typed provider-neutral resolution in `domain/types.ts:249-258` + normalization + settlement (T3) before provider parsers/renderers (T5, four files named). `HumanDecisionMemoryService { remember, list, revoke }` injected into `SessionCommandDeps` (`session-commands.ts:89-135`); `admin_permission_list` NOT extended. Any missing seam?
4. One `AutoLaneAnalysis` computed at the route guard (`ipc-permission-classifier-decision.ts:184-218`) feeding both the coordinator memory stage and the veto merge (:307-330); eight write-capable `find` actions refused; parser untouched.
5. Path-only write destination table (Write/Edit `file_path`; `file write` scope+path; `promote_scratch` targetScope+targetPath; shell writes never path-only). Provable protected-path exclusion?
6. Ownership: T1 pre-splits the IPC tail into five helpers and owns the file; T2 edits `permission-classifier.ts:304-356` + application/permissions; T3 owns memory/coordinator/types/chokepoints incl. inline `afterDecision`; T4 edits exactly T1's route-guard helper + the inline projection helper (:408-445); T5 channels/session; T6 latch + typed status in `permission-classifier.ts:58-66` + T1's consult helper. Still not bounded? Would you merge or split?
7. Decisions 0153/0154 consistent with 0121/0118 and with the plan (destructive: only No remembered; deny exact).
8. Typed classifier status `answered | unavailable | skipped` derived from `failureCode` (`permission-classifier.ts:58-66`) driving the latch.
9. Anything NEW the revision introduced: over-build, rails widening, invariance holes (ask, auto_strict, autonomous, YOLO, inline-scheduled), AF-AC8 ownership (S1 T2, S2 T3, S3 T1, S4 T5, S5 T4, S6 T6, listing T5).

OUTPUT: numbered findings — claim, file:line, class (correctness|scope|contract-gap|decision-conflict|over-build), severity (blocker|gap|nit), smallest fix. Then OWNER-LEVEL questions verbatim (none if none). End with "PLAN SOUND" or the blocker count. No edits.
