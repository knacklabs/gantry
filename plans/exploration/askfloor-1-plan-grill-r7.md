# ASKFLOOR-1 PLAN grill — round 7 (read-only, adversarial, emit under ~450 words; factory/prompts/griller.md --gate plan)

You did NOT author this plan. Round 6 returned 4 blockers + 3 gaps + 1 owner question. The plan at `plans/exploration/askfloor-1-plan-source.md` (skip the frontmatter decision list) was revised:
1. T2 adds ONE `(scope, path)` read predicate = generic virtual protection (`protected-virtual-path.ts:23-57`) + prompt-profile-scope protection; applied after id resolution and during listing; ordinary-scope counter-tests.
2. OWNER RULING: `attachment_open` of an attachment already in the conversation is a TRUE input-gated birthright in the deterministic rails in EVERY mode (`permission-deterministic-rails.ts:53-103`) — the story's only rails edit, owned by T2, pinned by per-mode rails tests; AC2/AC6/Scope/Decisions updated (extends 0052; recorded in 0154's consequences).
3. T2 extracts ONE typed effect-shape classifier from `auto-permission-read-only-gate.ts:248-308` (verb/argument → read-only shape, no containment); containment stays a separate hard boundary; rails, read gate and T3 kind matching reuse it.
4. Inline live runtime: T3 adds the decision-memory port to `inline-agent-loop-tool-types.ts:23-49` + the inline memory consult (:399-445), T4 the audit port, T6 the latch hook there too.
5. Native write containment = prospective-write contract shared with `gantry-facade-file-safety.ts:39-67`.
6. T5 owns `group-processing-session-command-handlers.ts:140-170` caller wiring + identity tests.
7. S4 reworded.

Verify each is closed at the seams (read only what you need). On item 2 specifically: is a per-mode input-gated birthright in the rails safe for autonomous runs (0121) and ask mode, and does the decision hygiene hold without a new decision record? Then hunt anything NEW: over-build, other rails widening, invariance holes, AF-AC8 ownership, task boundedness.

OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "PLAN SOUND" or the blocker count. No edits.
