# ASKFLOOR-1 seam exploration #3 (read-only; answer with file:line facts, under 900 words; no edits)

Plan round 2 (`plans/exploration/askfloor-1-plan-source.md`) raised seam gaps. For each item give the exact file:line, the current shape, and the smallest seam the plan should name.

1. `mcp__gantry__file` capability contract: the literal capability id(s) the `file` tool binds to, how `approvedCapabilityIds` reach the native-risk path (`runtime/permission-classifier.ts:327-367`) and `application/permissions/gantry-tool-risk.ts`, and the exact argument shapes for `list`/`read`/`write`/`promote_scratch` (`runner/mcp/tools/file.ts:12-62`: scope/path vs targetScope/targetPath). Also FileWrite/FileEdit native tool input field names as seen by the classifier caller.
2. Permission callback contract: where `allow_once | allow_persistent_rule | cancel` is parsed per provider (Telegram, Slack, Discord, Teams callback parsers) and where the typed provenance union lives (`domain/types.ts:249-258`); what adding a `remember` resolution with an outcome would touch, file by file.
3. `/permissions` session command: `session-command-parse.ts:25-39`, `session-commands.ts:580-585` — what dependencies the session command handler receives, whether any repository/port for permission-decision memory is reachable there, and the shortest wiring to add a host-owned application service with `remember | list | revoke`. Also how the runner-local `admin_permission_list` tool (`runner/mcp/tools/admin-permissions.ts:115-167`) calls the host today (the IPC task shape at `ipc-admin-handlers.ts:353-390`).
4. Acting-person identity (decision 0118): how host-derived `personId` reaches the interactive permission request and the job permission request today; which fields the `permission_decision_memory` row and lookups would need; what an unresolved job identity looks like.
5. `ipc-permission-classifier-decision.ts:173-452`: the structure of the tail function — name the internal phases in order with line ranges (lane/job guard, rails callback, native risk, classifier consult, veto, cache, prompt), so the plan can pre-split it into typed seams owned by one task each. Same for `inline-agent-loop-tools.ts` around :408 (scheduled projection point) and :503-550 (afterDecision).
6. Classifier result shape: `runtime/permission-classifier.ts:58-66` failureCode values — which mean "judge unavailable" versus local truncation/cancellation/skip, so a typed `answered | unavailable | skipped` status can be derived.
7. GNU/BSD `find` write-capable actions beyond `-delete -exec -execdir -ok -okdir`: confirm `-fls -fprint -fprint0 -fprintf` and anything else the parser at `shared/bash-command-parser.ts:563-573` should treat as writing.

Output: numbered facts, then a short "plan should say" line per item.
