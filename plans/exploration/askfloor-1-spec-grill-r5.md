# ASKFLOOR-1 SPEC grill — round 5, AMENDMENT (read-only, adversarial, emit under ~300 words; factory/prompts/griller.md --gate spec)

You did NOT author this spec. The confirmed spec `docs/specs/askfloor-1-judge-actually-judges.md` is AMENDED by an owner ruling (2026-09-03): in interactive auto, gantry-native tools auto-allow BY DEFAULT — every `browser_*` action included — and only a closed HIGH-RISK list still asks: `send_message` to a destination that is not one of the agent's own registered channels (0052), destructive or protected writes (delete/overwrite outside the workspace; protected/secret paths), scheduler and admin mutations; ambiguous shapes (unmapped tools, malformed input) go to the classifier instead of being forced HIGH. The closed browser matrix and per-argument `file` risk are dropped. Ask mode, auto_strict and jobs are unchanged. A new decision `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md` records it (re-calibrating 0043/#212's static buckets).

Interrogate the amended AC1/AC2/AC4 and Behaviour sections against decisions 0043, 0052, 0121, 0154, the runtime evidence `plans/exploration/askfloor-1-runtime-evidence-2026-09-02.md`, and the code the spec names (`application/permissions/gantry-tool-risk.ts`, `domain/permission-deterministic-rails.ts`, `runner/mcp/tools/file.ts`; read only what you need). Hunt: is the high-risk list complete enough to be safe (what auto-allows now that should not?), is it small enough to be simple, does any row leak into rails/ask/auto_strict/jobs, are the ACs testable, does the amendment contradict any retained AC or owner ruling.

OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "SPEC SOUND" or the blocker count. No edits.
