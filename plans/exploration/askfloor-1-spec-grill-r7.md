# ASKFLOOR-1 SPEC grill — round 7, AMENDMENT (read-only, adversarial, emit under ~200 words; factory/prompts/griller.md --gate spec)

You did NOT author this spec. Round 6 returned 5 blockers; `docs/specs/askfloor-1-judge-actually-judges.md` and `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md` (now ACCEPTED by the owner) were revised: (1) routing wording is `permissionMode === 'auto' && !hostVerifiedJobId` everywhere; (2) AC2 enumerates the complete per-action `file` contract (`file.ts:12-62`, handlers `:59-96,200-223`), unprotected virtual-store writes are LOW (recommended answer taken), virtual-path protection is distinct from filesystem containment; (3) admin rows cite `:129-159` AND `DELEGATION_DISPATCHERS` at `:172-177`; (4) the browser row is bound to `file_attach` with a raw filesystem path (outside-workspace/protected/secret), `file_upload` stays LOW (`browser-artifact-policy.ts:57-99`); (5) 0155 accepted.

Verify each is closed; hunt anything NEW. OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "SPEC SOUND" or the blocker count. No edits.
