# ASKFLOOR-1-T1 TASK grill — round 9 (read-only, adversarial, emit under ~200 words; factory/prompts/griller.md --gate task)

You did NOT author this contract. Round 8 closed the round-7 items and returned 1 blocker (recommended answer taken). The task `ASKFLOOR-1-T1` in `.factory/stories/ASKFLOOR-1/decomposition.json` and the saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T1.md` were revised: assumption A-0070 (`plans/assumptions.md`, on the approved plan) declares `domain/permission-deterministic-rails.ts` a T1/T2a shared file — T1 owns ONLY the canonical `RailSignal` value `unsupported_meta_executor` (domain enum) and its emission for the existing meta-executor refusal (today `privileged`, `:41-46,164-171`), ASK verdict unchanged; the file and a rails leaf ("emits the canonical unsupported_meta_executor rail signal … verdict unchanged") are in write_scope/required_tests; every `read_only_meta_executor` provenance reference is replaced by the signal `unsupported_meta_executor` plus the analyzer's `readOnlyMetaExecutor`; the Scope's "Out" list no longer excludes this one rails edit; sixteen leaves.

Verify closure; hunt anything NEW. OUTPUT: numbered findings — claim, file:line, class, severity (blocker|gap|nit), smallest fix. OWNER-LEVEL questions verbatim (none if none). End with "CONTRACT SOUND" or the blocker count. No edits.
