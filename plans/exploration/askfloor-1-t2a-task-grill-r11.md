# ASKFLOOR-1-T2a task-contract grill — round 11 (owner widening)

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2a` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2a-task-grill.md`); rounds 2–10 briefs `-r2.md` … `-r10.md` (round 10 was CONTRACT SOUND).

OWNER RULING 2026-09-04 after round 10: every HOST-VALID attachment id must be zero-tap ("Widen T2a"). Fold: the display sanitizer (`runtime/ipc-tool-input-sanitization.ts`, `sanitizeValue`) keeps string entries whose path's first segment is `attachment_ids` unshortened up to `ATTACHMENT_ID_DISPLAY_LIMIT = 512` (the host handler's `maxLen: 512`, `jobs/ipc-attachment-open-handler.ts:54-59`), marking neither `alteredPaths` nor `truncatedPaths` for them; redaction unchanged; every other path keeps `state.maxStringLength`. The rails keep every veto byte-for-byte; a ≤512 id therefore never asks, a >512 id is shortened and asks, a redacted id asks. Scope +2 files (`ipc-tool-input-sanitization.ts`, `test/unit/runtime/ipc-auth-boundary.test.ts`), 17 files, 13 required tests, budget 18/1500.

Verify with `path:line` citations: (a) that `sanitizeValue` can see the key at the string branch (the `path` argument: is the first segment `attachment_ids` for array entries, i.e. `attachment_ids.0`?) and that the change is confined to that branch; (b) that the display copy is what the rails' sanitization veto reads (`permission-deterministic-rails.ts:125` reads `toolInputSanitized` / `toolInputSanitizedPaths` — confirm they derive from `alteredPaths`), so leaving a ≤512 id unmarked removes the veto for it; (c) that the classifier copy (16,000 limit) needs no change; (d) that no other consumer of `alteredPaths`/`truncatedPaths` (e.g. `input_truncated` detection in the classifier) is affected by an unmarked 512-char string; (e) consistency across task plan, `plan_contracts`, `acceptance_criteria`, `required_tests` (thirteen plain `it` titles), `reviewer_focus`, `write_scope`, `review_budget`. Blockers must be defects that would make the implementer build the wrong thing or make an AC unprovable.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
