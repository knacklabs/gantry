# ASKFLOOR-1-T2a task-contract grill — round 13

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2a` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2a-task-grill.md`); rounds 2–12 briefs `-r2.md` … `-r12.md`.

Round 12 found three blockers; all folded by adopting the typed pre-sanitization fact (the shape round 2 proposed): `PermissionApprovalRequest.attachmentOpenIds?: { wellFormed, count }` computed once in `runtime/ipc-parsing.ts` from the raw `decisionToolInput` before either sanitizer (well-formed = 1..12 strings, each non-blank and ≤512 after trim — the host's validity shape); the `attachment_open` row's typed-fact map entry REPLACES the completeness/sanitization/redaction checks for that row only; fact absent or false → existing ASK. The sanitizers are untouched; AC1, §2 and the workflow diagram state that ONE rule; the sanitizer file and its test left the scope, `ipc-parsing.ts`, `domain/types.ts` and `ipc-parsing.test.ts` joined it (18 files, 13 tests, budget 20/1600, reason corrected).

Verify with `path:line` citations: (a) that the parser has the raw input and the tool name at the point named and that the fact can ride on the request through every path the rails see (IPC parse → coordinator → rails; also whether T1's harness `replayPermissionRequest` constructs the request in a way that lets the S1 fixture supply the fact — name the field or the parser call it must use); (b) that no other consumer of `PermissionApprovalRequest` (signing, IPC codecs, runner decoders) needs the new optional field; (c) that replacing the three checks for one row cannot widen any other row or lane; (d) consistency across task plan, `plan_contracts`, `acceptance_criteria`, `required_tests` (thirteen plain `it` titles), `reviewer_focus`, `write_scope`, `review_budget`. Blockers must be defects that would make the implementer build the wrong thing or make an AC unprovable.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
