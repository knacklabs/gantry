# ASKFLOOR-1-T2a task-contract grill — round 4

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2a` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2a-task-grill.md`); rounds 2–3 briefs `askfloor-1-t2a-task-grill-r2.md`, `-r3.md`. The harness on this branch was re-vendored to symphony-forge 4bac95e (upstream #158–#164) since round 3; apply the current `factory/prompts/griller.md`.

Round 3 found two blockers; both folded by simplification:
1. The rails judge SHAPE ONLY on the display copy `toolInput` (array of 1..12 non-blank strings; truncation and redaction keep count and string-ness — `runtime/ipc-tool-input-sanitization.ts:6,55-64,73`); for a tool with a predicate entry a pass takes precedence over the sanitization veto (`permission-deterministic-rails.ts:125`) and the redaction veto (`:264`); no length rule at the rails; no `classifierToolInput` dependence; no `ipc-parsing.ts` change. Length (>512) and origin stay host-validated as TOOL ERRORS, never cards (`ipc-attachment-open-handler.ts:54-59,43-84`); one 512/513 boundary leaf in `ipc-attachment-open-handler.test.ts` with the handler source untouched. Decision 0154's "malformed id asks" is read as malformed = wrong shape.
2. S1 fixture needs no harness change (`TapBudgetFixture` carries `toolName` + `toolInput`).

Verify: (a) is the shape-only reading consistent with spec AF-AC2 ("a malformed id asks") and decision 0154 — quote both and say whether a too-long id being a tool error rather than a card contradicts either; if it does, state the minimal wording fix (spec/decision amendment) rather than a mechanism change; (b) that the veto-precedence exemption is keyed strictly by the predicate-map entry and cannot widen any other input-gated row; (c) that `toolInput` is always present on `PermissionApprovalRequest` in every lane the S1 fixture replays; (d) the thirteen ids are plain `it` titles; (e) write_scope 19 files / budget 19/1500 is exactly what is needed; (f) consistency across task plan, `plan_contracts`, `acceptance_criteria`, `required_tests`, `reviewer_focus`, `write_scope`. Then re-check items 5–9 of the round-1 brief briefly.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
