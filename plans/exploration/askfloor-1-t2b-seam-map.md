# ASKFLOOR-1-T2b seam exploration (read-only)

Read-only cold-read exploration, no file edits, no writes. Report facts with `path:line` citations only. Do not propose designs beyond what is asked.

Context: story plan `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§2 "Default-allow risk table (T2b)" and the T2b line under Task Decomposition), spec `docs/specs/askfloor-1-judge-actually-judges.md` (AF-AC1, AF-AC2 file half, AF-AC4 native-risk half), decisions 0043, 0052, 0121, 0154, 0155. T1 and T2a are merged on this branch: the native-risk branch of the classifier now lives in `apps/core/src/runtime/permission-classifier-native-risk.ts` (`evaluateNativeRiskBranch`), the pure effect-shape classifier in `apps/core/src/shared/permission-effect-shape.ts`, the hard-boundaries evaluator in `apps/core/src/shared/permission-hard-boundaries.ts`, and `PermissionClassifierResult.status` is required.

Answer each item with citations:

1. `apps/core/src/application/permissions/gantry-tool-risk.ts`: every export, the current risk model (which tools are HIGH/LOW/unmapped, by identity or by argument), the result type, and every caller (grep the repo). Which callers pass arguments and which pass only the tool name?
2. `apps/core/src/runtime/permission-classifier-native-risk.ts` after T2a: the exact input type `evaluateNativeRiskBranch` receives (does it already carry the tool arguments / `classifierToolInput`?), what it returns, and where in `permission-classifier.ts` it is called. Cite the T2a test that pins its verdict.
3. The consultation eligibility gate `apps/core/src/application/permissions/permission-classifier.ts` (around :11): its signature, every predicate it applies, why native `Write` / `Edit` exit before argument-aware logic today, and every caller.
4. Registry sets in `apps/core/src/shared/admin-mcp-tools.ts`: the scheduler-mutation set (around :32-39), the admin-mutation exports (around :129-159), `DELEGATION_DISPATCHERS` (around :172-177) — exact export names, element shapes (tool ids? suffixes?), and how tool ids are matched elsewhere (full `mcp__gantry__…` name vs suffix).
5. Browser tools: the registered `browser_*` action names (runner tool registration file and the action enum), the `file_attach` / `file_upload` payload-source shapes, and `apps/core/src/adapters/browser/browser-artifact-policy.ts` (around :57-98, :132-145): where a raw filesystem path is accepted and where `source: { type: "path" }` bypasses the raw-path rejection.
6. `file` tool: `apps/core/src/runner/mcp/tools/file.ts` (:12-62 schema, :65-74 registration), handlers `apps/core/src/jobs/ipc-file-artifact-handlers.ts` (:59-96, :109-147, :168-175, :200-223): the per-action argument contract (list/read/write/promote_scratch), where list is unfiltered and read never calls a protected predicate, and the exact protected-virtual-path predicate in `apps/core/src/domain/file-artifacts/protected-virtual-path.ts` (:23-57) plus the prompt-profile scope's own protection (file and predicate name).
7. Native Write/Edit: `gantry-facade-tools.ts` (:180-189) argument shapes; `gantry-facade-file-safety.ts` (:39-67) prospective-write containment semantics (symlink handling, non-existent target) and its exports; whether any existing pure helper already does prospective containment that a new `application/permissions/native-file-write-risk.ts` could reuse or must wrap.
8. Provenance: how a native-risk LOW verdict is stamped today (`decidedBy` / `railProvenance` / risk provenance field names) in the IPC helper `apps/core/src/runtime/ipc-permission-classifier-decision.ts` and in the inline live consult `apps/core/src/bootstrap/inline-agent-loop-tools.ts` (:399-445): is the native-risk verdict reached identically on both paths?
9. Lanes: where `interactive_auto` is derived (T1's `apps/core/src/domain/permission-lane.ts`) and whether the native-risk branch is already lane-gated (auto only) or would need the gate.
10. Existing unit tests for: gantry-tool-risk, the eligibility gate, the native-risk helper, browser-artifact-policy, the file handlers, the facade file safety, admin-mcp-tools. Paths and describe titles.
11. Any other consumer of `gantry-tool-risk.ts` exports (e.g. UI labels, audit, permission cards) that would break if the risk model changes to a typed table with `low | high | ambiguous`.
12. The AF-AC8 harness after T2a (`apps/core/test/unit/runtime/askfloor-tap-budget-harness.ts`): can a fixture drive the classifier's native-risk branch (or is the harness rails-only), and which existing S-scenario fixtures exist.

Output: a numbered list mirroring the items above, facts only.


## Answers (Codex gpt-5.6-terra @ high, read-only, 2026-09-05)

1. `GantryToolRisk`, `gantryNativeCanonicalToolName`, and `gantryToolDefaultRisk` are the only exports. The risk function accepts only `toolName: string`; it has no arguments parameter. It returns `undefined` for non-Gantry tools; known/unmapped Gantry names are identity-classified as low, medium, or high. High includes authority/delegation/decision buckets, destructive verbs, `file`, listed scheduler mutations, `send_message`, mutating admin tools, mutating `browser_*`, and unmapped Gantry names; low is read-verb/display tools; medium is remaining `grantable-exact` tools. `file` is high by identity, not argument shape. `evaluateNativeRiskBranch` is the only production caller and passes only `canonicalToolName`; test callers also pass only tool names. `apps/core/src/application/permissions/gantry-tool-risk.ts:59`, `apps/core/src/application/permissions/gantry-tool-risk.ts:69`, `apps/core/src/application/permissions/gantry-tool-risk.ts:98`, `apps/core/src/application/permissions/gantry-tool-risk.ts:105`, `apps/core/src/application/permissions/gantry-tool-risk.ts:109`, `apps/core/src/application/permissions/gantry-tool-risk.ts:120`, `apps/core/src/application/permissions/gantry-tool-risk.ts:125`, `apps/core/src/application/permissions/gantry-tool-risk.ts:130`, `apps/core/src/application/permissions/gantry-tool-risk.ts:135`, `apps/core/src/application/permissions/gantry-tool-risk.ts:140`, `apps/core/src/application/permissions/gantry-tool-risk.ts:146`, `apps/core/src/application/permissions/gantry-tool-risk.ts:155`, `apps/core/src/runtime/permission-classifier-native-risk.ts:19`, `apps/core/test/unit/application/gantry-tool-risk.test.ts:39`.

2. `evaluateNativeRiskBranch` receives `{ canonicalToolName, inputTruncated, yoloDenylistHit, requestFamily }`; it does not receive `toolInput` or `classifierToolInput`. It returns `PermissionClassifierResult | undefined`; its returned native result has `risk_level`, `reason`, `latencyMs: 0`, and `status: Skipped`. `permission-classifier.ts` constructs that four-field input after it has constructed `classifierToolInput`, but does not pass that input to the helper. The T2a test pins the unchanged helper verdict for `mcp__gantry__memory_save`. `apps/core/src/runtime/permission-classifier-native-risk.ts:6`, `apps/core/src/runtime/permission-classifier-native-risk.ts:19`, `apps/core/src/runtime/permission-classifier-native-risk.ts:21`, `apps/core/src/runtime/permission-classifier.ts:302`, `apps/core/src/runtime/permission-classifier.ts:331`, `apps/core/test/unit/runtime/permission-classifier.test.ts:201`.

3. `isPermissionClassifierEligible(canonicalToolName, requestFamily)` rejects every non-`tool` family, accepts only `Bash`/`RunCommand`, names matching the MCP regex, or bare names recognized by `gantryNativeCanonicalToolName`. `Write` and `Edit` satisfy none of those predicates, so `consultPermissionClassifierBeforePrompt` returns before reaching native-risk evaluation. Its only production caller is `consultPermissionClassifierBeforePrompt`; its unit test is the other caller. `apps/core/src/application/permissions/permission-classifier.ts:11`, `apps/core/src/application/permissions/permission-classifier.ts:15`, `apps/core/src/application/permissions/permission-classifier.ts:17`, `apps/core/src/application/permissions/permission-classifier.ts:25`, `apps/core/src/application/permissions/permission-classifier.ts:28`, `apps/core/src/runtime/permission-classifier.ts:253`, `apps/core/src/runtime/permission-classifier.ts:259`, `apps/core/test/unit/application/permission-classifier.test.ts:5`.

4. `SCHEDULER_MUTATION_MCP_TOOL_NAMES` exports suffix strings: `scheduler_upsert_job`, `scheduler_update_job`, `scheduler_delete_job`, `scheduler_pause_job`, `scheduler_resume_job`, and `scheduler_run_now`. The adjacent mutation-related exports are `AUTHORITY_CHANGING_GANTRY_MCP_TOOL_NAMES`, `DECISION_ACTOR_GANTRY_MCP_TOOL_NAMES`, and `DURABLE_GRANT_EXCLUDED_DISPATCHERS`; `DELEGATION_DISPATCHERS` is `delegate_task` and `task_message`. These sets contain suffixes, while full names are separately generated as `mcp__gantry__${toolName}`. Full-name predicates parse the prefix and compare the extracted suffix to the sets. `apps/core/src/shared/admin-mcp-tools.ts:32`, `apps/core/src/shared/admin-mcp-tools.ts:129`, `apps/core/src/shared/admin-mcp-tools.ts:145`, `apps/core/src/shared/admin-mcp-tools.ts:154`, `apps/core/src/shared/admin-mcp-tools.ts:174`, `apps/core/src/shared/admin-mcp-tools.ts:225`, `apps/core/src/shared/admin-mcp-tools.ts:295`, `apps/core/src/shared/admin-mcp-tools.ts:304`, `apps/core/src/shared/admin-mcp-tools.ts:332`.

5. The registered public browser tools are `browser_status`, `browser_open`, `browser_inspect`, `browser_act`, and `browser_close`; the `browser_act` action union includes `navigate`, `back`, tab actions, click/type/wait/screenshot/evaluate, input actions, `file_upload`, and `file_attach`. `file_upload` and `file_attach` accept `source`, `files`, or `paths`; a source has `type: bytes | path | artifact`. A direct `file_upload` raw `paths` payload is rejected, but a `source: { type: "path", path|paths }` payload is normalized after `rawPathsPresent` is computed, so it bypasses that raw-path rejection and is resolved through the attach-path resolver. `apps/core/src/runner/mcp/tools/browser.ts:10`, `apps/core/src/runner/mcp/tools/browser.ts:28`, `apps/core/src/runner/mcp/tools/browser.ts:227`, `apps/core/src/shared/browser-backend-actions.ts:1`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:42`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:57`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:81`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:91`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:103`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:132`.

6. `file` has actions `list|read|write|promote_scratch`. Only write and promote invoke `authorizeProtectedPromptMutation`. Generic virtual protection normalizes then rejects `.codex/skills/` and path segments `SOUL.md`, `settings.yaml`, `.mcp.json`, or `SKILL.md`. Prompt-profile protection is separately `isAgentProfileArtifactWrite(scope, path)`, which applies only when `scope === "prompt-profile"` and the path has `SOUL.md` or `AGENTS.md`. `apps/core/src/runner/mcp/tools/file.ts:12`, `apps/core/src/runner/mcp/tools/file.ts:65`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:109`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:124`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:162`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:200`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:273`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:316`, `apps/core/src/domain/file-artifacts/protected-virtual-path.ts:23,37,50`.

7. Facade `FileEdit` maps to native `Edit { file_path, patch }`; `FileWrite` maps to native `Write { file_path, content }`. `resolveWritableWorkspacePath` resolves against the workspace root, rejects lexical escape and symlink parents/targets, permits a missing target only for `FileWrite`, and realpaths existing targets before rechecking containment. `permission-trusted-paths.ts` has a private filesystem-backed `realResolve`, not an exported pure prospective-write helper. `apps/core/src/adapters/llm/deepagents-langchain/runner/gantry-facade-tools.ts:180,185`, `apps/core/src/adapters/llm/deepagents-langchain/runner/gantry-facade-file-safety.ts:11,21,39,54,88`, `apps/core/src/shared/permission-trusted-paths.ts:93`.

8. In IPC, a classifier allow becomes `decidedBy: "auto_classifier"`/`source: "auto_classifier"`; `railProvenance` is attached only when a classifier allow relaxes a named rail veto — there is no native-risk-specific provenance field. Inline calls the same consultation function but returns `decisionForMode(..., "auto_classifier")` directly, bypassing IPC's rail merge/provenance copying. `apps/core/src/runtime/ipc-permission-classifier-decision.ts:347,367,413,417,497,583`, `apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:389,404,409,440`.

9. `interactive_auto` is derived only when there is no `hostJobId` and `permissionMode === "auto"`; a present `hostJobId` produces `autonomous`. `evaluateNativeRiskBranch` has no lane input/predicate. IPC invokes it for both `interactive_auto` and `auto_strict`; the later auto-strict deterministic guard can override its result. `apps/core/src/application/permissions/auto-lane-analysis.ts:97,106`, `apps/core/src/runtime/permission-classifier-native-risk.ts:6`, `apps/core/src/runtime/ipc-permission-classifier-decision.ts:325`, `apps/core/src/runtime/permission-classifier.ts:348`.

10. Existing test coverage (representative citations): `gantry-tool-risk.test.ts:10,39`; eligibility gate `permission-classifier.test.ts:5`; native-risk helper `permission-classifier.test.ts:201`; browser artifact policy — no standalone test, covered indirectly via `browser-direct-driver.test.ts:341,371,422,440`; file handlers `ipc-file-artifact-handlers.test.ts:113,263,343,384`; facade file safety — no standalone test, covered via `deepagents-gantry-facade-tools.test.ts:336,362,383,404`; admin registry `admin-mcp-tools.test.ts:18`.

11. Production consumers of the risk/eligibility exports are limited to the native-risk helper and the eligibility gate; no UI, audit, or permission-card source imports them. `apps/core/src/runtime/permission-classifier-native-risk.ts:1,19`, `apps/core/src/application/permissions/permission-classifier.ts:1,28`, `apps/core/test/unit/application/gantry-tool-risk.test.ts:3`, `apps/core/test/unit/runner/tool-permission-gate.test.ts:9`.

12. The harness accepts `toolName`/`toolInput`, invokes `resolvePermissionIpcDecision` with a fake LLM `classifierConsult`; a Gantry-native fixture can enter the native-risk branch. Existing scenarios (S1 attachment-open, S3 stderr-redirection/read-only-find) are not native-risk table scenarios. `apps/core/test/unit/runtime/askfloor-tap-budget-harness.ts:11,56,63,91`, `apps/core/test/unit/runtime/askfloor-tap-budget.test.ts:8,37`.
