# ASKFLOOR-1-T2b seam exploration (read-only)

Read-only cold-read exploration, no file edits, no writes. Report facts with `path:line` citations only. Do not propose designs beyond what is asked.

Context: story plan `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§2 "Default-allow risk table (T2b)" and the T2b line under Task Decomposition), spec `docs/specs/askfloor-1-judge-actually-judges.md` (AF-AC1, AF-AC2 file half, AF-AC4 native-risk half), decisions 0043, 0052, 0121, 0154, 0155. T1 and T2a are merged on this branch: the native-risk branch of the classifier now lives in `apps/core/src/runtime/permission-classifier-native-risk.ts` (`evaluateNativeRiskBranch`), the pure effect-shape classifier in `apps/core/src/shared/permission-effect-shape.ts`, the hard-boundaries evaluator in `apps/core/src/shared/permission-hard-boundaries.ts`, and `PermissionClassifierResult.status` is required.

Answer each item with citations:

1. `apps/core/src/application/permissions/gantry-tool-risk.ts`: every export, the current risk model (which tools are HIGH/LOW/unmapped, by identity or by argument), the result type, and every caller (grep the repo). Which callers pass arguments and which pass only the tool name?
2. `apps/core/src/runtime/permission-classifier-native-risk.ts` after T2a: the exact input type `evaluateNativeRiskBranch` receives (does it already carry the tool arguments / `classifierToolInput`?), what it returns, and where in `permission-classifier.ts` it is called. Cite the T2a test that pins its verdict.
3. The consultation eligibility gate `apps/core/src/application/permissions/permission-classifier.ts` (around :11): its signature, every predicate it applies, why native `Write` / `Edit` exit before argument-aware logic today, and every caller.
4. Registry sets in `apps/core/src/shared/admin-mcp-tools.ts`: the scheduler-mutation set (around :32-39), the admin-mutation exports (around :129-159), `DELEGATION_DISPATCHERS` (around :172-177) — exact export names, element shapes (tool ids? suffixes?), and how tool ids are matched elsewhere (full `mcp__gantry__…` name vs suffix).
5. Browser tools: the registered `browser_*` action names (runner tool registration file and the action enum), the `file_attach` / `file_upload` payload-source shapes, and `apps/core/src/adapters/browser/browser-artifact-policy.ts` (around :57-98, :132-145): where a raw filesystem path is accepted and where `source: { type: "path" }` bypasses the raw-path rejection.
6. `file` tool: `apps/core/src/runner/mcp/tools/file.ts` (:12-62 schema, :65-74 registration), handlers `apps/core/src/jobs/ipc-file-artifact-handlers.ts` (:59-96, :109-147, :168-175, :200-223): the per-action argument contract (list/read/write/promote_scratch), where list is unfiltered and read never calls a protected predicate, and the exact protected-virtual-path predicate in `apps/core/src/domain/file-artifacts/protected-virtual-path.ts` (:23-57) plus the prompt-profile scope's own protection (file and predicate name).
7. Native Write/Edit: `gantry-facade-tools.ts` (:180-189) argument shapes; `gantry-facade-file-safety.ts` (:39-67) prospective-write containment semantics (symlink handling, non-existent target) and its exports; whether any existing pure helper already does prospective containment that a new `application/permissions/native-file-write-risk.ts` could reuse or must wrap.
8. Provenance: how a native-risk LOW verdict is stamped today (`decidedBy` / `railProvenance` / risk provenance field names) in the IPC helper `apps/core/src/runtime/ipc-permission-classifier-decision.ts` and in the inline live consult `apps/core/src/bootstrap/inline-agent-loop-tools.ts` (:399-445): is the native-risk verdict reached identically on both paths?
9. Lanes: where `interactive_auto` is derived (T1's `apps/core/src/domain/permission-lane.ts`) and whether the native-risk branch is already lane-gated (auto only) or would need the gate.
10. Existing unit tests for: gantry-tool-risk, the eligibility gate, the native-risk helper, browser-artifact-policy, the file handlers, the facade file safety, admin-mcp-tools. Paths and describe titles.
11. Any other consumer of `gantry-tool-risk.ts` exports (e.g. UI labels, audit, permission cards) that would break if the risk model changes to a typed table with `low | high | ambiguous`.
12. The AF-AC8 harness after T2a (`apps/core/test/unit/runtime/askfloor-tap-budget-harness.ts`): can a fixture drive the classifier's native-risk branch (or is the harness rails-only), and which existing S-scenario fixtures exist.

Output: a numbered list mirroring the items above, facts only.
