[2026-09-05T03:47:04.365Z] Final output
1. The generic-executor ruling is unreachable. The ordered HIGH row includes `DURABLE_GRANT_EXCLUDED_DISPATCHERS`, whose members are exactly `async_run_command`, `async_mcp_call`, `mcp_call_tool`, and `capability_run`; the following `ambiguous` row can never win. This also conflicts with the newer owner ruling and accepted Decision 0155’s older wording. `.factory/stories/ASKFLOOR-1/decomposition.json:346`, `apps/core/src/shared/admin-mcp-tools.ts:154-159`, `plans/exploration/askfloor-1-t2b-seam-map-2.md:49-51`, `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md:13-14`. Minimal fix: amend/reconcile Decision 0155 and the story plan, then exclude that set from HIGH or place the explicit executor-ambiguous rule first.

2. The risk API has contradictory inputs and synchrony. AC1 declares `{ toolName, toolInput }`, but the technical approach requires `workspaceRoot`; the filesystem containment helper is explicitly async while `evaluateNativeRiskBranch` is currently synchronous. `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:10,18-20`, `apps/core/src/runtime/permission-classifier-native-risk.ts:6-11`, `apps/core/src/runtime/permission-classifier.ts:331-364`. Minimal fix: pin exact async signatures, including `workspaceRoot`, `Promise` return types, missing-root behavior, and awaited call sites in every AC/plan contract.

3. The `file` shapes reject valid defaulted calls. The contract makes `scope` mandatory for list/read/write and `targetScope` mandatory for promotion, while the approved spec and live handler allow omitted/defaulted scopes and path-only reads. `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:10`, `docs/specs/askfloor-1-judge-actually-judges.md:71`, `apps/core/src/runner/mcp/tools/file.ts:18-25,46-62`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:124-166,200-205`. Minimal fix: explicitly accept every defaulted form and add omitted-scope/omitted-targetScope tests.

4. The browser matrix misclassifies direct `file_upload.paths`. T2b treats `paths` as an inside-workspace LOW upload source, but the runtime rejects direct upload paths; only `source: {type:"path"}` follows the attach-path route. `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:10`, `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:159`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:47-98`. Minimal fix: distinguish `file_attach.paths` from `file_upload.paths`; the latter must be `ambiguous`, while path-source upload receives containment judgment.

5. The promised malformed-shape validation has no reachable source of truth. Decision 0155 says any tool input failing its own schema is ambiguous, but T2b tests malformed inputs only for `file`/browser and otherwise defaults every registered suffix LOW. Moreover, `fileToolSchema` is private to the runner, browser payload is merely `z.record(...unknown())`, and application code may not import the runner or Zod. `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md:13-14`, `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:10,18`, `apps/core/src/runner/mcp/tools/file.ts:1-2,12-63`, `apps/core/src/runner/mcp/tools/browser.ts:194-196`, `scripts/architecture-map.json:84-93`. Minimal fix: either scope and own canonical lower-layer validators for every promised row or amend the decision/plan to a narrower explicit validation contract; then recalculate scope and budget.

6. “Jobs unchanged” conflicts with the proposed autonomous bypass. Today inline scheduled execution calls the classifier; a native LOW/MEDIUM identity verdict returns `allow_once` before the scheduled cancellation branch. Returning `undefined` or skipping native risk for `autonomous` therefore changes today’s result. `.factory/stories/ASKFLOOR-1/decomposition.json:347`, `apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:408-455`, `apps/core/src/runtime/permission-classifier-native-risk.ts:19-27`, `docs/decisions/0121-autodet-no-classifier-autonomous.md:26-34`. Minimal fix: under `interactive_auto` use the new table; wherever the branch currently runs outside that lane, preserve today’s identity verdict exactly. If T2b instead repairs the Decision 0121 violation, record that as an intentional behavior change and reapprove the plan/ACs.

7. The inline lane change is neither plan-owned nor tested. The approved T2b decomposition does not own the IPC/inline orchestration seam, yet the task adds both source files; it adds no `bootstrap/inline-agent-loop-tools.test.ts` write scope or required leaf despite AC2 requiring an inline scheduled proof. `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:185-190`, `.factory/stories/ASKFLOOR-1/decomposition.json:396,400-408,457-459`, `apps/core/test/unit/bootstrap/inline-agent-loop-tools.test.ts:1150-1203`. Minimal fix: amend/reapprove task ownership and add a selectable inline leaf, then recompute the 20-file budget.

8. The prospective filesystem helper violates the architecture map as placed. The approved plan calls it pure, while the task puts `lstat`/`realpath` I/O in `application/permissions`; application permits no external imports, and existing code explicitly keeps filesystem probes outside application. `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:159`, `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:12,20`, `scripts/architecture-map.json:84-93,159-170`, `apps/core/src/shared/provider-attachment-materialization.ts:500-503`. Minimal fix: place filesystem containment in the shared/runtime layer and keep application policy over its typed result.

9. Protection ownership and cited predicates are incomplete. `tool-execution-protected-paths.ts:40-102` does not detect arbitrary hidden segments or secret-like filenames; those checks live elsewhere. Also, leaving `isProtectedArtifactEntry` optionally handler-local prevents the application risk table from sharing the scope-aware prompt-profile rule without duplication or an illegal runtime import. `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:12,21`, `apps/core/src/shared/tool-execution-protected-paths.ts:40-102`, `apps/core/src/shared/auto-permission-read-only-catalog.ts:143-147`, `apps/core/src/shared/permission-hard-boundaries.ts:62-64,83-84`, `apps/core/src/domain/file-artifacts/protected-virtual-path.ts:14-21,50-57`. Minimal fix: name all three checks explicitly and pin the combined `(scope,path)` predicate in the domain module for both table and handler consumption.

10. AC5 invents incompatible AF-AC8 scenario identities. T2b calls browser/file cases S2/S4/S5/S6, but the approved plan assigns those IDs to shell learning, destructive memory, scheduled projection, and outage behavior; T2b traces no AF-AC8 scenario. `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:14`, `.factory/stories/ASKFLOOR-1/decomposition.json:350,472-484`, `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:155,189`. Minimal fix: relabel these as T2b-specific table fixtures and remove the AF-AC8 claim, or amend/reapprove the story’s scenario ownership.

Non-blocking notes: the six acceptance criteria and six `plan_contracts` are textually equal; there are 19 write-scope files and 13 unique required-test IDs. `apps/core/test/unit/runner/mcp/file-tool.test.ts` exists. `protectedHidden` and `protected_entry` need no additional IPC codec: generic `data`, `code`, and `error` already traverse the signed envelope; only the scoped file runner rendering must change. `apps/core/src/jobs/ipc-shared.ts:120-150`, `apps/core/src/runner/mcp/ipc.ts:334-375`, `apps/core/src/runner/mcp/tools/file.ts:101-109`.
