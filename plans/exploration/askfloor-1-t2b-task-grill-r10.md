# ASKFLOOR-1-T2b task-contract grill — round 10

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2b-task-grill.md`); rounds 1–9 briefs and answers (rounds 7–9 were run on two models each: `-rN-answer.md` by gpt-5.6-sol @ xhigh, `-rN-answer-astra.md` by gpt-6-astra).

Round 9 (astra @ high) found one blocker; folded: the objective's malformed clause is again limited to the argument-shaped rows ("malformed file/browser rows are ambiguous").
Round 9 (sol @ xhigh) found three blockers; all folded: (1) CACHE ISOLATION at the IPC seam (0155 amendment bullet 10): a native verdict (status skipped) is never written to the classifier-verdict cache, and the IPC cache read is bypassed for `capability_run` in every consultable lane (the existing job bypass predicate at `ipc-permission-classifier-decision.ts:175` is extended); the IPC leaf seeds the cache and proves both; the lane-less cache for LLM allows (effect hash without lane, `permission-effect-key.ts:67`) is recorded as pre-existing deferral D-0075; (2) the objective names artifact-sourced `file_upload`, malformed file/browser argument rows, and `file` write/promote to a protected target OR `protected:true`; (3) the host-protection diagram is redrawn as separate list / read / attach flows with explicit protected and ordinary outcomes; `reviewer_focus` no longer says "other lanes verbatim" or "byte-identical" for the inline path.

Round 10 is the final consistency check: (a) `objective`, `acceptance_criteria`, `plan_contracts`, the task plan (diagram, Scope, Surface Impact, Risks, Decisions) and `reviewer_focus` agree on every rule; (b) `required_tests` (fifteen single-leaf `-t` titles), `write_scope` (23), `review_budget` (25/2100); (c) any remaining defect that would make the implementer build the wrong thing, make an AC unprovable, or contradict an accepted decision in `docs/decisions/*.md`.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
