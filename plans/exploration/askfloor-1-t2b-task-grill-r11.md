# ASKFLOOR-1-T2b task-contract grill — round 11

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2b-task-grill.md`); rounds 1–10 briefs and answers (rounds 7–10 were run on two models each: `-rN-answer.md` by gpt-5.6-sol @ xhigh, `-rN-answer-astra.md` by gpt-6-astra).

Round 10 (astra @ high) found one blocker; folded: cache isolation is widened (0155 amendment bullet 11) — from interactive auto NO allow for a gantry-native tool or a native file-write facade is written to the classifier-verdict cache, neither a native verdict nor an LLM answer for a row the table judged `ambiguous`, so the newly ambiguous executors cannot earn a cached allow that replays in `auto_strict`; the IPC leaf now includes the interactive-auto → `auto_strict` replay regression for `async_run_command`; shell and non-gantry MCP allows keep today's cache behaviour (D-0075 narrowed to them); `reviewer_focus` names both deferrals D-0074 and D-0075.
Round 10 (sol @ xhigh) found the same one blocker independently (the contract still excluded only native verdicts from the cache while 0155 bullet 11 already required every interactive-auto gantry/facade allow to be skipped); the astra fold above resolves it — AC2, its `plan_contract`, the diagram, Decisions, `reviewer_focus`, D-0075 and the IPC leaf now all state the bullet-11 rule.

Round 11 is the final consistency check: (a) `objective`, `acceptance_criteria`, `plan_contracts`, the task plan (diagram, Scope, Surface Impact, Risks, Decisions) and `reviewer_focus` agree on every rule; (b) `required_tests` (fifteen single-leaf `-t` titles), `write_scope` (23), `review_budget` (25/2100); (c) any remaining defect that would make the implementer build the wrong thing, make an AC unprovable, or contradict an accepted decision in `docs/decisions/*.md`.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
