# Astra @ xhigh trial on the pre-r12 contract (10a8ba657, r12 brief) — 2026-09-05; benchmark sol@xhigh r12 found 4 items
1. **The inline exception omits removal of existing HIGH/ask verdicts.** Scope says the lane-less inline path “loses only the identity-based native allow,” while AC2 removes **every** native verdict except `capability_run` ([task plan:7](.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md#L7), [decomposition.json:347](.factory/stories/ASKFLOOR-1/decomposition.json#L347)). Today `scheduler_update_job` receives native HIGH ([gantry-tool-risk.ts:130](apps/core/src/application/permissions/gantry-tool-risk.ts#L130)). Under AC2, the same lane-less `auto` request reaches the LLM; LOW then becomes allow, which the inline caller accepts ([permission-classifier.ts:364](apps/core/src/runtime/permission-classifier.ts#L364), [inline-agent-loop-tools.ts:440](apps/core/src/app/bootstrap/inline-agent-loop-tools.ts#L440)). Thus the change also permits previously native-HIGH requests to allow; Decision 0155’s “more conservative outcome” description is inaccurate ([0155:31](docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md#L31)). **Minimal fix:** preserve the accepted no-legacy rule, explicitly document this additional consequence across Scope, AC2/AC6, their `plan_contracts`, Surface Impact, reviewer focus and Decision 0155, and extend the classifier leaf with a lane-less previously-HIGH tool against an allow-leaning LLM stub.

Non-blocking notes:

- All six ACs exactly match their `plan_contracts` and saved-plan counterparts. Confirmed 23 unique scoped paths, 15 unique selectable test titles within scope, and budget 25 files/2100 lines.
- The round-11 cached-`capability_run` exception is reflected in the requested sections.
- No files written; no tests run.
