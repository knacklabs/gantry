1. The new verdict-only APIs cannot construct the required classifier result without inventing a user-facing `reason`. The contract refers to nonexistent “existing native result constructors”; the current seam instead copies `risk.reason` directly into the required field. Specify exact reason-bearing outputs or deterministic reason mappings and tests. `.factory/stories/ASKFLOOR-1/decomposition.json:346`, `.factory/stories/ASKFLOOR-1/decomposition.json:348`, `.factory/stories/ASKFLOOR-1/decomposition.json:504`, `apps/core/src/runtime/permission-classifier-native-risk.ts:19`, `apps/core/src/runtime/permission-classifier.ts:98`

2. `promote_scratch` with `protected: true` has contradictory classification. The objective and accepted decision require HIGH, but AC1 omits `protected` from that action’s allowed shape and declares every other shape ambiguous. The callable schema accepts `protected`, and the promote handler uses the shared protected-mutation authorization. Add optional `protected` explicitly to the promote shape and pin HIGH when true. `.factory/stories/ASKFLOOR-1/decomposition.json:344`, `.factory/stories/ASKFLOOR-1/decomposition.json:346`, `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md:14`, `apps/core/src/runner/mcp/tools/file.ts:54`, `apps/core/src/jobs/ipc-file-artifact-handlers.ts:207`

3. Cache isolation is narrower than Decision 0155. The decision requires every native table/native-file verdict to bypass cache writes, then additionally excludes LLM allows for these tools; the contract and required test forbid only `allow` writes. Because the current writer persists every classifier decision, an allow-only implementation would still cache deterministic HIGH/ask verdicts. Specify both fences separately and extend the existing IPC leaf to prove native LOW and HIGH results never call `putClassifierVerdict`. `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md:34`, `docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md:35`, `.factory/stories/ASKFLOOR-1/decomposition.json:347`, `.factory/stories/ASKFLOOR-1/decomposition.json:461`, `apps/core/src/runtime/ipc-permission-classifier-decision.ts:462`

4. The lane-less inline impact is misstated. AC2 removes every non-`capability_run` native verdict when the lane is absent, and its test explicitly includes `browser_act`; today `browser_act` is native HIGH. It therefore loses a native ask and can fall through to an LLM allow—not merely lose identity-based allows or become uniformly “more conservative.” Update Scope, Surface Impact, AC6, Decision-summary prose, and `reviewer_focus` to state that all legacy LOW/MEDIUM/HIGH verdicts disappear on the lane-less path except `capability_run`. `.factory/stories/ASKFLOOR-1/decomposition.json:347`, `.factory/stories/ASKFLOOR-1/decomposition.json:351`, `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:7`, `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:29`, `apps/core/src/application/permissions/gantry-tool-risk.ts:155`
