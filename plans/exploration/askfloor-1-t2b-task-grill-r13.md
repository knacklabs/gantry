# ASKFLOOR-1-T2b task-contract grill — round 13

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2b-task-grill.md`); rounds 1–12 briefs and answers (rounds 7–12 were run on two models each: `-rN-answer.md` by gpt-5.6-sol @ xhigh, `-rN-answer-astra.md` by gpt-6-astra @ high).

Round 12: astra @ high returned CONTRACT SOUND; sol @ xhigh found four refinements, all folded (0155 amendment bullet 12): (1) the table and `judgeNativeFileWrite` return `{ verdict, reason }` with a deterministic fixed reason per rule, copied into the classifier result's required `reason` (the helper copies `risk.reason` today); (2) `promote_scratch` accepts optional `protected` and is HIGH when true; (3) cache isolation has TWO fences — no native verdict (allow OR ask) is ever written, and no interactive-auto LLM allow for a gantry tool or file-write facade is written — and the IPC leaf proves native LOW and HIGH never call `putClassifierVerdict`; (4) the table's HIGH verdict is LANE-INDEPENDENT (generalising the `capability_run` guard): a `high` row yields the native ask in every lane, LOW needs `interactive_auto`, `ambiguous` yields nothing — so the lane-less inline consult keeps native asks for HIGH rows and loses the old identity allows AND asks only for rows now LOW or ambiguous; Scope, AC6, Surface Impact, the diagram and `reviewer_focus` say so.

Round 13 is the final consistency check: (a) `objective`, `acceptance_criteria`, `plan_contracts`, the task plan (diagram, Scope, Surface Impact, Risks, Decisions) and `reviewer_focus` agree on every rule; (b) `required_tests` (fifteen single-leaf `-t` titles), `write_scope` (23), `review_budget` (25/2100); (c) any remaining defect that would make the implementer build the wrong thing, make an AC unprovable, or contradict an accepted decision in `docs/decisions/*.md`. Keep the investigation focused on the recorded contract, the saved task plan, decisions 0130 and 0155, and only the source lines the ACs cite.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
