# ASKFLOOR-1-T2b task-contract grill — round 15

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2b-task-grill.md`); rounds 1–14 briefs and answers.

Round 14 returned CONTRACT SOUND (astra @ high; sol @ xhigh sound on round 13). The implementation then landed (commit `3d31be483`) and the host verify exposed ONE contract gap: the inline live consult (`apps/core/src/app/bootstrap/inline-agent-loop-tools.ts`, which passes no lane) has an existing expectation that an UNREGISTERED gantry suffix (`mcp__gantry__tool_consent`) is NOT consulted in mode auto (`apps/core/test/unit/bootstrap/inline-agent-loop-tools.test.ts:1253-1275`). Under the round-12 rule ("ambiguous yields no native verdict anywhere") that suffix now reached the consult on the lane-less path and the inline suite went red, although AC6 forbids any inline expectation change.

Round 15 folds the refinement as decision 0155 amendment bullet 13 (`docs/decisions/0155-default-allow-gantry-tools-interactive-auto.md`, last bullet): with the lane ABSENT an `ambiguous` gantry row yields the native ask result (no classifier-derived allow is possible on a lane-less path, decision 0121); with `interactive_auto` it still goes to the classifier; with `auto_strict` it still yields nothing. Only LOW rows lose their old identity verdict on the lane-less path. Folded into AC2 (lane rules and tests), AC6 (the inline exception wording), the diagram `NV` node, Scope, Surface Impact, Decisions, `reviewer_focus` and the `permission-classifier.test.ts` required-test leaf title ("an unregistered suffix ... native ask" with the lane absent).

Check ONLY: (a) the bullet-13 rule is stated identically in AC2, AC6, the plan's Scope/Surface Impact/diagram/Decisions and `reviewer_focus` — no sentence still says ambiguous yields no native verdict "anywhere" or that the lane-less path loses asks for ambiguous rows; (b) the rule is consistent with decisions 0121, 0130 and 0155 bullets 7–12; (c) the required-test leaf in `apps/core/test/unit/runtime/permission-classifier.test.ts` is provable as titled; (d) the current source of `apps/core/src/runtime/permission-classifier-native-risk.ts` versus the refined AC2 — name the minimal code change the implementer must make (expected: the ambiguous case returns the native ask result when `lane` is undefined). Do not re-litigate settled rulings.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
