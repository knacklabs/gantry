# ASKFLOOR-1-T2b task-contract grill — round 2

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2b-task-grill.md`); round 1 answer `plans/exploration/askfloor-1-t2b-task-grill-r1-answer.md`.

Round 1 found ten blockers; all folded:
1. The executor row is placed BEFORE the HIGH rows and its source set IS `DURABLE_GRANT_EXCLUDED_DISPATCHERS` (imported); the admin-mutation row imports the other three sets only. Decision 0155 carries an appended amendment (2026-09-05, confirmed by the owner in the ledgered round) recording the executor ruling, the narrowed validation clause and "other lanes verbatim"; the task plan states the amendment supersedes the story plan's §2 "dispatcher" wording.
2. Signatures pinned: `gantryToolRisk({ toolName, toolInput, workspaceRoot }) → Promise<verdict>`; `evaluateNativeRiskBranch` async, awaited at `permission-classifier.ts:331`; `workspaceRoot` undefined → containment rows `high`.
3. `file` rows accept host-defaulted `scope`/`targetScope` and path-only reads; tests cover the defaulted forms.
4. `file_attach` (paths or `source.type` path) and `file_upload` with `source.type` path → containment; `file_upload` with direct `paths` → `ambiguous`; artifact/bytes/`files` → low.
5. Validation narrowed to the two argument-shaped rows with the table's OWN structural predicates (no zod, no runner import); every other row is name-keyed (0155 amendment).
6. Outside `interactive_auto` the branch runs `legacyGantryToolIdentityRisk` (today's mapping, same file, unchanged) through today's result construction — job and strict semantics byte-identical; the inline 0121 gap is deferral D-0074 (`plans/deferrals.md`).
7. The inline consult left the scope entirely; the lane is passed by the IPC consult helper (one call site) into an optional `lane` on the classifier input; absent lane = legacy path. No inline test needed; the IPC leaf pins the lane threading.
8. Filesystem containment moved to NEW `shared/permission-prospective-write.ts`; `application/permissions/native-file-write-risk.ts` is policy over its typed result.
9. All three protection checks named by export (`isProtectedCapabilityPathLike`, `hasHiddenPathSegment`, `isSecretLikeValue`); `isProtectedArtifactEntry` PINNED in `domain/file-artifacts/protected-virtual-path.ts`, consumed by both the table and the handler.
10. Fixtures relabelled TB1–TB4 (T2b table fixtures); no AF-AC8 claim; traces AF-AC1, AF-AC2 (file), AF-AC4 (native-risk), AF-AC7.
Scope is now 20 files (10 source incl. two NEW, 10 test incl. two NEW); budget 22/1800; thirteen plain `it` titles.

Verify with `path:line` citations, adversarially: (a) every current-code claim in the ACs (the eligibility gate predicates; the facade ids and argument keys; the classifier input fields and where `workspaceRoot` comes from on the IPC path; the IPC consult helper's derived lane; the helper's call site; the registry export names and element shapes; the `file` handler's shapes and reject convention; the exports named in AC3/AC4); (b) that "absent lane → legacy path" makes every non-IPC and non-interactive outcome provably byte-identical, including inline and `auto_strict`; (c) whether the legacy mapping's retention in the same file conflicts with AC1's "one typed table" wording or the constitution; (d) whether the prospective boundary's rules are complete and testable without touching `gantry-facade-file-safety.ts`; (e) consistency across the task plan, `plan_contracts`, `acceptance_criteria`, `required_tests` (thirteen — each a single `-t` selectable leaf), `reviewer_focus`, `write_scope` (20) and `review_budget` (22/1800); (f) whether the 0155 amendment and the story plan §2 wording are reconciled enough for an implementer (the story plan is not re-edited; the decision is authoritative) — flag as a blocker only if the implementer would build the wrong thing.

Blockers must be defects that would make the implementer build the wrong thing or make an AC unprovable. Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
