1. AC4 names nonexistent fields on the resolved artifact. It requires `isProtectedArtifactEntry(artifact.scope, artifact.path)`, but `FileArtifact` exposes `virtualScope` and `virtualPath`; the resolver already reads `result.artifact.virtualPath`. A literal implementation will not type-check or may bypass protection if forced through casts. `.factory/stories/ASKFLOOR-1/decomposition.json:349`, `apps/core/src/domain/file-artifacts/file-artifact.ts:7-24`, `apps/core/src/runtime/browser-file-attach-source.ts:43-63`. Minimal fix: pin `result.artifact.virtualScope` and `result.artifact.virtualPath` throughout AC4, `plan_contracts`, and `reviewer_focus`.

2. Native-write path normalization is contradictory. AC3 makes every non-absolute candidate fail closed, but DeepAgents accepts only relative facade paths and passes that original `{ path, ... }` input to permission IPC. Without a specified conversion, every valid DeepAgents `FileWrite`/`FileEdit` becomes HIGH instead of allowing inside-workspace writes. `.factory/stories/ASKFLOOR-1/decomposition.json:348`, `apps/core/src/shared/gantry-tool-facades.ts:455-475`, `apps/core/src/adapters/llm/deepagents-langchain/runner/gantry-facade-tools.ts:121-130`. Minimal fix: specify shared-layer resolution of provider-relative destinations against the verified workspace root before boundary evaluation, and pin provider-realistic relative `path`/`file_path` cases in the required native-write leaf.

3. The browser table describes a nonexistent `source.type: "files"` while omitting the valid top-level `payload.files` form. Runtime accepts `source`, `files`, or `paths` as mutually exclusive top-level sources; `source.type` supports only `bytes`, `path`, and `artifact`. The required browser leaf also omits `files`, so a valid inline upload can incorrectly become `ambiguous` instead of default LOW. `.factory/stories/ASKFLOOR-1/decomposition.json:346`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:47-80`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:103-153`, `.factory/stories/ASKFLOOR-1/decomposition.json:435-437`. Minimal fix: state `source.type === "bytes"` or top-level `payload.files` for both actions, and extend the existing browser leaf to cover both forms.

Non-blocking notes: the decomposition has six equal acceptance-criterion/`plan_contracts` pairs, 14 unique required-test leaves, 22 unique write-scope paths, and a 24-file/2000-line budget. `.factory/stories/ASKFLOOR-1/decomposition.json:345-421`, `.factory/stories/ASKFLOOR-1/decomposition.json:423-493`. The task plan’s “source, 10” label is clerical; it enumerates 11 source files and correctly totals 22 files. `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md:31-32`.
