1. Browser raw-path risk is evaluated against a different filesystem root from the executor. AC1 sends `file_attach` and path-source `file_upload` through AC3’s workspace-root containment, including relative paths (`.factory/stories/ASKFLOOR-1/decomposition.json:346-348`). The classifier receives `AGENTS_DIR/<agent>` (`apps/core/src/runtime/ipc-permission-classifier-decision.ts:367-375`, `apps/core/src/platform/workspace-folder.ts:21-25`), but browser execution resolves relative paths beneath `DATA_DIR/sessions/<agent>/extra` and accepts only that root or the OS temp directory (`apps/core/src/runtime/ipc-browser-handler.ts:383-400`, `apps/core/src/adapters/browser/browser-artifact-policy.ts:287-320`; the roots are distinct at `apps/core/src/config/index.ts:247-248`). Consequently, a relative path can be judged LOW as `<workspace>/<path>` while execution reads `<data>/sessions/<agent>/extra/<path>`, and an absolute in-workspace path promised LOW is normally rejected by execution. The required table leaf cannot prove the executable behavior (`.factory/stories/ASKFLOOR-1/decomposition.json:435-437`). Minimal fix: choose one canonical browser-path resolution root, reconcile Decision 0155 and the task contract with it, add any required policy file to `write_scope`, and require an end-to-end absolute/relative path leaf proving permission evaluation and execution resolve the same file.

Non-blocking notes: the six acceptance criteria equal the six `plan_contracts`; all fourteen required-test IDs and 22 write-scope paths are unique; the budget is 24 files/2000 lines (`.factory/stories/ASKFLOOR-1/decomposition.json:345-493`). Citation cleanup remains: file-list rendering is at `apps/core/src/runner/mcp/tools/file.ts:113-123`, not `:101-109`; the browser resolver call is at `apps/core/src/runtime/ipc-browser-handler.ts:390-394`, not the import at `:37`; and `reviewer_focus` still cites `permission-classifier.ts:302` although the classifier-input construction is at `:290-304` (`.factory/stories/ASKFLOOR-1/decomposition.json:498`). The “host-defaulted” wording for list/read scope is also inaccurate: omitted scope remains unfiltered at `apps/core/src/jobs/ipc-file-artifact-handlers.ts:109-143`; only write and promotion default it at `:162-166,200-205`.
