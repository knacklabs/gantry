# ASKFLOOR-1-T2b task-contract grill — round 8

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2b-task-grill.md`); rounds 1–7 briefs and answers (round 7 was run twice: `-r7-answer.md` by gpt-5.6-sol @ xhigh, `-r7-answer-astra.md` by gpt-6-astra @ medium).

Round 7 (sol) found two blockers; both folded, plus a further owner ruling:
1. OWNER RULING 2026-09-05 (ledgered; decision 0155 amendment bullet 6): decision 0130 (CAPSAFE-1) wins — `capability_run` stays HIGH in every lane and is carved out of the ambiguous executor row, which is now `DURABLE_GRANT_EXCLUDED_DISPATCHERS` minus `capability_run`.
2. The objective now says "malformed file/browser shapes are ambiguous" and names `capability_run per 0130` among the HIGH rows.
3. OWNER RULING 2026-09-05 (chat, "We don't need legacy tools or code"; 0155 amendment bullet 7): NO legacy identity mapping survives — `gantryToolDefaultRisk` and `medium` are deleted; the table is the only gantry judgment; under any lane other than `interactive_auto` (or with no lane) the native-risk branch yields no verdict (`undefined`): `auto_strict` is unaffected (strict guard first), IPC `ask`/job requests never consult, and the lane-less inline consult loses its identity allow and falls through to the LLM consult it already performs (a more conservative outcome; D-0074 stays open). The CAPSAFE-1-BOUNDARY pin (`test/unit/runner/tool-permission-gate.test.ts:1347-1358`) joins the write scope, re-pointed at `gantryToolRisk`, still asserting `capability_run` HIGH — the fourth permitted existing-expectation change. Scope 23 files, fifteen required tests, budget 25/2100.

Round 8 is the final consistency check: (a) `objective`, `acceptance_criteria`, `plan_contracts`, the task plan (diagram, Scope, Surface Impact, Risks, Decisions) and `reviewer_focus` agree on every rule, including the `capability_run` carve-out, the deletion of the legacy mapping and the no-verdict rule for other lanes; (b) whether deleting `gantryToolDefaultRisk` breaks any consumer other than the native-risk helper and the CAPSAFE pin (grep the repo); (c) `required_tests` (fifteen single-leaf `-t` titles), `write_scope` (23), `review_budget` (25/2100); (d) any remaining defect that would make the implementer build the wrong thing, make an AC unprovable, or contradict an accepted decision in `docs/decisions/*.md`.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
