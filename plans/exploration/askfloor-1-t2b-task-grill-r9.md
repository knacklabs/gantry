# ASKFLOOR-1-T2b task-contract grill — round 9

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t2b-task-grill.md`); rounds 1–8 briefs and answers (rounds 7 and 8 were run on two models each: `-rN-answer.md` by gpt-5.6-sol @ xhigh, `-rN-answer-astra.md` by gpt-6-astra).

Round 8 (astra @ high) found two blockers; both folded: (1) the `capability_run` prohibition is LANE-INDEPENDENT inside the native-risk helper — it yields the native HIGH result before any lane check and before the LLM fallback (0155 amendment bullet 8), with a required lane-absent regression against an allow-leaning classifier stub; "job verdicts unchanged" is qualified as IPC-only and the lane-less inline consult is the one documented behaviour change; (2) the existing unmapped-gantry-tool test (`permission-classifier.test.ts:1042-1064`, asks without the LLM) is the fifth permitted existing-expectation change, replaced by an unknown-suffix-reaches-the-LLM pin; the budget rationale and D-0074's rationale no longer speak of a retained legacy mapping.
Round 8 (sol @ xhigh) found two consistency residues; both folded: (1) every blanket "interactive-auto only" / "no native verdict in other lanes" statement (objective, Scope, Decisions, 0155 bullets) now carries the `capability_run` exception, and 0155 gained a clarification bullet: the CAPSAFE assertion's semantics are untouched while its import/call is re-pointed at `gantryToolRisk`; (2) the review-budget rationale no longer mentions a retained legacy mapping — it names the lane-independent `capability_run` guard and states that no legacy mapping survives; the "tests (11)" label is corrected to 12.

Round 9 is the final consistency check: (a) `objective`, `acceptance_criteria`, `plan_contracts`, the task plan (diagram, Scope, Surface Impact, Risks, Decisions) and `reviewer_focus` agree on every rule; (b) `required_tests` (fifteen single-leaf `-t` titles), `write_scope` (23), `review_budget` (25/2100); (c) any remaining defect that would make the implementer build the wrong thing, make an AC unprovable, or contradict an accepted decision in `docs/decisions/*.md`.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
