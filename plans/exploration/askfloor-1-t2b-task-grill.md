# ASKFLOOR-1-T2b task-contract grill — round 1

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T2b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2b.md`.

Inputs of record: spec `docs/specs/askfloor-1-judge-actually-judges.md` (AF-AC1 owner ruling 2026-09-03 default-allow posture; AF-AC2 file half; AF-AC4 native-risk half; AF-AC7; AF-AC8), the story plan `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§2 "Default-allow risk table (T2b)" and the T2b line under Task Decomposition), decisions 0043, 0052, 0121, 0154, 0155, the seam explorations `plans/exploration/askfloor-1-t2b-seam-map.md` and `-2.md` (with the OWNER RULING 2026-09-05: the four generic executors are `ambiguous`), and the shipped T1/T2a code on this branch (`runtime/permission-classifier-native-risk.ts`, `application/permissions/auto-lane-analysis.ts`, `shared/permission-effect-shape.ts`, `shared/permission-hard-boundaries.ts`, `test/unit/runtime/askfloor-tap-budget-harness.ts`).

Verify with `path:line` citations, adversarially:
(a) that every claim in the ACs about current code is true at the cited lines (the eligibility gate's predicates; the facade ids and argument keys each runner sends for native Write/Edit; the classifier input's fields — does it already carry `hostJobId`? does the inline consult know its job id?; what `workspaceRoot` the classifier receives on the IPC and inline paths; the `file` handler's response shapes and reject convention; the registry export names and their element shapes);
(b) that the lane gate is sufficient and minimal: with the table applied under `interactive_auto` only, are `auto_strict`, `ask` and job outcomes provably byte-identical, including the inline path — or is there a path where the native-risk branch already runs for a job today (in which case "unchanged" requires the branch to keep TODAY's identity verdict there rather than return `undefined`) — name the minimal rule;
(c) whether removing `medium` and the identity-HIGH default can change any verdict outside `interactive_auto`;
(d) whether the prospective-write helper's placement (`application/permissions`, async, fs-touching) violates the architecture map or the constitution's layering, and whether `isProtectedArtifactEntry` belongs in `domain/file-artifacts/protected-virtual-path.ts` (pure) or in the handler module;
(e) whether the `protectedHidden` field and the fixed texts can reach the agent through the existing `file` runner tool without a contract change elsewhere (IPC codecs, response validators);
(f) consistency across the task plan, `plan_contracts`, `acceptance_criteria`, `required_tests` (thirteen plain `it` titles — each must be a single leaf that can be selected by `-t`), `reviewer_focus`, `write_scope` (19) and `review_budget` (20/1700); the test path `test/unit/runner/mcp/file-tool.test.ts` must exist or be the right NEW path.

Blockers must be defects that would make the implementer build the wrong thing or make an AC unprovable. Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
