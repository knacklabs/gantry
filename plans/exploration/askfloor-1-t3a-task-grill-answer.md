# ASKFLOOR-1-T3a task-contract grill — round 1 answer (gpt-5.6-sol @ xhigh)

1. **Scope-key API contradicts itself.** `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md:12,20` declares the function synchronous, omits `canonicalRoot` and `trustGrowthTool` from its input, then requires both and awaits the asynchronous boundary at `apps/core/src/shared/permission-prospective-write.ts:13-16`. Returning only `undefined` also cannot identify AC4's specific rejection reason without duplicating derivation logic. Minimal fix: specify one async signature containing every input and a discriminated success/failure result carrying the closed reason; align AC3, decomposition, reviewer focus, and leaf titles.
2. **Kind/facade key derivation is under-specified.** T3a receives a `PermissionApprovalRequest`, but `classifyPermissionEffectShape` accepts one parsed `BashCommandLeaf` plus `stdinOk` (`apps/core/src/shared/permission-effect-shape.ts:57-60`). No rule covers parsing/multiple leaves, virtual `file` list/read, facade aliases, or equivalence of bare `file` and `mcp__gantry__file`; the two scope-test titles do not pin these or optional virtual-scope defaulting. Minimal fix: provide the complete request-to-category/canonical-tool table and matching tests.
3. **`revokeById` drops app and agent-folder scope.** The approved plan requires `revokeById(appId, agentFolder, actingPersonId, recordId)`, while T3a specifies only `{ id, actingPersonId, nowIso }`. Minimal fix: add `appId` and `agentFolder` to repository/service revoke inputs and test cross-app, cross-folder, and cross-person `not_found`.
4. **UUID and short-ID rules contradict the approved plan.** The plan requires validated v4 UUIDs, six dash-free lowercase characters initially, and collision-driven extension. T3a specifies a fixed eight-character display ID, no extension, and no UUID-shape guard. Minimal fix: restore the approved derivation/validation and add malformed-ID and collision tests.
5. **The provenance codec omits `railVersion`.** The approved T3 design requires human provenance to carry scope and rail version. Minimal fix: include and validate `railVersion` in encode/decode and the codec leaf.
6. **T3a does not define the structured remember resolution T3c must consume.** The plan requires a provider-neutral `remember { outcome, scope }` option/claim DTO beside the scalar modes; `HumanDecisionRememberRequest` is instead a service-write DTO whose `mode` remains scalar. Minimal fix: add the additive structured resolution/claim type in T3a while leaving settlement and provider codecs to T3c.
7. **Active-duplicate upsert identity is incoherent.** T3a generates and returns a new UUID, but an active-key conflict retains the stored id; the prescribed update set also omits `outcome`, `decision`, and `provenance`. Minimal fix: make `putHumanDecision` return the effective stored id, define opposite-outcome behavior, preserve id/provenance consistency, and test duplicate service returns and outcome conflicts.
8. **The 1,500-line review budget cannot close.** The generated Drizzle snapshot is 18,937 lines and stage measurement counts all additions/deletions; `stage amend-scope` changes scope, not budget. Minimal fix: raise the line budget to ~22k or change the migration policy.
9. **AC6's full Postgres-suite claim is not enforced.** The task plan requires `npm run test:integration:postgres` as stage evidence, but decomposition omits the full lane from `verify_commands`. Minimal fix: add the full command with `GANTRY_TEST_DATABASE_URL` required, or narrow AC6.

Non-blocking notes: the six acceptance criteria exactly equal the six `plan_contracts`; all eight required-test paths are in scope and three use the Postgres config. The intended CHECK, two `uniqueIndex().where(...)` indexes, timestamp migration convention, `principal = toolName`, `user_facing: false`, and T3a/T3b/T3c ownership boundary are otherwise coherent.

## Fold (orchestrator, contract v2)

All nine folded: (1) one async `deriveHumanDecisionScopeKey` with every input and a discriminated `{ ok, scopeKey, pathOnly } | { ok: false, reason }` result, the closed reason enum owned by the scope module and relayed by the service; (2) the closed request-to-category table (`parseBashCommand` + `classifyPermissionEffectShape` over every leaf with the gate's `stdinOk` rule; `virtual_file_read`; `web_search`/`web_read`; trust-growth `kind:tool:<canonicalTool>`), `canonicalTool = gantryNativeCanonicalToolName(...) ?? toolName`, omitted virtual scope defaulted by the normaliser; (3) `revokeById({ appId, agentFolder, actingPersonId, recordId, nowIso })` with cross-app/folder/person tests; (4) v4 UUID validated on write, `deriveHumanDecisionShortId(id, siblings)` = 6 dash-free hex chars extended only on a sibling collision; (5) provenance codec carries `railVersion`, decode fails closed per field; (6) `PermissionRememberResolution` + `PermissionApprovalResolution` with two guards added to `domain/types.ts`, the DTO carries `resolution`; (7) `putHumanDecision` runs select-then-refresh-or-insert in one transaction, returns `{ id: stored, status }`, refresh set includes outcome/decision/provenance re-encoded with the stored id, newest decision wins; (8) budget 16 / 22,000 with the snapshot reason; (9) `npm run test:integration:postgres` added to `verify_commands`.
