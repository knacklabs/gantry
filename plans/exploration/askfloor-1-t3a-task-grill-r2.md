# ASKFLOOR-1-T3a task-contract grill — round 2

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T3a` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t3a-task-grill.md`); the round-1 answer and its fold note are at `plans/exploration/askfloor-1-t3a-task-grill-answer.md`.

Round 1 found nine blockers; all nine are folded into contract v2 (the fold note lists each). Round 2 checks the fold: (a) every folded rule is stated identically in the acceptance criteria, the plan's Technical Approach / Decisions / Task Decomposition / Workflow diagram, `reviewer_focus` and the eight leaf titles (in particular: the async discriminated scope-key API, the closed kind table and `canonicalTool`, `revokeById` on three axes, v4 UUID + 6-char short id with collision extension, `railVersion` in the codec, the structured `PermissionApprovalResolution`, `putHumanDecision` returning the stored id with the full refresh set, budget 16 / 22,000, the full Postgres lane in `verify_commands`); (b) any NEW contradiction the fold introduced with decisions 0154 / 0118 / 0107 / 0054 or with the story plan's T3a ownership bullet; (c) any leaf that is now unprovable as titled. Do not re-litigate settled rulings (revoked rows are history; T3c resolves the person; canonical destination formats; the service owns id/clock injection).

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
