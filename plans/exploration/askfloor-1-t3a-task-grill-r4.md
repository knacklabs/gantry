# ASKFLOOR-1-T3a task-contract grill — round 4

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T3a` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md`. Inputs of record as in round 1 (`plans/exploration/askfloor-1-t3a-task-grill.md`); rounds 1–3 answers with fold notes at `-answer.md`, `-r2-answer.md`, `-r3-answer.md`.

Round 3 found six code-facing gaps; all six (and the two notes) are folded as contract v4 — see the r3 fold note. Round 4 checks ONLY: (a) each of the six folds is stated consistently in the AC, `reviewer_focus` and the leaf that proves it (legacy-method refusals + unfiltered list exclusion; `?.canonical`; the shared boundary's `canonicalPath` and its file + test in `write_scope`; the gate-identical `stdinOk` rule; every native destination boundary-checked before the count rule; `includeRevoked` semantics and short-id disambiguation before `limit`); (b) the six folds do not contradict decisions 0154 / 0118 / 0107 / 0054 or the story plan's T3a ownership bullet (the shared boundary file is T2b's; T3a extends its success result additively — confirm no other task owns it); (c) every folded rule can be implemented against the CURRENT source (`gantry-tool-risk.ts:46`, `permission-prospective-write.ts:8-16`, `auto-permission-read-only-gate.ts:93-106`, `bash-command-parser.ts`, the repository's legacy methods) without improvisation. Do not re-open settled rulings and do not re-derive the implementation beyond those citations.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
