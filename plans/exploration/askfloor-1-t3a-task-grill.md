# ASKFLOOR-1-T3a task-contract grill — round 1

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T3a` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3a.md`.

Inputs of record: the story plan `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§3 "Human-decision memory with generic scope" and the T3a ownership bullet); the spec `docs/specs/askfloor-1-judge-actually-judges.md`; decisions 0154 (primary), 0118, 0107, 0054, 0043, 0121, 0130, 0138, 0155; the seam map `plans/exploration/askfloor-1-t3a-seam-map.md` (Codex terra @ high, with the orchestrator's rulings on its five open questions at the end); the shipped T2a/T2b contracts (`.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T2a.md`, `-T2b.md`) whose helpers T3a reuses (`shared/permission-effect-shape.ts`, `shared/permission-prospective-write.ts`, `application/permissions/gantry-tool-risk.ts`, `domain/file-artifacts/*`).

Grill the contract for: (a) contradictions with 0154 / 0118 / 0107 (person scope, no reactivation of revoked rows, outcome-aware keys, typed source) or with the story plan's T3a ownership bullet (files owned by T3b/T3c must not be in scope); (b) an AC that cannot be proven by its named leaf test, or a leaf whose title does not match an AC; (c) schema/migration hazards — the two partial unique indexes as drizzle `uniqueIndex().where`, the CHECK, the generated-migration + drift-gate flow, the retired sequential naming, the `principal = toolName` reuse; (d) the scope-key rules versus the real request shapes and T2b's helpers (single-destination detection, dual native keys, virtual scope defaulting, protected checks, `classifyPermissionEffectShape` category ids, the facade identities); (e) anything in `write_scope`, `required_tests` (eight leaves; three run through the Postgres config with `GANTRY_TEST_DATABASE_URL`), `review_budget` (16 / 1500; the generated migration files are added by `stage amend-scope` at close) or `reviewer_focus` that would make the implementer build the wrong thing.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.
