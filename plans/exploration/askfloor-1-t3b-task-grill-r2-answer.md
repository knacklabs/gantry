# ASKFLOOR-1-T3b task-contract grill — round 2 answer (gpt-5.6-sol @ xhigh)

1–4. **The round-1 folds are present in the saved plan but missing from the recorded contract** (decomposition AC1 still `HumanDecisionRow`; AC2 "since you last allowed this"; AC4 still directs a `runtime-services.ts:493` wire and omits `skipClassifierVerdictCache: true` + the `getClassifierVerdict` pin; the budget reason miscounts source/test files). Cause: the generator reads the SAVED task plan and the decomposition was regenerated before `task plan save`. Minimal fix: save first, regenerate, re-record. The confirmed spec (`docs/specs/askfloor-1-judge-actually-judges.md:32`) also still says "allowed this".
5. **The stale reason is not guaranteed to survive the coordinator path.** The stage mutates `request.decisionReason`, but the reviewed-rule and rail stages (`permission-decision-coordinator.ts:143,200`) can overwrite it; a stale deny uses the full hash while eligible file-write Allows use a path-only key, so the allow stage need not rediscover it. Minimal fix: AC3 preserves/re-applies the stale-deny reason before cache/tail; coordinator test for a stale file-write deny whose intermediate reason is overwritten.
6. **Trust-growth `kind:tool` records have no defined consult-key path.** T3a emits `kind:tool:<tool>` only with `trustGrowthTool: true` (`human-decision-scope.ts:202`); AC2 asks for one unspecified kind key, so an implementer can leave those rows unreadable. Minimal fix: specify the rule and add a stage-leaf case (seeded trust-growth kind match and near-miss).

Non-blocking: no T3c/T4/T5 file in scope; five leaf titles unique and valid `-t` patterns; 14 files within the 16-file budget; the inline ceiling is feasible with the module split.

## Fold (orchestrator, contract v3 / plan v3)

(1–4) plan v2 text now flows into the contract (order: edit → `task plan save` → regenerate → record); the spec line aligned to "decided this"; budget reason counts eight source + six test files. (5) AC3: the coordinator re-applies the stale line to `request.decisionReason` immediately before the classifier-cache read; coordinator test added to AC3's list. (6) AC2: allow candidates exact → `kind:tool:<canonicalTool>` (derivation with `trustGrowthTool: true`) → category kind (`trustGrowthTool: false`) → place; the stage never decides which tools are trust-growth; stage-leaf case seeds a `kind:tool:<tool>` row (match for that tool, miss for another). Wording/ordering folds only; no owner round.
