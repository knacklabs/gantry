# ASKFLOOR-1-T3b task-contract grill — round 2

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T3b` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3b.md`.

Inputs of record: the story plan `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§3 "Human-decision memory with generic scope" and the T3b ownership bullet); the spec `docs/specs/askfloor-1-judge-actually-judges.md`; decisions 0154 (primary: consult order, scopes, reason lines), 0118 (person scope), 0121 (no consult on job lanes), 0043 (authorization stays deterministic), 0153 (jobs consume projections only), 0155 (lane posture); the seam map `plans/exploration/askfloor-1-t3b-seam-map.md`; T3a as merged on trunk (`domain/ports/permission-decision-memory.ts`, `adapters/storage/postgres/repositories/permission-decision-memory-repository.postgres.ts`, `application/permissions/human-decision-scope.ts`, `application/permissions/human-decision-memory-service.ts`, `domain/human-decision.ts`, `shared/human-decision-id.ts`, `shared/permission-prospective-write.ts`); the coordinator `runtime/permission-decision-coordinator.ts` (stage order constant and the two reserved no-op slots), `runtime/ipc-permission-classifier-decision.ts` (the IPC caller that already resolves the workspace root), `domain/permission-decision.ts` (`PERMISSION_PROVENANCE_BY_DECIDER`, `decisionForMode`), `application/permissions/auto-lane-analysis.ts` (`deriveAutoLaneAnalysis`), `app/bootstrap/inline-agent-loop-tools.ts` (the inline coordinator call; the file sits at its 750-line architecture ceiling), `app/bootstrap/inline-agent-loop-tool-types.ts`, `app/bootstrap/runtime-services.ts`, `scripts/architecture-map.json` (layer rules and line ceilings), and the existing suites named in the contract's required tests.

Grill the contract for: (a) contradictions with 0154 / 0118 / 0121 / 0043 or with the story plan's T3b ownership bullet (files owned by T3c, T4, T5 must not be in scope; the remember WRITE, the card path, job projection and `/permissions` are out); (b) an AC that cannot be proven by its named leaf test, or a leaf whose title does not match an AC, or a leaf title that would not be a valid vitest `-t` pattern for one concrete test; (c) ORDER hazards — the remembered No must run before hard restrictions, locked preset and fixed image; the remembered Allow must run after the rails and the trusted-root stage and before the classifier cache; the stage-order constant and its existing pin must survive; (d) LANE hazards — any path by which `ask`, `auto_strict` or a job lane could reach the memory port, or a request without a person could; on the inline path any lane derivation other than `deriveAutoLaneAnalysis`; (e) KEY hazards — candidate keys derived anywhere other than `deriveHumanDecisionScopeKey`; the deny candidate must be the full effect hash; the allow order exact → kind → place; the stale (rail-version) handling must not decide; (f) the repository lookup — person + app + folder scoping, active rows only, caller order honoured, one query; (g) the inline module split — whether the new bootstrap module keeps `inline-agent-loop-tools.ts` under 750 lines with only the edits the contract permits, and whether the new optional dep wiring in `runtime-services.ts` is one line beside `getPermissionPromotionRepository`; (h) type-system feasibility — `CoordinatePermissionDecisionInput` gaining only `workspaceRoot?`; the provenance map entry; whether `decisionForMode` already routes a machine decider through the map; (i) the review budget (16 files / 1,600 lines) against the listed 14 files; (j) anything a Codex implementer would have to guess.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.

Round 2 context: round 1 (`plans/exploration/askfloor-1-t3b-task-grill-answer.md`) raised four blockers, all folded into contract v2 and plan v2: (1) the inline coordinator call passes the existing `skipClassifierVerdictCache: true` and the inline leaf pins that `getClassifierVerdict` is never called; (2) the lookup returns `PermissionDecisionMemoryRow`; (3) `runtime-services.ts` left the write scope (the getter already flows through `resolved`) and `test/unit/application/human-decision-memory-service.test.ts` entered it (typed fake gains `findHumanDecision`); (4) the stale line is "since you last decided this". Check each fold is complete and consistent across the decomposition entry and the saved plan, then re-run the full checklist above cold. Output `CONTRACT SOUND` or numbered blockers as before.
