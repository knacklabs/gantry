# ASKFLOOR-1-T3b task-contract grill — round 3 answer (gpt-5.6-sol @ xhigh)

1. **Stale exact decisions are unreachable after a rails bump.** The full effect hash (`domain/permission-effect-key.ts:67-76`) includes `RAIL_CATALOG_VERSION`, so after a bump the candidate hash never matches a stored exact row; a seeded stale exact row tests an impossible state. Options: a version-stable exact identity (T3a change) or accept version-bound exact keys.
2. **Ordered lookup undefined with a stale earlier and a current later candidate.** "FIRST match" vs "stale when the only match" conflict. Fix: first current-version match in caller order; only when none, the first stale match with `stale: true`.
3. **Remembered Allows not barred from hard-floor rail ASKs.** Every analyzed rail ASK reaches the allow slot (`permission-decision-coordinator.ts:187-213`); AC2 gates only on lane/person/port/hash. Fix: allow consult only on clean fall-through or the two overridable cases (`out_of_trusted_root`; `unsupported_meta_executor` with `readOnlyMetaExecutor`); hard-floor test proving the port is not called.
4. **A `place` key does not encode "kind".** T3a emits `place:<root>` (`human-decision-scope.ts:66-70`) while 0154:14 and the spec promise "anything of this kind under <root>". Fix: the centrally derived place key carries the closed category/tool-kind identity; the scope helper and its test enter T3b's scope; pin same-kind match and different-kind miss.
5. **Leaves do not pin folds 5 and 6.** Extend the coordinator and stage leaf titles.

Non-blocking: AC1–AC5 equal `plan_contracts`; round-1 folds present; 14 files, no T3c/T4/T5 ownership; titles valid `-t` patterns; AC4's "existing" `skipClassifierVerdictCache` is an additive edit on the inline call.

## Fold (orchestrator, contract v4 / plan v4)

(1) OWNER RULING (Ravi, 2026-09-07, "Keep it simple"): exact keys stay version-bound. Consequence: the deny stage (exact only) never sees a stale row and never sets the re-ask line; the stale line arises only from the allow stage's kind/place candidates, set immediately before the classifier-cache read — so round 2's "re-apply the stale-deny reason" clause and its test are REMOVED (no overwrite window exists). T5b marks outdated rows in `/permissions`. (2) Lookup order: first current-version match in caller order, else the first stale match with `stale: true`; Postgres case: stale kind + current place → the place row, not stale. (3) AC3: the allow consult runs only on clean fall-through or the two overridable rail cases; a hard-floor rail ask never calls the port (coordinator test). (4) The place key is `place:<kind identity>:<root>` where the kind identity is exactly what the kind key uses (the closed category, or `tool:<canonicalTool>` for a trust-growth tool); `application/permissions/human-decision-scope.ts` + its unit test join the write scope (16 files; budget 18 / 1,800); stage leaf pins same-kind match and different-kind miss under one root. (5) Coordinator and stage leaf titles name the hard-floor, trust-growth and place-kind cases.
