# ASKFLOOR-1-T3b task-contract grill — round 4 answer (gpt-5.6-sol @ xhigh)

1. **The version-bound exact-key ruling is incomplete.** `human-decision-scope.ts:75` emits unversioned `exact:path:*` keys for native and virtual writes (pinned at `human-decision-scope.test.ts:91`), so a path-only exact Allow survives a rails bump. Fix: include `RAIL_CATALOG_VERSION` in both path-only exact-key forms; update tests and AC wording; no migration (no rows yet).
2. **A stale kind/place match can be swallowed silently.** The stage only sets `decisionReason` and falls through; the IPC cache read (`ipc-permission-classifier-decision.ts:440`) or the inline tail (`inline-agent-loop-tools.ts:438`) may auto-allow, so no card and no rails-update line. Fix: propagate a typed stale result and bypass cache/classifier Allow, with IPC coverage in scope.
3. **The place-key fold leaves a candidate choice open.** AC2 permits category or `tool:<canonicalTool>` place identities but lists one place candidate. Fix: derive and consult both place candidates after both kind candidates; pin matches and cross-kind misses.
4. **"Hard-floor" contradicts the rail type.** `unsupported_meta_executor` is constructed with `hardFloor: true` (`permission-deterministic-rails.ts:189`) and `out_of_trusted_root` can be hard-floor (`:248`); gating on the flag would block both overridable cases. Fix: say "non-overridable rail ASK" with the exact predicate; fix the leaf title.
5. **The architecture record is excluded** while the key contract changes (`docs/architecture/permission-decision-memory.md:20`). Fix: add it to scope and update the key forms.

Non-blocking: saved plan, `acceptance_criteria` and `plan_contracts` byte-equal; 16 files under 18/1,800; no T3c/T4/T5 ownership; `runtime-services.ts` needs no edit; provenance route feasible; leaf titles valid.

## Fold (orchestrator, contract v5 / plan v5)

(1) Path-only exact keys carry `RAIL_CATALOG_VERSION` (both native and virtual forms) — same T3a scope helper already in scope; its suite pins the shape. (2) SIMPLER than propagation: a stale kind/place match makes the allow stage return an explicit ASK decision (the coordinator's deterministic-ask shape, `decidedBy: 'human_decision'`, reason = the stale line); the coordinator returns it before the cache read and the tail, so no cache or classifier Allow can swallow it and both IPC and inline render it exactly as they render a rail ask today (no IPC scope needed; the stage and coordinator leaves prove it). (3) Allow candidates: exact → `kind:tool` → `kind:<category>` → `place:tool:<tool>:<root>` → `place:<category>:<root>`; stage leaf pins both place matches and a cross-kind miss. (4) "Non-overridable rail ASK" = any rail ASK other than the two cases the slot already distinguishes (`out_of_trusted_root`; `unsupported_meta_executor` with `readOnlyMetaExecutor`) — never the `hardFloor` flag; leaf title updated. (5) `docs/architecture/permission-decision-memory.md` joins the scope (17 files; budget 18 / 1,800 holds).
