# ASKFLOOR-1-T3b task-contract grill — round 5 answer (gpt-5.6-sol @ xhigh)

1. **Stale matches cannot render the promised re-ask card.** `PermissionApprovalDecision` has no ASK outcome (`domain/types.ts:251-260,300-314`); rail ASK is a separate type and both tails render cards only inside themselves (`ipc-permission-classifier-decision.ts:512-605`; `inline-agent-loop-tools.ts:395-540`). Returning an "ask decision" would be a terminal denial. Fix: a tagged stale result plus a force-prompt path through both tails (IPC file into scope), tests on `requestPermissionApproval`.
2. **The inline contract assumes a cache bypass that does not exist.** The inline call does not pass `skipClassifierVerdictCache: true` today; say T3b ADDS it.
3. **The IPC workspace-root handoff is unspecified.** IPC passes the root only inside `deterministicRailsInput` (`:83,121-136`); the new top-level `workspaceRoot?` would arrive empty and native path-only keys would refuse. Fix: the coordinator uses `input.workspaceRoot ?? input.deterministicRailsInput?.workspaceRoot`; fix the stale `:367-375` citation.
4. **Leaves do not prove all key/lookup clauses.** Postgres title lacks first-of-two-current precedence; stage title lacks the `kind:tool` same-tool match/other-tool miss; no leaf pins the versioned path-exact and kind-carrying place forms in the scope suite.

Non-blocking: ACs and `plan_contracts` byte-equal; five-candidate order, non-overridable predicate, doc scope and 17/18/1,800 folds present; `runtime-services.ts` needs no edit; leaf ids valid; inline file 739/750.

## Fold (orchestrator, contract v6 / plan v6)

(1) OWNER RULING (Ravi, 2026-09-07, "Same as exact: stop applying"): a kind/place row from another rails version simply never matches — `findHumanDecision` filters `rail_version = railVersion` in the query and returns `PermissionDecisionMemoryRow | null` (no `stale` flag); no re-ask line exists anywhere in T3b; T5b marks such rows outdated in `/permissions`; the spec line is aligned. (2) AC4 says T3b ADDS `skipClassifierVerdictCache: true` at the inline call. (3) AC3: the coordinator resolves `workspaceRoot = input.workspaceRoot ?? input.deterministicRailsInput?.workspaceRoot` (the IPC caller already carries it there; no IPC edit); citation fixed. (4) Postgres leaf title pins first-of-two-current precedence and another-version never matching; stage leaf title pins the `kind:tool` same-tool match and other-tool miss; a sixth required leaf in `test/unit/application/human-decision-scope.test.ts` pins both versioned path-exact forms and both kind-carrying place forms.
