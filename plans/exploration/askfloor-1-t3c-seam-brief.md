# ASKFLOOR-1-T3c seam map — exploration brief (read-only)

Purpose: give the T3c task plan (Remember settlement and learning writes) an exact, line-cited map of every seam it must touch, so the plan names real symbols and the contract's leaf tests land on real files. Read-only: no edits, no writes, no test runs.

Read first: `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§3 and the T3c ownership bullet — it OWNS: the `remember` codec set outside `domain/types.ts` (normalization, settlement, `pending-interaction-prompt-binding.ts`, `pending-interaction-permission-callback.ts`, `pending-interaction-permission-recovery-orchestrator.ts`, `worker-coordination-permission-prompt.postgres.ts`, `telegram/channel-shared.ts`, `slack/permission-action-id.ts`), the two human-resolution chokepoints (`ipc-interaction-processing.ts` ~:334-359; `inline-agent-loop-tools.ts` `afterDecision` ~:503-540) with the lane re-derivation, recovery/replay + excluded-lane tests, the end-to-end S2 fixture tap → persist → reuse); `docs/specs/askfloor-1-judge-actually-judges.md`; decisions 0154, 0118, 0121, 0124, 0138; `docs/architecture/permission-decision-memory.md` (as of T3b).

What T3a and T3b already provide (cite exact lines): `domain/human-decision.ts` (`PermissionRememberResolution`, `PermissionApprovalResolution`, the remember DTO, provenance codec); `application/permissions/human-decision-memory-service.ts` (`remember`, its refusal reasons, `HumanDecisionMemoryService` construction and where the runtime builds it — is it wired anywhere yet?); `application/permissions/human-decision-scope.ts` (`deriveHumanDecisionScopeKey`, versioned exact keys, kind-carrying place keys); `domain/ports/permission-decision-memory.ts` (`putHumanDecision`, `findHumanDecision`); `runtime/permission-human-memory-stage.ts` (what T3b consults — so T3c's write must produce keys the consult can find: same derivation, same `trustGrowthTool` choice, same rails version); `application/permissions/auto-lane-analysis.ts` (`deriveAutoLaneAnalysis` — the lane re-derivation the chokepoints must use); the `rememberEligible` host stamp (where is it computed, where is it carried on the prompt/claim?).

Map, with file:line for each: (1) every codec/normalisation site where a card tap's scalar mode (`allow_once`, `allow_future`, `deny`, …) becomes a resolution — Telegram (`telegram/channel-shared.ts` and the callback split modules), Slack (`slack/permission-action-id.ts`), Discord/Teams if they encode modes, the IPC prompt binding and callback reconstruction, the recovery orchestrator, the Postgres claim mapping (`worker-coordination-permission-prompt.postgres.ts`); which of them already carry a structured `remember { outcome, scope }` shape (T3a's DTO) and which still only carry scalars; (2) the two human-resolution chokepoints: exact function names and lines in `ipc-interaction-processing.ts` and the inline `afterDecision`, what they know at that point (request, resolution, person, lane inputs, workspace root, effect hash), and what each is missing to call `HumanDecisionMemoryService.remember`; (3) the once-only settlement guards that already exist (CARDSIMPLE/CARDFIX opId, claim protocol, replay protection) that a forged/replayed/ineligible remember callback must fall through; (4) how the current request is applied after a remembered tap (does the callback path already apply an `allow_once`-shaped decision for the current request? where?); (5) the unresolved-person path (0118): where personId is resolved for a card tap and what happens when it is blank; (6) the S2 fixture: which existing end-to-end/agent-e2e fixture (`cd ~/Workdir/<repo> && ls && git log`) exists for tap budgets (T1/T2a shipped S1/S3), its file, and how T3c would add "tap once → persist → second run 0 taps"; (7) the excluded lanes: how ask, auto_strict, group and job prompts are distinguishable at the chokepoints (0121/0155), so a remember from them can be refused without a card-side change (T5a owns card copy); (8) architecture-map line ceilings for every file in scope and any layer rule the write path would hit.

Output: a Markdown seam map with sections matching (1)–(8), each entry `path:line — symbol — what it does today — what T3c must change (one sentence)`, then a list of OPEN QUESTIONS for the orchestrator (only real choices; no rhetorical ones). Keep it under ~250 lines.
