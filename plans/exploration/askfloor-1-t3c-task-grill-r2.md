# ASKFLOOR-1-T3c task-contract grill — round 2

Read-only cold-read review, no file edits, no writes. Follow `factory/prompts/griller.md --gate task` for the `ASKFLOOR-1-T3c` entry in `.factory/stories/ASKFLOOR-1/decomposition.json` and its saved task plan `.factory/stories/ASKFLOOR-1/task-plans/ASKFLOOR-1-T3c.md`.

Inputs of record: the story plan `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§3 and the T3c ownership bullet); the spec `docs/specs/askfloor-1-judge-actually-judges.md`; decisions 0154 (learning write, scopes), 0118 (person), 0121 (no learning on job lanes), 0124 (delivery/claim authority), 0138, 0043; the seam map `plans/exploration/askfloor-1-t3c-seam-map.md` (its open questions are ruled in the plan's "Orchestrator rulings" section — treat those rulings as inputs, grill their consequences, not the choice); T3a and T3b as merged on trunk (`domain/human-decision.ts`, `application/permissions/human-decision-memory-service.ts`, `application/permissions/human-decision-scope.ts`, `runtime/permission-human-memory-stage.ts`, `app/bootstrap/inline-permission-memory.ts`, `docs/architecture/permission-decision-memory.md`); the durable interaction path (`application/interactions/pending-interaction-prompt-binding.ts`, `pending-interaction-permission-callback.ts`, `pending-interaction-permission-recovery-orchestrator.ts`, `pending-interaction-permission-claim.ts`, `durable-interaction-handler.ts`, `adapters/storage/postgres/repositories/worker-coordination-permission-prompt.postgres.ts`, `schema/worker-coordination.ts:171`); the two chokepoints (`runtime/ipc-interaction-processing.ts:132-158,240-382`; `app/bootstrap/inline-agent-loop-tools.ts:360-398,483-560`); the four provider codecs and handlers named in the write scope; `scripts/architecture-map.json`; the tap-budget harness (`test/unit/runtime/askfloor-tap-budget-harness.ts`, `askfloor-tap-budget.test.ts`).

Grill the contract for: (a) contradictions with 0154 / 0118 / 0121 / 0124 or with the story plan's T3c ownership bullet (T4, T5a, T5b, T6 files must not be in scope; card buttons, labels and copy are T5a's); (b) an AC that cannot be proven by its named leaf, a leaf whose title does not match an AC, or a leaf title that is not a valid vitest `-t` pattern for one concrete test, or a test file in the write scope that does not exist under that exact path (name the real owner suite); (c) ONCE-ONLY hazards — any path by which a remember code could persist twice (replay, recovery, retry, provider double-delivery) or persist before the claim boundary / before the current-request application; (d) FORGERY hazards — a remember code arriving on an ineligible card (ask, auto_strict, group, job, blank person) or from an unauthorized tapper: does the contract degrade it to its scalar base and settle once, with no write, on every provider and on recovery? (e) the durable representation — the four codes in the existing mode slot and `claim_mode` text column: every consumer of `PermissionApprovalDecisionMode` that would now see a code (type widening sites, exhaustive switches, validation regexes such as Telegram's pattern and its 64-byte callback-data budget, Slack action ids, Discord custom-id length, Teams payload), and whether recovery can rebuild the intent from the code alone; (f) the eligibility stamp on the prompt BINDING record — where it is computed on each path, whether both paths have the lane inputs at prompt-construction time, and whether the settlement re-derivation uses the same inputs; (g) KEY MATCH — the write's derivation inputs versus the T3b consult (`permission-human-memory-stage.ts`), `trustGrowthTool: false`, rails version, person source; (h) the line ceilings (three files at their ceiling) and layer rules (application may not import runtime; channels may import application); (i) the review budget (32 files / 3,200 lines) against the listed ~29 files; (j) anything a Codex implementer would have to guess.

Output: `CONTRACT SOUND` or a numbered list of blockers (what, where, why, minimal fix), then non-blocking notes.

Round 2 context: round 1 (`plans/exploration/askfloor-1-t3c-task-grill-answer.md`) raised eight blockers, all folded into plan v2 / contract v2 (generated from the saved plan): (1) every provider handler and renderer left the scope — T3c owns only `telegram/channel-shared.ts` and `slack/permission-action-id.ts` per the story plan, T5a owns the rest; (2) typed carriers: the claim/recovery types move net-zero from `domain/types.ts` (at its 740-line ceiling) into NEW `domain/permission-interaction.ts` and widen to accept `PermissionRememberCode`; the Postgres prompt mapper validates `claim_mode` through the codec on read; (3) the eligibility stamp has ONE host producer (`derivePermissionRememberEligibility`: interactive_auto lane, non-blank person as the DM fact per 0118, not scheduled, `matchKind === individual`) stamped at the host-owned pending-interaction member creation (JSONB payload, no migration) on both paths and re-derived at settlement; (4) learning runs only AFTER application on all three paths — IPC after `applyPermissionInteractionDecision` (`ipc-interaction-processing.ts:326`), inline after `runDurablePermissionInteraction` resolves (`:543-546`, never in `afterDecision`), recovery after `authorityApplied` (`pending-interaction-permission-callback.ts:521`) via an optional `learn` dependency; (5) recovery learns through the same helper; idempotency = T3a's refresh (double delivery, crash between learning and settlement); (6) the helper takes `canonicalRoot?`, pins `EFFECT_SCHEMA_VERSION`, the reason constant and an undefined label, and returns a discriminated `LearnResult` carrying the service refusal reason; place without a root → `no_root`; (7) real test owners (`ipc-interaction-handler.test.ts`, `pending-interaction-durability.test.ts`, the recovery orchestrator suite, `worker-coordination.postgres.integration.test.ts`), seven leaves; (8) S2 traverses claim → application → learning through a provider-neutral durable fixture and the second run through the real coordinator. Check every fold is complete and consistent (decomposition entry and saved plan carry byte-equal AC text), then re-run the full checklist cold, with particular attention to: whether the IPC creation site (`:171`) can actually obtain the lane facts (permission mode, host job id) without a provider edit — name the exact source; whether `matchKind` is known at member creation; whether the recovery `learn` dependency can be wired without a layer-rule violation; and the review budget (30 files / 3,000 lines) against the 27 listed files plus the importers touched by the type move. Output `CONTRACT SOUND` or numbered blockers as before.
