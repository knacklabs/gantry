# ASKFLOOR-1-T4 seam map — exploration brief (read-only)

Purpose: give the T4 task plan (Live job projection of learned decisions, decision 0153) an exact, line-cited map of every seam it must touch. Read-only: no edits, no writes, no test runs.

Read first: `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (§3 and the T4 ownership bullet — T4 edits EXACTLY two named helpers: T1's route-guard helper (autonomous branch: learned records as a declared-grant input, person-scoped, fail closed) and the inline scheduled projection helper of `inline-agent-loop-tools.ts` (~:408-445); threads the canonical job owner `execution_context.personId` into BOTH job permission paths (today scheduled `AgentInput` carries conversation-derived `memoryUserId` at `jobs/execution-phases-run.ts` ~:185,296 and the inline request omits sender identity at `inline-agent-loop-tools.ts` ~:375) — unresolved identity projects nothing; owns the audit write of the matched record id in both job lanes (`permission-management-service.ts` ~:489-521; the `recordDecision` port threaded into the inline helper); rewrites `inline-agent-loop-tools.test.ts` ~:1194-1249; projection matrix tests); `docs/specs/askfloor-1-judge-actually-judges.md`; decisions 0153 (jobs consume projections only), 0121 (no classifier on autonomous runs), 0118 (person scope), 0154; `docs/architecture/permission-decision-memory.md`; the T4 stub in `.factory/stories/ASKFLOOR-1/decomposition.json` (objective + four acceptance criteria).

What T3a/T3b/T3c provide (cite lines): `domain/ports/permission-decision-memory.ts` (`findHumanDecision(candidates, railVersion)`, `listHumanDecisions`), `runtime/permission-human-memory-stage.ts` (the consult stage: lane guard interactive_auto only — T4 must NOT reuse it for jobs; it needs a separate, deterministic declared-grant projection), `application/permissions/human-decision-scope.ts` (`deriveHumanDecisionScopeKey` — the candidate keys a job request must derive identically), `application/permissions/human-decision-learning.ts` (`PermissionRememberContext`, kindVariant), `domain/permission-decision.ts` (`PERMISSION_PROVENANCE_BY_DECIDER.human_decision`, `decisionForMode`).

Map, with file:line for each: (1) T1's route-guard helper: its exact name/file, the autonomous branch, what inputs it has (request, lane analysis, effect hash, workspace root, personId?), where a "declared grant" input enters today and how a learned record would be presented as one (allow outcome only; deny outcome and other persons never match; rails version mismatch never matches — T3b's findHumanDecision already enforces the version and active filters); (2) the inline scheduled projection helper in `inline-agent-loop-tools.ts` (~:408-445): what it consults today (classifier? the coordinator with lane autonomous?), and where the projection replaces classifier consultation; (3) identity: where `execution_context.personId` lives for a scheduled job (job row / execution context) and how it reaches (a) `jobs/execution-phases-run.ts` ~:185,296 `AgentInput.memoryUserId` and (b) the inline request (~:375) — name the exact fields and the host-side source; (4) the audit write of the matched record id: `permission-management-service.ts` ~:489-521 (`recordDecision` port, its shape, where `jobId` is stamped) and the inline path's equivalent; (5) the existing inline-scheduled tests at `inline-agent-loop-tools.test.ts` ~:1194-1249 that assert classifier consultation and must become projection assertions; (6) the "projection matrix" (AF-AC6, 0153): exact/kind/place match → allow with human_decision provenance; near-miss, rails-bump, revoked, deny-outcome, other-person → no projection (job asks/denies as today); enumerate where each case is decided; (7) architecture-map ceilings for every file in play (`inline-agent-loop-tools.ts` is at 749/750 — any inline edit must go through `inline-permission-memory.ts` or a new small module) and layer rules; (8) which existing suites own each seam (route guard, execution phases, permission-management-service, inline).

Output: a Markdown seam map with sections (1)–(8), each entry `path:line — symbol — what it does today — what T4 must change (one sentence)`, then OPEN QUESTIONS for the orchestrator (real choices only). Under ~220 lines.
