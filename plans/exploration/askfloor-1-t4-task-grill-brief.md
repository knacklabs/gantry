# ASKFLOOR-1-T4 task-contract grill — brief (read-only, cold read)

You are grilling the task plan `plans/exploration/askfloor-1-t4-taskplan.md` (T4: live job projection of learned decisions, decision 0153) against the code in this worktree, which carries T3a+T3b+T3c over trunk. Read-only. Do not edit anything.

Read: the task plan; `plans/exploration/askfloor-1-t4-seam-map.md`; the story plan's T4 bullet in `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md`; `docs/specs/askfloor-1-judge-actually-judges.md`; decisions 0153, 0121, 0118, 0154, 0043; `docs/architecture/permission-decision-memory.md`; and every file the plan cites (verify each line citation).

Find every place where the plan is wrong, impossible, ambiguous, under-specified, contradicts a decision or a T3a/T3b/T3c invariant, or where a named test cannot pin what it claims. Specifically check: (a) whether `reviewedRuleDecision` is really the seam where a declared autonomous rule allow is reviewed and what precedence that allow has against rails and the route guard — does ruling 1 actually give the projection "a declared rule's precedence", and is that safe against non-overridable rails; (b) how the IPC lane can resolve the job owner host-side from `hostJobId` (which port/repository is reachable from `resolvePermissionIpcDecision`'s deps) without a worker-supplied field; (c) whether `AgentInput` can take `jobOwnerPersonId` without breaking the contracts package or the fixed-image worker; (d) whether the inline scheduled tail can consult the projection with ≤ 5 changed lines in `inline-agent-loop-tools.ts` (749/750) and where the `recordDecision` capability can be threaded from; (e) whether the derivation inputs a job request carries (effect hash, workspace root, laneInput) are the SAME as the interactive derivation so keys match; (f) whether any named test file, line range or leaf can pin the claimed behaviour; (g) the file budget (18/1,800) against the real edit set; (h) anything else.

Output: numbered BLOCKING findings (each: what is wrong, file:line evidence, the concrete fix to the plan text), then NON-BLOCKING notes, then a one-line verdict `CLEAN` or `NOT CLEAN`. Be terse and specific. Under 120 lines.
