## BLOCKING findings

1. **Ruling 1 names a single “declared rule’s precedence,” but the coordinator has two incompatible precedences.** Assumption: the ruling refers to the live coordinator; no undocumented third rule class exists. Non-family reviewed Allows return before all deterministic rails; family Allows are rechecked against rails. A projected match therefore either bypasses every rail and the route guard, or cannot provide the intended broad read projection. [task plan:7](plans/exploration/askfloor-1-t4-taskplan.md:7), [permission-decision-coordinator.ts:138](apps/core/src/runtime/permission-decision-coordinator.ts:138), [permission-decision-coordinator.ts:173](apps/core/src/runtime/permission-decision-coordinator.ts:173), [ipc-permission-classifier-decision.ts:261](apps/core/src/runtime/ipc-permission-classifier-decision.ts:261), [decision 0153:19](docs/decisions/0153-learned-decisions-project-into-job-grants.md:19).
   **Plan fix:** replace ruling 1 with an explicit exact/tool-kind/category-kind/place precedence table, including which rail signals each may override and whether a matched projection may bypass an absent approver route. Name the coordinator input and test every row against a hard/non-overridable rail.

2. **The proposed helper cannot derive a safe place candidate.** Its signature has no `canonicalRoot` or rail result, while `deriveHumanDecisionScopeKey` refuses place without a root. The interactive path supplies a root only after rerunning the same rails and proving that trusting the root clears the ask; substituting `workspaceRoot` before that check could let an escaping target match a place record. [task plan:16](plans/exploration/askfloor-1-t4-taskplan.md:16), [human-decision-scope.ts:67](apps/core/src/application/permissions/human-decision-scope.ts:67), [permission-decision-coordinator.ts:347](apps/core/src/runtime/permission-decision-coordinator.ts:347), [permission-human-memory-stage.ts:112](apps/core/src/runtime/permission-human-memory-stage.ts:112).
   **Plan fix:** move projection after the coordinator’s validated root stage, or pass a coordinator-derived `{railDecision, canonicalRoot}` whose same-rails `clears(root)` check has succeeded. Require exact, tool-kind, category-kind, tool-place, category-place ordering plus escaping-target and non-overridable-rail tests.

3. **The helper API is not a complete compilable contract.** `decision: allow-with-human_decision-provenance` is not a type; the cited provenance table is private; and the application helper is required to emit one warning without receiving a warning capability, while application code cannot import infrastructure. Feeding a normal `ToolPolicyDecision` into the cited seam also hardcodes `reviewed_rule` provenance. [task plan:16](plans/exploration/askfloor-1-t4-taskplan.md:16), [permission-decision.ts:69](apps/core/src/domain/permission-decision.ts:69), [permission-decision-coordinator.ts:147](apps/core/src/runtime/permission-decision-coordinator.ts:147), [architecture-map.json:84](scripts/architecture-map.json:84).
   **Plan fix:** define a concrete `ProjectedHumanDecisionGrant` carrying `recordId`, scope/key class, and the typed decision; inject `warn`; and require the coordinator to construct the result through `decisionForMode(request, 'allow_once', 'human_decision', 'machine')`.

4. **There is no carrier or production audit path for `humanDecisionRecordId`.** `recordDecision` can flatten `auditMetadata`, but the IPC post-apply caller supplies none; `PermissionApprovalDecision` has no record-id metadata; inline `afterDecision` runs only for prompted interactions, while automatic coordinator results return outside it; and `InlineCoreToolHostDeps` has no audit capability. [task plan:8](plans/exploration/askfloor-1-t4-taskplan.md:8), [domain/types.ts:300](apps/core/src/domain/types.ts:300), [ipc-interaction-processing.ts:327](apps/core/src/runtime/ipc-interaction-processing.ts:327), [inline-agent-loop-tools.ts:510](apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:510), [inline-agent-loop-tool-types.ts:24](apps/core/src/app/bootstrap/inline-agent-loop-tool-types.ts:24).
   **Plan fix:** specify a typed record-id carrier through the final decision, forward it as `{humanDecisionRecordId}` from the existing IPC post-apply audit call, and thread one narrow inline `recordDecision` closure from `runtime-services.ts`, invoked only after the projection actually wins. Add an `ipc-interaction-handler.test.ts` production-forwarding leaf; the named service test alone cannot prove this wiring.

5. **IPC owner resolution is under-specified and its citation is wrong.** The authoritative `hostJobId` is obtained at line 97, not within the cited `75–91`; the reachable source is `deps.opsRepository.getJobById`, but the plan does not name it or define missing-row/throw behavior. [task plan:9](plans/exploration/askfloor-1-t4-taskplan.md:9), [ipc-permission-classifier-decision.ts:91](apps/core/src/runtime/ipc-permission-classifier-decision.ts:91), [ipc-domain-types.ts:108](apps/core/src/runtime/ipc-domain-types.ts:108), [ops-repo.ts:223](apps/core/src/domain/repositories/ops-repo.ts:223).
   **Plan fix:** say exactly: call `deps.opsRepository.getJobById(hostJobId)`, use only trimmed `job.execution_context?.personId`, and treat missing job, blank person, or repository failure as no projection with one warning. Test a forged request `personId`, missing job, blank owner, and throwing repository.

6. **`bindActiveJobRun` is not the `AgentInput` construction seam.** Line 102 only derives conversation memory identity; the scheduled `AgentInput` is created in `runActiveJobAgent` at lines 288–305. [task plan:9](plans/exploration/askfloor-1-t4-taskplan.md:9), [execution-phases-run.ts:102](apps/core/src/jobs/execution-phases-run.ts:102), [execution-phases-run.ts:288](apps/core/src/jobs/execution-phases-run.ts:288), [agent-spawn-types.ts:58](apps/core/src/runtime/agent-spawn-types.ts:58).
   **Plan fix:** amend ruling 3 and AC4 to add `jobOwnerPersonId?: string | null` in `runtime/agent-spawn-types.ts` and set it in `baseInput` from `currentJob.execution_context?.personId ?? null`; assert it in the existing `runAgent` expectation, while separately asserting `memoryUserId` is unchanged.

7. **The inline contract does not identify an implementable five-line seam.** The existing reviewed decision enters the coordinator at line 395, before the tail; the tail calls the classifier at lines 412–448; and the final coordinator result is only visible at line 567. “Consult in the scheduled tail,” “declared-grant input,” “audit it,” and “≤5 changed lines” therefore describe different seams. Also, no projection is not behavior-identical today: a low classifier currently allows. [task plan:18](plans/exploration/askfloor-1-t4-taskplan.md:18), [inline-agent-loop-tools.ts:390](apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:390), [inline-agent-loop-tools.ts:412](apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:412), [inline-agent-loop-tools.ts:450](apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:450), [inline-agent-loop-tools.ts:567](apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:567).
   **Plan fix:** name one helper call supplying the new typed coordinator input, explicitly exclude scheduled runs from classifier consultation, and place the audit after the final projected Allow. Replace “unchanged” with the intended deterministic miss behavior and test that a no-projection scheduled request cancels even when the classifier stub would return low.

8. **The proof and file-budget contract is not closed.** The “about 14 files” list omits the required coordinator, IPC audit caller, typed carrier, and IPC production-audit test. Its cited permission-service leaf is a persistent skill-action audit, and the cited execution line asserts the memory query rather than `AgentInput`. Several required files are already at or one line below their ceilings. [task plan:20](plans/exploration/askfloor-1-t4-taskplan.md:20), [task plan:37](plans/exploration/askfloor-1-t4-taskplan.md:37), [permission-management-service.test.ts:241](apps/core/test/unit/application/permission-management-service.test.ts:241), [execution.test.ts:1368](apps/core/test/unit/jobs/execution.test.ts:1368), [architecture-map.json:31](scripts/architecture-map.json:31).
   **Plan fix:** publish an exact source/test write list including the missing files, name the new audit-forwarding and precedence leaves, and recount against 18/1,800 after the carrier/precedence design is settled; raise the budget or name the required extraction if it no longer fits.

## NON-BLOCKING notes

- No new IPC repository port is needed: `IpcDeps.opsRepository` already exposes `getJobById`.
- `AgentInput` is an internal runtime type, not an `@gantry/contracts` DTO. It is spread into runner JSON, whose runners structurally cast parsed input, so an added optional field does not itself require a contracts-package or fixed-image schema change. [agent-spawn-input-projection.ts:53](apps/core/src/runtime/agent-spawn-input-projection.ts:53), [runner index.ts:330](apps/core/src/adapters/llm/deepagents-langchain/runner/index.ts:330). IPC must still ignore any worker echo of that field.
- The Postgres repository already enforces app/folder/person/current-rails/active filtering and caller-ordered candidates; the named integration suite is suitable for real projection coverage. [permission-decision-memory-repository.postgres.ts:237](apps/core/src/adapters/storage/postgres/repositories/permission-decision-memory-repository.postgres.ts:237), [permission-decision-memory.postgres.integration.test.ts:677](apps/core/test/integration/permission-decision-memory.postgres.integration.test.ts:677).

NOT CLEAN
