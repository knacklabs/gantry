## BLOCKING findings

1. **AC2 still defines the obsolete, incomplete projection input.** Ruling 1 requires `{ ownerPersonId, memory, guard, warn }`, but AC2 reverts to `{ ownerPersonId; memory: PermissionDecisionMemoryPort }`; that port does not exist—the live contract is `PermissionDecisionMemoryRepository`. Evidence: [taskplan.md:7](<plans/exploration/askfloor-1-t4-taskplan.md:7>), [taskplan.md:17](<plans/exploration/askfloor-1-t4-taskplan.md:17>), [permission-decision-memory.ts:149](apps/core/src/domain/ports/permission-decision-memory.ts:149).
   **Plan fix:** replace AC2’s type with the complete Ruling-1 shape, using `PermissionDecisionMemoryRepository` and concrete `guard`/`warn` signatures.

2. **The stated IPC guard delegation is not implementable “unchanged,” and its return semantics are wrong.** The proposed sibling module cannot call the private `applyIpcPermissionRouteGuard` or name its private input type. Moreover, the guard sanitizes request risk and returns `undefined` when the route exists; only an absent route returns a decision. Therefore “a sanitised result stands and no lookup happens” would incorrectly suppress every valid lookup. Evidence: [taskplan.md:7](<plans/exploration/askfloor-1-t4-taskplan.md:7>), [taskplan.md:17](<plans/exploration/askfloor-1-t4-taskplan.md:17>), [ipc-permission-classifier-decision.ts:204](apps/core/src/runtime/ipc-permission-classifier-decision.ts:204), [ipc-permission-classifier-decision.ts:272](apps/core/src/runtime/ipc-permission-classifier-decision.ts:272).
   **Plan fix:** construct the callback inside `ipc-permission-classifier-decision.ts`, closing over `applyIpcPermissionRouteGuard`; let the new module resolve owner/memory only. Specify: returned decision short-circuits; `undefined` means sanitization completed and lookup proceeds. Add an IPC-resolver test with a host job, owner, would-match repository and absent route, asserting denial, sanitized risk, and zero lookup calls—the coordinator-only fake-guard test cannot prove delegation.

3. **AC1 assigns proofs to a helper that does not own them.** The helper returns only `{recordId, scope}`, yet its unit leaf must prove a completed `human_decision` decision; revoked/person/version filtering is repository-owned, so a fake-port “test” is either vacuous or forces duplicated filtering. Evidence: [taskplan.md:10](<plans/exploration/askfloor-1-t4-taskplan.md:10>), [taskplan.md:16](<plans/exploration/askfloor-1-t4-taskplan.md:16>), [permission-decision-memory-repository.postgres.ts:249](apps/core/src/adapters/storage/postgres/repositories/permission-decision-memory-repository.postgres.ts:249), [permission-decision.ts:207](apps/core/src/domain/permission-decision.ts:207).
   **Plan fix:** limit the helper leaf to candidate parity/order, match extraction, deny outcome, refusal and failure behavior; move provenance plus `humanDecisionRecordId` assertions to the coordinator leaf; prove other-person/revoked/old-rail filtering through the Postgres projection leaf. Correct the stale `decisionForMode` citation from `:69` to `:207`.

## NON-BLOCKING notes

- The rail-rechecked consult position is now correct: non-family reviewed Allows bypass rails, family Allows are rechecked, and T3b remembered Allows sit after validated-root handling. [permission-decision-coordinator.ts:138](apps/core/src/runtime/permission-decision-coordinator.ts:138), [permission-decision-coordinator.ts:173](apps/core/src/runtime/permission-decision-coordinator.ts:173), [permission-decision-coordinator.ts:227](apps/core/src/runtime/permission-decision-coordinator.ts:227).
- `{effectHash, workspaceRoot, canonicalRoot?}` and exact/tool-kind/category-kind/tool-place/category-place ordering match T3b exactly. [permission-human-memory-stage.ts:52](apps/core/src/runtime/permission-human-memory-stage.ts:52), [permission-human-memory-stage.ts:104](apps/core/src/runtime/permission-human-memory-stage.ts:104).
- Host IPC owner lookup is reachable through `opsRepository.getJobById`; adding the optional `AgentInput` field requires no contracts-package or fixed-image schema change because runner input is structurally spread and parsed by cast. [ipc-domain-types.ts:108](apps/core/src/runtime/ipc-domain-types.ts:108), [agent-spawn-input-projection.ts:53](apps/core/src/runtime/agent-spawn-input-projection.ts:53).
- The architecture document and production runtime-services leaf are now included. The scope recount is 22 files; `permission-decision-coordinator.test.ts` already exists, so its `NEW` label is merely inaccurate. The 740-line `types.ts` and 700-line `execution-phases-run.ts` changes are feasible line-neutrally.

NOT CLEAN
