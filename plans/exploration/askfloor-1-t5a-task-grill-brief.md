# ASKFLOOR-1-T5a task-contract grill — brief (read-only, cold read)

You are grilling the task plan `plans/exploration/askfloor-1-t5a-taskplan.md` (T5a: card UX + provider surfaces; user_facing) against the code in this worktree (trunk after T4). Read-only. Do not change anything.

Read: the task plan; `plans/exploration/askfloor-1-t5a-seam-map.md`; the story plan's T5a bullet (line ~194) and section 5 card copy (line ~162) in `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md`; decisions 0154, 0155, 0118, 0124; and ONLY the files and line ranges the plan cites (do not sweep the tree).

Find every place where the plan is wrong, impossible, ambiguous, contradicts a decision or a T3a/T3b/T3c/T4 invariant, or where a named test cannot pin what it claims. Specifically: (a) can the render-time `PermissionCardAffordances` be derived on BOTH prompt paths from the T3c context plus the two host facts, and persisted on the member payload without breaking T3c's settlement accessor or the batch finalisation inside the Postgres bind transaction; (b) is `countExactAllowsByTool` callable at prompt time on both paths (deps reachable) and is the high-risk gantry tool set the T2b typed table; (c) the validated place root: where does the coordinator expose it at prompt time, and is "Allow only in this folder when a broader default applies" decidable there; (d) each provider seam: can the tapped code reach the durable claim verbatim with the cited edits, and do the Discord/Teams codecs plus Telegram's 64-byte budget hold; (e) `memory_forget` through the message-action router with authenticated person identity — is the router's identity source the provider person and does `revokeById` take exactly the axes the plan names; (f) byte-for-byte ineligible lanes — is a snapshot per lane per provider feasible with the existing suites; (g) ceilings (700 default; Slack channel-interactions 712; Telegram callback-handlers 900) against the real edit set; (h) the 36-file scope and 40/4,000 budget; (i) any owner-level choice the plan still leaves open.

Output: numbered BLOCKING findings (what is wrong, file:line evidence, the concrete plan-text fix), NON-BLOCKING notes, then `CLEAN` or `NOT CLEAN`. Under 100 lines. Finish within about 12 minutes of reading.
