BLOCKING findings

1. Rails-version re-ask behavior contradicts the accepted contract. The task suppresses the safety-rules-updated line and says T5b marks stale rows (`askfloor-1-t5a-taskplan.md:9`), while Decision 0154 requires the next ask to explain the invalidation (`docs/decisions/0154-human-decision-memory-generic-scope.md:14`) and the story repeats that copy (`plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:162`). Concrete fix: either restore the re-ask line and name its stale-record carrier/test, or obtain an owner-approved amendment to Decision 0154 and update the story plan before T5a.

2. The persisted render-model carrier has no complete transport or recovery path. `PermissionApprovalRequest` has no affordance field (`apps/core/src/domain/types.ts:179-241`), and the durable snapshot explicitly copies a closed field list (`apps/core/src/application/interactions/pending-interaction-permission-envelope.ts:10-56`). The actual IPC persistence seam is `rememberingPermissionApprovalRequester` (`apps/core/src/runtime/permission-remember-settlement.ts:31-57,77-108`), not either file named for this responsibility in the task scope. Concrete fix: specify the typed request/payload field, parser, durable snapshot/recovery behavior, and add `domain/types.ts`, `pending-interaction-permission-envelope.ts`, and `permission-remember-settlement.ts` plus recovery tests to scope.

3. Batch finalisation would leave the persisted affordance model inconsistent. The Postgres bind transaction atomically changes only `rememberContext.eligible` for batch members (`apps/core/src/adapters/storage/postgres/repositories/worker-coordination-permission-prompt.postgres.ts:306-347`), while settlement later reads that member context (`apps/core/src/application/interactions/pending-interaction-permission-callback.ts:448-469`). The repository is absent from the T5a write scope (`askfloor-1-t5a-taskplan.md:42`). Concrete fix: add the repository and its Postgres test; atomically finalise both context and affordances to the scalar batch shape in the same transaction.

4. The validated place root is not exposed at either prompt seam. `PermissionDecisionTailContext` carries only analysis, rail decision, and cache data (`apps/core/src/runtime/permission-decision-coordinator.ts:63-67`); the canonical root remains local to the trusted-root stage (`apps/core/src/runtime/permission-decision-coordinator.ts:217-224,277-284`). IPC forwards only `workspaceRoot` (`apps/core/src/runtime/ipc-permission-classifier-decision.ts:591-595,633-637`), and inline declares `tail: async () =>` and discards the context (`apps/core/src/app/bootstrap/inline-agent-loop-tools.ts:390-401`). Concrete fix: add `canonicalRoot` to the tail context, thread it through both IPC and inline prompt construction, and scope the coordinator, IPC-tail, inline-loop, and corresponding tests. Define folder eligibility as `default kind candidate is valid && place candidate from canonicalRoot is valid`.

5. Trust-growth counting includes rails-stale records. `countExactAllowsByTool` accepts no rail version (`apps/core/src/application/permissions/human-decision-memory-service.ts:208-214`), and its repository query filters revocation but not `railVersion` (`apps/core/src/adapters/storage/postgres/repositories/permission-decision-memory-repository.postgres.ts:315-335`), contradicting Decision 0154’s invalidation rule. Also, the T2b source is an argument-aware function, not a reusable “high-risk set” (`apps/core/src/application/permissions/gantry-tool-risk.ts:59-85`). Concrete fix: pass/filter the current rail version; define trust growth as `gantryToolRisk({toolName, toolInput}).level === 'high'` after destructive/protected exclusion; suppress the alternative with a warning on missing repository/query failure. Scope the port, service, repository, and tests.

6. “Unoffered remember code degrades to scalar” is not a T3c guarantee. Settlement decodes the claimed code and context without checking persisted offered options/model (`apps/core/src/application/interactions/pending-interaction-permission-callback.ts:448-469`), and the context retains every candidate (`apps/core/src/application/permissions/human-decision-learning.ts:24-43,231-247`). Current providers reject unoffered options instead of scalarising them, for example Telegram (`apps/core/src/channels/telegram/callback-handlers.ts:729-743`) and Teams (`apps/core/src/channels/teams/interaction-handlers.ts:350-363`). Concrete fix: define one central persisted-offer authorization check that converts an unoffered remember code to its scalar base before application and learning, then scope its callback/recovery tests; remove the “no memory write path change” exclusion if that seam must change.

7. The provider scope omits the actual Forget render/settlement files. Telegram’s generic buttons are encoded in `apps/core/src/channels/telegram/message-action-affordances.ts:25-40,69-118`; Slack encodes them in `apps/core/src/channels/slack/message-action-affordances.ts:69-120` and handles taps in `apps/core/src/channels/slack/channel-message-action-handler.ts:50-105`. None appears in the manifest (`askfloor-1-t5a-taskplan.md:42`). Concrete fix: add these files and their focused render/settlement tests; specify a bounded Telegram `memory_forget` codec.

8. The Forget host contract lacks a trusted identity, scope label, and settled ownership. Provider callbacks supply raw provider `userId` values (`apps/core/src/channels/telegram/callback-handlers.ts:451-465`; `apps/core/src/channels/discord/interactions.ts:294-308`), but `revoke` requires canonical app, agent-folder, acting-person, and record axes (`apps/core/src/application/permissions/human-decision-memory-service.ts:199-205`). The router has no resolver/service dependencies (`apps/core/src/app/bootstrap/channel-message-action-router.ts:62-118`), and revoke returns only a status, not the trusted scope needed for “Forgot: <scope>” (`apps/core/src/domain/ports/permission-decision-memory.ts:116-119,197-209`). The story assigns the host handler to T5b (`plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:195`), while T5a claims it (`askfloor-1-t5a-taskplan.md:15,24,42`). Concrete fix: owner must choose T5a or T5b ownership. Recommended: T5a owns provider-neutral transport plus a dedicated handler hook; T5b binds the host handler, resolves canonical person/app/agent from authenticated conversation context, and loads/formats the person-scoped row before revocation. Never trust callback `label` as the scope.

9. Exact user-facing alternative copy is unspecified. AC1 requires “the alternative’s own line” and exact-line tests but supplies no literal family/trust/folder templates (`askfloor-1-t5a-taskplan.md:21`). The required human tool label also lacks a fallback even though `displayName` is optional (`apps/core/src/domain/types.ts:209-210`). Concrete fix: add literal templates for all three alternatives and an owner-approved missing-label behavior that never exposes the canonical id.

10. The scope and budget are not honest after the required seams are included. The manifest contains 35 files, not approximately 36 (`askfloor-1-t5a-taskplan.md:42`). Findings 2–8 require at least 13 additional source seams before their tests, exceeding the 40-file cap. Concrete fix: publish an exact file manifest and either raise the approved budget or split render-model/persistence from Forget/provider transport.

NON-BLOCKING notes

- Repository access is already reachable on both prompt paths (`apps/core/src/runtime/ipc-domain-types.ts:135-137`; `apps/core/src/app/bootstrap/inline-agent-loop-tool-types.ts:51-53`).
- Telegram and Slack already encode all four remember codes, and the existing parity test pins a UUID callback under Telegram’s 64-byte cap (`apps/core/test/unit/channels/provider-affordance-parity.test.ts:184-205`).
- Four-provider ineligible-lane golden snapshots are feasible, but they must be captured from the pre-change renderers with fixed callback IDs before implementation; the current suite only round-trips codecs (`apps/core/test/unit/channels/provider-affordance-parity.test.ts:118-205`).
- The named ceilings are tight but feasible with extraction: current counts are permission interaction 693/700, Discord interactions 699/700, Teams cards 688/700, Slack interactions 661/712, and Telegram callbacks 863/900.

NOT CLEAN
