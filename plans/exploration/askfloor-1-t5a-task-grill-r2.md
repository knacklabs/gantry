BLOCKING findings

1. The rails-bump ruling is not fully reconciled. V2 and Decision 0154’s amendment drop the re-ask line (`askfloor-1-t5a-taskplan.md:8,31`; `0154-human-decision-memory-generic-scope.md:24-26`), but the active story still requires it (`ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:162`). Decision 0154 also says `/permissions all` lists current-rails records only (`0154...md:18`) while the amendment says T5b marks old rows outdated. Concrete fix: amend/reapprove the active story and state exactly which T5b view exposes outdated rows.

2. The persisted render carrier has no viable route into rendering or recovery. `PermissionApprovalRequest.decisionOptions` accepts scalar modes only (`domain/types.ts:230` versus remember codes at `:252-254`); central context lines are derived solely from that request (`permission-interaction.ts:191-200,350-383`). A sibling payload field therefore cannot reach the formatters while the plan forbids a request field (`taskplan.md:12`). The cited “envelope” range is only a request snapshot (`pending-interaction-permission-envelope.ts:10-56`); the actual envelope type and constructor are `domain/types.ts:261-270` and `pending-interaction-prompt-binding.ts:57-65`. Concrete fix: make `cardAffordances` a typed request/render carrier, widen `decisionOptions`, snapshot and validate it durably, and have batch binding reset that authoritative field; alternatively specify the larger second-argument plumbing explicitly.

3. Folder buttons still cannot learn a place decision. `PermissionRememberContext` has no place candidate (`human-decision-learning.ts:24-43`), derivation supplies only `workspaceRoot` (`:65-69`), and settlement always refuses place with `no_root` (`:231-247`), although the scope-key helper already accepts `canonicalRoot` (`human-decision-scope.ts:46-77`). AC3 tests only folder eligibility, not a stored place decision (`taskplan.md:26`). Concrete fix: derive and persist a `place` candidate from `canonicalRoot`, parse it fail-closed, consume it during settlement, and add IPC, inline, and recovered `remember_allow_place` tests asserting the stored scope key.

4. Post-tap copy cannot identify the selected remember action. Decoding collapses remembered Allow to `allow_once` and remembered No to `cancel` (`permission-remember-codec.ts:33-59`); recovery preserves only that scalar decision plus an id/scope-only claim reference (`pending-interaction-permission-callback.ts:620-635`; `domain/types.ts:284-287`). `formatPermissionReceiptText` receives only request plus collapsed decision (`permission-interaction.ts:133-163`), so it cannot distinguish Allow, folder/trust alternative, Just this once, or remembered No. Concrete fix: define a closed selected-code field on the decision/recovery receipt, populate it from the persisted claim on live and recovered paths, and test every receipt on both paths.

5. The Forget label contract contradicts the existing short-ID contract. V2 gives outbound `memory_forget` no label/short-id field and always derives the first six hex characters (`taskplan.md:7,17`), while collision-aware IDs extend past six (`human-decision-memory-service.test.ts:400-409`). Existing outbound affordances carry render labels separately from callback inputs (`domain/message-actions.ts:23-64,66-139`), and encoders consume those labels (`telegram/message-action-affordances.ts:74-105`). Concrete fix: outbound `memory_forget` carries T5b’s collision-aware `shortId` or trusted render label plus full `recordId`; decoded callback input carries only `recordId` and authenticated conversation identity.

6. The 57-file manifest is not the real edit set and breaches its 60-file cap. At minimum it omits `domain/types.ts`, `pending-interaction-durability.ts` (the IPC payload updater is at `:74-90`), `durable-interaction-handler.ts` (inline payload creation at `:150-179`), `pending-interaction-prompt-binding.ts` (actual envelope construction), and `telegram/permission-prompt-settlement.ts` (live claim currently collapses the mode at `:55-61`). That is at least 62 files before story reconciliation. Concrete fix: publish the actual manifest and raise the budget or split carrier/settlement from provider/Forget transport.

NON-BLOCKING notes

- The rail-version-filtered count and argument-aware `gantryToolRisk` fold is correct; repository dependencies are reachable on both prompt paths (`ipc-domain-types.ts:135-137`; `inline-agent-loop-tool-types.ts:51-53`).
- The canonical-root threading proposal correctly names the current gaps. Because the tail context is frozen before root resolution (`permission-decision-coordinator.ts:179-182`), implementation must construct the final context after the trusted-root stage.
- Alternative precedence and literal trust/folder templates are now explicit (`taskplan.md:16`); the family line is identifiable at `permission-tool-input-format.ts:65-67`.
- T5a transport/router-hook versus T5 
  handler ownership now matches the active story (`active plan:194-195`), and `revokeById` has the named app/folder/person/record axes (`permission-decision-memory.ts:197-203`).
- Pre-change ineligible snapshots, current Telegram/Slack remember-code bounds, and the stated ceilings are feasible. Worktree remained clean.

NOT CLEAN
