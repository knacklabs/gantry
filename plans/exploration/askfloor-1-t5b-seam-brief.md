# ASKFLOOR-1-T5b seam map — exploration brief (read-only)

Purpose: give the T5b task plan (`/permissions`: remembered list, `all`, `forget <id>`; the `memory_forget` host handler; user_facing: true) an exact, line-cited map of every seam it must touch, and list the OWNER-LEVEL choices the plan text leaves open so they can be asked ONCE, up front. Read-only exploration: no file changes, no test runs.

Read first: the T5b ownership bullet in `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md` (line ~195) and section 5 (line ~162: the `/permissions` copy — rows "Allow · read-only reads · anywhere · 2 Sep · Ravi", "· used by job <name>" (max two names then "+N jobs", ACTUAL consumption only), the ten-newest rule with "Showing the 10 newest with buttons. <N> older." and "Send /permissions all for the full list, or /permissions forget <id>.", `/permissions all` plain text with the short id FIRST, `forget <id>` prefix ≥ 4 chars against this person's ACTIVE records only with the three replies, the empty state, the group variants' non-mutating guidance line, the one operating-guidance line in `prompt-profile-service.ts`); `docs/specs/askfloor-1-judge-actually-judges.md`; decisions 0154 (incl. its 2026-09-08 Amendment: old-version rows are simply not listed), 0118, 0153, 0132; what T5a shipped: `domain/message-actions.ts` `memory_forget { recordId, label }`, the router hook `OnMemoryForgetMessageAction` / `setMemoryForgetHandler` in `app/bootstrap/channel-message-action-router.ts` ("Not available yet." unbound), the provider render/decode for Forget, the scope-noun formatter in `channels/permission-card-affordances.ts`, `permissionHumanToolLabel` in `application/permissions/permission-card-affordances.ts`; what T3a/T4 shipped: `HumanDecisionMemoryService { list, revoke, revokeById?, countExactAllowsByTool }` and the port `domain/ports/permission-decision-memory.ts` (`revokeById(appId, agentFolder, actingPersonId, recordId) → applied | already_revoked | not_found`, `listHumanDecisions`), the short-id derivation (first 6 hex of the v4 uuid, extended on collision — `human-decision-memory-service.test.ts:400-409`), and T4's `humanDecisionRecordId` audit stamp beside `jobId` in `PermissionManagementService.recordDecision` (the "records used by job" source).

Map, with file:line for each:

1. `session-command-parse.ts` (~:25-39) — today's `/permissions` grammar (show/set/default); where `all` and `forget <id>` join.
2. `session-commands.ts` (~:580-585 and the deps at ~:89-135) — the mode line today; where the remembered rows, the ten-newest footer, the empty state and the plain-text `all` listing render; the `SessionCommandDeps` shape and how ONE host-owned `HumanDecisionMemoryService` is injected.
3. `group-processing-session-command-handlers.ts:140-170` — the caller: what identity it holds (app, agent folder, resolved person) and how a DM vs group route is known; the group non-mutating variants.
4. The `memory_forget` HOST handler: where `setMemoryForgetHandler` is bound (runtime-services or the session-command wiring — note: app/bootstrap is the runtime layer and may NOT import channels; `channel-wiring.ts`/`index.ts` are at their channel-import caps), how the authenticated conversation identity reaches it, how the person-scoped row is loaded BEFORE revocation for the "Forgot: <scope>" reply, and the in-place list edit per provider (which providers can edit a sent message).
5. "Records used by job": `domain/ports/repositories.ts:613-618` (`PermissionRepository` cannot list decisions today) and the Postgres adapter — the typed read query keyed on `humanDecisionRecordId` in the audit context, plus job-name hydration (job repository).
6. `prompt-profile-service.ts` — the operating-guidance block; where the one line goes.
7. The approver label: the row must show the snapshotted approver's display name (0118), never "you" — where T3a stored `acting_person_label` and whether it is populated (T3c left `personLabel` undefined on both paths — name the gap and the host source that can fill it).
8. The listing fixture and the S5 tap-budget fixture (remembered → job 0 cards → Forget → cards again): `askfloor-tap-budget-harness.ts` shape after S4.
9. Architecture-map ceilings for every file in play; layer rules (session commands live where?).
10. Existing suite ownership per seam.

Then OWNER QUESTIONS: list every product-visible choice the plan text leaves open (e.g. the exact "used by job" suffix format when a job is deleted; the reply when `forget` is sent in a group; whether `/permissions all` paginates; what the row shows when the approver label is missing). Give each a recommended default so they can be asked as one bundled question.

Output: Markdown sections (1)–(10) with `path:line — symbol — what it does today — what T5b must change`, then OWNER QUESTIONS. Under ~220 lines.
