# ASKFLOOR-1-T5b task-contract grill — brief (read-only, cold read)

You are grilling the task plan `plans/exploration/askfloor-1-t5b-taskplan.md` (T5b: `/permissions` remembered list, `all`, `forget <id>`, the Forget host handler; user_facing) against the code in this worktree (trunk after T4, plus T5a). Read-only. Do not change anything.

Read: the task plan; `plans/exploration/askfloor-1-t5b-seam-map.md`; the story plan's T5b bullet (line ~195) and the `/permissions` copy in section 5 (line ~162) of `plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md`; decisions 0154 (incl. its Amendment), 0118, 0153, 0132; and ONLY the files and line ranges the plan cites (do not sweep the tree).

Find every place where the plan is wrong, impossible, ambiguous, contradicts a decision or a T3a–T5a invariant, or where a named test cannot pin what it claims. Specifically: (a) can `SessionCommandDeps` carry the service + used-by read + identity triple, and can the caller at `group-processing-session-command-handlers.ts` really bind the canonical DM person (is `resolveCanonicalMemoryPersonId` reachable there and DM-gated); (b) the Forget host handler through a `ChannelWiring` setter — is that seam real, can `runtime-services.ts` (1186 ceiling) bind it without new lines beyond budget, and can the handler load the person-scoped row before `revokeById` (does the service expose a person-scoped get-by-id, or must `list` be filtered); (c) the in-place edit: does `MemoryForgetMessageActionInput` need the source message id, do the four provider seams cited have edit APIs, and does re-rendering the remaining rows from a provider require importing the session listing module across layers (channels may import application/domain/shared, not session — name where the listing formatter must live for both `session/` and `channels/` to use it); (d) the used-by read: is `actorContext.humanDecisionRecordId` queryable in `domain-repositories.postgres.ts` (JSONB), what index/order is available, and does job-name hydration have a repository method; (e) the approver label: which host object carries the sender display name at the two prompt-construction seams, and can it reach them without a worker value; (f) ceilings (session-commands 740, prompt-profile-service 783, runtime-services 1186, channel-wiring 790 with its import cap exhausted, 700 default) against the real edit set; (g) the 35-file scope and 40/4,000 budget; (h) every leaf's provability; (i) any owner-level choice still open.

Output: numbered BLOCKING findings (what is wrong, file:line evidence, the concrete plan-text fix), NON-BLOCKING notes, then `CLEAN` or `NOT CLEAN`. Under 100 lines. Finish within about 12 minutes of reading.
