BLOCKING findings

1. **The session-command dependency injection stops one layer too early.** `SessionCommandDeps` has no memory service/read/identity fields ([session-commands.ts:89](apps/core/src/session/session-commands.ts:89)); its caller receives only a lazy `memoryUserId`, not the identity resolver’s inputs or the proposed services ([group-processing-session-command-handlers.ts:33](apps/core/src/runtime/group-processing-session-command-handlers.ts:33), [group-processing-session-command-handlers.ts:64](apps/core/src/runtime/group-processing-session-command-handlers.ts:64)). It cannot itself rerun `resolveCanonicalMemoryPersonId`.
   **Plan fix:** use `await resolveMemoryUserId()` as the already host-resolved, DM-gated person; name the upstream callsite/type files that inject `HumanDecisionMemoryService` and the used-by read. Group tests must assert zero list, audit-read, and revoke calls—not merely zero writes.

2. **There is no trusted approver-label carrier to either prompt seam.** IPC settlement accepts and forwards only `personId` ([permission-remember-settlement.ts:86](apps/core/src/runtime/permission-remember-settlement.ts:86)); inline does likewise ([inline-permission-memory.ts:117](apps/core/src/app/bootstrap/inline-permission-memory.ts:117)). The inbound display name exists only while resolving identity ([group-person-identity.ts:136](apps/core/src/runtime/group-person-identity.ts:136)), although learning can persist `personLabel` ([human-decision-learning.ts:54](apps/core/src/application/permissions/human-decision-learning.ts:54)).
   **Plan fix:** define a host-stamped `{ personId, personLabel }` carrier at identity resolution and enumerate every IPC and inline callsite/type it traverses. Add end-to-end tests starting with inbound `sender_name`, including a conflicting worker value that is ignored.

3. **The listing formatter is assigned to an illegal shared layer.** The plan puts it under `session/` while requiring four `channels/` consumers, and also asks session code to reuse a formatter from `channels/` ([taskplan.md:13](plans/exploration/askfloor-1-t5b-taskplan.md:13), [taskplan.md:14](plans/exploration/askfloor-1-t5b-taskplan.md:14)).
   **Plan fix:** place the pure formatter in `application/permissions/permission-memory-listing.ts` (or `shared/`), move the scope-noun helper there, and add `channels/permission-card-affordances.ts` to scope. Both `session/` and `channels/` may then consume it lawfully.

4. **The Forget host contract lacks the authority and replacement data it needs.** The action carries conversation/account/thread/user/record only—no app, agent folder, or DM/group fact ([message-actions.ts:112](apps/core/src/domain/message-actions.ts:112)). The service exposes `list` and `revoke`, not a public `revokeById` ([human-decision-memory-service.ts:181](apps/core/src/application/permissions/human-decision-memory-service.ts:181)). Adding a source-message ID does not supply the missing identity triple or remaining rows.
   **Plan fix:** specify a host handler factory with app/route lookup, DM-gated identity resolver, service, used-by reader, and formatter. Load the exact row through person-scoped `service.list`, then call `service.revoke`; return a provider-neutral replacement view. Providers should retain their native message ID/token locally rather than sending it through the host action.

5. **Current-rails filtering is neither assigned nor tested.** Decision 0154 requires old-version rows to disappear ([0154:24](docs/decisions/0154-human-decision-memory-generic-scope.md:24)), but `service.list` accepts no rail version and forwards its input directly ([human-decision-memory-service.ts:181](apps/core/src/application/permissions/human-decision-memory-service.ts:181)); the cited service test has no stale-row case ([human-decision-memory-service.test.ts:400](apps/core/test/unit/application/human-decision-memory-service.test.ts:400)).
   **Plan fix:** assign current-rail filtering explicitly to the service/repository API and test bare/all/prefix omission. **Owner choice:** define stale-button behavior after a rail bump; recommended `not_found` with no mutation, matching the current-rails visible set.

6. **The used-by query contract is incomplete and can duplicate one job.** The cited port range does not contain `PermissionRepository`; it begins at line 619, not 613 ([repositories.ts:619](apps/core/src/domain/ports/repositories.ts:619)). The adapter only demonstrates serialized `actorContextJson` writes and ID lookup, not JSON type, index, extraction, or ordering ([domain-repositories.postgres.ts:1808](apps/core/src/adapters/storage/postgres/repositories/domain-repositories.postgres.ts:1808)). Repeated uses by one job would otherwise render duplicate names and inflate `+N`.
   **Plan fix:** specify one row per `(recordId, jobId)` using latest `createdAt`, deterministic `lastUsedAt DESC, jobId` ordering, deleted-job removal before `+N`, and the JSON extraction/cast. Either add an expression index plus migration to scope or explicitly accept and justify the scan. `getJobById` already supplies hydration ([ops-repo.ts:221](apps/core/src/domain/repositories/ops-repo.ts:221)).

7. **The declared edit set cannot satisfy the ceilings as written.** `runtime-services.ts` is already exactly 1,186 lines against its 1,186 cap ([architecture-map.json:34](scripts/architecture-map.json:34)); nevertheless the plan adds binding logic without naming an extraction. The declared 35 files also omit the formatter relocation, trusted-label carrier/callsites, and any query index migration ([taskplan.md:45](plans/exploration/askfloor-1-t5b-taskplan.md:45)).
   **Plan fix:** name a small extracted Forget-handler module and the exact block moved out of `runtime-services.ts`; add every missing carrier/helper/migration file, then recount against 40 files/4,000 lines. Keep the prompt guidance to its single remaining physical line.

8. **Several acceptance leaves cannot prove their stated contract.** The router test proves only forwarding an arbitrary handler result ([channel-message-action-router.test.ts:170](apps/core/test/unit/bootstrap/channel-message-action-router.test.ts:170)); it cannot prove person scoping, load-before-revoke, reply mapping, or rerendering. S5 in the story requires the near-miss to card, but T5b-AC7 omits it ([story-plan.md:155](plans/active/ASKFLOOR-1-the-judge-actually-judges-context-aware-first-asks-in-auto-mode.md:155), [taskplan.md:30](plans/exploration/askfloor-1-t5b-taskplan.md:30)).
   **Plan fix:** test the real handler factory; add list-before-revoke ordering/race/cross-person cases, case-insensitive prefix matching, stale rails, distinct-job deduplication, group zero-read/write, trusted-label provenance, and the S5 near-miss.

9. **The exact used-by copy contradicts accepted decision text.** The task requires `· used by job`, while decision 0153 says `also used by job` ([taskplan.md:8](plans/exploration/askfloor-1-t5b-taskplan.md:8), [0153:17](docs/decisions/0153-learned-decisions-project-into-job-grants.md:17)).
   **Plan fix:** choose one literal and update the plan/decision together. Recommended: retain the story’s later exact `used by job` copy and amend 0153’s consequence wording.

NON-BLOCKING notes

- The `ChannelWiring` setter seam is real, though the cited lines drifted: existing mappings are at `channel-wiring.ts:726-730`, not `:741-747`.
- All four providers have an edit mechanism: Telegram contextual edit, Slack `chat.update`, Discord message edit, and Teams `updateAdaptiveCard` ([Telegram:556](apps/core/src/channels/telegram/callback-handlers.ts:556), [Slack:226](apps/core/src/channels/slack/channel-message-action-handler.ts:226), [Discord:188](apps/core/src/channels/discord/index.ts:188), [Teams:242](apps/core/src/channels/teams/message-actions.ts:242)).
- Decision 0132 is browser authentication and has no T5b relevance ([0132:8](docs/decisions/0132-adaptive-browser-authentication-access.md:8)); remove it from the task’s decision list.
- No files were edited.

NOT CLEAN
