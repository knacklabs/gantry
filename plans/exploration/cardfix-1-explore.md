READ-ONLY exploration for CARDFIX-1 (do not edit files). Goal: every pause/setup story delivery carries at least one real action affordance, attached in the NEUTRAL layer (owner directive: one fix for all providers, never per-provider). Ruled action set for a compound-command denial (0134): "Allow once for this run" (one-time interactive ask-and-wait re-run; never a durable grant) + "Pause job". Evidence: formatSchedulerSetupStory (apps/core/src/application/jobs/scheduler-setup-story.ts) returns text only; live buttonless card for job card-check-2 after "Piped RunCommand commands cannot be authorized from per-leaf rules".
Report with file:line citations:
1. Every call site of formatSchedulerSetupStory and each source value (preflight_setup, final_setup, permission_denied, permission_timeout, transient_permission, partial_recovery): which delivery path sends the story, and whether MessageSendOptions.actionAffordances could be attached there.
2. The existing neutral action plumbing to reuse: MessageActionAffordance kinds, channel-message-action-router.ts routing, SCHED-4B's actionable job cards (how scheduler_run_now / scheduler_pause_job affordances are built and handled) — the pattern CARDFIX-1 should copy.
3. What "Allow once for this run" needs mechanically: how a one-time interactive approval for a compound command could be represented (existing kinds? a rerun with a pre-authorized once-decision? relation to the JOBPERM ask-and-wait need path) — enumerate the smallest honest options with their owners, do not decide.
4. "Pause job": whether scheduler_pause_job affordance already exists end-to-end (build + router + provider renderers + tests).
5. Existing tests per provider for actionAffordances rendering (telegram/slack/discord/teams) and for the setup-story deliveries; where AC1's "never action-less" invariant could be asserted once, neutrally.
6. Risks: double-delivery of pause stories (notification + card), idempotency of Allow-once, decision 0134/0115/0121 constraints that bound what the button may do.
Return a structured report; no code changes.
