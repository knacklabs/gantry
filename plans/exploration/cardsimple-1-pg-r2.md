# CARDSIMPLE-1 plan grill — round 2 cold read (read-only, no edits, keep reading TIGHT)

You did not author this plan. Round 1 (three passes) produced 14 findings; ALL were folded into `plans/exploration/cardsimple-1-plan-draft.md`. Read the AMENDED draft cold, against `docs/specs/cardsimple-1-one-permission-surface.md` and `constitution/`, and judge whether the fold is complete and coherent. Owner rulings since round 1: decision 0134 is ACCEPTED (chat gate recorded).

Verify specifically that the amended draft now contains, coherently and without internal contradiction:
1. Typed match classification (family_rule|exact|capability) from policy evaluation to the coordinator; rails only on typed family grants; typed family-rail-hit forcing suggestions-absent + allow_once|cancel in EVERY prompt constructor; shared synthesizer as sole source for all three lanes with the lane-parity matrix.
2. Fingerprint-keyed `attachSetupNeed` ingress (no waiter, durable request snapshot, all pause sites funneled); setup-origin delivery state (bounded attempts → operator recovery, same identity, never handoff_pending); prompt-fold as a MIGRATION of the bootstrap re-raise loop, partial recovery, and wiring guards; Pause deletion scoped to the affordance/codec layer only.
3. The live|late|stale matrix per needId+askingEpoch with mixed-batch rejection; typed durable receipt outcome + idempotent receipt marker; deterministic Run-now trigger identity {jobId, needId, askingEpoch, decision}; activation-before-trigger; failed-dispatch settlement; late Deny = receipt, no Run now, Reconsider kept.
4. AC1/AC2 preservation clauses and the AC3 Allow/Deny split; the new decision amending 0040 (narrow), 0121/0144 (family rail-hit permanence), and JOBPERM-2 grant selection; T3 depending on BOTH T1 and T2 with the added write-scope files and per-task verify contracts.

Then hunt ONE more time for anything round 1 missed — but only report findings that would change what an implementer builds. Nits about wording do not count this round.

Output: numbered findings — claim, file:line, severity (blocker | design-gap), smallest plan amendment. If the plan is implementable as amended, say "CLEAN" explicitly. No edits anywhere.
