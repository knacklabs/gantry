# CARDSIMPLE-1 plan grill — cold read, round 1 (read-only, no edits)

You did not author this plan. Read it cold, as an adversary trying to break the handover before implementation starts. Target: the plan draft `plans/exploration/cardsimple-1-plan-draft.md`, its confirmed spec `docs/specs/cardsimple-1-one-permission-surface.md`, and the law in `constitution/` — hold the plan AGAINST the law. (The spec was amended after the plan draft was written — requirements grill 2026-09-01: AC1 redefined over the real action-shape×blocker-type unions; rails scoped to FAMILY matches only with an explicit 0121/0144 amendment; late Deny = denial receipt + Reconsider kept, no Run now; 0124 bounded delivery preserved with operator-initiated same-identity recovery; non-job cards out of scope. Flag every place the plan draft must be updated to match the amended spec, in addition to your own findings.)

Interrogate specifically:
1. **Contract completeness.** Do the four ACs cover every Behaviour bullet of the spec, and does the three-task split (T1 family grants / T2 canonical card / T3 late tap, T3 depends on T2) leave any spec obligation unowned? Is anything double-owned so two tasks would collide in write scope?
2. **T1 correctness.** Verify the plan's claims against code: the family shape `RunCommand(<argv0> *)` in `shared/tool-rule-matcher.ts`, the validator rejection in `shared/durable-access-policy.ts`, the three synthesis lanes it must unify, and the rails-before-reviewed-rule ordering change in `runtime/permission-decision-coordinator.ts`. Does the "rail hit ⇒ ask, Allow once-only" flow have a coherent enforcement point, and does anything else depend on the return-early behavior being changed?
3. **T2 blast radius.** The plan removes the setup-pause prose send and folds `setup-pause-permission-prompt.ts` into the job permission card. Hunt for consumers the plan misses — anything beyond `notified_fingerprint` ownership, terminal-send suppression, `transient_permission` receipts and the CARDFIX-1 affordance deletion that breaks when the prose send dies. Is fingerprint-backed lease-free card creation actually reachable from every pause site the plan lists?
4. **T3 sufficiency.** Does deleting the rerun-barrier fanout break any legitimate consumer of `job_permission_handoff` triggers? Is the tap-time lease check well-defined for every card state (`handoff_pending`, `handed_off`, retired, expired)?
5. **Law and decisions.** Any conflict with `constitution/` rules or active decisions (0106, 0121, 0124, 0127, 0134, 0144, JOBPERM-2 grant shapes)? Does the planned NEW decision (rails-before-reviewed-rule) actually need to supersede or amend an existing decision rather than stand alone?
6. **Executability.** Are the verify plan and per-task budgets realistic? Name any file the tasks must touch that the plan never mentions.

Output: a numbered findings list — each finding gives claim, file:line evidence, severity (blocker | design-gap | nit), and the smallest plan amendment that fixes it. If a round is clean, say "CLEAN" explicitly. No edits anywhere.
