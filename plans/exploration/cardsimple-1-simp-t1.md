# CARDSIMPLE-1 simplicity pass — T1 only (read-only, no edits, keep reading TIGHT)

You are a lazy senior engineer. Read ONLY the T1 (family-wide grants) section of `plans/active/CARDSIMPLE-1-one-permission-surface-family-wide-grants.md` and answer fast. Question: the minimum T1 that still delivers AC2 (Allow on `curl X` covers `curl Y`; rail hit inside a family ⇒ ask, Allow-once; pipes never get Allow; exact rules unchanged).

Interrogate for over-build:
1. Does the typed match classification need three values (family_rule|exact|capability), or does one boolean `isFamilyRule` on the matched rule suffice?
2. Does the rail-hit-forces-allow-once propagation genuinely need changes in EVERY prompt constructor, or is there one choke point where suggestions are attached?
3. Is the "one shared synthesizer across three lanes" necessary NOW, or does changing only the host lane + the durable validator deliver AC2, with the SDK/autonomous lanes deferred (when-trigger: first observed exact-argv rule minted by those lanes)?
4. Does the lane-parity test matrix earn its keep if fewer lanes change?
5. Is the new decision record's triple amendment (0040/0121/0144 + JOBPERM-2) right-sized, or is one narrow decision with links enough?

Do NOT weaken: family behavior, rail gating on family matches, pipes-never-Allow, exact rules byte-for-byte unchanged.

Output: numbered CUT / DEFER(with when) / KEEP items + a 6-line minimum-T1 sketch. Verdict: "SIMPLE ENOUGH" or "OVER-BUILT: N cuts". No edits.
