# CARDSIMPLE-1 spec validation (read-only, sol@xhigh)

Read-only validation — make NO edits anywhere. Adversarially validate the draft spec `docs/specs/cardsimple-1-one-permission-surface.md` against the actual codebase. For each Behaviour bullet and each AC, verify the claimed current behavior is real (cite file:line), then hunt specifically for:

1. **Setup-card removal blast radius.** Consumers and flows that depend on the "Setup needed" pause-story message the spec deletes — `apps/core/src/jobs/execution-notifications.ts`, `apps/core/src/application/jobs/scheduler-setup-story.ts`, `apps/core/src/app/bootstrap/setup-pause-permission-wiring.ts`, the four provider renderers. What breaks if it stops being sent? Note that the just-shipped CARDFIX-1 pause-card actions (`scheduler_retry_ask`, `scheduler_pause_job` affordances built by `setupStoryActionAffordances`) currently ride exactly that delivery — where do they live if the prose card dies and the permission card becomes the only surface?

2. **Family-breadth grants.** Where exact-argv rules are synthesized today (`apps/core/src/application/permissions/permission-suggestion-synthesis.ts` `commandRules`, `apps/core/src/shared/bash-command-parser.ts` `normalizeBashLeafRuleContent`) and EVERY matcher that consumes `RunCommand(...)` rules. Is a family/prefix rule shape (e.g. curl with any args) already representable in the rule grammar and matchers, or is that a new grammar? Which risk-analyzer path (PERM-3 risk classifier, decisions 0040-0042 lineage) would still force a fresh ask for destructive shapes inside an allowed family — does the spec's "risk-gated" claim have a real enforcement point today?

3. **Late-tap flow.** Where `job_permission_handoff` triggers are created per prior run. Live bug 2026-08-31: ONE Allow tap on job `card-check-2` created THREE forever-pending handoff triggers, one per dead priorRunId (c20dd132, adab8dd8, 835e23e4). Find the fanout site, why each dead prior-run row produced a trigger, and why they stay pending forever. Also find where (or whether) a tap on a request whose run already ended is detected today.

4. **Decision contradictions.** Check the spec against recorded decisions: 0134 (compound commands never durably granted for autonomous runs), 0144 (denied rows keep a live card with Reconsider), 0106 (runs/triggers never mutate job definitions), JOBPERM-2 rule|once grant shapes, and the just-merged CARDFIX-1 PRs #460/#462 (neutral pause-card actions, host-lane mutation provenance). Flag anything the spec contradicts or silently supersedes.

Output: a NUMBERED findings list. Each finding = one-line claim, file:line evidence, severity (blocker | design-gap | nit), and the smallest spec amendment that fixes it. End with an overall verdict: is the spec implementable as written, and which findings must be folded in before planning.
