# Autoreview brief — CARDSIMPLE-1-T1: family-wide grants

Contract slice (AC2/AC4 of plans/active/CARDSIMPLE-1-one-permission-surface-family-wide-grants.md): Allow on a simple command records the canonical family rule `RunCommand(<literal argv0> *)` via ONE shared synthesizer used by all three rule-minting lanes, so a later run with the same argv0 and different args proceeds without asking; a destructive/privileged/egress rail hit inside an allowed family still asks and permits Allow-once only, decided at the coordinator (the sole seam — the inline lane's tail must honor coordinator-mutated suggestions/options); pipes present no Allow in ANY lane; SDK-supplied RunCommand suggestions are never returned raw AND never converted to families — a persistent family may only be offered for a command whose exact input the rails can inspect, so an SDK-only suggestion (no toolInput) yields no persistent suggestion at all (the request still asks; Allow-once remains); safe non-piped compounds resolve per-leaf with any-family-leaf ⇒ family-provisional; exact reviewed rules and capability grants are byte-for-byte unchanged; pinned local_cli readiness matching untouched; `isFamilyRule?: true` is additive-optional; ONE narrow decision record amends 0040 only.

Review with these invariants pinned:
- Family predicate precision: argv exactly [<literal argv0>, '*'], no wildcard/script-leaf argv0; reviewed-script rule forms (normalizeScriptLeafRuleContent output) stay REJECTED as families; the old gh */aws */abs-path rejections are deliberately flipped to admitted (feature), with script-shape regressions added.
- Rails ordering: rails run ONLY on isFamilyRule matches; exact/capability early return preserved byte-for-byte; autonomous stays classifier-free (0121); YOLO not the gate.
- The family-rail-hit reason string must be stable (ASKFLOOR-1's successor invariance suite will reference it); gantryToolDefaultRisk and classifier entry conditions untouched.
- Deviations from the recorded task plan (.factory/stories/CARDSIMPLE-1/task-plans/CARDSIMPLE-1-T1.md) are findings.
