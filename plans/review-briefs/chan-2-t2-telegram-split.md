# Review brief — CHAN-2-T2 (split the Telegram callback by action kind)

Contract: the `bot.on('callback_query:data')` callback in `apps/core/src/channels/telegram/channel-connect.ts` becomes a thin dispatcher (parse data → build one `TelegramCallbackContext` → dispatch by prefix → existing default); each action kind (user-question answers other/done, `lt:stop`, `jp:` job-permission card taps, `r:` compact retry, `perm:` classic prompt) lives in a named function in ONE sibling module `apps/core/src/channels/telegram/callback-handlers.ts`. Pure move: call sites move, helpers are not re-implemented. NO behaviour change; existing telegram unit tests pass unchanged (additions only). Ceilings: each handler CC <= 25, dispatcher CC <= 15.

Focus: (1) every branch moved verbatim — same guards, same order, same answerCallbackQuery/edit calls, same early returns; (2) context built once carries exactly the values the branches read (message id, chat id/jid, thread id, user id, provider account id) — a missing one changes behaviour silently; (3) the `jp:` handler keeps the JOBPERM-2 batch Allow/Deny semantics and once/expired rows non-decisionable; (4) no existing test edited. Report ONLY behaviour defects. Ignore style.
