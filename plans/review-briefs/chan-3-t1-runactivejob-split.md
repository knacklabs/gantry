# Review brief — CHAN-3-T1 (split runActiveJob by phase)

Contract: `runActiveJob` in `apps/core/src/jobs/execution.ts` becomes a phase sequencer (AST CC <= 15) over one `ActiveJobRunContext` built once; each phase is a named function (CC <= 25) in ONE sibling module `apps/core/src/jobs/execution-phases.ts`. Strictly verbatim moves: call sites move, helpers (`resolveExecutionContextOrDeadLetter`, `claimSchedulerRunLease`, `runJobAgentWithFailover`, finalization) are called not re-implemented; no dedup, no hoists. The existing `try/catch/finally` (lease release, heartbeat stop, finalization) stays in the sequencer with identical ordering; early returns (dead-letter, paused-for-setup) stay early returns. Zero test edits; jobs suites pass unchanged.

Focus: (1) every phase moved verbatim — same guards, same order, same awaits, same error paths; (2) the context carries every value a phase read from an outer const (a missed one changes behaviour silently, e.g. a stale runId/lease token); (3) try/catch/finally ordering and what each catch rethrows/records is byte-identical (the required test 'records a failed terminal run when execution throws before normal settlement' guards this); (4) no existing test edited. Report ONLY behaviour defects. Ignore style.
