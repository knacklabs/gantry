# Review brief — CHAN-3-T2 (split runQuery by phase)

Contract: `runQuery` in `apps/core/src/adapters/llm/anthropic-claude-agent/runner/query-loop.ts` becomes a `for await` dispatcher (AST CC <= 15) over one `QueryLoopContext` built once; SDK options/sandbox setup (the 0040 two-axis block and env wiring), the per-message handlers (`system` init/api_retry/other, `assistant`, `stream_event` text deltas, `result` — including the Q-0105 one-shot finish nudge and the Q-0108 stream.end decisions), and close/end are named functions (CC <= 25) in ONE sibling module `runner/query-loop-phases.ts`. Strictly verbatim moves: no dedup, no hoists, helpers called not re-implemented. Zero test edits; runner suites pass unchanged.

Focus: (1) every branch moved verbatim — same guards, order, awaits, early continues/returns inside the loop; (2) the context carries every accumulator and flag the handlers mutate (visible text since last result, nudged/closedDuringQuery, enableIpcFollowups/isScheduledJob, steering gate) — a copy instead of a shared reference silently breaks the nudge; (3) stream.end() call sites and their conditions identical (the required test 'keeps one-shot scheduled input open only for an accepted finish nudge' guards this); (4) permission-wait / approval-context handling unchanged; (5) no existing test edited. Report ONLY behaviour defects. Ignore style.
